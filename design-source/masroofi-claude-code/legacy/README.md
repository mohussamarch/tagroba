# مراجع قديمة، لم تعدل في هذه الحزمة

parse_alrajhi.py وdetect_subs.py وrules_categories.csv نُسخت كما هي من ملفات المستخدم. ليست مكتبة Android جاهزة. اقرأ spec/05-import-and-legacy.md قبل التشغيل؛ لا تنفذ detect_subs.py بمجرد import لأنه يقرأ ويكتب على مستوى الوحدة.

محلل PDF يتطلب pdfplumber عند تشغيله كأداة محلية. لم يُختبر على PDF حقيقي هنا ولم تُثبت حزمة اعتمادات مقفلة. اختيار الإصدار والتحقق من توافقه جزء من التنفيذ. لا تستخدم مجلد الملفات الأصلية وجهة إخراج حتى لا تستبدل سجلات سابقة.
