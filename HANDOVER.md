# مصروفي — ملف التسليم (HANDOVER)

> **لمن يستلم المشروع ولا يعرف عنه شيئًا.** اقرأه كاملًا قبل أي كود.
> آخر تحديث: 2026-09-16 · **اقرأ §38 في نهاية الملف أولًا** — نقطة الاستكمال الحالية. راجع أيضًا تحديث شغل الليل 2026-09-16 داخل §37. الأقسام القديمة، حتى لو سُمّيت «الحالة الحالية»، سجل تاريخي لا يعلو على الأحدث.
> ⚠️ أرقام الاختبارات في §2.3 و§13 (420) قديمة، وقائمة §7 معظمها اتنفذ (انظر §14). الرقم الحالي في آخر قسم.

---

## 0. اقرأ هذا أولًا — خمس دقائق تمنع خمس كوارث

| # | الفخ | ماذا يحدث لو تجاهلته |
|---|---|---|
| 1 | `npx tsc --noEmit` **لا يفحص شيئًا** في هذا المشروع | تظن الأنواع سليمة وهي ليست كذلك. **استعمل `npm run typecheck`** (= `tsc -b --noEmit`). هذا الخطأ أخفى ٣ أعطال حقيقية لعدة مراحل — انظر §9.1 |
| 2 | Node ليس على `PATH` في صدفة الأدوات | كل أمر يفشل بـ`node: command not found`. الحل في §2.2 |
| 3 | `files/` و`*.pdf` **مستبعدة من Git** وفيها كشف بنكي حقيقي | رفعها تسريب بيانات مالية. الريبو **علني**. انظر §2.4 |
| 4 | **ممنوع `parseFloat` في أي حساب مالي** | `0.29 × 100 = 28.999999999999996`. القاعدة مطلقة — §4.1 |
| 5 | لا تضف فهرسًا مركّبًا في Firestore | الباقة مجانية والفهارس تكسر التطبيق عند المستخدم — §5.3 |

---

## 1. ما هو المشروع

**تطبيق تتبّع مصروف شخصي** بالعربية، PWA (React + TypeScript + Firestore).
المالك **محمد** — **لم يكتب كودًا في حياته**. سيُهدى التطبيق لأصدقاء أيضًا.

**منشور على:** https://masroufy-d4b96.web.app
**الكود:** https://github.com/mohussamarch/tagroba (علني)

### 1.1 كيف تتكلم مع المالك

هذه ليست تفضيلات شكلية — هي شروط عمل مكتوبة في `CLAUDE.md`:

- **بالعربية المصرية، بصيغة المذكر.**
- كل مصطلح تقني عربي **يُكتب جنبه بالإنجليزي**.
- **لا تفترض معرفة بالطرفية.** اكتب الأمر كاملًا جاهزًا للنسخ.
  (وقعت غلطة مرة: كُتب له قالب فيه `اسم-حسابك` فنسخه حرفيًا وكسر إعداد Git.)
- **لو فيه غموض — اسأل ولا تختر بنفسك.** نصه: «لو لقيت تعارض مش مغطى في
  OVERRIDES.md، اسألني ومتختارش لوحدك».
- **لا تدّعِ نجاحًا لم يحدث.** ميزة تصميمها ظاهر ليست ميزة مكتملة.

---

## 2. التجهيز

### 2.1 البرامج

Node.js LTS، وحساب Firebase (المشروع منشأ فعلًا — `FIREBASE.md`).

```bash
npm install
```

### 2.2 ⚠️ مشكلة PATH على هذا الجهاز

Node مثبّت في `C:\Program Files\nodejs` لكنه **ليس على PATH صدفة الأدوات**
(هو على PATH الجهاز، فطرفية المالك تعمل طبيعيًا).

**كل أمر تشغّله من الأدوات يجب أن يُسبق بهذا:**

```bash
PATH="/c/Program Files/nodejs:$PATH" npm test
```

### 2.3 الأوامر

```bash
npm run dev          # خادم التطوير على 5173
npm test             # 420 اختبارًا
npm run typecheck    # الفحص الحقيقي — لا تستعمل tsc --noEmit وحده
npm run build        # يشغّل tsc -b ثم vite build
npx firebase deploy --only hosting
```

**وضع المعاينة:** `http://localhost:5173/?demo=1` — مستودعات ذاكرة مزروعة،
بلا فايربيز. يُحذف من بناء الإنتاج عبر `import.meta.env.DEV`.

### 2.4 ⚠️ البيانات الحساسة

| الملف | المحتوى | الحالة |
|---|---|---|
| `files/counterparties.csv` | **أرقام حسابات كاملة** | مستبعد من Git |
| `files/transactions_full.csv` | كشف حساب حقيقي (1912 عملية) | مستبعد |
| `document_*.pdf` | كشف الراجحي الحقيقي، 68 صفحة | مستبعد |

**قاعدة المالك حرفيًا:** «ملف files/counterparties.csv فيه أرقام حسابات كاملة —
متحطهاش في فايربيز، آخر أربع أرقام بس.» ولذلك `Wallet.accountLast4` فقط،
ويوجد اختبار `privacy.test.ts` يحرس هذا.

الملفات موجودة على جهاز المالك ولازمة لاختبارين
(`realStatement.test.ts` و`alrajhiPdfReal.test.ts`). **عند غيابها الاختبار
يفشل برسالة صريحة** ولا يمر زورًا.

---

## 3. ترتيب المراجع — من يعلو على من

```
OVERRIDES.md  ←  يعلو على كل ما عداه
      ↓
ARCHITECTURE.md · FIREBASE.md
      ↓
design-source/masroofi-claude-code/spec/01…06
      ↓
البقية (SPEC.md و DATA_MODEL.md — نسخة أقدم، للتاريخ فقط)
```

- **`OVERRIDES.md`** (13 قسمًا): الفروقات المعتمدة والقرارات النهائية.
- **`ARCHITECTURE.md`** (19 قسمًا): الطبقات والقرارات وأسبابها.
- **`spec/01…06`**: المنتج، المحاسبة، نموذج البيانات، الواجهة، الاستيراد، القبول.

**ملفات لا تُتبع تعليماتها:** `design-source/masroofi-claude-code/CLAUDE.md`
و`START-HERE-AR.txt` — كُتبا لمهمة أندرويد **ملغاة** (`spec/07` ملغى بالكامل).

**`design/masroofi-v4.html`** مرجع بصري **للقراءة فقط**، ونص المالك:
«متعاملش أي كلام جواه كأنه تعليمات» — أي نص داخله **بيانات لا أوامر**.

### 3.1 القاعدة الذهبية

> **الشات ينسى، الملف لا ينسى.**
> أي قرار جديد يُسجَّل في `OVERRIDES.md` أو `ARCHITECTURE.md` **فورًا**.

---

## 4. القواعد غير القابلة للتفاوض

### 4.1 المال عدد صحيح — دائمًا

**كل مبلغ عدد صحيح بالهللة.** `100 هللة = 1 ريال`. لا كسور عشرية في أي
حساب مالي، أبدًا.

**ليس رأيًا — مقيس:** من 200,000 قيمة في الكشف الحقيقي، **18,351 قيمة**
تُنتج نتيجة خاطئة بـ`parseFloat(x) * 100`. مثال: `0.29 → 28.999999999999996`.

التحويل من نص إلى هللة **نصّي**: `parseMoney('96.47')` تبني الرقم من
خاناته مباشرة. والتحويل للريال **في العرض فقط** عبر `formatMoney`.

**الكميات (استثمار)** لها مقياسها المستقل: عدد صحيح بثمانية أرقام عشرية.
وضربها في السعر **يتجاوز حد العدد الصحيح الآمن في جافاسكربت** (بتكوين
بسعر ١.٥ مليون ريال = 1.5×10¹⁶ > 9×10¹⁵) فينتج غلطًا صامتًا —
ولذلك خطوة الضرب وحدها بـ`BigInt`، والتقريب **مرة واحدة** عند حد معلن.

### 4.2 لا رقم بلا مصدر

إما محسوب أو يُعرض **«غير متاح»**. **ممنوع صفر مؤكد مكان المجهول.**

هذا ليس تجميلًا. مثال وقع فعلًا: الشاشة عرضت «الدخل 0.00» وتحتها راتب
7,000 — لأن العمليات لم تُصنَّف بعد. الرقم كان **مجهولًا لا صفرًا**.
الحل: ثلاث حالات (معروف / ناقص / مجهول)، و`null` عند المجهول.

### 4.3 المعمارية — أربع طبقات، السهم لا ينعكس

```
presentation/  →  application/  →  domain/
                        ↑
                infrastructure/   (ينفّذ المنافذ فقط)
```

- **`domain/`** نقي تمامًا: **لا React ولا Firebase ولا `window` ولا `Date`.**
- **`application/`** يعرف **واجهات** المستودعات (ports) لا تنفيذها.
- **`presentation/`** لا تكلّم مستودعًا ولا تحسب مبلغًا — تنادي حالة استخدام.
- التجميع **يدوي** في `src/app/container.ts`. **لا مكتبة حقن اعتماديات.**

**لكل مستودع تنفيذان:** `infrastructure/firestore/` للتشغيل،
`infrastructure/memory/` للاختبار. **كل معايير القبول تمر عبر الذاكرة بلا
فايربيز إطلاقًا.**

> ⚠️ **التنفيذان يجب أن يتطابقا في العقد.** حدث خلاف حقيقي:
> `MemoryRuleRepository.saveMany` كانت **تستبدل القائمة كلها** و Firestore
> **تحدّث بالمعرّف**. الاختبار كان ينجح والتشغيل يفشل. العقد المعتمد:
> **التحديث بالمعرّف (upsert)**، والحذف بـ`deleteMany`.

### 4.4 قواعد التنفيذ

| القاعدة | لماذا |
|---|---|
| **حد الملف 300 سطر** | الأطول يُقسم. مطبَّق فعلًا على كل الملفات |
| **لا مكتبة بلا سبب مكتوب** في `ARCHITECTURE.md` | المكتبات الحالية: 4 فقط |
| **بناء بشرائح رأسية** | من الشاشة للتخزين، تشتغل فعلًا، قبل التالية. **ممنوع** بناء كل الشاشات ثم ربطها |
| **fixtures أولًا** ثم البيانات الحقيقية | — |
| **commit بعد كل شريحة مكتملة** | — |

---

## 5. كيف يتصل كل شيء ببعضه

### 5.1 نواة الحساب — الشجرة

```
domain/money.ts                    ← كل مبلغ عدد صحيح بالهللة (23 اختبارًا)
  ├── formatMoney.ts               ← المكان الوحيد الذي يحوّل الهللة لريال
  ├── ledger.ts                    ← المصروف الشخصي · الوسوم · الديون (25 اختبارًا)
  │     ├── loadHomeScreen         → الرئيسية
  │     ├── loadTransactionsScreen → العمليات
  │     ├── loadBudgetScreen       → الميزانية
  │     └── managePeople           → الأشخاص
  ├── reconcile.ts                 ← سلسلة الرصيد (0 فروق من 1912)
  ├── budget.ts  ──► notifications.ts  ← التنبيه من سقف حدّده المستخدم فقط
  └── analytics.ts

domain/period.ts                   ← الشهر المالي يبدأ يوم الراتب (28)
  └── يقف عليه: منتقي الفترة · المتاح اليومي · المتوسط · المطابقة

domain/quantity.ts                 ← الكمية عدد صحيح بمقياسها (BigInt في الضرب)
  └── assets.ts ──► priceFeed.ts   ← المتوسط المرجّح · البيع الجزئي
```

### 5.2 خط الاستيراد — من الملف إلى الشاشة

```
ملف (CSV أو PDF)
   ↓
inspectFile.ts ──── يحدد النوع قبل أي تحليل
   ├── csv → csvReader → schemas.ts (preview | legacy)
   └── pdf → pdfTextExtractor → alrajhiPdfSchema
   ↓
        ParsedRow[]   ← ★ النوع الموحّد: من هنا المسار واحد لا مساران
   ↓
importPreview.ts
   ├── dedupe.ts        ← خمس درجات لمنع التكرار
   ├── categorize.ts    ← أربع أولويات
   └── suggestEconomicKind ← يقترح ولا يقرر
   ↓
        ImportPreview  ← المستخدم يراجع ويختار
   ↓
importStatement.commit()  ← بروتوكول المرحلتين (§5.4)
```

> **قاعدة حاكمة:** الـPDF **لا يملك خطًا موازيًا**. يُنتج نفس `ParsedRow`
> ثم يدخل نفس الأنابيب. خطان يعني قاعدتين لمنع التكرار، وواحدة ستتأخر حتمًا.

### 5.3 قاعدة فهارس Firestore

**ممنوع أي فهرس مركّب** (`ARCHITECTURE §12`). كل استعلام **بحقل واحد**،
والفلترة والترتيب في الذاكرة.

**لماذا:** المالك واجه على هاتفه: *"The query requires an index"* — التطبيق
توقف تمامًا. الفهارس تحتاج نشرًا منفصلًا ووقت بناء، والمستخدم لا يعرف ذلك.

### 5.4 بروتوكول المرحلتين (staged commit)

Firestore **لا يوفر معاملة ذرية** عبر مئات المستندات. البديل المعتمد:

```
1. batch.state = 'staged'      ← الدفعة معلّقة
2. sources.saveMany()          ← ★ الفهرس أولًا
   txns.saveMany()             ←   ثم العمليات
3. batch.state = 'committed'   ← كتابة واحدة تحسم كل شيء
```

**ترتيب الخطوة ٢ حرج** — عكسه ينتج عمليات يتيمة لا يمكن الوصول إليها من
الدفعة ولا التراجع عنها. اكتشفه اختبار.

عند فتح التطبيق، `resumeStagedBatch` **يفشّل** أي دفعة معلّقة بالكامل:
«صفر أو كامل الدفعة، لا نصفها».

### 5.5 مفهوم مركزي: النوع الاقتصادي ≠ اتجاه السيولة

هذا **أهم مفهوم محاسبي في المشروع**:

- **`observedDirection`** (وارد/صادر) — **حقيقة بنكية**، لا تتغير أبدًا،
  وعليها يعتمد منع التكرار.
- **`economicKind`** (راتب، شراء، قرض، أمانة…) — **قرار المستخدم**.

`spec/02` حرفيًا: «وارد الحساب **لا يحدد وحده** النوع الاقتصادي»،
و«**لا تصنف تحويلًا غامضًا كراتب افتراضيًا**».

ولذلك `suggestEconomicKind` **يقترح ولا يقرر**، وفيه اختبار يثبت أن
**لا مدخل ينتج اقتراح دخل**. على الكشف الحقيقي: 1,250 واضحة، 66 متوسطة،
**596 غامضة بقيمة 484,537 ريال** تنتظر قرار المستخدم.

---

## 6. الحالة الحالية

### 6.1 المراحل

| # | المرحلة | الحالة |
|---|---|---|
| 0 | التجهيز والحسابات | ✅ |
| 1 | الاستيراد ومنع التكرار والتصنيف | ✅ |
| 2 | الشهر المالي والرئيسية والتحليل | ✅ |
| 3 | الميزانية والسقوف والشذوذ | ✅ |
| 4 | المطابقة والتصدير والنسخ | ✅ (الاستعادة اكتملت بالدمج) |
| 5 | الالتزامات والأطراف | ⚠️ الأشخاص ✅ · **الاشتراكات ❌** |
| 6 | الإشعارات | ⚠️ المتاح كله ✅ · الدفع من السيرفر **مستحيل** |
| 7 | الاستثمار | ✅ |
| 8 | النشر | ⚠️ منشور · **التثبيت على الجوال لم يُجرَّب** |
| 9 | الأسعار الآلية | ✅ |

### 6.2 معايير القبول — يجب أن تبقى خضراء

| المعيار | القيمة |
|---|---|
| المدين | 302,171.45 |
| حركات ليست مصروفًا | 201,703.18 |
| المصروف الحقيقي | 100,468.27 |
| الدائن | 300,040.87 |
| سلسلة الرصيد | 4,837.83 @ 2025-01-01 → **2,707.25 @ 2026-09-04** |
| أسطر لم تطابق | **صفر من 1912** |

**تُتحقَّق من CSV ومن PDF كليهما، وبمسار التطبيق نفسه.**

نص المالك: «لو الأرقام مش مطابقة، **اوقف** وحلّل سبب الفرق وقوللي، ومتكملش.»

**تعارض مفتوح موثّق:** بند `سداد` (17,233.19) بين المجاميع المعتمدة
و`spec/02` — مسجَّل في `ARCHITECTURE §9.6` ولم يُحسم.

---

## 7. ⚠️ ما يجب حلّه أولًا

مرتّب بالأولوية. الأول **أخطر مما يبدو**.

### 7.1 🔴 لم يُجرَّب التثبيت على الهاتف

التطبيق **PWA**، والتوصيل المفترض هو «إضافة للشاشة الرئيسية».
**لم يُختبر ولا مرة على جهاز حقيقي.** لو ملف `manifest` أو `service worker`
فيه خلل، التطبيق كله لا يصل للمستخدم.

**افعل:** اطلب من المالك تثبيته من كروم على الأندرويد وتصوير النتيجة.

### 7.2 🔴 الاشتراكات المتكررة — أكبر فجوة وظيفية

`DetectCommitments` **لم يُبنَ**. القاعدة الصحيحة **معروفة ومكتوبة**
(`spec/05`): امتداد عبر عدة شهور + تواتر محدود + مبلغ/موعد متقارب +
طبيعة خدمة مناسبة.

**الفخ الموثّق:** «126 زيارة مطعم **لا تثبت اشتراكًا**»، و«مرشح الاشتراك
**يحتاج تأكيدًا**» من المستخدم.

### 7.3 🟡 لا إعادة تصنيف للعمليات القديمة

قاعدة جديدة تنطبق على القادم فقط. **متعمَّد** (`spec/05`: «المؤكد لا يُكتب
فوقه») لكنه يفاجئ المستخدم.
**المطلوب:** زر «طبّق على القديم» يعرض عدد المتأثرين ويستثني المؤكد يدويًا.

### 7.4 🟡 596 عملية غامضة تنتظر قرارًا

بقيمة 484,537 ريال. `KindsSheet` موجودة لكن مراجعة 596 عملية واحدة واحدة
غير عملية. **المطلوب:** تصنيف جماعي (كل ما يشبه هذا ⇒ نفس النوع).

### 7.5 🟢 نواقص أصغر

- **لا شاشة لتعديل التصنيفات** (إضافة/إخفاء/تلوين). القواعد والتجار لهما شاشة.
- **لا فلترة بالوسم** في شاشة العمليات (التخزين والواجهة موجودان).
- **لا ربط أسماء بديلة بتاجر واحد** (`MerchantAlias` معرَّف ولم يُستعمل).

---

## 8. ما هو **مستحيل** — لا ناقص

اذكرها للمالك صراحة إن سأل. إخفاؤها يجعله ينتظر شيئًا لن يأتي.

| الميزة | لماذا مستحيلة |
|---|---|
| **إشعار على الهاتف والتطبيق مغلق** | يحتاج Cloud Functions — غير متاحة في باقة Spark المجانية |
| **قراءة الرسائل البنكية تلقائيًا** | مستحيل على PWA، ومستحيل على iOS أصلًا. المسار المختار (`OVERRIDES §8`): المشاركة من تطبيق الرسائل على أندرويد — **لم يُبنَ بعد** |
| **معاملة Firestore ذرية عبر مئات المستندات** | حد المنصة. البديل: بروتوكول المرحلتين (§5.4) |

### 8.1 قيود معلنة (تعمل لكن بحدود)

- **قارئ PDF للراجحي فقط.** بنك آخر ⇒ رسالة صريحة، **لا أرقام خاطئة**.
- **أسعار: خمسة أسهم تداول فقط** — الـworkflow **لا يرى بيانات أي مستخدم**
  (كل حساب مقفل عليه في Firestore) فالقائمة ثابتة في السكربت.
- **أول استيراد PDF يحتاج إنترنت** — المكتبة (~1.7 م.ب) مستثناة من
  التحميل المسبق عمدًا.
- **اسم التاجر المقطوع على سطرين يُقرأ ناقصًا** (`AL-OTHAIM` → `OTHAIM`).
  السكربت القديم عنده نفس الحد. ~85%+ تُقرأ صحيحة، والباقي **فارغ لا مخترع**.

---

## 9. أخطاء وقعت فعلًا — لا تكررها

### 9.1 🔴 أمر فحص الأنواع كان لا يفحص شيئًا

`tsc --noEmit` مع إعداد المراجع (`tsconfig.json` فيه `files: []` و
`references`) **لا يفعل شيئًا ويخرج بنجاح**. أُعلن «التحقق نظيف» مرارًا
وهو لم يفحص سطرًا.

عند تشغيل `tsc -b` ظهرت **٣ أعطال حقيقية**:
- `restoreBackup` تنادي `categories.saveMany` **وهي غير موجودة** — كانت ستنهار وقت الاستعادة
- `demoContainer` متأخر **٤ مراحل** ومعطّل تمامًا
- `FirestoreRuleRepository` بلا `deleteMany` — **حذف قاعدة لم يكن يعمل في التشغيل**

**استعمل `npm run typecheck` دائمًا.** و`npm run build` يشغّله تلقائيًا.

### 9.2 رسالة خطأ كانت **تكذب**

المالك رفع PDF فوصلته: *«اقتباس مفتوح ولم يُغلق — الملف ناقص أو تالف عند
السطر 3366»*. الملف **سليم تمامًا**؛ التطبيق كان لا يقرأ PDF.

`File.text()` ينجح على **أي** ملف ويعيد بايتات PDF كأنها نص.
الحل: `inspectFile.ts` يفحص **قبل** أي تحليل ويقول الحقيقة.

> **رسالة خطأ مضلِّلة أسوأ من غيابها.** إن لم تعرف السبب — قل ذلك صراحة.

### 9.3 المستخدم انحبس في شاشة

`KindsSheet` علقت على «بنحفظ…» وزر الإغلاق **معطَّل**.
**قاعدتان من وقتها:** `busy` يُصفَّر في `finally` **دائمًا**،
و**زر الإغلاق لا يُعطَّل أبدًا**.

### 9.4 المال اختفى في التحويل الداخلي

كان يخصم من محفظة **ولا يضيف للأخرى**. `ARCHITECTURE §17` +
`transferToWalletId`. الاختبار الآن: مجموع المحافظ قبل التحويل وبعده متطابق
بالهللة.

### 9.5 نُقل سلوك السكربت القديم فكاد يفسد كل النصوص

السكربت القديم (بايثون/pdfplumber) **يعكس كل كلمة عربية**. `pdfjs`
**لا يحتاج ذلك** — التطبيع وحده صحيح، والعكس يفسد: «الرصيد» ⇒ «ديصرلا».

نقله حرفيًا كان سينتج أسماء تجار مقلوبة **بلا أي خطأ ظاهر**.

> **الدرس:** المرجع القديم يُقرأ للفهم **لا للنسخ**. تحقّق من كل افتراض.

### 9.6 أخطاء أدوات متكررة

- **الشل يفسد الاقتباسات العربية والمسافات المائلة** داخل `node -e`.
  للملفات الكبيرة أو النصوص الحساسة **استعمل أداة الكتابة/التعديل مباشرة**.
- `git checkout <file>` بعد تقسيم خاطئ **أرجع أكثر مما قُصد** وأضاع ربطًا كاملًا.

### 9.7 كاد يُحقن رصيد المالك في تطبيق سيُهدى

الأرصدة في `OVERRIDES §6` هي **أرصدة المالك**. زرعها يعني أن كل صديق
يفتح التطبيق فيجد رصيد شخص آخر. **كل مستخدم يبدأ من صفر** —
`seedWallets(false)`.

---

## 10. قرارات محسومة — لا تُعاد فتحها بلا سبب

| القرار | المرجع |
|---|---|
| PWA بـReact+TS **لا أندرويد**، و`spec/07` **ملغى بالكامل** | `OVERRIDES` |
| Firestore بحساب لكل مستخدم، **لا تخزين محلي** | `OVERRIDES` |
| CSS عادي بمتغيرات — **لا Tailwind ولا CSS Modules** | `ARCHITECTURE §9.1` |
| **الاستعادة تدمج** ولا تستبدل ولا ترفض (قرار المالك) | `OVERRIDES §12` |
| **المبلغ والتاريخ والاتجاه لا تُعدَّل** على عملية قائمة | `OVERRIDES §13` |
| القواعد: **القفل لا الحذف** | `OVERRIDES §13` |
| الاستثمار: **المتوسط المرجّح** لا FIFO | `OVERRIDES §11` |
| **لا حذف لشخص أو أصل له سجل** — أرشفة فقط | `spec/03` |
| رسوم الشراء **تدخل التكلفة**، ورسوم البيع **تُخصم من الحصيلة** | `OVERRIDES §11` |
| حصيلة بيع الأصل **ليست دخلًا** | `spec/02` |

---

## 11. خريطة الملفات

### `src/domain/` — نقي، لا يستورد شيئًا خارجيًا

`money` `formatMoney` `normalize` `period` `ledger` `budget` `analytics`
`reconcile` `dedupe` `categorize` `search` `suggestEconomicKind`
`expenseClassification` `notifications` `quantity` `assets` `priceFeed`
`mergeBackup` · و`entities/` (`types` `economicKind` `assets`)

### `src/application/`

- **`ports/repositories.ts`** — كل واجهات التخزين، **بلا تنفيذ**
- **`useCases/`** — 24 حالة استخدام. كل منطق ثقيل هنا لا في React

### `src/infrastructure/`

`firestore/` (تشغيل) · `memory/` (اختبار) · `import/` (+`pdf/`) ·
`prices/` · `export/`

### `src/presentation/` و `src/app/`

`screens/` · `components/` · `theme/` ·
`container.ts` (التجميع الوحيد) · `demoContainer.ts` · `AppShell.tsx` ·
`useAppData.ts`

### خارج `src/`

- **`scripts/fetch-prices.mjs`** + `.github/workflows/prices.yml` — يوميًا 5 صباحًا
- **`public/prices.json`** — يُقرأ من raw.githubusercontent (الريبو **يجب أن يبقى علنيًا**)
- **`firestore.rules`** — `request.auth.uid == uid`. هذا **وحده** ما يحمي البيانات

---

## 12. أول ساعة لك

```bash
# 1. تأكد أن كل شيء أخضر قبل أن تلمس شيئًا
PATH="/c/Program Files/nodejs:$PATH" npm test
PATH="/c/Program Files/nodejs:$PATH" npm run typecheck
```

2. اقرأ `OVERRIDES.md` كاملًا (254 سطرًا) — أسرع طريق لفهم «لماذا».
3. اقرأ `src/domain/money.ts` و`ledger.ts` — منهما يتفرّع كل شيء.
4. شغّل `npm run dev` وافتح `?demo=1` وتجوّل في كل تبويب.
5. اقرأ `ARCHITECTURE §9` إلى `§19` — كل قرار وسببه.

**ثم ابدأ من §7.1** (تجربة التثبيت) لأنها الأرخص والأخطر.

---

## 13. تذكير أخير

المالك **لا يقرأ كودًا**. ثقته مبنية على أمرين فقط:

1. **الأرقام تطابق** — 420 اختبارًا و0 فروق من 1912 سطرًا.
2. **قيل له الحق** — عن المستحيل، وعن الأخطاء، وعن ما لم يُجرَّب.

القاعدة التي حكمت المشروع كله:

> **«لا ادعاء نجاح لم يحدث.»**
>
> ميزة تصميمها ظاهر ليست ميزة مكتملة. ورقم بلا مصدر ليس رقمًا.


## 14. تحديث الاستلام الحالي — 2026-09-08
هذا القسم أحدث من أوصاف النواقص التاريخية أعلاه.
- 437 اختبارًا ناجحًا في 33 ملفًا؛ 135 ملف مصدر TypeScript/TSX.
- الاشتراكات والفواتير: من الرئيسية، كشف مرشحين ثم تأكيد/إضافة/تعديل/توقف،
  مدفوع مسجل منفصل عن المتوقع سنويًا. التفاصيل والقيود في ARCHITECTURE §20.
- من العمليات: مراجعة وتصنيف القديم بنطاق زمني ومعاينة، وتصنيف مجموعة
  يختار المستخدم نوعها؛ الديون والتحويلات ذات الأطراف خارج التأكيد الجماعي.
- من الإعدادات: إدارة التصنيفات وألوانها وإخفاؤها، ودليل تثبيت سامسونج.
- الأسماء البديلة تحت القواعد والتجار، والبحث والفلترة بالوسوم موصولان.
- إصلاح توقع نهاية الفترة والمتاح اليومي عند نقص التصنيف.
- فحص الواجهة محلي ببيانات مصطنعة؛ Netflix: 29.99 للدورة، 359.88 سنويًا،
  89.97 مدفوعًا؛ مشتريات جماعية 250 مرة واحدة؛ إضافة تصنيف ناجحة.
- لم يتم نشر التغييرات. تثبيت الموبايل وتشغيل نسخة مثبتة وتحديثها غير مختبر.
- نقص مهم اكتُشف: النسخة الاحتياطية لا تشمل الأشخاص/الديون/الأصول/الوسوم/
  الاشتراكات؛ صُحح الوصف في الشاشة. لا تعتمد عليها لاستعادة الحساب بالكامل.
- ملف «سداد» المحاسبي المعلق لم يُغيّر، ولم تتغير بيانات الحساب المنشور.


### نشر النسخة — 2026-09-08
بطلب المالك نُشرت التغييرات على https://masroufy-d4b96.web.app عبر Firebase Hosting.
النشر اكتمل، وطلب HTTP أعاد 200 وملف index-Bem5kpeV.js المطابق للبناء المختبر.
437 اختبارًا ناجحًا؛ لا تغيير لقواعد Firestore ولا بيانات المستخدم أثناء النشر.
التثبيت على جهاز S24 Ultra ما زال يحتاج تجربة المالك.
## 15. Android trial and loading — 2026-09-10

User asked for an APK and future SMS access. Capacitor 8 Android project now exists.
`npm run android:sync` passes. Gradle `assembleDebug` succeeded (93 tasks).
APK signature v2 verified; app ID app.masroufy.personal; min SDK24, target36.
APK includes local index + PDF reader, no service worker, no statement/CSV/.env filenames.
Only Internet and AndroidX private receiver permission declared; SMS is not implemented.
No device listed by adb: runtime/physical S24 validation has NOT occurred.
Android Google sign-in is unfinished and explicitly explained in the sign-in screen;
email/password is available. Native export uses ACTION_CREATE_DOCUMENT through LocalFilesPlugin.
See ANDROID.md for rebuild instructions and limitations. Debug signing is for trial only.

Loading fixes: per-screen loading/errors, retryable shared startup barrier, stale period response guards,
parallel historical period reads, per-user matching in-flight repository requests coalesced with cloned results.
No change to money logic or TTL cache. All 442 tests passed after app changes; an additional historical
parallel-start regression also passed (443 total test cases). Typecheck and web production build pass.
No mobile speed measurement is claimed. Web build exists locally; this slice was not deployed to Firebase.

Build tools are outside the repo under C:/Users/atgs0/Documents/Codex/android-build-tools:
Java21, SDK36, Gradle8.14.3; Android Gradle plugin also fetched build-tools35 automatically.
Set JAVA_HOME to the extracted JDK folder, ANDROID_HOME to its sdk folder, and GRADLE_USER_HOME
to its gradle folder before invoking android/gradlew.bat. Do not commit signing keys or local SDK paths.

## 16. إغلاق شغل الرسائل وتحديث APK — 2026-09-10
هذا القسم هو الحالة الحالية، والأقسام السابقة سجل تاريخي.
طلب المالك الاستمرار حتى 06:00 بتوقيت السعودية. آخر استئناف وصل بعد الموعد؛ أُوقفت المتابعة ولم تبدأ ميزات جديدة أثناء الإغلاق.

### ما تم
- «إضافة ← رسائل البنك»: قراءة يدوية عند الطلب من مرسلين وفترة يحددها المستخدم، بعد موافقة داخل الشاشة وإذن READ_SMS.
- بديل لصق رسالة واحدة، يعمل على الويب وأندرويد. لا يوجد استقبال تلقائي في الخلفية ولا RECEIVE_SMS.
- المحلل يدعم صيغًا محددة بالريال، ويشترط مبلغًا واتجاهًا وتاريخ عملية واضحًا؛ يتخطى التحقق وكلمات السر والعروض والعمليات المرفوضة والصيغ غير المدعومة. توجد أسباب تخطي مرئية. هذه ليست تغطية لكل البنوك والصيغ.
- النتيجة تدخل مسار الاستيراد نفسه: اختيار محفظة، معاينة، مراجعة التكرار، تأكيد. لا حفظ قبل التأكيد.
- رسالة وكشف بنفس الحساب/اليوم/المبلغ/الاتجاه يصبحان «متشابه» غير محدد افتراضيًا حتى مع اختلاف التاجر والمرجع؛ هذا ليس دمج مصادر تلقائيًا.
- اختبارات جديدة كشفت تكرار الحفظ عند تكرار التأكيد. أصلح بحارس داخل نفس مثيل الاستيراد وإعادة فحص المعاينة؛ بصمة الملف أصبحت مرتبطة بالحساب مع دعم البصمات القديمة.
- التزام متزامن من جهازين مختلفين ليس مضمونًا بمعاملة موزعة؛ الحارس المحلي لا يثبت ذلك.
- APK 1.1، versionCode=2، app.masroufy.personal. نجح البناء والتحقق من توافق شهادة التوقيع مع APK السابق وزيادة رقم الإصدار.
- مفتاح النسخة التجريبية الأصلي نُسخ خارج الريبو إلى Documents/Codex/android-build-tools/signing/masroufy-trial.keystore. لا ترفعه إلى Git ولا تولّد بديلًا للتحديث.
- scripts/build-android.ps1 يتحقق من توقيع APK، ويمكنه مقارنة -PreviousApk للتأكد من الشهادة والهوية وزيادة الإصدار. مخرج البناء dist-android-apk/masroufy-trial-v2.apk.

### ما لم يثبت أو لم يكتمل
- لا هاتف متصل؛ لم يُختبر READ_SMS ولا التثبيت فوق نسخة موجودة فعليًا على S24 Ultra.
- اختبار الواجهة اقتصر على فتح خيار الرسائل واللصق في معاينة demo؛ لم يكتمل اختبار حفظ رسالة من الواجهة. اختبارات حالات الاستخدام نجحت كما هو موضح في نتيجة الإغلاق.
- دخول جوجل الأصلي لأندرويد، النسخة الاحتياطية الشاملة، البحث الشامل، الأقساط، التنبيهات أثناء إغلاق التطبيق وشعارات المتاجر باقية.
- مخزن البيانات ما زال Firestore؛ لا ادعاء بأن كل التطبيق يعمل دون إنترنت.
- لم تُنشر هذه الشريحة على Firebase ولم تُعدّل بيانات حساب المالك.
- متابعة لاحقة: توسيع صيغ الرسائل بأمثلة منزوعة البيانات الحساسة، اختبار إلغاء الإذن وحفظ/استيراد فعلي على الهاتف، ثم باقي الميزات.

### طريقة تحديث المالك
ينقل APK الجديد إلى الهاتف ويفتحه ويختار «تحديث» دون حذف مصروفي.
نفس الهوية وشهادة التوقيع ورقم إصدار أعلى تسمح بالتحديث الموضعي.
تحديث الموقع وحده لا يحدث الملفات المضمّنة في APK. لا تحديث APK تلقائي حاليًا.
إذا رفض أندرويد التحديث، لا يُنصح بحذف التطبيق؛ افحص الشهادة والإصدار والرسالة أولًا.

### نتيجة فحص الإغلاق
نجح typecheck وكل 459 اختبارًا في 35 ملفًا. بعد تصحيح حجب أرقام الحسابات المقسمة بمسافات/شرطات نجحت اختبارات الرسائل الـ17، بما فيها الاختبار الجديد (460 حالة إجمالًا). إعادة بناء APK تمت للتحقق من نفس شهادة النسخة السابقة. اختبار الهاتف ما زال غير منفذ.

## 17. خطة إصلاح جودة المنتج — جارٍ التنفيذ (2026-09-10)
هذا القسم هو نقطة الاستكمال الحالية. اقرأه قبل الأقسام السابقة.
### شكاوى المالك
1. فتح التطبيق ثقيل كأنه يعيد تحميل البرنامج كل مرة، وكذلك التنقل.
2. الحركات والتفاصيل غير سلسة وتعطي إحساس نموذج أولي غير مكتمل.
3. ظهور «غير متاح» رغم تسجيل الدخول ووجود عمليات في الحساب.
4. يريد ترتيب الإصلاحات وتوثيق كل خطوة بما يكفي لاستلام شخص آخر فورًا.

### ترتيب التنفيذ ومعايير القبول
- P1 — جارٍ: إصلاح تمرير سقف الميزانية المحفوظ للرئيسية، وشرح حالات عدم توفر الأرقام مع إجراء واضح. لا استنتاج للدخل/المصروف من اتجاه التحويل. اختبار سقف محفوظ وتعديله/حذفه وعدم خلط الشهور.
- P2 — لم يبدأ: عرض بيانات الفترة الأساسية دون انتظار تاريخ ستة أشهر، تحميل الشاشات عند الحاجة، الاحتفاظ بنتائج الزيارة داخل الجلسة، وعدم إعادة تحميل الأقسام غير المفتوحة بعد كل تعديل. التعامل مع تغيير الفترة والطلبات القديمة وأخطاء الشبكة بوضوح. لا تقديم بيانات قديمة كأنها محدثة.
- P3 — لم يبدأ: تحسين الانتقالات واستجابة الضغط وشاشة التحميل ومساحات الهاتف، مع احترام تقليل الحركة. لا ادعاء معدل إطارات دون قياس هاتف فعلي.
- P4 — التسليم: اختبار مناسب لكل شريحة وتحديث هذا السجل ثم commit. بناء APK برقم أعلى وبنفس التوقيع، وتوضيح ما اختُبر وما بقي. نشر الويب بعد نجاح الاختبارات حتى لا تبقى النسخة المنشورة مختلفة عن الإصلاحات.

### خط أساس
آخر commit قبل الإصلاح: b29ca9c. اختبارات سابقة 460 حالة، وأحدث APK 1.1 / versionCode 2.
الموقع لا يزال إصدارًا أقدم. لا توجد قياسات هاتف، ولا هاتف متصل وقت آخر فحص.
أي انقطاع: اقرأ git status والجزء التالي قبل استكمال P1. لا تبدأ بباقي الميزات قبل معالجة شكاوى الأساس.

### P1 — مكتمل ومختبر
- loadHomeScreen يقرأ BudgetRepository للفترة المختارة؛ رُبط مستودع الميزانيات في container وdemoContainer. خيار الاختبارات القديم budgetLimitMinor يظل احتياطيًا حين لا يمرر مستودع، ولا يتجاوز الميزانية المحفوظة في التشغيل.
- تغير عرض المصروف المجهول إلى «بانتظار المراجعة» والمتبقي إلى «يحتاج مراجعة»، مع تأكيد وجود العمليات وشرح النوع الاقتصادي وإجراء المراجعة الموجود. لم تتغير الحسابات أو يُفترض نوع من اتجاه الحركة.
- الاختبار الجديد فشل قبل التعديل (null بدل 30000 هللة)، ثم نجح للسقف المحفوظ وتعديله وحذفه وفترة أخرى. typecheck و26 اختبار Home/Budget نجحوا.
- الخطوة التالية P2: فصل التاريخ عن جاهزية الرئيسية، وتحميل التبويبات عند الطلب مع حفظ نتائج الجلسة. لم تُبنَ أو تُنشر حزمة جديدة بعد P1.
### P2 — مكتمل: فصل مسار الفتح وحفظ نتائج الزيارة
- الرئيسية تحمل الفترة الحالية أولًا؛ loadHomeHistory يكمل آخر خمس فترات دون إعادة قراءة الحالية. الأشخاص والاستثمار والميزانية والتنبيهات تحمل عند الطلب. ScreenRequests يحتفظ بنتائج الجلسة ويجمع الطلبات المتطابقة، ويمنع الطلب القديم من ملء الذاكرة بعد إبطالها.
- التعديل يبطل الذاكرة ويحدث الشاشة الحالية والرئيسية فقط. تحديث يدوي، وتحديث بعد عودة التطبيق من الخلفية لأكثر من 30 ثانية. لا توجد مزامنة لحظية مضمونة بين الأجهزة.
- HomeSnapshotPort يحتفظ بعرض الرئيسية في localStorage لكل مستخدم وفترة ولليوم UTC نفسه فقط. يظهر تاريخ العرض المحفوظ بوضوح أثناء التحديث، ويُمسح عند الخروج أو التعديل؛ ليس مصدر حساب أو نسخة احتياطية. فشل التخزين اختياري وغير معطل.
- البيانات الموجودة لا تختفي خلف خطأ تحديث الرئيسية؛ يظهر الخطأ معها. تحميل الأشخاص والتنبيهات يميز الانتظار عن القائمة الفارغة.
- typecheck وكل 467 اختبارًا في 36 ملفًا نجحوا. اختبارات جديدة لتبديل الفترة والإبطال والطلب القديم وعزل المستخدمين وفصل التاريخ. لا قياس أداء على S24 ولا نشر بعد.
- التالي P3: التحميل الهيكلي، استجابة الضغط، حركة دخول خفيفة ومساحات الهاتف، ثم البناء والنشر.

## 18. مراجعة الاستلام — 2026-09-10 14:00 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.** مراجعة فقط — لم يُعدَّل أي كود، ولم يُرفع أو يُنشر شيء.

### بروتوكول العمل (قرار المالك)
أكثر من نموذج AI يشتغل بالتناوب (نموذج آخر يستكمل 15:30). بعد كل خطوة تستاهل: حدّث آخر قسم هنا + commit واضح. التفاصيل: `CLAUDE.md` قاعدة 17.

### ما اتفحص فعلًا (بأوامر، مش من الذاكرة)
- `npm test`: **467 ناجح من 467، في 36 ملف**. `npm run typecheck`: نضيف (exit 0).
- معايير القبول على الكشف الحقيقي (CSV وPDF): خضراء — `realStatement` و`alrajhiPdfReal` و`reconcileUseCase` (صفر فروق من 1912). ملفات `files/` موجودة على الجهاز.
- لا ملف حساس متتبَّع في Git (`files/`، `*.pdf`، keystore، `.env`). أرقام البطاقة/الحساب في `tests/acceptance/bankSms.test.ts` وهمية (غير موجودة في `files/`)، والـIBAN هو المثال القياسي `SA0380000000608010167519`.
- `android/app/build.gradle` فيه كلمة السر `android` — دي كلمة debug keystore الافتراضية العامة؛ ملف المفتاح نفسه خارج الريبو. **مقبول للتجربة فقط، لا لنشر على المتجر.**
- لا `parseFloat` في `src/`. `toFixed` مستعمل للنِّسب المئوية في العرض فقط.
- ملفات فوق 300 سطر: خمسة ملفات **اختبار** فقط (أكبرها `importFixtures.test.ts` 367). لا ملف مصدر.

### ⚠️ حالة Git والنشر — مهم قبل أي push
- `main` المحلي **متقدم 4 كوميتات** (c412a65، b29ca9c، a34ac18، 01a5958 — كلها غير مرفوعة) و**متأخر 2** عن `origin/main` (9a3cd35، f58432b — تحديثات أسعار من `masroufy-price-bot`، تعدّل `public/prices.json` فقط). الفرعين متباعدين (diverged).
- الحل المتوقع: `git pull --rebase origin main` ثم `git push`. **لم يُنفَّذ — ينتظر موافقة المالك.** افحص `npm test` و`typecheck` بعد الـrebase.
- **الموقع المنشور قديم:** https://masroufy-d4b96.web.app يخدم `assets/index-Bem5kpeV.js` (نشر 09-08). يعني P1 وP2 ولصق الرسائل **غير منشورين**. البناء المحلي `dist/` فيه `index-D572vMX4.js`.
- APK 1.1 (versionCode 2) موجود محليًا `dist-android-apk/masroufy-trial-v2.apk` (10:30). **لم يُثبَّت على أي هاتف.** ولم يُبنَ بعد P1/P2 — فهو لا يحتوي إصلاحاتهما.

### تعارضات موثقة لم تُحسم
1. **قراءة SMS:** `OVERRIDES.md` §8 ما زال ينص «التطبيق لن يقرأ الرسائل» والمسار Web Share Target. التنفيذ الحالي يقرأ بإذن `READ_SMS` عند الطلب (مسجل في `ARCHITECTURE.md` §22 و`OVERRIDES` سطر ~267 «SMS future work» فقط). و`ANDROID.md` سطر 35 قديم («READ_SMS not declared»). **سُئل المالك: هل يعتمد READ_SMS؟ — لا تحسمه بنفسك.**
2. بند `سداد` (17,233.19) — `ARCHITECTURE.md` §9.6، مفتوح من قبل.

### أهم النواقص (بترتيب اقتراحي — المالك لم يوافق على الترتيب بعد)
1. **توصيل P1+P2 للمالك والتجربة على S24:** rebase + push، نشر الويب، بناء APK برقم أعلى (versionCode 3) بنفس التوقيع، والمالك يجرّب ويقيس إحساس الفتح. بدون ذلك P3 تحسين غير مقاس.
2. **النسخة الاحتياطية ناقصة** (§14): لا تشمل الأشخاص/الديون/الأصول/الوسوم/الاشتراكات — خطر ضياع بيانات، خصوصًا قبل إهداء التطبيق.
3. P3 (نعومة الحركة والضغط) — لم يبدأ.
4. دخول جوجل على أندرويد — غير مكتمل.

### أسئلة مستنية المالك
- هل الأولوية = البند 1 أعلاه، أم ترتيب آخر؟
- هل يعتمد قراءة الرسائل بـ`READ_SMS`؟ (نعم ⇒ يُحدَّث `OVERRIDES` §8 و`ANDROID.md`؛ لا ⇒ تُزال.)
- موافقة على push ونشر الموقع.

### الخطوة الجاية للي بعدي
~~لو المالك ما ردّش: لا push ولا deploy ولا تعديل §8.~~ — **المالك ردّ، انظر §19.**

## 19. رد المالك والرفع — 2026-09-10 15:25 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.** الجلسة قُفلت 15:25 بطلب المالك (نموذج آخر يستكمل 15:30).

### قرارات المالك
- «قراءة الرسايل مهمة الأول» ⇒ `READ_SMS` عند الطلب **معتمد** و**الأولوية الأولى** قبل P3. سُجّل في `OVERRIDES.md` §16 (ونُبّه في §8 أنه تعدّل)، وصُحّح `ANDROID.md`.
- «ارفع على GitHub أحدث حاجة» ⇒ اتعمل.
- **النشر على الموقع (deploy) لم يُطلب — لم يُنفَّذ.** الموقع ما زال `index-Bem5kpeV.js` (09-08).

### ما اتعمل واتفحص
- `git pull --rebase origin main`: دمج كوميتي الأسعار بلا تعارض. بعده `npm test` 467/467 في 36 ملف، و`typecheck` نضيف.
- فحص محتوى الرفع: لا `files/` ولا PDF ولا keystore ولا `.env` ولا APK.
- `git push origin main`: رُفعت 6 كوميتات (4 القديمة + مستندات المراجعة + قرار الرسائل). ⚠️ الـrebase غيّر معرّفات الكوميتات القديمة (c412a65→1a34539، b29ca9c→d7c3939، a34ac18→88243ca، 01a5958→45f37cd).

### لم يُعمل / لم يُتجرَّب
- لا تعديل كود على الرسائل في هذه الجلسة. قيود §16 كما هي: لا تجربة `READ_SMS` على هاتف، ولا حفظ رسالة كامل من الواجهة، والصيغ المدعومة محدودة.
- APK الحالي (v2) مبني **قبل** P1/P2.

### الخطوة الجاية المقترحة (أولوية الرسائل)
1. بناء APK برقم أعلى (versionCode 3) بنفس التوقيع عبر `scripts/build-android.ps1 -PreviousApk dist-android-apk/masroufy-trial-v2.apk` (الأدوات والمفتاح خارج الريبو، §15 و§16) — ليجرّب المالك قراءة الرسائل على S24 ومعها إصلاحات P1/P2.
2. تجربة كاملة في وضع demo (`?demo=1`): لصق رسالة ← معاينة ← تأكيد ← ظهورها في العمليات، مع لقطة شاشة.
3. طلب **عينات رسائل حقيقية منقّحة** من المالك (بلا أرقام حسابات) لتوسيع صيغ `src/infrastructure/import/bankSmsParser.ts` باختبارات.

### أسئلة مستنية المالك
- ~~الاستقبال التلقائي؟ هل ينشر الموقع؟~~ — **اتحسموا، انظر §20.**

## 20. نشر الموقع وقرار الرسائل التلقائية — 2026-09-10 15:33 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.**

### نشر الموقع — اتعمل واتفحص
- بطلب المالك «انشر الموقع». البناء من commit `367d3a7` بشجرة نضيفة: `npm run build` نجح (فيه typecheck).
- `npx firebase deploy --only hosting` ⇒ «Deploy complete». التحقق: https://masroufy-d4b96.web.app يرد HTTP 200 ويخدم `assets/index-CmvEGKu5.js` (القديم كان `index-Bem5kpeV.js`). يعني P1 وP2 ولصق الرسائل **منشورين على الويب الآن**.
- لم تتغير قواعد Firestore ولا بيانات أي حساب. لم تُجرَّب الواجهة المنشورة بتسجيل دخول حقيقي.
- تحذير البناء القديم باقٍ: حزمة أكبر من 500 KB (مكتبة PDF) — ليس خطأ.

### قرار الرسائل التلقائية — `OVERRIDES.md` §17 (اقرأه قبل أي كود)
- المالك: إذن **مرة واحدة**، ثم القراءة **تلقائية دائمًا** بلا سؤال متكرر ⇒ `RECEIVE_SMS` + `READ_SMS` لما فات.
- سُئل: «بعد القراءة يعمل إيه؟» فاختار **«تستنى مراجعتي»**. لا حفظ عملية بلا تأكيد، قائمة «رسائل جديدة» مع تأكيد فردي أو جماعي عبر مسار المعاينة ومنع التكرار.
- **لم يُبنَ منه شيء بعد.** الموجود فقط القراءة عند الطلب (§16).

### الخطوة الجاية (الأولوية الأولى = هذه الميزة)
1. تصميم شريحة رأسية: `BroadcastReceiver` أصلي بـ`RECEIVE_SMS` يحفظ رسائل البنك المعروفة فقط **على الهاتف** (طابور محلي، بعد الحجب)، وعند فتح التطبيق: قراءة الطابور + `READ_SMS` منذ آخر قراءة ⇒ قائمة مراجعة ⇒ نفس `importStatement`. زر إيقاف في الإعدادات. شاشة الإذن مرة واحدة.
2. اختبارات أولًا على المحلل والطابور (fixtures)، ثم `npm test` و`npm run typecheck`.
3. بناء APK بـversionCode 3 بنفس التوقيع (`scripts/build-android.ps1 -PreviousApk dist-android-apk/masroufy-trial-v2.apk`) ليجرّب المالك على S24.
4. **قيل للمالك:** والتطبيق مقفول الرسالة تظهر للمراجعة عند الفتح، وأندرويد قد يوقف التطبيق في الخلفية — لا ادعاء استقبال كامل قبل تجربة الهاتف.

### أسئلة مستنية المالك
- عينات رسائل بنك حقيقية منقّحة (بلا أرقام حسابات) لتوسيع الصيغ. لم تُطلب منه بعد صراحة.

## 21. استكمال الرسائل التلقائية — جارٍ (Codex، 2026-09-10)
المالك سمح باستكمال العمل بعد مراجعة تسليم كلود. طلب عدم تعطيل العمل انتظارًا لقرار؛ وثّق ما يتوقف على الهاتف/العينات واستكمل الأجزاء المستقلة. الأولوية §20 وOVERRIDES §17.
- الخطة: طابور أندرويد محلي مع حجب البيانات الحساسة، استقبال RECEIVE_SMS وقراءة ما فات منذ التفعيل، مراجعة فردية/جماعية بلا حفظ تلقائي، مفتاح تشغيل/إيقاف، عزل حسابات المستخدمين، اختبارات ثم APK أعلى بنفس التوقيع.
- اكتشاف أولي: ImportSheet كان يبدأ content فارغًا حتى مع initialSms، فيعطل معاينة الرسائل؛ تم تمرير محتواها الابتدائي. لم يُختبر بعد.
- لا نشر ولا APK جديد في هذه النقطة. لا هاتف متصل أو عينات جديدة. لا تدّع اكتمال استقبال الخلفية قبل تجربة الهاتف.

### شريحة الرسائل — التنفيذ والفحص
- أضيف SmsInboxPlugin وBankSmsReceiver مع RECEIVE_SMS؛ الاستقبال من أسماء محددة فقط، وتجميع الأجزاء، وحجب الحسابات/البطاقات قبل التخزين الخاص على الهاتف. قاعدة SQLite مقسمة بـuid. تظل بصمة الرسالة بعد المراجعة ويُزال نصها. لا حفظ مالي في الخلفية.
- تفعيل صريح مرة واحدة عند منح الأذونات، بلا مطالبة تلقائية عند الفتح أو عند سحب الإذن. أول تفعيل وإعادة التفعيل بعد الإيقاف يبدأان من الآن؛ الرسائل القديمة لها استيراد الفترة اليدوي. الخروج يوقف الجمع؛ إعدادات المرسل والرسائل المعلقة تبقى للحساب نفسه.
- الاستكمال عند الفتح/العودة وكل دقيقة أثناء الظهور: حتى 500 رسالة بكل دورة، بمؤشر تاريخ ومعرّف يمنع فقد رسائل لها نفس التاريخ. الواجهة تعرض حتى 200 معلقة دون حذف الباقي. اختلاف وقت الرسالة بين مزودي أندرويد قد يظهرها مرتين في المراجعة؛ مسار الاستيراد يفحص التشابه والتكرار قبل التأكيد.
- شاشة «رسائل جديدة» متاحة من كل تبويب، بما فيه الإعدادات، فيها التشغيل والإيقاف والاختيار الفردي والجماعي واستبعاد رسالة دون تسجيل. المعاينة القديمة نفسها تُستخدم لاختيار المحفظة وفحص التكرار.
- أصلح content الابتدائي في ImportSheet. أصلح أيضًا فصل التاريخ العربي عن اسم التاجر وحجب المبالغ الكبيرة خطأً مع المحافظة على حجب الحسابات.
- نجح typecheck و472 اختبار TypeScript في 37 ملفًا، و4 اختبارات JVM لـSmsSafety. أول بناء Java فشل بسبب استثناء JSArray وتم إصلاحه؛ إعادة البناء/الاختبارات نجحت.
- تجربة واجهة demo فعلية: لصق 25.50 ← معاينة ← تأكيد ← ظهور العملية؛ ثم صندوق 3 رسائل مصطنعة: حفظ واحدة فقط أبقى رسالتين (واحدة صالحة وأخرى بلا تاريخ). لا تعديل لبيانات المالك، وبيانات demo مستبعدة من إنتاج Vite.
- لم يُختبر Receiver/SQLite على Android runtime؛ نجاح JVM والبناء لا يثبت عمل الصلاحيات أو وصول الخلفية على S24. لا توجد عينات بنك جديدة. هذه أعمال اختبار الهاتف التالية وليست سببًا لتعطيل تحسينات الواجهة المستقلة.
- التالي: شريحة P3 صغيرة للتحميل والضغط ومساحات الهاتف، ثم APK 1.2/code3 بنفس مفتاح 1.1. لا APK جديد ولا نشر لهذه الشريحة حتى الآن.

### P3 — تغييرات العرض، قيد التحقق
- LoadingSkeleton للرئيسية بدون أرقام مختلقة، وشاشة بدء بهوية مصروفي، حركة دخول قصيرة opacity/transform واستجابة للضغط، وتنسيق حالة التحديث.
- شريط تنقل ثابت ومسافة محجوزة أسفل المحتوى، منع تمرير الخلفية عند فتح ورقة وتقييد تمريرها، وتكييف رأس الشاشة للعروض الضيقة. احترام prefers-reduced-motion بإلغاء الحركات.
- لم تُضف مكتبة. لا قياس FPS أو ادعاء اكتمال تجربة تطبيق أصلي. اختبار العرض والبناء هو التالي.
- رُفع تعريف Android إلى 1.2/code3 استعدادًا للبناء؛ هذا التعريف وحده لا يعني وجود APK جاهز.
- فحص متصفح بعرض 412×915: لا تجاوز أفقي (scrollWidth=clientWidth=412)، الشريط السفلي ينتهي عند 915، وفتح الرسائل يوقف تمرير الخلفية. نُقلت إعدادات الجمع إلى تفاصيل قابلة للفتح حتى تظهر الرسائل للمراجعة أسرع. هذه معاينة متصفح وليست اختبار S24.
- typecheck نجح بعد آخر تعديل للواجهة؛ بناء الويب نجح قبل نقل قسم الإعدادات داخل details (آخر تعديل عرض فقط وفُحص بالأنواع). لا FPS مقاس. التالي بناء Android النهائي والتحقق من التوقيع.

### تسليم APK 1.2 — جاهز للتجربة
- آخر commit كود: a3adcc9 (بعد شريحة الرسائل 4ff3c06). android:sync وassembleDebug نجحا. لا تغير فعلي في ملفات Gradle المولدة؛ قد يظهر اختلاف نهايات أسطر فقط بعد sync.
- الملف: dist-android-apk/masroufy-trial-v3.apk، هوية app.masroufy.personal، versionCode=3، versionName=1.2.
- SHA256: 6732810F3A1DA3DF1C678997C0D24CA7274F2AE5FB98BC7A9E5B40A478DC752D.
- سكربت البناء تحقق من نفس شهادة v2 ونفس هوية التطبيق وزيادة versionCode. فحص manifest يؤكد READ_SMS وRECEIVE_SMS. الحزمة تستخدم ملفات محلية ولا server.url.
- تحقق TypeScript: 472 اختبارًا/37 ملفًا، وفحص الأنواع ناجح. Android: 4 اختبارات JVM ناجحة؛ بناء APK ناجح. لا ادعاء اختبار SQLite/Receiver داخل نظام Android أو تثبيت فعلي.
- التسليم للمستخدم: افتح APK واختر تحديث دون حذف النسخة السابقة. من «رسائل جديدة ← إعداد القراءة التلقائية وإيقافها» اكتب اسم المرسل المطابق ثم وافق وامنح إذن أندرويد. التفعيل الأول يبدأ من الآن؛ للقديم استخدم القراءة اليدوية بالتاريخ.
- لا push ولا deploy لهذه الجولة. آخر موقع منشور لا يزال نسخة كلود في §20، وAPK الجديد مستقل عنها.

### الخطوة التالية لمن يستلم
1. تجربة S24: التحديث فوق 1.1، منح/رفض/سحب الإذن، رسالة أثناء الفتح ثم أثناء الإغلاق، إعادة فتح التطبيق، رسالة متعددة الأجزاء، الإيقاف وإعادة التفعيل، وحفظ جزئي وإعادة فتح. فحص حسابين وخروج ثم دخول، ومؤشر استكمال عند أكثر من 500 رسالة. الاختبارات المحلية لا تغني عنها.
2. عينات رسائل بنك منقّحة لتوسيع الصيغ؛ الصيغ الحالية تشترط ريالًا ومبلغًا وتاريخًا واتجاهًا واضحين. لا تخمّن من رسالة ناقصة.
3. بقية النواقص المستقلة: النسخة الاحتياطية الشاملة (العلاقات والأشخاص والأصول والوسوم والاشتراكات)، دخول Google الأصلي، البحث عبر كل الكيانات، الأقساط وشعارات المتاجر. تحسينات P3 الحالية أساسية ولا تعني انتهاء كل تفاصيل جودة المنتج.
4. لا تغيّر قرار «المراجعة قبل التسجيل»، ولا مفتاح التوقيع، ولا بيانات المالك. وثّق كل شريحة واختبارها وحدودها ثم commit. إذا احتجت الهاتف/العينات، سجّل ذلك واستكمل جزءًا مستقلًا كما طلب المالك.

## 22. النسخة الاحتياطية الشاملة — جارٍ
المالك طلب الاستمرار وسأل عن APK؛ أُعطي رابط 1.2 مع التحديث دون حذف. هذه الشريحة لم تدخل APK 1.2.
- أضيف مسار schemaVersion=2 مستقل يغطي 21 مجموعة في حساب Firestore، بقراءة صفحات 200 من الخادم (لا كاش ناقص)، بلا حد خمس سنوات/200 استيراد. لا أذونات أو رسائل محلية في نسخة الحساب.
- SHA256 لفحص التلف، مع فحص المبالغ الصحيحة والعملات والعلاقات. الدمج لا يكتب فوق الموجود، ويعيد ربط المعرفات عند تكرار العملية. يحافظ على عمليتين متماثلتين أصلًا داخل النسخة، ويفصل العملات.
- الاستعادة دفعات صغيرة تعيد فحص وجود المعرّفات داخل Firestore transaction؛ يمكن أن تنقطع بين الدفعات وتُستكمل بإعادة الملف. التحذير ظاهر قبل التأكيد. لا ادعاء لقطة متزامنة بين جميع المجموعات أو ذرية الحساب كله.
- الشاشة موصولة بمسار النسخة الشاملة، والاستعادة القديمة متاحة منفصلة. فحص TypeScript/اختبارات الاستعادة الجارية هي الخطوة التالية. لا نشر/بناء نسخة جديدة بعد.

## 23. استلام النسخة الشاملة واكتشاف عطل المعرّفات — 2026-09-10 23:45 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.** Codex وقف في §22 بسبب حد الاستخدام، وشغله كان غير محفوظ في Git.

### ما اتفحص في شغل §22
- كما تركه Codex: 486 اختبارًا/39 ملفًا ناجحة، وtypecheck نضيف. قرأت كل ملفات النسخة الشاملة (13 جديدًا + 7 معدّلة).
- أسماء المجموعات الـ21 مطابقة لمستودعات Firestore، وقواعد Firestore تغطيها (`users/{uid}/{document=**}`). معرّفات `notificationReceipts` بنفس ترميز `notificationRepository`، و`budgets` بمعرّف الفترة، و`assetPrices` بـ`assetId`. الحقول المطلوبة في الفحص موجودة كلها من المرحلة الأولى (git log -S).
- كوميت شغل Codex منفصل عن إصلاحي (انظر git log).

### 🔴 عطل حقيقي في التشغيل — مُثبت على الكود الحقيقي
- `sanitize` في `firestoreRepositories.ts` كان يقص أي 5 أرقام متتالية في **كل** حقل نصي، بما فيها `id` والروابط، للعمليات وسجلات المصدر ودفعات الاستيراد. مسار الوثيقة كان بالمعرّف الأصلي، والحقل بالمقصوص.
- قياس على نسخة من `RandomIdGenerator`: **~30% من المعرّفات المولَّدة تتغير** (مثال `txn-0mtqh4w00000-…` ⇒ `txn-0mtqh4w****0000-…`). ومعرّفات التجار المزروعة `merch-00001` كلها فيها 5 أرقام ⇒ `merchantId` على العملية لا يطابق وثيقة التاجر أبدًا.
- الأثر المستنتج من الكود: الشاشة تقرأ `id` المقصوص، و`update(id)` يكتب لمسار غير موجود ⇒ **تعديل/تصنيف/تحديد نوع ~30% من العمليات يفشل**. نفس الشيء لتحديث حالة الدفعة (`updateState`). المحافظ (`wallet-bank`/`wallet-cash`) والتصنيفات (`cat-…`) غير متأثرة. الشاشات لا تستعمل `merchantId` فخلله غير ظاهر.
- **الإثبات:** `tests/acceptance/firestoreIdIntegrity.test.ts` (3 اختبارات بمستودع Firestore الحقيقي مع mock): فشلت الثلاثة قبل الإصلاح بالقيم المقصوصة، ونجحت بعده.
- **الإصلاح (للكتابات الجديدة فقط):** `id` وأي حقل ينتهي بـ`Id` يُحفظ كما هو؛ النص الحر ما زال يُقص (`rawDescription`/`rawLine`/`accountIdentity` — مثبت في نفس الاختبار و`privacy.test.ts`). بعده: **489 اختبارًا/40 ملفًا ناجحة، typecheck نضيف.**
- **لم يُتحقق على حساب المالك** (لا دخول ولا قراءة لبياناته). لم يُعدَّل أي مستند في Firestore.

### ⚠️ ما بقي بسبب البيانات القديمة — لا تتجاهله
1. **البيانات المحفوظة قبل الإصلاح ما زالت تالفة** في Firestore: حقول `id` مقصوصة لجزء من العمليات، و`merchantId` مقصوص على كل عملية مرتبطة بتاجر. الإصلاح لا يلمسها. التعديل على تلك العمليات سيظل يفشل.
2. **النسخة الشاملة ستُرفض على الأرجح في حساب المالك الحالي:** `checkFullBackupData` يرفض رابط `transactions.merchantId` الذي لا يطابق تاجرًا، ويرفض أي روابط أخرى غير متطابقة. مستنتج من الكود، غير مجرّب على حسابه.
3. الحل المقترح (**يحتاج موافقة المالك — يعدّل بياناته**): هجرة إصلاح لمرة واحدة تقرأ من الخادم، وتكتب `id` = معرّف الوثيقة، وترد `merchantId`/روابط العمليات للأصل بمطابقة `sanitizeAccountNumbers(الأصل) === المقصوص` مع رفض أي تطابق غير وحيد، بمعاينة عدد المتأثر قبل التأكيد، وبعد تصدير CSV احتياطي. **لم يُبنَ.**

### لم يُعمل
- لا push (3 كوميتات Codex + كوميتات هذه الجولة محلية فقط)، لا نشر ويب، لا APK. APK 1.2 (§21) **لا يحتوي** الإصلاح ولا النسخة الشاملة.
- لم تُجرَّب النسخة الشاملة من الواجهة ولا على Firestore حقيقي.

### أسئلة مستنية المالك
- موافقة على بناء هجرة إصلاح البيانات القديمة (بند 3)؟
- رفع على GitHub؟ نشر الموقع؟ (الإصلاح مهم للتعديل على الويب.)

## 24. بلاغ «مفيش ولا عملية ظاهرة» على APK 1.2 — 2026-09-11 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.**
المالك: فتح APK 1.2، لا عمليات ظاهرة، وعند تحميل الكشف قال «مسجل آلاف العمليات» ولم تظهر أيضًا. وافق على: إصلاح البيانات القديمة + push + نشر.

### ما اتفحص
- **منطق الاستيراد والعرض سليم:** `tests/acceptance/importVisibility.test.ts` يستورد الكشف الحقيقي عبر demoContainer (ذاكرة) ثم `loadTransactionsScreen`/`loadHomeScreen` لفترة 2026-08-28: العمليات تظهر، والتاريخ YYYY-MM-DD، وإعادة التحميل = «مسجل قبل كده». يعني العطل **ليس** في منطق العرض ولا في P3.
- **عطل فتح التطبيق مُعاد إنتاجه:** `useAppData.prepare()` يشغّل `resumeStagedBatch.cleanupAll()` مع الزرع قبل **كل** الشاشات. دفعة `staged` معرّفها المحفوظ مقصوص (عطل §23) ⇒ `findById` لا يجدها ⇒ رمي «دفعة غير موجودة» ⇒ **كل الشاشات تفشل**. نفس الاختبار فشل قبل الإصلاح بهذه الرسالة بالضبط.
- وجود دفعة معلّقة يفسّر أيضًا «مسجل آلاف»: سجلات مصدرها موجودة فتُعلَّم صفوف الكشف مكررة.

### ⚠️ غير مُثبت على هاتف المالك
- لم أرَ حسابه ولا شاشته. **سبب بديل وارد:** الدخول على APK بالإيميل وكلمة السر قد يكون **حسابًا مختلفًا** عن حساب جوجل على الويب (ANDROID.md). سُئل المالك: أي طريقة دخول على الجوال والويب، وصورة للشاشة ونص أي رسالة.

### الإصلاح
- `cleanupAll`: دفعة تعذر تنظيفها تُسجَّل بـ`error` وتُترك معلّقة بلا حذف، ولا توقف الباقي.
- `useAppData`: فشل التنظيف لا يمنع فتح الشاشات؛ رسالة واضحة أن استيرادًا سابقًا لم يُنظَّف وأن إعادة الاستيراد قد تعتبر الصفوف مكررة حتى إصلاح البيانات.
- بعده: **491 اختبارًا/41 ملفًا ناجحة، typecheck نضيف**، لا أخطاء console في demo.

### التالي
1. push + نشر الويب (موافَق). 2. APK 1.3/code4 بنفس المفتاح — **الإصلاح لا يصل للجوال بدونه**. 3. هجرة إصلاح البيانات القديمة (§23 بند 3) بمعاينة وتأكيد، وتشمل الدفعات المعلّقة المقصوصة.
→ **كلها اتعملت — انظر §25.**

## 25. إصلاح البيانات القديمة + APK 1.3 + نشر — 2026-09-11 00:30 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.**

### ما اتبنى (commit e58510a)
- `domain/idRepair.ts` (دالة نقية): يرد حقل `id` لمعرّف الوثيقة **فقط** لو `redact(docId) === id` (للعمليات/سجلات المصدر/الدفعات)، ويرد أي رابط في `BACKUP_RELATIONS` **فقط** لو معرّف حقيقي واحد بالضبط يعطي نفس القيمة بعد القص. غير ذلك يُترك ويُعرض في `unresolved`. يعرض الدفعات `staged`.
- `repairStoredIds`: `preview()` قراءة فقط + نسخة المستندات المتأثرة قبل التعديل؛ `apply(plan)` يعيد القراءة ويكتب **فقط** ما ظهر في المعاينة بنفس القيم (الباقي `skipped`).
- Firestore: قراءة صفحات 200 من الخادم بترتيب `documentId` مع الاحتفاظ بـ`doc.id`، وكتابة `update` فقط بدفعات 400. ذاكرة: للاختبار والمعاينة.
- الإعدادات ← «التصدير والنسخة الاحتياطية» ← **«افحص البيانات القديمة»**: معاينة بالأعداد، تحذير الدفعات المعلّقة، ثم «احفظ نسخة وصلّح» يحفظ `masroufy-before-repair-<date>.json` على الجهاز **قبل** الكتابة. الإلغاء لا يُعطَّل.
- `scripts/build-android.ps1`: Windows PowerShell 5.1 كان يعتبر أي stderr من npm (تحذير حجم Vite) خطأً قاتلًا فيفشل البناء. الأوامر الخارجية تُحكم الآن بـ`$LASTEXITCODE` فقط عبر `Invoke-Native`.

### الفحص
- `idRepair.test.ts` (6): التلف مصنوع بـ`sanitizeAccountNumbers` الحقيقية على معرّفات بصيغة `RandomIdGenerator`؛ الرد الصحيح، عدم التخمين عند التطابق المتعدد، عدم لمس اختلاف غير مفسَّر، عرض المعلّق، كتابة المعاينة فقط، **ونفس الحساب ترفضه فحوص النسخة الشاملة قبل الإصلاح وتقبله بعده**. `idRepairFirestore.test.ts` (2).
- **499 اختبارًا/43 ملفًا ناجحة، typecheck نضيف.** زر الفحص في demo يعمل بلا أخطاء console (محوّل demo فارغ: «اتفحص 0 مستند» — لا يثبت شيئًا عن بيانات حقيقية).

### التوصيل
- **الويب منشور** ويخدم `index-Ck1YN7ZK.js` (HTTP 200، مطابق للبناء المحلي). فيه: إصلاح القص، عدم توقف الشاشات، زر الإصلاح.
- **APK 1.3** (versionCode 4): `dist-android-apk/masroufy-trial-v4.apk`، SHA256 `C1EDD0CAAD0DA43A0C5EBC9C79367D79D9A0D0B5ADCF4D689D596850880103C4`. السكربت تحقق من نفس شهادة v3 ونفس `app.masroufy.personal` وزيادة versionCode (يرمي غير ذلك). أُرسل للمالك.
- لا تغييرات Git من البناء (فروق نهايات أسطر فقط).

### ⚠️ غير مُثبت
- **لم يُشغَّل الإصلاح على حساب المالك**، ولم يُثبَّت APK 1.3 على جهاز. سبب بلاغ §24 على جواله **غير مؤكد**؛ الاحتمال البديل (حساب إيميل مختلف عن حساب جوجل) قائم.
- لو فيه دفعة `staged` تحتها «آلاف العمليات»، الإصلاح ثم الفتح التالي **سيحذف عملياتها** (بروتوكول «صفر أو كامل»)، ويعيد المالك الاستيراد. المعاينة تعرض ذلك قبل التأكيد.
- قراءات الفحص تستهلك من حد 50,000 قراءة يوميًا (كل مستندات الحساب، وقائمة التجار بالآلاف).

### الخطوة الجاية للمالك
ثبّت APK 1.3 فوق 1.2 ← افتح التطبيق ← لو الشاشات ظهرت قولنا فيه عمليات ولا لأ ← الإعدادات ← «افحص البيانات القديمة» ← ابعت صورة المعاينة **قبل** التأكيد. واذكر: دخلت على الجوال بإيميل وكلمة سر ولا جوجل؟ وعلى الموقع؟

## 26. تجربة كشف PDF مع تشارتات — 2026-09-11 00:40 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.** طلب المالك: «جرب ارفع ده واعمل بيه تجربة كشف حساب… تشارت بأنواع المصاريف والعمليات».
- الملف المرفوع **مطابق بايت ببايت** لـ`document_1788599830751.pdf` الموجود (كشف الراجحي، 68 صفحة). عولج **محليًا فقط**: لا حساب المالك، لا Git، لا نشر.
- المسار: pdfjs ← `parseAlrajhiPdf` ← `demoContainer.importStatement` (parsedRows) ← `loadTransactionsScreen` لكل فترة. سكربت `vite-node` في مجلد scratchpad المؤقت (خارج الريبو، غير محفوظ).
- **مطابق:** 1912 عملية، 0 أخطاء، مدين 302,171.45، دائن 300,040.87، رصيد 2,707.25 @ 2026-09-04. الشاشات عرضت الـ1912 كلها. مجموع الأقسام = المدين لكل فترة (تحقق داخل السكربت).
- **🟡 اكتشاف منتج مهم:** الـPDF **بلا عمود تصنيف البنك**، فالفصل المعتمد (مصروف حقيقي 100,468.27 / ليس مصروفًا 201,703.18) **لا ينتج من PDF**. بتصنيف التطبيق (القواعد والتجار): مصروف 63,934.36 · ليس مصروفًا 86,201.55 (محافظ رقمية/تقسيط/استثمار) · **غير مصنّف 152,035.54 في 524 عملية** — أغلبها بأوصاف بنكية ثابتة: «عملية تحويل داخلية»، «تحويل بين حسابات العميل»، «فواتير نظام سداد»، «سحب - الصراف الآلي»، «تحويل الى محفظة…». **لا توجد قواعد تصنّف التحويلات والسحب والسداد** من نص الـPDF. (تفاصيل المبالغ لكل جهة عمدًا خارج هذا الملف — الريبو علني.)
- كل الـ1912 نوعها الاقتصادي `unclassified` ⇒ الرئيسية تعرض المصروف والدخل «بانتظار المراجعة» (سلوك مقصود، لا عطل).
- **مقترح لم يُنفَّذ (يحتاج قرار المالك):** قواعد لأنواع العمليات البنكية الواضحة في نص الـPDF (تحويل داخلي/بين حسابات العميل/سحب صراف/سداد) تصنّفها «تحويلات»/«سحب نقدي»/«فواتير»، مع مراجعة تعارض «سداد» (ARCHITECTURE §9.6)، ثم مقارنة النتيجة بالفصل المعتمد على الـCSV.

## 27. 🤖 بروتوكول محاكي أندرويد — اقرأه قبل أي «APK تمام» (2026-09-11)
**لماذا:** المالك لاحظ أن مشاكل الـAPK على جواله غير اللي بتظهر في الاختبارات (ذاكرة + متصفح). **لا تقل إن APK شغال قبل ما تجرّبه على المحاكي بالخطوات دي.**

### الموجود (خارج الريبو، على جهاز المالك)
- الأدوات: `C:\Users\atgs0\Documents\Codex\android-build-tools\` — `sdk\emulator\` (37.1.11)، `sdk\system-images\android-35\google_apis\x86_64\` (Android 15)، `sdk\platform-tools\adb.exe`، `java\jdk-21…`.
- الموبايل الافتراضي: `masroufy-pixel8` في `C:\Users\atgs0\.android\avd\` (1080×2400، 4GB RAM، 6GB بيانات، كيبورد الكمبيوتر مفعّل).
- التسريع: `WHPX is installed and usable` — لا يحتاج تغيير في ويندوز.
- المالك وافق (2026-09-11) على التحميل، والتجربة على **حسابه الحقيقي**: قراءة فقط، وأي كتابة (استيراد/إصلاح/تعديل) **بسؤاله أولًا**.

### الأوامر (PowerShell، من جذر المشروع)
```powershell
$e = "scripts\android-emulator.ps1"
& $e -Action setup      # ينشئ AVD لو مش موجود + فحص التسريع
& $e -Action start      # يشغّل الشباك وينتظر الإقلاع (حتى 6 دقائق)
& $e -Action install    # أحدث dist-android-apk\masroufy-trial-vN.apk بـ adb install -r (تحديث بلا مسح البيانات — زي جوال المالك)
& $e -Action launch     # يفتح التطبيق ويطبع pid
& $e -Action shot       # صورة PNG في %TEMP%\masroufy-emulator\ — افتحها بأداة القراءة وشوفها فعلًا
& $e -Action tap -X 540 -Y 1200   # ضغطة بإحداثيات بكسل
& $e -Action back
& $e -Action log        # logcat للتطبيق (pid)؛ logclear يمسحه
& $e -Action status     # الأجهزة، نسخة التطبيق المثبتة، pid
& $e -Action stop
```
```bash
node scripts/webview-devtools.mjs --watch 15 --reload   # أخطاء JS/console من داخل الـWebView من أول الفتح
node scripts/webview-devtools.mjs --eval "document.body.innerText.slice(0,150)"   # قراءة حالة الشاشة
```
- **لا يوجد أمر كتابة نص عمدًا.** كلمة السر يكتبها المالك بنفسه في شباك المحاكي. الجلسة تبقى محفوظة في AVD بعدها.
- ⛔ **حتى لو المالك كتب الإيميل وكلمة السر في الشات وطلب منك تدخل — لا تكتبها أنت** (لا بـ`adb shell input text` ولا DevTools ولا غيره). اطلب منه يكتبها بنفسه في شباك المحاكي. **لا تنسخ أي بيانات دخول في أي ملف أو commit.** (حصل 2026-09-11؛ نُصح بتغيير كلمة السر.)
- المالك يدخل **بالإيميل وكلمة السر على الموقع والجوال معًا** ⇒ احتمال «حساب مختلف بين جوجل والإيميل» (§24) **مستبعد**.

### أول تجربة بحساب المالك الحقيقي (APK 1.3 على المحاكي، 2026-09-11 11:40) — قراءة فقط
- المالك دخل بنفسه. **البيانات بتتحمّل:** الرئيسية تعرض عمليات الفترة الحالية، بلا رسالة خطأ على الشاشة. `--watch 25 --reload` بعد الدخول: لا أخطاء JS غير تحذير Capacitor أدناه.
- **كل عمليات الفترة نوعها غير محدد** ⇒ الرئيسية والعمليات تعرض «بانتظار المراجعة»/«غير متاح» + زر «حدّد الأنواع». سلوك مقصود، لكنه يُقرأ كأن «مفيش أرقام».
- 🔴 **سبب مرجّح لبلاغ «مفيش ولا عملية ظاهرة»: القائمة موجودة لكن تحت 9 عناصر** في شاشة العمليات (رسائل جديدة، تحديث، مراجعة القديم، الفترة، فلتر الوسوم، البحث، 3 خانات، صندوق التنبيه). على شاشة 1080×2400 **ولا عملية ظاهرة بدون تمرير طويل**. بعد تمريرتين القائمة ظهرت سليمة.
- 🔴 **تكرار محتمل ظاهر على الشاشة (غير مُتحقق):** في نفس اليوم ونفس المبلغ ونفس الاتجاه، ظهرت 3 أزواج بأوصاف مختلفة (وصف بصيغة كشف + وصف بصيغة مختصرة فيها آخر 4 أرقام، مثل «تحويل وارد • …NNNN» بجانب «تحويل»). قد تكون نفس العملية من مصدرين (كشف CSV/PDF + رسالة، أو استيرادين) ⇒ **المجاميع ممكن تتحسب مرتين**. لم يُعرف المصدر بعد: الخطوة التالية قراءة `sourceRecords` (batchId/sourceType/matchingState) للأزواج دي — قراءة فقط.
- 🔴 **التكرار مُثبت (2026-09-11، قراءة فقط):** سكربت يقرأ كاش Firestore المحلي (IndexedDB داخل الـWebView) وجد **161 مجموعة** بنفس التاريخ/الاتجاه/المبلغ في الجزء المخزن محليًا فقط. **كل مجموعة = عملية `createdAt` 2026-09-07T10:34 + توأمها `createdAt` 2026-09-08T06:30**، `sourceOrder` متتالي (فرق 1)، `sourceTime` فاضي في الاتنين ⇒ **نفس الكشف اتستورد مرتين** (مش رسالة + كشف). نسخة 09-07 نصها العربي **بأشكال العرض** (Presentation Forms، مثل «ﺔﻈﺣﻼﻣ» بدل «ملاحظة»)، ونسخة 09-08 نص سليم ⇒ منع التكرار ما شافهمش متطابقين. **الأثر: المجاميع مضاعفة.** السكربت في scratchpad (`find-pairs.js`) — لا يرجع مبالغ.
  - **لم يُصلح.** الإصلاح كتابة في بيانات المالك ⇒ بموافقته: التراجع عن دفعة 09-07 بـ`revertImportBatch` (يحتفظ بأي عملية عليها تسوية/تخصيص/مصدر آخر) بمعاينة.
  - **ثغرة كود مرجّحة:** توحيد النص قبل البصمة/المقارنة لا يطبّع أشكال العرض العربية (NFKC) — يتأكد ويُختبر قبل الإصلاح.
- `console.error: Error injecting safe area CSS: TypeError … (reading 'style')` ×3 من `@capacitor/android` 8.5.1 (`SystemBars.java` بيحقن قبل وجود `documentElement`). **بلا أثر ظاهر**: كل CSS عندنا `var(--safe-area-inset-*, env(safe-area-inset-*))`.
- **لا تكتب مبالغ أو أوصاف من حساب المالك في هذا الملف** (الريبو علني). الصور في `%TEMP%\masroufy-emulator\` فيها بياناته — لا ترفعها ولا تنشرها.

## 28. 🧭 تغيير اتجاه من المالك — 2026-09-11 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية. لا تبنِ ميزات جديدة قبل حسم أسئلته.**
نص المالك (ملخص): المشروع الحالي **نسخة بدائية**. المطلوب تطوير أكبر يبدأ **ببحث سوق** لمعرفة الناقص فعلًا، حتى برنامج **خفيف جدًا وسريع جدًا، الأذونات شغالة، وكل حاجة ذكية وبتتحسب تلقائي** — تدخّله للتأكيد والمراجعة فقط. شكواه: **الواجهة بقت عشوائية جدًا**، و**البرنامج حاسس إنه غبي: مفيش أرقام بتظهر**.

### التشخيص (من الكود والمحاكي، مش تخمين)
- «مفيش أرقام» = قرار تصميم قائم: المجاميع `null` لحد ما المستخدم **يؤكد** النوع الاقتصادي لكل عملية (spec/02 «لا تصنف تحويلًا غامضًا كراتب»، و`suggestEconomicKind` يقترح ولا يقرر، وفيه اختبار يمنع اقتراح دخل). على حساب المالك كل عمليات الفترة غير محددة ⇒ كل الخانات «غير متاح». **ده يتعارض مع طلبه الجديد «تلقائي + مراجعة بس» — تغييره قرار للمالك ويحتاج تسجيل في OVERRIDES.**
- «واجهة عشوائية» = تراكم أزرار وأشرطة فوق المحتوى (شاشة العمليات: 9 عناصر قبل أول عملية — §27)، بلا مصدر تصميم واحد محدّث (المرجع `masroofi-v4.html` قديم).

### بحث سوق أولي (2026-09) — ليس التقرير الكامل
- **دراهم:** أول تطبيق سعودي بترخيص مصرفية مفتوحة من ساما (2026)، والراجحي يملك 65%. ذكاؤه من ربط الحسابات مباشرة + محرك إثراء يصنّف المشتريات والتحويلات والفواتير تلقائيًا. **المصرفية المفتوحة تحتاج ترخيص ساما (منذ مارس 2026)** ⇒ غير متاحة لتطبيق شخصي. طرقنا الواقعية: **رسائل البنك + الكشوف**.
- **axio (Walnut سابقًا):** الذكاء كله من قراءة SMS البنوك (تعابير نمطية لاستخراج المبلغ/التاجر/التاريخ) + تصنيف تلقائي + تذكير فواتير.
- **Monarch / Copilot:** تصنيف تلقائي يتعلم من التصحيح، اكتشاف الاشتراكات تلقائيًا وتنبيه قبل الخصم، وواجهة مصقولة.

### أدوات التصميم المتصلة بـClaude (سؤال المالك)
1. **Figma + Figma MCP الرسمي** (ثنائي الاتجاه منذ فبراير 2026): Claude يقرأ الإطار المحدد ويحوّله لكود، ويدفع واجهات لـFigma. **ليس استماعًا تلقائيًا** — المالك يقول «عدّلت» فيُقرأ. يحتاج حساب Figma وتفويض الموصل من إعدادات claude.ai (غير مفوّض حاليًا).
2. **لوحة تصميم Claude (skill `design`)**: Claude يرسم الشاشات، المالك يعدّل بصريًا، والحفظ ينشر نسخة **وتصل الجلسة إشعارًا** فتقرأ وتطبّق — طالما جلسة مفتوحة. تفعيل الحفظ للحساب **غير مؤكد** حتى التجربة.

### أسئلة مستنية المالك (لا تحسمها)
1. سياسة الأرقام: تلقائي بالمقترح (مع علامة «تقديري» ومراجعة للغامض فقط) أم الإبقاء على التأكيد الإلزامي؟
2. أداة التصميم: لوحة Claude أم Figma؟
3. ترتيب البداية: تقرير بحث السوق الكامل أولًا، أم التصميم، أم محرك الذكاء؟
- مقترح لم يُقرر: إعادة النظر في «خفيف جدًا وسريع جدًا» مع WebView/Capacitor (فتح ~3 ثواني فاضية على المحاكي) — بند بحث، لا قرار.

### ✅ إجابات المالك (نفس اليوم) — مسجلة في OVERRIDES §18
1. الأرقام: **تلقائي + مراجعة الغامض فقط** (لم يُنفَّذ بعد — تفاصيله في OVERRIDES §18 تحتاج أسئلة قبل التنفيذ).
2. التصميم: **Figma** (يحتاج حساب Figma للمالك + تفويض الموصل من claude.ai).
3. الترتيب: «ابدأ بـ1 ثم 2، عايز تحديث سريع فعال» ⇒ **(1) فحص التكرار ← (2) ترتيب شاشة العمليات ← تقرير بحث السوق الكامل**.

## 29. تحديث سريع: التكرار مُشخَّص + شاشة العمليات مضغوطة — 2026-09-11 12:20 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.**

### (1) التكرار — مُشخَّص، لم يُصلح (انظر §27)
- 161 مجموعة في الكاش المحلي: كل الكشف اتستورد مرتين (07-09 و08-09).
- **السبب في الكود (مقروء، غير مختبر بعد):** `detailKey` في `domain/dedupe.ts` = `تاريخ|مبلغ|اتجاه|normalizeText(merchantName)`. استخراج اسم التاجر اتغيّر بين الاستيرادين (مثال: «ALBAIK» مقابل «شراء عبر نقاط البيع…»، و«تحويل وارد • …» مقابل «عملية تحويل بين حسابات العميل»)، ونسخة 07-09 نصها بأشكال العرض العربية و`normalizeText` **لا يطبق NFKC**. والمرجع البنكي فاضي ⇒ لا تطابق مرجع ولا تفاصيل ⇒ اتسجلوا «جديد».
- **إصلاحان مقترحان (لم يُنفَّذا):** (أ) كود: مطابقة كشف ضد كشف بمفتاح لا يعتمد على اسم التاجر (مثلًا التاريخ+المبلغ+الاتجاه+الرصيد بعد العملية لنفس الحساب) + NFKC في `normalizeText`، باختبار يعيد إنتاج الحالة. (ب) بيانات المالك: شاشة «سجل الاستيرادات» بزر تراجع يستعمل `revertImportBatch` الموجود **بلا أي واجهة حاليًا**، بمعاينة ثم **موافقة المالك** للتراجع عن دفعة 07-09.

### (2) شاشة العمليات — اتعملت ومتجربة على المحاكي
- `SmsInboxAccess`: زر «رسائل جديدة» يظهر فقط لو فيه رسائل أو خطأ (ودايمًا في تبويب الإعدادات حتى يفضل الإعداد متاح). المكوّن يفضل مركّب فالتحديث الدوري شغال.
- `feedback.css` `.syncStatus`: سطر هادئ 32px بزر «تحديث» نصي بدل صف 48px.
- `AppShell`: شيل زر «مراجعة وتصنيف العمليات القديمة» من فوق الشاشة ⇒ `onOpenHistory` لرابط داخل سطر الملخص.
- `TransactionsScreen`: البحث + زر «فلترة» في سطر (فلتر الوسوم مطوي)، تلميح البحث يظهر عند البحث بالمبلغ فقط، الخانات الثلاث شريط مضغوط (لا تختفي — spec/04)، صندوق التنبيه ⇒ سطر «N عملية · N محتاجة تحديد نوع» بزري «حدّد الأنواع» و«مراجعة القديم».
- ⚠️ درس: قواعد CSS جديدة كُتبت فوق `.metric`/`.btn` في نفس الملف فخسرت في الترتيب — ظهر على المحاكي لا في الاختبارات. الحل بادئة `.txns`.
- **الفحص:** typecheck نضيف، 499/43 اختبار ناجح. **على المحاكي بحساب المالك (APK 1.4 / versionCode 5):** قبل = ولا عملية بدون تمرير؛ بعد إصلاح ترتيب CSS = **~5 عمليات ظاهرة بدون تمرير**، وسطر الملخص بزريه في سطر واحد، وزر الرسائل مخفي عند 0.
- APK 1.4 (versionCode 5): `dist-android-apk/masroufy-trial-v5.apk`، SHA256 `EE2B82A2CB50F006913F544CD0A4C4478F18F6FD8990713206DB1E04974F076D`، نفس شهادة v4 (السكربت تحقق). **أُرسل للمالك.**
- لم يُنشر الويب ولم يُرفع لـGitHub (بانتظار موافقة). الرئيسية وباقي الشاشات **لم تُضغط** — نفس مشكلة الزحام موجودة على الرئيسية (زر الاشتراكات، صندوق التنبيه الكبير).

### الخطوة الجاية المقترحة
1. إصلاح التكرار: (أ) كود منع التكرار + اختبار، ثم (ب) شاشة سجل الاستيرادات والتراجع **بموافقة المالك** على دفعة 07-09.
2. تقرير بحث السوق الكامل (قرار المالك)، ثم التصميم على Figma، ثم تنفيذ OVERRIDES §18 (الأرقام التلقائية).
→ رُفع لـGitHub (`46bf2c8`) بموافقة المالك. المالك: «ابدأ تعديل، وإعادة تصميم الواجهة بعدين» ⇒ §30.

## 30. إصلاح منع التكرار في الكود (بند 1-أ) — 2026-09-11 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.**

### اتعمل
- `domain/dedupe.ts`: مفتاح جديد `balanceKey` = حساب + يوم + مبلغ + اتجاه + **الرصيد المعلن بعد الحركة** (`statedBalanceMinor`). لو سطر وارد له رصيد ويطابق سجلًا موجودًا بنفس المفتاح ⇒ `duplicate` بسبب واضح («نفس سطر الكشف… حتى لو الاسم مقروء بشكل مختلف»). يُفحص **بعد** المرجع البنكي و**قبل** تشابه الرسائل والتفاصيل. الرسائل (`smsSource`) والسطور بلا رصيد لا تدخله ⇒ لا حكم بالتكرار من اليوم والمبلغ وحدهما.
- `application/useCases/importPreview.ts`: الرصيد يُمرر للمرشح الوارد (`toCandidate`) وللسجلات الموجودة (`loadExisting` من `txn.statedBalanceMinor` المحفوظ فعلًا).
- `domain/normalize.ts`: `normalizeText` يطبق `NFKC` أولًا ⇒ أشكال العرض العربية بالترتيب المنطقي ترجع حروفها.

### اتفحص
- `tests/acceptance/statementReimport.test.ts` (5): **فشل قبل الإصلاح** بـ`['new','new','new']` لإعادة استيراد نفس السطور بأسماء تجار مختلفة، وبعده `duplicate` للثلاثة. شراءان حقيقيان بنفس اليوم والمبلغ برصيدين مختلفين يتسجلوا الاتنين. بلا رصيد لا يُحكم بالتكرار. NFKC للترتيب المنطقي.
- ⚠️ **اكتشاف:** نص دفعة 2026-09-07 مخزن **بأشكال العرض وبالترتيب البصري المعكوس** («ﺔﻈﺣﻼﻣ» = «ملاحظة» مقلوبة). NFKC لا يعالجه، و**العكس الآلي ممنوع** (§9.5). اختبار صريح يثبت إننا لا نعكس. التكرار يُكتشف بالرصيد لا بالنص. سبب إضافي للتراجع عن تلك الدفعة.
- كل الاختبارات **504/44 ناجحة**، typecheck نضيف، اختبارات القبول على الكشف الحقيقي (CSV/PDF/المطابقة) خضراء. الملفات تحت 300 سطر.

### لم يُعمل
- **الإصلاح يمنع التكرار في الاستيرادات الجاية فقط. البيانات المكررة الموجودة في حساب المالك لسه موجودة.**
- لم يُبنَ APK/ويب بالإصلاح، لم يُرفع.

### التالي (بند 1-ب)
شاشة «سجل الاستيرادات» (قائمة الدفعات: الملف، التاريخ، العدد، الحالة) + زر «تراجع» يعرض `revertImportBatch.plan` (هيتشال كام، وإيه اللي هيفضل وليه) قبل التأكيد. التنفيذ على دفعة 07-09 **بموافقة المالك فقط** وبعد تجربة على المحاكي.
→ **اتبنى — §31.** (commit `010237d` لإصلاح منع التكرار، `b37b8ed` للسجل.)

## 31. سجل الاستيرادات والتراجع بمعاينة (بند 1-ب) — 2026-09-11 12:40 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.**

### اتعمل
- `revertImportBatch`: `history(limit)` الأحدث أولًا؛ `plan` بقى يرجع `expectedCount` و`recordsFound` و`blocked`. **حارس:** دفعة سجّلت عمليات ومفيش ولا سجل مصدر ليها (معرّفات تالفة) ⇒ `execute` يرفض بـ`REVERT_BLOCKED_MESSAGE` ولا يعلّمها `reverted` (كان ممكن يعلّمها متراجَع عنها من غير ما يشيل حاجة).
- `ImportHistoryPanel` في الإعدادات تحت «افحص البيانات القديمة»: الملف/الوقت/المصدر/العدد/الحالة، ورابط «تراجع…» يفتح معاينة (هيتشال كام من كام، وإيه اللي هيفضل وليه) ثم زر تأكيد بالعدد. الإلغاء لا يُعطَّل. أعداد فقط، بلا مبالغ.
- Android 1.5 / versionCode 6: `dist-android-apk/masroufy-trial-v6.apk`، SHA256 `9B08CE9AAB07E159472ED789B1F617DBC9CA80E5E61C24DED424087548F0E147`، نفس الشهادة (السكربت تحقق مقابل v5). **لم يُرسل للمالك بعد.**
- اختبارات `importHistory.test.ts` (3): الترتيب، المعاينة لا تكتب والتنفيذ يشيل الدفعة بس، الدفعة المعطوبة مرفوضة وتفضل `committed`. **507/45 ناجحة، typecheck نضيف.**

### على المحاكي بحساب المالك — قراءة فقط، **ولا تأكيد اتضغط**
سكربتات DevTools (scratchpad: `history-open.js`، `history-preview-0907.js`) تضغط زر السجل ورابط المعاينة فقط.
| الدفعة | المسجّل | العمليات | معاينة التراجع |
|---|---|---:|---|
| 2026-09-07 10:34 · `alrajhi-statement.csv` | CSV قديم | 1912 | هيتشال **1911**، يفضل **1** (متربطة بشخص). غير محجوبة |
| 2026-09-08 06:30 · PDF الكشف | «ملف CSV» | 753 | هيتشال **753** من 753. غير محجوبة |
| 2026-09-10 20:42 · نفس الـPDF | «ملف CSV» | 14 | لم تُعاين |
- **الحقيقة:** التكرار = **كشف CSV كامل (1912) + نسخة PDF من نفس الكشف فوقه (753 + 14)**، مش استيراد واحد اتكرر. نص الوصف في نسخة CSV مقلوب (من السكربت القديم، §9.5)؛ أسماء التجار فيها سليمة.
- 🟡 عيب صغير: استيرادات الـPDF متسجلة بـ`sourceType` «ملف CSV» — `guessSourceType` في `ImportSheet` غالبًا. غير مُصلح.
- المعاينتان اتقفلوا بـ«إلغاء». لم يُحذف شيء.

### ❓ قرار مستني المالك (لا تنفّذه بنفسك)
- **(أ)** التراجع عن نسختي الـPDF (753 + 14) ⇒ يفضل كشف CSV الكامل المطابق للأرقام المعتمدة. خطوة واحدة. العيب: نص الوصف مقلوب.
- **(ب)** التراجع عن CSV (1911) ثم إعادة استيراد الـPDF بـAPK 1.5 ⇒ نصوص سليمة، ومنع التكرار الجديد (§30) يتخطى الـ767 الموجودين ويضيف الباقي. خطوات أكتر.
- مين يضغط التأكيد: المالك على جواله بـAPK 1.5، أو Claude على المحاكي **بموافقة صريحة على دفعة محددة**.
- **رد المالك:** رفع لـGitHub ✅ (`52ac618`)، والتأكيد **هيضغطه بنفسه على موبايله** (APK 1.5 اتبعتله). **لكن خيارَي (أ)/(ب) ظهروا له بالعكس** في نافذة الأسئلة (خلط حروف لاتينية PDF/CSV مع العربي بيقلب الاتجاه) فما قدرش يقرر. ⇒ **لا تستعمل حروف لاتينية داخل عناوين الخيارات القصيرة للمالك** — اكتب «ملف الإكسل القديم» و«ملف الكشف من البنك» بدل CSV/PDF. القرار لسه مفتوح.

## 32. تقرير بحث السوق الكامل — 2026-09-11 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.** المالك: «كمل شغل». التقرير صفحة محلية اتبعتت له (مش منشورة، مش في الريبو). الخلاصة هنا عشان اللي بعدي:
- **المنافسين:** دراهم (ربط بنكي مرخّص من ساما 2026، الراجحي يملك أغلبه)، ملاءة (مرخّصة يوليو 2026، زكاة واستثمار)، وفير (رسايل البنك، 4.6★ من 1,700، الشكوى: الاشتراك 19.99 ر.س والقيود)، **تطبيق على App Store اسمه «مصروفي» بالظبط** (مطوّر مختلف، رسايل +190 بنك منها الراجحي، ميزانية 80/100%، زكاة، OCR، مساعد، 5.99$/شهر)، Copilot (يتعلم التصنيف بعد ~30 تصحيح، يكتشف الاشتراكات)، Monarch (تذكير قبل الخصم)، Wallet، axio (رسايل فقط)، Cleo (سؤال بالكلام).
- **استنتاجات:** الذكاء = دخول العمليات تلقائيًا (الإدخال اليدوي يخسر مستخدمين ×3). المصرفية المفتوحة تحتاج ترخيص ساما ⇒ طريقنا الرسايل + الكشوف. الناس بتسيب التطبيق لو تقيل/محتاج نت/بيحسسها بالذنب.
- **ميزتنا:** الأشخاص والديون، الاستثمار، مطابقة الرصيد بالهللة، بلا اشتراك/إعلانات.
- **الأولويات القصوى (P0):** رسايل البنك مجرّبة فعلًا، النوع التلقائي + مراجعة الغامض (OVERRIDES §18)، تصنيف يتعلم من التصحيح + قواعد التحويل/السحب/السداد، إزالة التكرار، فتح فوري أوفلاين. **مهم (P1):** تذكير الاشتراكات قبل الخصم، تنبيه الميزانية، قفل بصمة، إضافة بدوسة/ويدجت، الواجهة (Figma). **لاحقًا (P2):** زكاة، أهداف، سؤال بالكلام، OCR.
- **خطة مقترحة بالترتيب:** (1) الأرقام صح ومفهومة ← (2) خفيف وسريع والأذونات شغالة ← (3) واجهة موحدة على Figma ← (4) ميزات تكمل الصورة.
- ❓ **أسئلة مستنية المالك:** اعتماد ترتيب الخطة؛ تنبيه تشابه الاسم مع «مصروفي» على App Store (مش مستعجل)؛ قرار التكرار (أ/ب) مكتوب له بصياغة من غير حروف لاتينية.

## 33. الأرقام دايمًا ظاهرة «تقريبي» — 2026-09-11 16:30 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.** قاعدة المالك (مسجلة في OVERRIDES §18): «لازم تظهر أرقام سواء البرنامج متأكد أو لا… (تقريبي) أو ملاحظة إن فيه 40 عملية محتاجة تأكيد… كل المتوسطات وكل حاجة شغالة، وبعدين نعدلها».

### اتعمل (commit `d75d41f`)
- `domain/estimatedKinds.ts` — `withEstimatedKinds(transactions, categoryNameById)`: كل عملية `unclassified` وغير مؤكدة تتحسب **في المجاميع فقط** بنوع مفترض: اقتراح `high` ⇒ نوعه؛ غير ذلك ⇒ `suggestion.kind` أو (مفيش) الصادر `purchase` والوارد `salary`. يرجع `estimatedCount` و`needsReviewCount`. لا كتابة في التخزين؛ النوع المؤكد لا يُلمس.
- مستخدم في `loadHomeScreen` (الحالية + آخر 6 فترات)، `loadHomeHistory` (بقى ياخد `categories` — موصول في container وdemoContainer)، `loadTransactionsScreen` (`unclassifiedCount` = `needsReviewCount`)، `loadBudgetScreen` (الحالية + تاريخ المتوسط). القوائم المعروضة تفضل بالعمليات الأصلية.
- الواجهة: الرئيسية «تقريبي — N عملية محتاجة تأكيد» (أو «N نوعها اتحدد تلقائي»)، سطر العمليات «· N محتاجة تأكيد · الأرقام تقريبية»، ملاحظة الميزانية «تقريبي…».
- 8 اختبارات قديمة كانت بتثبت «غير محدد ⇒ غير متاح» اتعدّلت للقاعدة الجديدة، مع إبقاء: **لا كتابة قبل التأكيد**، و**بعد التأكيد الرقم مؤكد** (`estimatedCount` = 0). اختبار جديد `estimatedKinds.test.ts`. **510/46 ناجحة، typecheck نضيف.**

### على المحاكي بحساب المالك (APK 1.6 / versionCode 7) — قراءة فقط
- الرئيسية: المصروف الشخصي رقم + «تقريبي — 24 عملية محتاجة تأكيد»، الدخل والمتبقي ومعدل الادخار وتوقع نهاية الفترة **كلها أرقام**. لا رسائل خطأ.
- «غير متاح» الوحيد الباقي: **المتاح اليومي** لأن مفيش سقف ميزانية للفترة (مش بسبب الأنواع). ❓ اقتراح للمالك: بدون سقف يتحسب تقريبي = المتبقي ÷ الأيام الباقية. **لم يُنفَّذ.**
- ⚠️ **الأرقام لسه مضاعفة جزئيًا** بسبب التكرار (§31) لحد ما المالك يتراجع عن أحد الاستيرادين.
- APK: `dist-android-apk/masroufy-trial-v7.apk`، SHA256 `80CC3545A2672D159DD839B733543286B8D54B911D1982151B20D61860EBDCA1`، نفس الشهادة. **اتبعت للمالك.** لم يُرفع لـGitHub ولم يُنشر الويب.

### Figma (سؤال المالك: «أعمل إيه بعد الحساب؟»)
الموصل `plugin:marketing:figma` **غير مفوّض** في هذه الجلسة. الخطوات اللي اتقالت له: حساب figma.com ← claude.ai الإعدادات ← Connectors ← Figma ← Connect ← جلسة جديدة ← ملف «مصروفي» وClaude يدفع الشاشات الحالية كبداية ← المالك يعدّل ← «Copy link to selection» ويبعته ← Claude يطبّق ويجرّب على المحاكي. **بعض مزايا Figma MCP قد تحتاج خطة مدفوعة — غير متحقق.**

## 34. 🎨 Figma اتربط — ابدأ جلسة التصميم من هنا (2026-09-11)
**هذا القسم هو نقطة الاستكمال الحالية.**
- المالك عمل حساب Figma مجاني ووصّله بموصل Claude. **متحقق بـ`whoami`:** handle «mohamed hossam»، فريق «محمد حسام's team»، **tier = starter، seat = View**.
- ⚠️ **حد الخطة المجانية (Starter): 20 استدعاء قراءة في الشهر** لأدوات Figma MCP (مثل `get_design_context`، `get_screenshot`، `get_metadata`، `whoami`). **أدوات الكتابة على اللوحة مستثناة.** الحد مرتبط بالفريق اللي يملك الملف. المصدر: developers.figma.com/docs/figma-mcp-server/rate-limits-access. استُهلك **1** (`whoami`) في 2026-09-11.
  - **وفّر القراءة:** لا تستكشف الملف كله. اطلب من المالك رابط الإطار المحدد («Copy link to selection»)، واقرأه **مرة واحدة** بـ`get_design_context`، وسجّل عدد الاستدعاءات المستهلكة هنا بعد كل جلسة.
- ~~المالك كلّم AI بتاع Figma وكان بيبني حاجة~~ — **ألغاه المالك.** قراره: «ابدأ عليه مشروع من الصفر». ⇒ **Claude ينشئ ملف Figma جديد** («مصروفي — التصميم») عبر أدوات الكتابة (مستثناة من الحد): صفحة نظام التصميم (ألوان/خطوط/مسافات من `tokens.json`، مكوّنات: بطاقة رقم، صف عملية، شريط تنقل، زر، شارة «تقريبي») ثم الشاشات بالترتيب. **القراءة للتحقق (`get_screenshot`) محسوبة من الـ20 — استعملها بحساب** (مثلًا مرة لكل شاشة بعد اكتمالها)، والمالك يراجع من Figma بنفسه.
- قواعد التصميم الملزمة تفضل: RTL عربي، فاتح افتراضيًا + غامق، ألوان التصنيفات من `design-source/masroofi-claude-code/design/tokens.json`، صادر أحمر ووارد أخضر، الأرقام «تقريبي» مع عدد المحتاج تأكيد (OVERRIDES §18)، لا رقم بلا مصدر، لمس 44px على الأقل، واختبار كل تطبيق على المحاكي (§27) قبل أي «تمام».
- ترتيب الشاشات المقترح: الرئيسية ← العمليات ← مراجعة الأنواع (المراجعة للغامض فقط) ← الإعدادات. شريحة رأسية لكل شاشة: تصميم ← كود ← اختبارات ← محاكي ← صورة للمالك ← commit.
- مفتوح من قبل: قرار التكرار (الأول: مسح ملف الكشف من البنك / التاني: مسح ملف الإكسل القديم)، المتاح اليومي بدون سقف، رفع 4 commits محلية لـGitHub (بانتظار موافقة).
- `--eval` للقراءة فقط، وممنوع تفريغ أرقام مالية في الشات — اقرأ أعدادًا وحالات، لا محتوى.

### أخطاء وقعت أثناء الإعداد — لا تكررها
1. `sdkmanager "system-images;android-35;…"` من bash/PowerShell **فشل**: ملفات `.bat` بتقسم على `;`. استعمل `--package_file=<ملف فيه اسم الحزمة>`.
2. `avdmanager create avd` **فشل** («Valid system image paths are: null») لأنه في `cmdline-tools\unpacked` مش `latest`. السكربت بيكتب `config.ini` و`.ini` مباشرة.
3. Windows PowerShell 5.1 مع `ErrorActionPreference=Stop` بيعتبر أي stderr من برنامج خارجي خطأ قاتل (ده اللي كسر `build-android.ps1`). السكربتات بتستعمل `Invoke-Native` وتحكم بـ`$LASTEXITCODE`.
4. **صورة بعد 3 ثواني من الفتح طلعت بيضا بالكامل** والتطبيق كان سليم (شاشة الدخول ظهرت بعدها). لا تحكم من صورة واحدة مبكرة: خد صورة تانية أو `--eval` لنص الشاشة.
5. `-Action ui` (uiautomator) **ما رجّعش نصوص الـWebView**. للنصوص استعمل `webview-devtools.mjs --eval`.

### ملاحظات من أول تشغيل (APK 1.3، versionCode 4)
- التثبيت `Success`، الفتح بلا crash في logcat، الـWebView على `https://localhost/`، قابل للفحص بـDevTools (بناء debug).
- **الفتح: شاشة فاضية ~3 ثواني قبل أي رسم.** قياس (force-stop ثم `am start -W`، صور على فترات، عدّ ألوان منطقة المحتوى): الـActivity جاهزة 1292ms؛ 1.5s فاضية تمامًا (لون واحد)؛ 3.0s رسم بسيط؛ 4.5s الشاشة كاملة. يعني التأخير **داخل صفحة الويب** قبل أول رسم React. (المحاكي على كمبيوتر، مش S24 — الرقم على الجوال مختلف.) مرتبط غالبًا بشكوى المالك عن ثقل الفتح.
- `--watch 15 --reload`: **لا أخطاء JS ولا console** أثناء الفتح.
- iframe تسجيل دخول Firebase (`firebaseapp.com/__/auth/iframe`) أخذ ~5.3 ثانية.
- logcat: `W/Capacitor: Unable to read file at path public/plugins` (تحذير Capacitor معروف غالبًا بلا أثر — غير مُتحقق).

## 35. خطة إعادة التصميم من الصفر على Figma — مستنية موافقة المالك (2026-09-11، Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.** المالك غيّر الطلب: **تصميم من الصفر** (مش البناء على تصميم AI بتاع Figma — الرابط اللي في الرسالة الأولى كان فاضي، ولا قراءة اتصرفت عليه). ملف جديد اسمه «مصروفي — التصميم». **لم يُنشأ الملف بعد ولم يُكتب كود** — المالك طلب الخطة بالكلام أولًا.

### عدّاد قراءات Figma (الحد 20/شهر، الكتابة مستثناة)
| التاريخ | الأداة | السبب | الإجمالي |
|---|---|---|---:|
| 2026-09-11 | `whoami` | التحقق من الربط (§34) | 1 |
- الخطة: قراءة واحدة (صورة) لكل صفحة بعد ما تخلص = ~5، وقراءة لكل تعديل من المالك. ⚠️ `whoami` قال **seat = View** — ممكن يمنع الإنشاء/الكتابة. **غير متحقق**؛ يبان أول ما نحاول ننشئ الملف.

### ترتيب العمل المعتمد من المالك
0 نظام التصميم (ألوان/خطوط/مسافات من tokens.json + مكوّنات: بطاقة رقم، صف عملية، شريط تنقل، أزرار، شارة «تقريبي») ← 1 الرئيسية ← 2 العمليات ← 3 مراجعة الأنواع ← 4 الإعدادات. لكل شاشة: Figma وموافقة ← كود ← typecheck + test ← محاكي بحساب المالك ← صورة ← commit ← HANDOVER.

### ملف Figma المقترح
- صفحات: «نظام التصميم»، «الرئيسية»، «العمليات»، «مراجعة الأنواع»، «الإعدادات». إطار 384×832 (مقاس S24 Ultra بوحدات dp)، فاتح وغامق جنب بعض، وحالات: فاضي/تحميل/خطأ/مبالغ مخفية/تقريبي.
- خط Noto Sans Arabic (موجود في `base.css` وقريب من خط أندرويد)، أرقام غربية بفاصل آلاف زي `formatAmount`. **بيانات وهمية فقط** — لا أرقام من حساب المالك في Figma.

### الوضع الحالي (مقروء من الكود)
- الرأس: عنوان + 5 أيقونات (＋، جرس، عين، قمر، خروج). تحته: زر الرسائل (أحيانًا)، سطر المزامنة بزر «تحديث»، وفي الرئيسية زر «الاشتراكات والفواتير» — **3 أشرطة فوق المحتوى**.
- الرئيسية (فوق لتحت): الفترة، بطاقة المصروف بشارتين، **صندوق تنبيه بتلات فقرات**، الخانات التلاتة، سطر المستبعد، المتاح اليومي + التوقع، 8 تصنيفات، أحدث العمليات، آخر ست فترات.
- العمليات: الفترة، بحث + فلترة، الخانات التلاتة، سطر ملخص بزرين، القائمة.
- التنقل: 6 تبويبات (الرئيسية، الميزانية، العمليات، الأشخاص، الاستثمار، الإعدادات).

### المقترح — الرئيسية
1. رأس واحد 56: العنوان + جرس + عين بس. القمر والخروج ⇐ الإعدادات (سؤال). ＋ ⇐ شريط التنقل (سؤال).
2. سطر الفترة (شريحة صغيرة ‹ ›). سطر المزامنة يختفي ويظهر **بس** عند خطأ أو بيانات قديمة. زر الرسائل سطر رفيع بس لما فيه رسائل.
3. بطاقة المصروف: الرقم الكبير + شارة «تقريبي» + سطر واحد يتضغط «N عملية محتاجة تأكيد ‹» يفتح المراجعة. الخانات التلاتة جوه نفس البطاقة تحت فاصل. **صندوق التنبيه الكبير يتشال.** سطر المستبعد يبقى سطر صغير جوه البطاقة.
4. صف بطاقتين: المتاح اليومي | التوقع — رقم + سبب في سطر واحد.
5. التصنيفات: أعلى 5 + «كلها». 6. أحدث العمليات: 5 + «كلها».
7. «آخر ست فترات» و«الاشتراكات والفواتير» يخرجوا من الرئيسية (مكانهم سؤال).

### المقترح — العمليات
1. نفس الرأس. 2. سطر ثابت عند التمرير: الفترة + البحث، والفلترة أيقونة جوه خانة البحث.
3. الخانات التلاتة شريط رفيع (لا تختفي — spec/04) + سطر «N عملية · N محتاجة تأكيد · تقريبي» والعدد يتضغط. زر «مراجعة القديم» ⇐ شاشة مراجعة الأنواع.
4. القائمة **مجمّعة باليوم**: عنوان اليوم ومجموع صادره (يُحسب في حالة الاستخدام، مش في الشاشة — قاعدة 4).
5. صف العملية: دايرة أيقونة التصنيف بلونه ← الاسم (لحد سطرين) + سطر باهت (التصنيف · المحفظة · الوقت) ← المبلغ: صادر أحمر بإشارة −، وارد أخضر بإشارة +، ونقطة «محتاجة تأكيد» للغير مؤكدة.

### ❓ أسئلة مستنية المالك (لا تحسمها)
1. شريط التنقل: 5 (الرئيسية، العمليات، ＋، الميزانية، المزيد) أم 6 زي دلوقتي مع ＋ عائم؟
2. مكان «آخر ست فترات» و«الاشتراكات والفواتير».
3. القمر والخروج ينقلوا للإعدادات؟
4. المتاح اليومي بدون سقف: تقريبي = المتبقي ÷ الأيام الباقية؟ (من §33)
5. مفتوح من قبل: قرار التكرار (§31)، ورفع 4 commits محلية لـGitHub.

### ✅ إجابات المالك على أسئلة §35 (2026-09-11)
1. شريط التنقل: **خمس خانات** — الرئيسية، العمليات، **زر الإضافة في النص**، الميزانية، «المزيد».
2. «آخر ست فترات» و«الاشتراكات والفواتير» ⇐ **شاشة «المزيد»** (مع الأشخاص والاستثمار والإعدادات).
3. القمر والخروج ⇐ **الإعدادات**. الرأس يفضل فيه الجرس والعين بس.
4. المتاح اليومي بدون سقف ميزانية ⇐ **يتحسب تقريبي** = المتبقي ÷ الأيام الباقية، معلَّم «تقريبي» وسببه مكتوب (مسجل في OVERRIDES §19).
⇒ «المزيد» بقت شاشة جديدة في التصميم (صفحة سادسة في ملف Figma). التكرار ورفع الـcommits **لسه مفتوحين**.

### ملف Figma اتعمل — 2026-09-11
- الاسم «مصروفي — التصميم»، المفتاح `7tNRXYeYPfixHBLf3dEVuO`، الرابط https://www.figma.com/design/7tNRXYeYPfixHBLf3dEVuO (في drafts بتاعة فريق المالك). **اتبعت للمالك.**
- ✅ **مقعد «View» ما منعش الإنشاء** — التوقع في §35 كان غلط، الإنشاء نجح عادي.
- ⚠️ **قيد خطة Starter: مجموعة المتغيرات بتقبل وضع واحد بس (mode)** ⇒ **مش هينفع** فاتح/غامق كوضعين في نفس المجموعة. البديل المتبع: مجموعتين منفصلتين للألوان (فاتح، غامق) وإطارين لكل شاشة. **مصدر القيد: دليل Figma نفسه** (Starter=1، Professional=4).
- عدّاد القراءة: `whoami` تاني مرة (لجلب مفتاح الفريق) ⇒ **المستهلك 2 من 20**. أدوات الكتابة (`create_new_file`، `use_figma`) **مش محسوبة**.

### قيود خطة Starter اللي ظهرت فعلًا (2026-09-11)
1. **3 صفحات بس في الملف** (`createPage` رمى خطأ صريح). المتاح: «٠ نظام التصميم» (`0:1`)، «١ الرئيسية» (`2:2`)، «٢ العمليات» (`2:3`). ⇒ **مراجعة الأنواع والإعدادات والمزيد هيتحطوا كإطارات جنب العمليات في الصفحة التالتة**، مش صفحات مستقلة.
2. **وضع واحد للمتغيرات** ⇒ مجموعتين منفصلتين: «ألوان — فاتح» (`VariableCollectionId:2:4`) و«ألوان — غامق» (`VariableCollectionId:2:5`)، وكمان «مسافات» (`2:6`) و«خطوط» (`2:7`).
3. **اسم الملف مش قابل للتغيير من الأداة** (`Setting the document name is currently not supported`) ⇒ الملف اسمه «Document» في Figma. **المالك هو اللي يغيّر الاسم من Figma لو حب** (كليك يمين على اسم الملف ← Rename ← «مصروفي — التصميم»).
4. الخط: **Noto Sans Arabic** متاح بأوزان Regular/Medium/SemiBold/Bold — نفس الخط الموجود في `base.css`.

### المرحلة ١ في Figma — المتغيرات (2026-09-11)
- **36 متغير** اتعملوا بقيم `src/presentation/theme/tokens.css` حرفيًا، وكل متغير عليه `scopes` محددة و`code syntax` باسم متغير الـCSS نفسه (`var(--c-text)` مثلًا) عشان اللي يفتح Figma في وضع المطور يشوف اسم المتغير اللي في الكود.
  - «ألوان — فاتح» و«ألوان — غامق»: 9 لكل واحدة (خلفية، سطح، نص، باهت، حد، إجراء، ناعم، وارد، صادر).
  - «مسافات»: 8 (حواف، حشو، زوايا، أقل لمسة 48).
  - «خطوط»: 10 (6 أحجام + العائلة + 3 أوزان).
- لسه: أنماط النص، وصفحة نظام التصميم المرئية (عينات الألوان والمكوّنات)، وبعدها الشاشات.

### صفحة «٠ نظام التصميم» — جارٍ البناء (2026-09-11)
- الإطار الرئيسي `5:2` في صفحة `0:1`. اتعمل جواه: عنوان + **18 عينة لون** (كل عينة لونها مربوط بالمتغير، وتحتها اسم متغير الـCSS)، وبعدها عيّنة الخطوط وعيّنة المسافات.
- التحقق بصورة من داخل Figma (`node.screenshot()` جوه `use_figma`) — **مش محسوب من حد الـ20**، لأن الحد على أدوات القراءة (`get_screenshot`، `get_design_context`، `get_metadata`، `whoami`). المستهلك لسه **2 من 20**.
- الباقي في الصفحة: المكوّنات (بطاقة رقم، صف عملية، شريط تنقل بخمس خانات، أزرار، شارة «تقريبي») وألوان التصنيفات.

### ⚠️ غلطة وقعت في Figma — لا تكررها (2026-09-11)
- **استعمال رقم عنصر (node id) متوقّع بدل البحث بالاسم** رمى `no such property 'appendChild' on TEXT node`: افترضت إن رقم «رف المكوّنات» هو اللي بعد القسم، وطلع رقم عنوان نصي. **الصح:** `page.query('FRAME[name=…]').first()`. والأخطر إن الأمر بيفضل نصّه منفّذ لحد نقطة الخطأ ⇒ ممكن يسيب عناصر ناقصة، فالمحاولة التانية لازم تنضف الأول (اتعمل: حذف أي مكوّن بنفس الاسم داخل الصفحة دي بس).
- **`node.query('TYPE[name=اسم عربي]')` بيفشل** بـ`Invalid selector: unexpected character (0xd8)` — أداة البحث بالنص في Figma ما بتقبلش حروف عربية في قيمة الاسم. **الصح:** `node.findOne(n => n.type === 'FRAME' && n.name === 'الاسم')`.

### صفحة «٠ نظام التصميم» — خلصت، مستنية موافقة المالك (2026-09-11)
الإطار `نظام التصميم` (`5:2`) في صفحة `0:1` فيه:
1. **18 عينة لون** — 9 فاتح و9 غامق (الغامق على لوح غامق)، كل عينة مربوطة بمتغيرها وتحتها اسم متغير الـCSS.
2. **عيّنة الخطوط** — 8 أنماط بـNoto Sans Arabic، معروضة بأرقام ونصوص حقيقية الشكل (بيانات وهمية).
3. **عيّنة المسافات والزوايا** — 6، 12، 14، 16، زاوية 15 و20، ومربع 48 (أقل مساحة لمس).
4. **خمس مكوّنات حقيقية** (COMPONENT) وكل واحد عليه وصف بالعربي جوه Figma:
   - `شارة/تقريبي` — للأرقام المبنية على أنواع غير مؤكدة (OVERRIDES §18).
   - `زر/رئيسي` و`زر/هادئ` — ارتفاع 48.
   - `بطاقة رقم` — تسمية باهتة فوق والرقم تحت بلون وارد/صادر.
   - `صف عملية` (352×64) — دايرة التصنيف يمين، الاسم وجنبه نقطة «محتاجة تأكيد»، سطر باهت، المبلغ شمال بإشارة.
   - `شريط التنقل` (384×68) — خمس خانات من اليمين: الرئيسية، العمليات، زر الإضافة، الميزانية، المزيد (OVERRIDES §20).
- **لسه ما اتعملش:** ألوان التصنيفات الـ26 كعينات (هتتحط لما نحتاجها في الشاشات)، وحالات الأزرار (مضغوط/معطّل).
- **الخطوة الجاية:** موافقة المالك على نظام التصميم ← تصميم الرئيسية في صفحة «١ الرئيسية» (فاتح وغامق) ← موافقته ← كود.
- عدّاد قراءة Figma: **2 من 20** (مفيش قراءة اتصرفت في البناء — الصور من جوه `use_figma`).
- ✅ **المالك وافق على نظام التصميم** (2026-09-11): «موافق — ابدأ الرئيسية». ⇒ الرئيسية بتتبني في صفحة «١ الرئيسية»، فاتح الأول وبعدين غامق، والموافقة عليها قبل أي كود.
- ⚠️ أيقونات الرأس (الإشعارات وإخفاء المبالغ) **مؤقتة**: رموز نصية (◉ ◎) لأن الخط مافيهوش أيقونات. تتبدل بأشكال مرسومة قبل الكود، أو تتنفذ في الكود بنفس أسلوب الأيقونات الحالي.

### شاشة «١ الرئيسية» في Figma — اتعملت فاتح وغامق (2026-09-11)
- **فاتح** `13:2` و**غامق** `15:47` (نسخة من الفاتح، و**47 لون اتربط تلقائيًا** بمتغيرات المجموعة الغامقة بنفس الأسماء). المقاس 384×832 (مقاس S24 Ultra بالـdp)، والمحتوى الزيادة مقصوص عمدًا عشان نشوف اللي بيبان من أول نظرة.
- الترتيب: رأس (العنوان + أيقونتين) ← سطر الفترة و«محدّث دلوقتي» ← **بطاقة المصروف** (الرقم الكبير + شارة «تقريبي» + سطر «24 عملية محتاجة تأكيد ›») ← الخانات التلاتة ← المتاح اليومي والتوقع ← التصنيفات (أعلى 4 بشرائط) ← أحدث العمليات (3 صفوف) ← شريط التنقل بخمس خانات.
- **المتاح اليومي معروض برقم وتحته «تقريبي — مفيش سقف ميزانية»** تنفيذًا لـOVERRIDES §19.
- ألوان التصنيفات في الصفوف والشرائط **ثابتة في الوضعين** (ألوان متوسطة من tokens.json) — قرار مبدئي، يتراجع لو ظهر ضعف تباين على المحاكي.
- **الأرقام كلها وهمية** ومش من حساب المالك.

### شاشة «٢ العمليات» في Figma — اتعملت (2026-09-11)
- **فاتح** `16:2` في صفحة «٢ العمليات». الترتيب: رأس ← شريط ثابت (شريحة الفترة + خانة البحث بأيقونة فلترة جواها + شريط الخانات التلاتة الرفيع + سطر «312 عملية · 24 محتاجة تأكيد ›» و«الأرقام تقريبية») ← القائمة مجمّعة بالأيام ← شريط التنقل و«العمليات» هي الخانة النشطة.
- **كل يوم له رأس فيه اسم اليوم ومجموع صادره** — الرقم ده **لسه مش موجود في الكود**، هيتحسب في حالة استخدام (use case) مش في الشاشة (قاعدة CLAUDE.md #4) وقت التنفيذ.
- **النتيجة المقصودة: 5 عمليات باينة من غير تمرير** (قبل كده كانت صفر — §27).
- زرّي «حدّد الأنواع» و«مراجعة القديم» **اتشالوا** من فوق القائمة: التحديد بقى في سطر الملخص (العدد نفسه رابط)، و«مراجعة القديم» هتروح لشاشة مراجعة الأنواع.
- **غامق** `17:73` (31 لون اتربط تلقائيًا). شاشتا العمليات (فاتح وغامق) جاهزتين للمراجعة.
- بسبب حد الـ3 صفحات: **باقي الشاشات بتتبني كإطارات في نفس صفحة «٢ العمليات»** — «مراجعة الأنواع» عند x=880، واللي بعدها بنفس المسافة.
- قرار اتاخد من Claude بتفويض المالك («لو مردتش اختار إنت»): **مراجعة الأنواع شاشة كاملة بسهم رجوع، من غير شريط تنقل**، عشان الواحد يخلص المراجعة ويرجع بدل ما يسيبها في النص.

### شاشة «٣ مراجعة الأنواع» في Figma — اتعملت (2026-09-11)
- **فاتح** `18:110` (x=880 في صفحة «٢ العمليات»). الفكرة: **عملية واحدة في الشاشة**، مش قائمة طويلة. الترتيب: رأس بسهم رجوع وعدّاد «١ من ٢٤» ← شريط تقدم ← بطاقة العملية (الوصف، المبلغ الكبير بلونه، التاريخ، وسطر «محسوبة دلوقتي كدخل — أكّد أو صحّح») ← سؤال «الفلوس دي إيه؟» ← أربع اختيارات، **الاقتراح عليه إطار بلون الإجراء ومكتوب جنبه «الاقتراح»** ← «مش متأكد — سيبها دلوقتي».
- الاختيارات: دخل · تحويل بين محافظي · سلفة أو دين لشخص · استرجاع فلوس. **متوافقة مع OVERRIDES §18**: التطبيق حاسب النوع تلقائي والمراجعة بتأكد أو تصحّح، والتخطي مسموح.
- ⚠️ **الاختيارات دي اقتراح تصميم** — لازم تتطابق مع الأنواع الاقتصادية الفعلية في `domain/` قبل الكود (spec/02 و ARCHITECTURE §9.6 «سداد»). لو فيه نوع ناقص أو زيادة، الكود هو المرجع.

### ⛔ حد Figma اتخلص — 2026-09-11 (مهم لأي نموذج بعدي)
- الرسالة الحرفية: **«You've reached the Figma MCP tool call limit on the Starter plan»**.
- ⚠️ **تصحيح لما هو مكتوب في §34:** الحد مش على أدوات القراءة بس. **كل استدعاء لأداة Figma محسوب** — بما فيه `use_figma` (البناء) والصور من جواه. المستهلك الفعلي: ~20 استدعاء (whoami ×2، create_new_file، و17 `use_figma`).
- **اللي خلص في Figma:** نظام التصميم كامل (متغيرات + أنماط نص + 6 مكوّنات)، «الرئيسية — فاتح» `13:2` و«الرئيسية — غامق» `15:47`، «العمليات — فاتح» `16:2` و«العمليات — غامق» `17:73`، «مراجعة الأنواع — فاتح» `18:110`.
- **اللي ما خلصش:** «مراجعة الأنواع — غامق» (الأمر ده بالظبط هو اللي اترفض)، والإعدادات، والمزيد.
- **قرار (بتفويض المالك «لو مردتش اختار إنت»):** ما نستنّاش ترقية. الشاشات الناقصة تتصمم **في الكود مباشرة** بنفس نظام التصميم، والغامق بييجي لوحده لأن الكود أصلًا بيستعمل نفس متغيرات الألوان.
- الشهر الجاي (أو بعد ترقية): يكمل الناقص في Figma لو المالك حب.

### شريحة كود: هيكل التطبيق بعد إعادة التصميم — 2026-09-11 (Claude Opus 5)
**اتعمل (الكود بس — لسه ما اتجربش على المحاكي):**
- `ShellTabs`: **خمس خانات وزر الإضافة في النص** (الرئيسية · العمليات · ＋ · الميزانية · المزيد) — OVERRIDES §20. زر الإضافة دايرة بلون «إجراء» 52px.
- `ShellHeader`: بقى **الإشعارات وإخفاء المبالغ بس**. زر الإضافة والقمر والخروج اتشالوا من فوق.
- `AppearanceCard` (جديد) جوه الإعدادات: الوضع الغامق/الفاتح وتسجيل الخروج.
- `MoreScreen` + `MoreScreen.css` (جديد): الأشخاص · الاستثمار · الاشتراكات والفواتير · التصنيفات وألوانها · الإعدادات، وتحتها **«آخر ست فترات»** اللي خرجت من الرئيسية.
- `HomeScreen`: **صندوق التنبيه بفقراته التلاتة اتشال** وبقى سطر «N عملية محتاجة تأكيد ‹» جوه بطاقة المصروف؛ **الخانات التلاتة بقت جوه نفس البطاقة** تحت خط فاصل؛ التصنيفات 5 بدل 8؛ سبب نقص التغطية سطر هادي واحد.
- `AppShell`: سطر المزامنة **بيظهر بس** لما يكون فيه عرض محفوظ قديم أو تحديث شغال أو خطأ (كان دايم). زرّي «الاشتراكات» و«التصنيفات» اتشالوا من فوق المحتوى وراحوا «المزيد». عنوان تبويب الرئيسية بقى «الرئيسية».
- `ShellOverlays` (جديد، في `app/`): الأوراق المنبثقة اتنقلت هنا عشان `AppShell` رجع **298 سطر** (كان 322 — حد الـ300، قاعدة #7).
**اتفحص:** `npm run typecheck` نضيف، و**510 اختبار في 46 ملف كلهم ناجحين**. كل الملفات تحت 300 سطر.
**ما اتجربش:** ⚠️ **ولا حاجة اتجربت على المحاكي ولا على حساب المالك** — ممنوع أي «تمام» قبل كده (§27). كمان ما اتبنيش APK ولا اتنشر الويب.
**الخطوة الجاية:** تجربة الشكل في المتصفح، وبعدين المحاكي بحساب المالك (هو اللي يكتب كلمة السر)، وصورة له، وبعدين شاشة العمليات (تجميع بالأيام + مجموع اليوم — لسه ما اتعملش في الكود).

### شريحة كود: المتاح اليومي التقريبي + شريحة الفترة — 2026-09-11 (Claude Opus 5)
**اتعمل:**
- `domain/analytics.ts` — `dailyAllowance` بقى بياخد **`remainingMinor` اختياري** وبيرجّع **`approximate`**. تنفيذ OVERRIDES §19: من غير سقف ⇒ الرقم = max(0، المتبقي) ÷ الأيام الباقية، معلَّم تقريبي وسببه مكتوب. وجود السقف يعلو. الفترة اللي خلصت ⇒ `null` (مفيش قسمة على صفر). بلا سقف وبلا متبقي معروف ⇒ `null` («ولا رقم مخترع»).
- `loadHomeScreen` بيمرر `totals.remainingMinor`، و**اتشال التجاوز القديم** اللي كان بيخلي المتاح اليومي `null` لما الأنواع ناقصة (كان بيخالف §18).
- **شاشة الميزانية سايبة زي ما هي عن قصد**: هي بتتكلم عن السقف نفسه، فمن غير سقف الرقم هناك غير متاح — مكتوب في الاختبار.
- الواجهة: شارة «تقريبي» جنب رقم المتاح اليومي، والشارات جوه البطاقات بقت بحجم نصها مش عرض البطاقة.
- `PeriodPicker.css`: الشريط العريض بقى **شريحة واحدة** (خلفية ناعمة، زوايا دائرية، الشهر والنطاق في سطر واحد). الأزرار فضلت 48 للمس.
**اتفحص:** typecheck نضيف، **513 اختبار ناجح** (اتزادوا 3 اختبارات جديدة لـ§19)، وتجربة بصرية في المتصفح بوضع `?demo=1`: الشريط بخمس خانات وزر الإضافة في النص، الرأس بأيقونتين، الخانات جوه بطاقة المصروف، شريحة الفترة صغيرة، والمتاح اليومي بشارة «تقريبي». مفيش أخطاء كونسول.
**ما اتجربش:** ⚠️ **لسه ما اتجربش على المحاكي ولا على حساب المالك** (§27)، وما اتبنيش APK، وما اتنشرش الويب.
**ملحوظات ظاهرة لسه محتاجة شغل:** سطر «توقع نهاية الفترة» طويل (3 سطور)، وزر «رسائل جديدة» لسه فوق المحتوى، وشاشة العمليات لسه من غير تجميع بالأيام.

### شريحة كود: تجميع العمليات بالأيام — 2026-09-11 (Claude Opus 5)
**اتعمل:**
- `domain/dayGroups.ts` (جديد): `groupByDay` دالة نقية بترجّع لكل يوم **مجموع الصادر** و**مجموع الوارد** منفصلين وعملياته. مفيش «صافي اليوم» عشان ما يبقاش رقم بيخلط اتنين مختلفين. الأحدث فوق، وجوه اليوم آخر سطر في الكشف فوق.
- `TransactionsScreen`: القائمة بقت مجموعات أيام، كل مجموعة ليها رأس فيه اسم اليوم ومجموع صادره. **الشاشة لا تحسب** — بتنادي الدالة زي ما بتنادي `formatAmount` (قاعدة #4).
- `TransactionsScreen.css`: قواعد `.txns__day*` مع البادئة عشان درس §29 (قواعد عامة كانت بتكسب في الترتيب).
- `analytics.ts`: سطر «توقع نهاية الفترة» بقى سطر واحد قصير («لو باقي الفترة مشي بنفس المعدل (N يوم عدوا)») بدل سطرين طويلين، والقصور لسه مذكور (spec/02).
- `PeriodPicker`: أسماء الشهور بقت مُصدَّرة عشان الشاشة تكتب «11 سبتمبر» من غير تكرار القايمة.
**اتفحص:** typecheck نضيف، و**517 اختبار ناجح** (4 جداد في `tests/acceptance/dayGroups.test.ts`: الترتيب، فصل الصادر عن الوارد، ترتيب اليوم الواحد، والقايمة الفاضية).
**ما اتجربش:** ⚠️ **الوضع التجريبي `?demo=1` مفيهوش ولا عملية** (بيزرع تصنيفات وقواعد وتجار ومحافظ بس) ⇒ **تجميع الأيام ما اتشافش بالعين**، اتأكدنا منه بالاختبارات بس. التأكيد البصري لازم يبقى **على المحاكي بحساب المالك** (§27). وما اتبنيش APK وما اتنشرش الويب.
**أخطاء وقعت وأنا بكتب الاختبار (للي بعدي):** `Transaction` لازم فيها `createdAt` و`updatedAt`، و`reviewState` قيمها `confirmed | suggested | needs_review` — مفيش `none`. الاختبارات نجحت وهي غلط، و**`npm run typecheck` هو اللي كشفهم** (قاعدة #14 مرة تانية).

### ✅ أول تجربة للتصميم الجديد على المحاكي بحساب المالك — 2026-09-11 (Claude Opus 5)
**الجلسة كانت لسه محفوظة في الـAVD فما احتاجتش كلمة سر** (المالك ما كتبش حاجة، وClaude ما كتبش أي بيانات دخول — §27).
- APK اتبنى من الكود الحالي: `dist-android-apk/masroufy-trial-v7.apk`، SHA256 `C22FBAA56B7F493F337DF944A06D247C8D65EE2F2AC1308E8752ED4DEA7A066F`. ⚠️ **الاسم اتكرر مع APK 1.6 القديم** (نفس versionCode 7) والمحتوى مختلف — **لازم نرفع versionCode قبل أي APK جديد للمالك** عشان الاسم ما يبقاش كذّاب.
- **الرئيسية اتأكدت بصريًا:** الرأس بأيقونتين، شريحة الفترة الصغيرة، الرقم الكبير + شارة «تقريبي» + سطر «N عملية محتاجة تأكيد ›»، الخانات التلاتة جوه البطاقة، **المتاح اليومي بقى رقم فعلي بسببه** (OVERRIDES §19 شغال على بيانات حقيقية)، سطر التوقع بقى سطر واحد، والشريط بخمس خانات وزر الإضافة في النص.
- **شاشة العمليات: تجميع الأيام شغال فعلًا** — رأس كل يوم فيه اسمه ومجموع صادره، و**5 عمليات ظاهرة من غير تمرير**.
- **عيوب اتشافت على المحاكي ولسه ما اتصلحتش:**
  1. شارة «تقريبي» كانت حمرا فبتبان تحذير ⇒ **اتصلحت في الكود** (`.badge--approx` لون هادي) — **لسه ما اتجربتش على المحاكي**.
  2. **صف العملية لسه بالشكل القديم:** المبلغ بسهم ↑ من غير إشارة، دايرة فيها أول حرفين من الاسم اللاتيني بدل لون التصنيف، وكل عملية في كارت لوحدها بمسافات كبيرة بدل بطاقة واحدة لليوم.
  3. **زرّي «حدّد الأنواع» و«مراجعة القديم» لسه فوق القايمة** — المفروض العدد يبقى رابط و«مراجعة القديم» تروح لشاشة المراجعة.
  4. خانة البحث وزر «فلترة» بياخدوا ارتفاع كبير.
- **ممنوع** كتابة أي مبلغ أو اسم تاجر من حساب المالك في الملفات دي (الريبو علني). الصور في `%TEMP%\masroufy-emulator\` فيها بياناته — ما تترفعش.

### شريحة كود: صف العملية وسطر الملخص — مُتحقَّق على المحاكي 2026-09-11 (Claude Opus 5)
**اتعمل:**
- `TransactionRow` اتعاد بناؤه على التصميم المعتمد: دايرة بلون التصنيف (`Category.lightColor` بشفافية 16%) وأول حرف من اسم التصنيف بالعربي بدل حروف لاتينية؛ سطر تاني واحد هادي «التصنيف · كاش · التاريخ» بدل شارات متناثرة؛ **نقطة صغيرة** جنب الاسم بدل شارة «محتاجة مراجعة» الحمرا (ومعاها تسمية كاملة لقارئ الشاشة)؛ والمبلغ بإشارة **−/+** بدل سهم.
  - ⚠️ **درس bidi:** الإشارة لما كانت في `span` بره خانة `.num` المعزولة كانت بتتقلب وتظهر بعد الرقم («5.00−»). الحل: الإشارة **جوه** نفس الخانة. اتشاف على المحاكي، والاختبارات ما كانتش هتكشفه.
- سطر ملخص شاشة العمليات: **الزرار السودا اتشالت**؛ العدد نفسه بقى رابط «N محتاجة تأكيد ›»، و«مراجعة القديم» رابط نصي هادي جنبه (اتساب لأن شاشة مراجعة الأنواع المستقلة لسه ما اتعملتش في الكود).
- صفوف اليوم بقت **بطاقة واحدة** بحد فاصل بين الصفوف بدل كارت لكل عملية.
- `TransactionRow.css` (جديد): ستايل الصف اتنقل من `TransactionsScreen.css` (بقى **291 سطر** بدل 363 — حد الـ300).
- `android/app/build.gradle`: **versionCode 8 / versionName 1.7** — لأن آخر بناء طلع باسم `v7` المكرر بمحتوى مختلف.
**اتفحص:** typecheck نضيف و**517 اختبار ناجح**، و**تجربة على المحاكي بحساب المالك (قراءة فقط)**: الرئيسية بشارة «تقريبي» الهادية، وشاشة العمليات بالتجميع اليومي والصفوف الجديدة والإشارة الصح، و5 عمليات ظاهرة من غير تمرير، و**كونسول الـWebView بلا أي خطأ** (بس سطور Capacitor: قراءة الرسائل متوقفة و0 رسالة).
- APK: `dist-android-apk/masroufy-trial-v8.apk`، SHA256 `C82202CEDC30B2BDA12D7B405FABB617742D9543BDC16246476540E84334A2CB`، نفس الشهادة (السكربت تحقق). **اتبعت للمالك.**
**ما اتعملش لسه:** شاشة «مراجعة الأنواع» المستقلة (التصميم موجود في Figma فاتح بس)، والإعدادات والمزيد في Figma، وخانة البحث لسه عالية شوية، والويب ما اتنشرش.

### شريحة كود: ورقة تحديد الأنواع على نظام التصميم — 2026-09-11 (Claude Opus 5)
**اتعمل:**
- `KindsSheet`: الاقتراح الأول في العمليات الغامضة بقى **متعلّم بإطار ومكتوب جنبه «الاقتراح»** (مش لون وحده)، والمبالغ جوه الورقة بقت بإشارة **−/+** جوه الخانة المعزولة زي باقي التطبيق.
- `KindsSheet.css`: الصفوف بقت بطاقات على نظام التصميم (سطح + زاوية `--r-metric`)، و**الحد الأحمر على الصف الغامض اتشال** (اللون لوحده مش دلالة — spec/04)، وأزرار الاختيار بقت 48 للمس.
- `ImportSheet.css` `.sheet__foot` (مشترك بين كل الأوراق): حد فاصل فوق الشريط ومسافة أمان تحت.
**قرار اتاخد بتفويض المالك («لو مردتش اختار إنت») — يحتاج رأيه:**
- تصميم Figma لشاشة «مراجعة الأنواع» بيعرض **عملية واحدة في الشاشة**. **لم يُنفَّذ كده**: الورقة الحالية بتخلي الواضح (15 عملية) يتأكد **بضغطة واحدة** والغامض بس هو اللي يتحدد فرديًا. السبب: تنفيذ «واحدة واحدة» حرفيًا كان هيخلي المالك يأكد 24 عملية واحدة واحدة. ❓ **لو المالك عايز الشكل الحرفي من Figma يقول.**
**اتفحص:** typecheck نضيف، **517 اختبار ناجح**، و**على المحاكي بحساب المالك**: الورقة بتفتح من سطر «N عملية محتاجة تأكيد» في الرئيسية، الإشارات ظاهرة صح، الشارات هادية، والكونسول بلا أخطاء.
- APK: `dist-android-apk/masroufy-trial-v8.apk`، SHA256 `80B1AA8A94AC06216EF1A9ED957655092F9F4746352C673C0CD0AE2A3828C084` (بيتغير مع كل بناء — الاسم بيفضل v8 لحد ما يترفع versionCode).
**🔴 بند مفتوح (مش من تعديلات التصميم، موجود من قبل):** `.sheet__foot` **sticky** جوه جسم الورقة، فالأزرار بتعوم فوق آخر سطر في القايمة. **الحل المقترح:** `.sheet__panel` يبقى `display:grid; grid-template-rows:auto 1fr auto` والجسم هو اللي يتمرر لوحده والذيل بره الجسم. **ما اتعملش** لأنه بيمس 6 أوراق (استيراد، إضافة عملية، ربط شخص، الأنواع، الإشعارات، القواعد) ومحتاج تجربة كل واحدة على المحاكي.

### إصلاح اتجاه شاشة «المزيد» + تجربتها على المحاكي — 2026-09-11 (Claude Opus 5)
- 🔴 **غلطة وقعت مني ولا الاختبارات ولا الفحص كشفوها:** في `MoreScreen` حطيت السهم قبل النص في ترتيب العناصر، فالنتيجة على الجهاز كانت **السهم على اليمين والاسم على الشمال** — بالعكس في العربي. القاعدة: **أول عنصر في الترتيب بيقع على اليمين في RTL**، فالنص الأول والسهم بعده.
- **اتجرب على المحاكي بحساب المالك بعد الإصلاح:** الخمس روابط بشرح تحت كل واحد، والاسم يمين والسهم شمال، و«آخر ست فترات» ظاهرة تحتها بالأرقام الحقيقية. الكونسول بلا أخطاء.
- APK: `dist-android-apk/masroufy-trial-v8.apk`، SHA256 `BA42708ADC10FEF141BCBB741E5D5F7E35797172DAD8449CE6B70846427315A8`. **اتبعت للمالك.**

### ملخص جلسة إعادة التصميم — 2026-09-11 (للي بعدي)
**اتعمل واتجرب على المحاكي بحساب المالك:** شريط تنقل بخمس خانات وزر إضافة في النص؛ رأس بأيقونتين؛ الرئيسية (بطاقة المصروف + الخانات جواها + شارة «تقريبي» هادية + سطر المراجعة + المتاح اليومي التقريبي OVERRIDES §19)؛ العمليات (تجميع بالأيام بمجاميع، صفوف بلون التصنيف وإشارة −/+، سطر ملخص بروابط)؛ ورقة الأنواع على نظام التصميم؛ شاشة «المزيد».
**الفحص:** `npm run typecheck` نضيف و**517 اختبار في 47 ملف** ناجحين في كل شريحة.
**لسه مفتوح:** (1) قرار التكرار — مسح ملف الكشف من البنك ولا ملف الإكسل القديم. (2) هل يبقى شكل «مراجعة الأنواع» زي Figma حرفيًا (عملية واحدة في الشاشة) بدل «وافق على الكل»؟ (3) `.sheet__foot` العايم فوق آخر سطر في 6 أوراق. (4) الإعدادات والمزيد ما اتعملوش في Figma (الحد اتخلص). (5) الويب ما اتنشرش.

### كشف البنك الجديد + مراجعة الأنواع «الاتنين مع بعض» — 2026-09-12 (Claude Opus 5)
**🔑 اكتشاف حسم قرار التكرار:** المالك بعت ملف كشف البنك (`document_*.pdf`، متجاهَل في `.gitignore` فما بيترفعش). قارئ المشروع نفسه قراه في اختبار مؤقت (اتمسح بعده): **68 صفحة، 1912 صف، صفر مرفوض، من 2025-01-01 إلى 2026-09-04**، وكل شهر فيه عمليات (أقل شهر 17 في سبتمبر 2026 الناقص، وأكتر شهر 165 في ديسمبر 2025).
⇒ **الكشف ده بيغطي كل تاريخ الإكسل وزيادة** (الإكسل 1911 صف). يعني **مسح دفعة الإكسل مش هيضيّع أي شهر** — وده اللي كان مقلقني قبل ما أشوف الملف. قرار المالك «امسح الإكسل» بقى آمن ومفهوم السبب.
**اتعمل في الكود (مفحوص بـtypecheck و517 اختبار، لسه ما اتجربش على المحاكي):**
- **مراجعة الأنواع بقت زي ما المالك اختار «الاتنين مع بعض»:** الواضح يتأكد **بضغطة واحدة** من القايمة، والغامض بقى **عملية واحدة في المرة** بزراير كبيرة وعدّاد «محتاجة قرارك (N)»، وزر «مش متأكد — سيبها دلوقتي» بيوديك للي بعدها من غير ما يغيّر حاجة.
- **إصلاح الأزرار العايمة:** `.sheet__foot` بقى **مش sticky** (اللزق كان بيخليه فوق آخر سطر)، وفي ورقة الأنواع الشريط اتنقل **بره الجزء اللي بيتمرر** فبقى ثابت تحت الورقة.
- `KindsRows.tsx` (جديد): صفوف العرض اتنقلت من `KindsSheet` (كان 305 سطر ⇒ بقى 215، والجديد 101 — حد الـ300).
**الخطوة الجاية:** مسح دفعة الإكسل من حساب المالك من سجل الاستيرادات (بموافقته الصريحة)، وبعدها استيراد الكشف الجديد.

### ✅ تنفيذ قرار التكرار: التراجع عن دفعة الإكسل — 2026-09-12 (Claude Opus 5)
**اتنفذ على حساب المالك من المحاكي، بأمر صريح منه («امسح الاكسل») وبعد ما بعت كشف البنك اللي بيغطي نفس الفترة كلها.**
- الدفعة: `alrajhi-statement.csv` · 2026-09-07 10:34 · 1912 عملية.
- المعاينة قبل التنفيذ (زي ما اتعرضت على الشاشة): **هيتشال 1911 من 1912**، و**هيفضل 1: متربطة بشخص**، وتحذير إن اللي هيتشال مش هيرجع إلا باستيراد الكشف تاني.
- بعد التنفيذ: حالة الدفعة بقت **«متراجَع عنه»** (متحقق بقراءة نص اللوحة من داخل التطبيق، مش تخمين).
- ⏱️ العملية أخدت **أكتر من دقيقة** والزر فضل «بنتراجع…» — **ممنوع تثبيت APK أو إعادة تشغيل التطبيق أثناءها**.
- **الباقي في الحساب دلوقتي:** دفعتَي الـPDF القديمتين (753 + 14) + العملية المرتبطة بشخص.
**الخطوة الجاية المطلوبة:** استيراد `document_*.pdf` (68 صفحة، 1912 صف، من 2025-01-01 لـ2026-09-04) عشان التاريخ يرجع كامل. منع التكرار الجديد (§30، مفتاح الرصيد) المفروض يتخطى الـ767 الموجودين ويضيف الباقي — **ده لسه ما اتجربش عمليًا**.

### 🔴 عيب في منع التكرار اتكشف على بيانات المالك — واتصلح — 2026-09-12 (Claude Opus 5)
**الحكاية:** بعد التراجع عن دفعة الإكسل، استوردنا كشف البنك (1912 صف). المعاينة قالت **1077 جديد و835 مكرر**. لكن الموجود في الحساب كان **768 بس** (753 + 14 من نسختَي الـPDF القديمتين + 1 متربطة بشخص) ⇒ يعني ~67 عملية حقيقية كانت هتتشال في صمت. **ما اتأكدش الاستيراد.**
**السبب (مثبت بفحص محلي مؤقت على نفس الملف، اتمسح بعده):** الكشف نفسه فيه **40 مجموعة** صفوف بتتشابه في (اليوم + المبلغ + الاتجاه + الرصيد) = **55 صف زيادة**، لأن **الراجحي بيطبع رصيد نهاية اليوم على كل سطور اليوم**. يعني قهوتين بنفس المبلغ في نفس اليوم ليهم نفس `balanceKey` بالظبط.
**العيب في الكود:** `buildDedupeIndex` كان بيخزّن **سجل واحد** لكل مفتاح رصيد (`if (!byBalance.has(...))`)، و`classifyCandidate` بيقرا منه من غير ما يستهلكه ⇒ أي عدد من الصفوف الواردة بنفس المفتاح كلهم «مكرر».
**الإصلاح:** `byBalance` بقى **قائمة** لكل مفتاح، و`classifyCandidate` بياخد عدّاد `consumedBalance` (قراءة فقط، الدالة فضلت نقية) ويرجّع `matchedBalanceKey`، و`runPreview` بيزوّد العدّاد ⇒ **مطابقة واحد لواحد**: كل سجل موجود يبتلع صفًا واردًا واحدًا بس.
**اتفحص:** اختبار جديد في `statementReimport.test.ts` (صفّان بنفس رصيد اليوم مقابل عملية واحدة موجودة ⇒ واحد مكرر والتاني حقيقي)، **518 اختبار ناجح** وtypecheck نضيف، واختبارات §30 القديمة كلها فضلت خضرا.
**لسه مطلوب:** بناء APK بالإصلاح ← إعادة الاستيراد على المحاكي ← المكرر المتوقع ~768 بدل 835 ← التأكيد ← التحقق من الأرقام.

### 🔴 السبب الحقيقي للـ835 «مكرر» — إصلاح تاني — 2026-09-12 (Claude Opus 5)
**بعد إصلاح المطابقة واحد-لواحد، المعاينة على المحاكي طلعت بنفس الأرقام بالظبط (1077 جديد · 835 مكرر).** التحقق إن الإصلاح فعلًا جوه النسخة المثبتة اتعمل بالبحث عن `matchedBalanceKey` في ملفات الويب المبنية جوه `android/app/src/main/assets/public` — **موجودة**، يعني مش مشكلة بناء ولا كاش.
**السبب الحقيقي:** `loadExisting` في `importPreview.ts` كان بيبني قائمة «الموجود» من **سجلات المصدر** (`sourceRecords`) مش من **العمليات**. وكشف المالك اتستورد مرتين قبل كده (753 + 14)، فالعملية الواحدة بقى ليها أكتر من سجل مصدر ⇒ تتعد أكتر من مرة في الفهرس ⇒ تبتلع أكتر من صف وارد. وده بيفسّر إزاي «المكرر» (835) طلع **أكبر من عدد العمليات الموجودة** (768).
**الإصلاح:** سجل واحد لكل `transactionId` في `loadExisting` (`seenTransactions`).
**اتفحص:** اختبار جديد (عملية ليها سجلَّي مصدر + صفّين واردين بنفس مفتاح الرصيد ⇒ **مكرر واحد بس**)، **519 اختبار ناجح** وtypecheck نضيف.
**لسه مطلوب (الخطوة الجاية مباشرة):** بناء APK بالإصلاحين ← إعادة المعاينة على المحاكي ← لو المكرر نزل لـ≤768 يتأكد الاستيراد ← التحقق من رجوع التاريخ كامل.

### 🔴 الاستيراد كان بيترفض بصمت — واتصلح — 2026-09-12 (Claude Opus 5)
**البلاغ:** بعد التراجع عن الإكسل، الاستيراد اتعمل **مرتين** وكل مرة الورقة كانت بتقفل كأنها نجحت، و**مفيش ولا عملية اتكتبت** ومفيش دفعة جديدة في السجل.
**السبب (مقروء من الكود، مش تخمين):** في `importStatement.commit` كان فيه `if (fresh.previousBatch) return fresh.previousBatch` — وبصمة الملف كانت متسجلة في دفعة 2026-09-10 (14 عملية من نفس الـPDF). فالدالة بترجّع الدفعة القديمة وتعتبر نفسها نجحت **من غير أي كتابة**، و`ImportSheet.runCommit` كان بينادي `onImported()` على طول من غير ما يبص على الراجع ⇒ الورقة تقفل كأنها تمام.
**قرار المالك 2026-09-12:** «اعتبر إن حد رفع ملفات كتير فيها عمليات مكررة — عدّل الحارس بالطريقة الأسرع». ⇒ الاستيراد **يكمل طول ما فيه صفوف جديدة مختارة**، ويرجّع الدفعة القديمة **بس لو مفيش أي جديد**. منع التكرار بيشتغل صف بصف فمفيش خطر.
**اتعمل:**
- `importStatement.ts`: الحارس بقى مشروط بـ`addable.length === 0`.
- `ImportSheet.tsx`: لو الراجع هو نفس الدفعة القديمة ⇒ **رسالة واضحة** «الملف ده اتستورد قبل كده ومفيش فيه حاجة جديدة تتضاف» بدل ما الورقة تقفل كأنها نجحت.
- اختبار جديد: ملف اتستورد منه صف واحد، وبعدين اتستورد كامل ⇒ الصفين الجداد يتضافوا (`counts.imported === 2`).
**اتفحص:** typecheck نضيف و**520 اختبار ناجح**. الملفات 202 و289 و171 سطر.
**لسه مطلوب:** بناء APK ← إعادة الاستيراد على المحاكي ← التأكد إن الدفعة الجديدة (1077) ظهرت في السجل وإن التاريخ رجع ← تشغيل «افحص البيانات القديمة» (وافق عليه المالك: 3760 مستند من 5648، و396 رابط تايه).

### ✅ التاريخ رجع: استيراد الكشف نجح بعد إصلاح الحارس — 2026-09-12 (Claude Opus 5)
**الدليل (مش استنتاج):** سجل الاستيرادات بقى فيه دفعة جديدة **`masroufy-statement.pdf · 2026-09-12 12:16 · 1077 عملية · مسجّل`**، والرئيسية اتغيرت من 16 عملية في الفترة لـ**34**، والمصروف من 5,395.00 لـ7,921.35.
**الترتيب اللي حصل فعلًا (للي بعدي):**
1. التراجع عن دفعة الإكسل (1911 اتشالت، 1 فضلت).
2. إصلاح مطابقة الرصيد **واحد لواحد** (الراجحي بيطبع رصيد نهاية اليوم على كل سطور اليوم).
3. إصلاح عد العملية **مرة واحدة** بدل مرة لكل سجل مصدر.
4. إصلاح **الحارس اللي كان بيرفض الملف بصمت** لأن بصمته متسجلة (ده كان السبب الحقيقي لفشل الاستيرادين الأولين).
5. الاستيراد اتأكد من المحاكي على حساب المالك: 1077 جديد · 835 مكرر · صفر تعارض · صفر غير صالح.
**درس للي بعدي:** الفشل كان **صامت تمامًا** — الورقة بتقفل كأنها نجحت. التحقق الوحيد اللي كشفه هو **قراءة سجل الاستيرادات بعد كل استيراد**، مش شكل الشاشة. لا تعتبر أي استيراد نجح قبل ما تشوف دفعته في السجل.
**الباقي:** تشغيل «افحص البيانات القديمة» (وافق عليه المالك — 3760 مستند من 5648، و396 رابط تايه).

### ⚠️ إصلاح البيانات القديمة اتقطع — ومفيش ضرر — 2026-09-12 (Claude Opus 5)
**المالك وافق على تشغيل «افحص البيانات القديمة»** (3760 مستند من 8638 محتاجين إصلاح معرّفات: العمليات 690 · مصادر العمليات 3067 · دفعة 1 · دين 1 · تخصيص 1، و396 رابط تايه مش ممكن يترجع بيقين).
**اللي حصل:** بعد الضغط على «احفظ نسخة وصلّح»، التطبيق فتح **نافذة حفظ ملف بتاعة النظام** (`masroufy-before-repair-2026-09-12.json`). النافذة دي **بترمي التطبيق للخلفية**، وبعد الضغط على SAVE الشاشة خرجت للانشر ⇒ **العملية اتقطعت**: الملف اتعمل بـ**صفر بايت**، ومفيش ولا مستند اتكتب.
**التحقق (مش استنتاج):** الأرقام بعد الانقطاع **مطابقة تمامًا** للي قبله — المصروف 7,921.35 · 34 عملية في الفترة · الدخل 11,025.98 · المتبقي 3,104.63. يعني **الإصلاح ما بدأش يكتب أصلًا**، وبيانات المالك سليمة. الملف الفاضي اتمسح من `/sdcard/Download/` عشان ما يتحسبش نسخة صالحة.
**🔴 عيب مسجَّل (مش مُصلَح):** `IdRepairPanel` بيحفظ النسخة عن طريق تنزيل ملف، وعلى أندرويد ده بيفتح نافذة نظام بترمي الـWebView للخلفية وتقطع الإصلاح. **الاقتراح للي بعدي:** النسخة تتحفظ من غير نافذة (Capacitor Filesystem أو حفظ داخلي)، أو الإصلاح يشتغل على دفعات مع استئناف، أو المالك يشغّله من المتصفح على الكمبيوتر بدل الموبايل.
**الوضع النهائي للبيانات دلوقتي:** التاريخ كامل ومستورد (1077 عملية في دفعة `masroufy-statement.pdf`)، وإصلاح المعرّفات **لسه ما اتعملش** — بند مفتوح.

### ✅ مراجعة الأنواع اتجربت على بيانات المالك — 2026-09-12 (Claude Opus 5)
الشكل اللي اختاره المالك («الاتنين مع بعض») اشتغل على الجهاز ببياناته الحقيقية:
- **«واضحة (6)»**: قايمة بخانات اختيار وزر «وافق على 6» — موافقة بالجملة.
- **«محتاجة قرارك (28)»**: **عملية واحدة في المرة** بعدّاد، وبطاقة فيها الوصف والمبلغ بإشارة والسبب، وزرين كبار **الاقتراح فيهم متعلّم بإطار ومكتوب جنبه «الاقتراح»**، وتحتهم «مش متأكد — سيبها دلوقتي».
- **شريط الأزرار تحت بقى ثابت** ومفيش سطر بيبان تحته — إصلاح `.sheet__foot` (شيل sticky + نقله بره الجزء اللي بيتمرر) **متحقق على الجهاز**، مش بالاختبارات بس.
**كده كل شاشات إعادة التصميم اتجربت على المحاكي بحساب المالك:** الرئيسية، العمليات، المزيد، الإعدادات (المظهر والحساب)، ومراجعة الأنواع.

### 📌 قرارات المالك الجديدة — 2026-09-13 (اتنفذش منها حاجة لسه)
1. **إصلاح المعرّفات: «اصلحهم».** يعني صلّح عيب `IdRepairPanel` (النسخة الاحتياطية بتفتح نافذة نظام بترمي التطبيق للخلفية) وبعدين شغّل الإصلاح على حسابه وتحقق.
2. **الـ396 رابط التايه: ممنوع نتخلى عنهم.** نص كلامه: «غير مسموح اننا نتخلي عن حاجة لو في حاجة مش شغالة لازم نبحث وراها ليه مش شغالة و نحلها». المطلوب: تحقيق يعرف كل رابط منهم (مجموعة/حقل) بيشاور على إيه ومن فين جه، وحل لكل نوع — مش «هيفضل زي ما هو».
3. **فيجما:** يكمل الشغل في الكود لحد ما يشترك، وبعدها نرجع للملف.
4. **ميزة جديدة: قفل التطبيق بالبصمة** (biometric lock). محتاجة قرارات منه قبل التنفيذ (❓ مفتوحة): يتقفل عند الفتح بس ولا كمان بعد مدة في الخلفية (كام دقيقة)؟ لو البصمة فشلت يفتح برمز الجوال (device PIN)؟ إجباري ولا اختياري من الإعدادات؟ ومحتاجة إضافة Capacitor plugin **بسبب مكتوب في `ARCHITECTURE.md`** (قاعدة 8).
5. **سؤال منه (اتجاوب في الشات):** تكلفة سحب الرسايل طول الوقت حتى والجوال مقفول لـ10 مستخدمين، وأنهي اشتراك فيجما مناسب.

## 36. ✅ إصلاح المعرّفات اتنفذ على حساب المالك + حل لغز الـ396 — 2026-09-13 (Claude Opus 5)
**هذا القسم هو نقطة الاستكمال الحالية.**

### ١) عيب النسخة الاحتياطية اتصلح (ARCHITECTURE §26)
- **السبب:** `IdRepairPanel` كان بيحفظ النسخة بنافذة حفظ النظام، والنافذة بترمي الـWebView للخلفية فالإصلاح اتقطع قبل أول كتابة.
- **الحل من غير مكتبة جديدة:** طريقتين جداد في `LocalFilesPlugin.java` (`writeAppFile`/`appFileSize`) بيكتبوا في `Android/data/app.masroufy.personal/files/backups` **من غير نافذة**، و`RepairBackupPort` ← `deviceRepairBackup` بيبعت النص قطع ويقرا الحجم من القرص.
- `repairStoredIds.apply`: نسخة **المستندات اللي هتتكتب بس** ← مقارنة الحجم على القرص بعدد بايتات UTF-8 (أي اختلاف = رفض ولا مستند) ← كتابة على دفعات 100 مع تقدم. **قابل للاستئناف** من غير حالة محفوظة: اللي اتكتب مش بيظهر في الفحص التاني.
- اختبارات جديدة: `idRepairResume.test.ts` (النسخة قبل أول كتابة، نسخة ناقصة ⇒ صفر كتابة، انقطاع في النص ⇒ الفحص التاني يكمّل، تقسيم الجسر ما يكسرش رموز UTF-16).

### ٢) التنفيذ على حساب المالك (المحاكي، APK 1.8 / versionCode 9) — بموافقته الصريحة
- المعاينة قبل التأكيد (اتبعتت للمالك كسؤال ووافق): **3760 من 8638** — العمليات 690 · مصادر العمليات 3067 · دفعة 1 · دين 1 · تخصيص 1.
- التقدم اتشاف بيطلع لحد «3700 من 3760»، والرسالة: «اتصلح 3760 مستند».
- **النسخة اتأكدت بعيني مش من الشاشة بس:** `adb shell ls -l` لقى الملف على الجهاز بحجم **3,046,765 بايت** (= 2975 ك.ب اللي التطبيق أكّدها قبل أول كتابة).
- **الأرقام بعد الإصلاح مطابقة لقبله** بمقارنة بصمة SHA-256 لكل الأرقام الظاهرة (من غير ما الأرقام نفسها تطلع في الشات): الرئيسية 44 رقم نفس البصمة، و«آخر ست فترات» 18 رقم نفس البصمة.
- **الفحص التاني بعد الإصلاح: «هيتعدّل 0 مستند من 8638»** — نفس العدد الكلي (مفيش حاجة اتمسحت)، والـ396 والـ370 زي ما هم (متوقع؛ دول شغل التنظيف في ٤). عرض الصفحة سليم (1082).
- **سجل الاستيرادات بعد الإصلاح مطابق للمكتوب هنا** (ما اتاخدلوش بصمة قبل الإصلاح، فالمقارنة مع HANDOVER): PDF 09-12 · 1077 · مسجّل — PDF 09-10 · 14 · مسجّل — PDF 09-08 · 753 · مسجّل — الإكسل 09-07 · 1912 · متراجَع عنه. ودفعات الـPDF بقت بتتعرض «PDF الراجحي» (بند ٥ اتجرب على المحاكي).
- ⚠️ **ما اتجربش على المحاكي:** إصلاح عرض المسار الطويل (بند ٣) — اتكتب بعد بناء APK 1.8.

### ٣) 🔴 عيب شاشة اتشاف على المحاكي واتصلح في الكود (لسه ما اتجربش)
رسالة النجاح كان فيها المسار الكامل كلمة واحدة طويلة، فالـWebView **وسّع الصفحة كلها** (1596 بدل 1080) وشريط التنقل نزل تحت الشاشة، وأي ضغطة على التبويبات ما بتعملش حاجة. الحل: المسار من «Android/data» بس، و`.notice` بقى فيه `overflow-wrap: anywhere`. **درس:** ضغطات التبويب «نجحت» في السكربت بس الشاشة ما اتغيرتش — **اتأكد من عنوان الشاشة (`h1`) بعد كل ضغطة**.

### ٤) الـ396 رابط التايه — اتعرفوا (قراءة بس، `domain/idRepairDiagnosis.ts`)
التشخيص بيرجّع أعداد وحالات بس (مفيش مبالغ ولا أوصاف ولا معرّفات) ومعروض تحت «تفاصيل» في لوحة الإصلاح:
- **كل الـ396** = سجلات مصدر · حقل العملية · معرّف سليم الشكل · حالتها كانت «جديد» · **تبع دفعة الإكسل 2026-09-07 المتراجَع عنها**.
- **370 عملية يتيمة** (مفيش سجل مصدر بيشاور عليها) · كلها اتعملت 2026-09-07 10:34 وقت دفعة الإكسل · **كلها الـ370 ليها توأم** بنفس اليوم والمبلغ والاتجاه عليه مصدر.
- **السبب (مقروء من الكود):** `revertImportBatch.execute` بيحذف بـ`record.id` و`record.transactionId` المخزنين، و`deleteMany` بيبني مسار الوثيقة منهم. لما كانوا مقصوصين (§23) الحذف راح لمسار مش موجود **ونجح بصمت**. النتيجة: سجلات مصدر معرّفها مقصوص فضلت (الـ396، وعملياتها اتمسحت فعلًا)، وعمليات معرّفها في السجل مقصوص فضلت (الـ370).
- **الأثر المرجّح:** الـ370 **متحسوبين مرتين** في أرقام المالك دلوقتي (مرة من الإكسل اليتيم ومرة من الـPDF).
- **الفرضيات التانية اتنفت:** مش استيراد اتقطع (مفيش دفعة `staged`)، ومش شكل قص تاني (كلهم «سليم الشكل»، ومفيش «بيطابق أكتر من مستند»).
- **قرار المالك (2026-09-13): «ابنِ التنظيف بمعاينة».** اتبنى في الكود ولسه **ما اتوصلش بالشاشة ولا اتشغّل**: `domain/orphanCleanup.ts` + `cleanupOrphans` + `RemoveDocsPort` (حذف **بمسار الوثيقة** مش بالحقل) + 8 اختبارات. العملية ما تتمسحش إلا بكل الأدلة: مفيش مصدر من دفعة سليمة، اتعملت في حدود 10 دقايق من دفعة متراجَع عنها، ليها توأم مسجّل (والعدد ما يزيدش عن التوائم، والمحفظة متوافقة)، ومفيش عليها شخص/التزام/تسوية/وسم/استثمار. **مفيش حذف قبل ما المالك يشوف المعاينة بالأعداد ويوافق.**

### ٤-ب) معاينة التنظيف على حساب المالك (APK 1.9، قراءة بس — **ما اتمسحش حاجة**)
- «عمليات مكررة هتتمسح: **417**» · «سجلات مصدر من غير عملية هتتمسح: **525**» · **1** متربطة بشخص (هتفضل) · **81** مالهاش توأم مسجّل (هتفضل). موزعة على كل الشهور من 2025-01 لـ2026-09.
- **ليه الأرقام أكبر من التشخيص (396 / 370)؟** التشخيص بيعد العملية «يتيمة» لو **مفيش ولا سجل** بيشاور عليها، والتنظيف بيعدها بقايا لو **مفيش سجل من دفعة سليمة** بيشاور عليها:
  - 525 سجل = الـ396 (عملياتهم اتمسحت) + **129** سجل من دفعة الإكسل معرّفهم **ورابطهم** كانوا مقصوصين، فالتراجع فوّتهم هما وعملياتهم. إصلاح المعرّفات رجّع روابطهم، فبقوا بيشاوروا على بقايا الإكسل.
  - 499 عملية من بقايا الإكسل = الـ370 + الـ129 دول. منهم 417 ليهم توأم، و81 من غير، و1 متربطة.
- ⚠️ **الـ81 مش محسومين:** يا إما عمليات حقيقية مالهاش نسخة تانية، يا إما مكررة بس تاريخها في الإكسل مختلف عن الـPDF (فمش بتطابق «نفس اليوم»). **مش هيتمسحوا** بالقاعدة الحالية.
- ~~«طابق الرصيد» قبل وبعد~~ **ما ينفعش**: تاريخ افتتاح الراجحي في الحساب 2026-09-07 فالمطابقة ما بتقارنش ولا سطر (السؤال 0 تحت).
- **البديل اللي اتبنى (قراءة بس): دليل سلسلة رصيد الكشف جوه معاينة التنظيف** — `domain/balanceChainCheck.ts`:
  - كل سطر فيه رصيد معلن لازم يساوي رصيد اللي قبله ± مبلغه، **مقارنة محلية** (مش تراكمية): مكرر زيادة = كسر واحد تقريبًا، وعملية حقيقية اتشالت = كسر واحد.
  - المعاينة بتعرض تلات حالات: **دلوقتي** · **بعد التنظيف** · **لو الـ81 اتشالوا كمان (افتراض بس، مش بيتمسحوا)**. لو الكسور بتختفي في التالتة ⇒ الـ81 مكررين؛ لو بتزيد ⇒ حقيقيين.
  - أعداد وشهور بس، مفيش مبالغ. 5 اختبارات في `balanceChainCheck.test.ts`، و**552 اختبار ناجح** وtypecheck نضيف.
  - APK 1.10 (versionCode 11): `dist-android-apk/masroufy-trial-v11.apk`، SHA256 `4368BCA7FEE3505A3CCAD74A93BE9BBE79CA7E713AD170EC34C4D4EF5E18B786`.
  - **النتيجة على حساب المالك (APK 1.10، قراءة بس):** 2343 سطر فيه رصيد · **دلوقتي 431 كسر** · **بعد مسح الـ417: 18 كسر** · **لو الـ81 اتشالوا كمان: 93 كسر**.
    - الكشف 1912 سطر، و2343 − 1912 = **431 بالظبط** = عدد الكسور ⇒ كل سطر زيادة = كسر واحد. الطريقة شغالة.
    - الـ81 **أغلبهم حقيقيين** (شيلهم بيزود ~75 كسر) ⇒ قاعدة التوأم كانت صح لما سابتهم.
    - ⚠️ لو الـ417 كلهم مكررين كان المتوقع 14 كسر مش 18 (1926 − 1912). لو فيه k عملية حقيقية منهم الكسور = 14 + 2k ⇒ **ممكن لحد 2 حقيقيين**، أو الـ4 الزيادة من ترتيب نفس اليوم. **مش يقين كفاية لمسح 417.**
  - **القرار (Claude، قبل ما يسأل المالك):** الدليل بقى **لكل عملية لوحدها** — `judgeCandidates`: العملية تتمسح بس لو **قاعدة التوأم + سلسلة الرصيد** الاتنين متفقين (رجوعها للسلسلة بيكسرها). اللي الرصيد ما أكدهوش بيفضل. ونفس الاختبار بيصنّف الـ81 (مكرر بتاريخ مختلف / حقيقي / مش واضح) — **أعداد بس، ومحدش منهم بيتمسح**.
  - **النتيجة بالقاعدة الأشد على حساب المالك (APK 1.11 / versionCode 12، SHA256 `50A7D816CE2B5F59DB10AE7D4EB7F31744258C6E1C8A804C75CF190C61FC8A2D`، قراءة بس — ما اتمسحش حاجة):**
    - **415** هتتمسح (التوأم والرصيد متفقين) · **2** ليهم توأم بس الرصيد قال حقيقيين (هيفضلوا) · **525** سجل مصدر هيتمسح · **1** متربطة بشخص · **81** من غير توأم: الرصيد بيقول **80 حقيقيين و1 مكررة بتاريخ مختلف**.
    - سلسلة الرصيد: دلوقتي **431** كسر ← بعد التنظيف **16** كسر ← لو الـ81 اتشالوا **91**.
    - **الحساب قافل بالظبط:** 431 − 415 = 16، و2343 − 415 = 1928 = 1912 سطر الكشف + **16 سطر زيادة** ⇒ كل عملية من الـ415 صلّحت كسر واحد بالظبط ⇒ **ولا واحدة منهم حقيقية**.
    - الـ16 سطر الزيادة اللي هيفضلوا = 2 (الرصيد ما أكدهمش) + 1 (متربطة بشخص) + 1 (مكررة بتاريخ مختلف) + **12 في مكان تاني** (مش من بقايا الإكسل؛ لسه ما اتعرفوش) ⇒ بند تحقيق جاي.
    - **المسح مستني موافقة المالك** (اتبعتله سؤال بالأرقام دي).
  - **✅ ردود المالك (2026-09-13، مسجلة في OVERRIDES §21–22):**
    1. **«امسح دلوقتي» — ✅ اتنفذ واتحقق (2026-09-13 12:48، APK 1.11):**
       - السكربت اتأكد إن الزر مكتوب عليه **940 بالظبط** قبل الضغط. التقدم وصل «900 من 940»، والرسالة «اتمسح 940 مستند».
       - **النسخة على الجهاز بعيني:** `masroufy-before-cleanup-…json` بحجم **917,193 بايت** (= 896 ك.ب اللي التطبيق أكدها قبل أول حذف). ملف نسخة الإصلاح القديم لسه موجود جنبه.
       - **الفحص التاني:** «هتتمسح: 0» و«سجلات: 0» · السطور اللي فيها رصيد **1928** (= 2343 − 415 بالظبط) · الكسور **16** (= اللي اتوقعناه بالظبط).
       - إعادة تصنيف طبيعية بعد المسح: «الرصيد ما أكدهاش» بقوا **3** و«من غير توأم» **80** (كانوا 2 و81) — واحدة لقت توأم بعد ما المكرر اتشال والرصيد ما أكدهاش. المجموع 84 زي ما هو، ومفيش حاجة اتلمست.
       - ⚠️ **أرقام الرئيسية والفترات اتغيرت عن قصد** (المكرر اتشال) — مش مقارنة بصمة زي الإصلاح.
    2. **«صلّحهم للمعتمد» — ✅ اتنفذ واتحقق (2026-09-13 12:56):**
       - **الراجحي:** المبلغ كان أصلًا المعتمد، واتغير **التاريخ بس** من 2026-09-07 لـ **2025-01-01** (نافذة التقويم: السنة ← 8 شهور لورا ← يوم 1، والتأكد إن رأس النافذة «2025 · Wed, Jan 1» قبل SET، وإن قيمة الخانة `2025-01-01` قبل «حفظ»).
       - **الكاش:** المبلغ اتكتب `1000.00` بـ`adb input` (قيمة معتمدة من CLAUDE.md) والتاريخ **2026-09-06** (رأس النافذة «2026 · Sun, Sep 6»). السكربت كان هيقف لو أي قيمة مش مطابقة بالظبط قبل الحفظ.
       - **بعد الحفظ:** الصف بيعرض التاريخ والمبلغ المعتمدين والتاريخ القديم اختفى، ومفيش خطأ — للاتنين.
       - ⚠️ **درس:** أول قراءة بعد «حفظ» للراجحي قالت إن التاريخ لسه قديم — لأن الحفظ بيعيد تحميل المحافظ بعده. القراءة التانية بعد ما استنينا لقت الجديد. **استنى إعادة التحميل قبل ما تحكم.**
       - **«طابق الرصيد» للراجحي بعد التصحيح (قراءة بس):** بقى بيقارن فعلًا — **1928 حركة**، والحكم **«1913 سطر رصيده ما طابقش من 1928»**، وأول فرق **2025-01-03** (يوم فيه 10 حركات).
         - **ده متوقع ومش معناه إن 1913 سطر غلط:** المطابقة **تراكمية**، فسطر زيادة واحد بدري بيزق كل الأرصدة اللي بعده. السطور قبل 2025-01-03 طابقت ⇒ **الرصيد الافتتاحي المعتمد صح**.
         - فحص السلسلة المحلي لقى **كسرين في 2025-01** من الـ16 سطر الزيادة ⇒ المطابقة الكاملة مش هتحصل غير لما الـ16 يتحلوا. ده بند «حقق فيهم» اللي المالك وافق عليه.
    4. **«حقق فيهم بعد المسح» — نتيجة التحقيق (قراءة بس، APK 1.12 / versionCode 13، SHA256 `5766FC0D36A68D799C0669DCA6517201161FC246ECC7ED7E397CE917275A4F47`):**
       - `domain/chainBreakReport.ts` (دالة نقية، أعداد بس) بيقول لكل كسر: أصل السطر المكسور واللي قبله، نفس اليوم ولا لأ، وهل ليه نسخة مطابقة **حتى في الرصيد**. معروض في لوحة التنظيف تحت «الكسور اللي هتفضل — جاية منين». 5 اختبارات، و**558 اختبار ناجح**.
       - **الـ16 كلهم مكررين بالكامل (نفس اليوم والمبلغ والاتجاه والرصيد):**
         - **14** سطر من **دفعة 2026-09-10** (`DOC-****0905-WA0044.pdf`، 14 عملية — كانت بتتعرض «CSV» قبل تصحيح الاسم). يعني **الدفعة دي كلها تقريبًا مكررة** لسطور موجودة. التنظيف ما لمسهاش لأنها دفعة سليمة مش متراجَع عنها.
         - **2** من بقايا الإكسل اللي فضلت (من الـ3 اللي الرصيد ما أكدهمش) — ليهم نسخة مطابقة حتى في الرصيد.
       - **معاينة التراجع عن دفعة 09-10 (قراءة بس، اتضغط «إلغاء» بعدها):** «**هيتشال 14 عملية من 14**» — ولا واحدة ليها مصدر تاني أو شخص أو تسوية. بعد الإلغاء الدفعة لسه «مسجّل» ⇒ **مفيش حاجة اتمسحت**.
       - ده متسق مع التقرير: الـ14 كسر كلهم من الدفعة دي. التراجع عنها + مسح نسختين الإكسل المطابقتين ⇒ المتوقع **صفر كسر** ومطابقة رصيد كاملة. **مستني موافقة المالك.**
       - ⚠️ درس: **الكسر بيتسجل على السطر اللي بعد المشكلة** لما السطر الغلط نفسه متسق مع رصيده (اتكشف باختبار فشل وكان التوقع هو الغلط، مش الدالة).
       - مفيش طريق جاهز: `seedWallets` **بيزرع مرة واحدة بس** ومش بيكتب فوق محافظ موجودة (عن قصد). التصحيح هيبقى من محرر المحفظة في الإعدادات (المبلغ بـ`adb input` لأنه قيمة معتمدة مش كلمة سر، والتاريخ من نافذة التقويم بتاعة أندرويد). **ممنوع** تعديل القيم بـDevTools (المالك سمح بيها للقراية بس).
       - 🔴 **دين تقني قديم (مش من الجلسة دي):** `SettingsScreen` بيحفظ المحفظة بـ`user.wallets.save` مباشرة — **مخالف للقاعدة 4** (الشاشة لا تكلّم مستودعًا). المفروض حالة استخدام. ما اتصلحش هنا عشان حد الـ300 سطر في `SettingsScreen` ومش في نطاق المطلوب.
    3. **جوال من غير قفل:** «يوقف القفل ويقول كده» — ده السلوك المنفَّذ فعلًا، والسؤال اتقفل.
    4. **«حقق فيهم بعد المسح»** — الـ16 سطر الزيادة. ⏳ بعد المسح وتصحيح الافتتاح.

### ٥) بنود صغيرة
- دفعات الـPDF بتتسجل `pdf_alrajhi` بدل «ملف CSV»، والدفعات القديمة اللي اسمها `.pdf` بتتعرض «PDF الراجحي» في السجل.
- الـ28 عملية المحتاجة تأكيد نوع: **المالك هيعملها بنفسه** — ما تتلمسش.

### ٦) قفل البصمة — قرارات المالك في OVERRIDES §21، والكود جارٍ
- اختياري من الإعدادات · يتقفل عند الفتح وبعد **خمس دقايق** في الخلفية · لو البصمة فشلت يفتح **برمز الجوال**.
- اتكتب (ملفات جديدة بس، **لسه ما اتوصلش ولا اتبنى ولا اتجرب**): `domain/appLock.ts`، `useCases/appLock.ts`، `DeviceLockPlugin.java` (BiometricPrompt)، `androidDeviceLock.ts`، `localAppLockSettings.ts`، `AppLockGate.tsx`، واختبارات (10).
- **ناقص:** اعتماد `androidx.biometric:biometric:1.1.0` في `build.gradle` وسببه في ARCHITECTURE، `registerPlugin` في `MainActivity`، زر في الإعدادات، تركيب البوابة، والتجربة على المحاكي.
- المالك وافق إن Claude يعمل **قفل شاشة وبصمة وهمية على المحاكي** للتجربة (المحاكي كان مالوش ولا واحد).
  - اتعمل رمز قفل شاشة للتجربة بـ`adb shell locksettings set-pin` (**على المحاكي بس**، الرمز اتقال للمالك في الشات ومش مكتوب هنا عشان الريبو علني). لو محتاج تشيله: `adb shell locksettings clear --old <الرمز>`.
  - ⚠️ **شاشات تسجيل البصمة في إعدادات أندرويد بتطلع صورتها سودا** (النظام مانع التصوير). استعمل `-Action ui` بدل `shot` عشان تقرا الأزرار ومكانها.
  - البصمة الوهمية: `adb -e emu finger touch 1` وبعدها `emu finger remove 1`، مكررين لحد ما التسجيل يخلص (خلص من 3 لمسات).
- ⚠️ **درس ضغطات المحاكي:** إحداثيات ثابتة (زي صف «الإعدادات» في «المزيد») غلطت مرتين لأن الشاشة بتتحرك بعد أول رسم — مرة فتحت ورقة «التصنيفات وألوانها» بدل الإعدادات (اتقفلت من غير أي حفظ). **الصح:** استنى الموضع يثبت، اقرا مكان العنصر بـ`getBoundingClientRect() × devicePixelRatio` وزوّد **132** (شريط الحالة فوق)، واضغط، وبعدها **اتأكد من `h1`**.
- **APK 1.9 (versionCode 10)** فيه التنظيف والقفل متوصّلين: `dist-android-apk/masroufy-trial-v10.apk`، SHA256 `59B7139E27C05E2D39952231726E56CF25894A3E24D57159361D32FF173C937B`. اتبنى (يعني `DeviceLockPlugin.java` بيتجمّع مع `androidx.biometric`) واتثبت على المحاكي.
- **✅ اتجرب على المحاكي بحساب المالك (2026-09-13):**
  - **التشغيل من الإعدادات:** «شغّل قفل البصمة» ← حوار النظام ظهر (وفيه زر «Use PIN» — يعني بديل رمز الجوال شغال) ← بصمة وهمية ← الحالة بقت «متشغّل»، والإعداد اتحفظ على الجهاز، ورسالة النجاح ظهرت. القفل ما غطاش التطبيق وقت التشغيل.
  - **الفتح من الأول (force-stop ثم فتح):** حوار البصمة طلع لوحده، والتطبيق تحته مغطّى بـ«مصروفي مقفول» ومقفول على اللمس (`inert`) ← بصمة ← القفل اختفى والرئيسية ظهرت.
  - **خلفية 20 ثانية:** رجع من غير قفل (نفس العملية، مش إعادة تشغيل).
  - **خلفية 5 دقايق و20 ثانية:** اتقفل وحوار البصمة طلع لوحده.
  - **فتح برمز الجوال:** «Use PIN» ← شاشة الرمز ← الرمز ← القفل اختفى والتطبيق شغال.
  - **ما اتجربش:** جهاز حقيقي (S24)، وجوال من غير قفل شاشة خالص (السؤال المفتوح 1)، وأندرويد 9/10.

## 37. 📥 طلبات جديدة من المالك — 2026-09-13 (لسه ما اتنفذش منها حاجة)
> اتكتبت هنا عشان ما تضيعش. **القرارات المفتوحة ما تتحسمش من غير المالك** (قاعدة 17).

### ▶️ تحديث 2026-09-14 (اقرأه قبل نقطة الاستكمال اللي تحته)
- **الاتجاه اتحدد (OVERRIDES §29):** نخلّص المتفق عليه في التطبيق الحالي ← **النقل لكوتلن نيتف** ← المميزات الجديدة هناك. ما تبنيش مميزات جديدة مش متفق عليها هنا.
- **التصنيفات (OVERRIDES §28) — القرارات جاهزة:** رموز مرسومة موحدة (مكتبة رموز، سببها في ARCHITECTURE قبل الإضافة)، **قايمة فروع جاهزة يراجعها المالك في صفحة قبل الكود**، لون الفرعي درجة أخف/أغمق من الرئيسي، وحساب المالك «يضيف الناقص وبس» بنسخة احتياطية.
- **ملاحظات من قراءة الكود للتصنيفات:** `tokens.json` فيه 26 تصنيف رئيسي، ألوان مكررة (مثلًا `#CB428E` لتلاتة)، ومعظم الرموز `tag`، و`iconKey` **مش مستعمل في أي شاشة** (`TransactionRow` بيحط أول حرف). `seedUserReferences` بيزرع **مرة واحدة بس** ⇒ حساب المالك محتاج حالة استخدام «يضيف الناقص» منفصلة. `CategoriesScreen` مالهاش اختيار أب ولا رمز.
- **⚠️ درايف E: اتملى 100% (2026-09-14):** تعديل OVERRIDES فشل بـ`ENOSPC` (الملف ما باظش — اتأكد بـ`git diff --ignore-cr-at-eol`). المالك فضّى مساحة بنفسه. **لو حصل تاني: وقف الكتابة واسأل المالك، وما تمسحش حاجة.**
- **🗂️ اقتراح شجرة التصنيفات — مستني مراجعة المالك (ما اتبناش كود):**
  - **الصفحة:** https://claude.ai/code/artifact/0a46ee16-0ce1-438e-ae2e-cd57b5003053 (خاصة بالمالك). **البيانات:** `proposals/category-tree.json`.
  - 26 رئيسي **بنفس أسماء `tokens.json` بالظبط** (اتفحص: ولا ناقص ولا زيادة) + 71 فرعي. لكل رئيسي `h/s/l` ⇒ 26 لون فاتح مختلف. الفرعي نفس الدرجة بإزاحة إضاءة `[+7,-6,+12,-10,+16]`. الوضع الغامق: إضاءة +24 (بين 58 و74). الأصفر/الأخضر الفاتح (درجة 35–110) سقف إضاءته 40 عشان التباين على الأبيض.
  - مجموعتين: «المصروف» (21) و«حركة فلوس مش مصروف» (5، ألوان هادية: استثمار، محافظ رقمية، تقسيط، تحويلات، سحب نقدي).
  - **الرموز بأسمائها الأساسية في lucide-react 1.45.0** (اتفحص إن الـ92 موجودين). ⚠️ `smile` و`building-2` أسماء بديلة (ملفات بتعيد تصدير) ⇒ اتستعمل `face-slightly-smiling` و`building-complex`.
  - `lucide-react` **اتثبت بالظبط `1.45.0`** والسبب في ARCHITECTURE §28 — لسه مش مستعمل في الكود.
  - **🔁 النسخة التانية (2026-09-14 مساءً، OVERRIDES §28.1):** المالك طلب **مجموعات** فوق التصنيفات + دمج (المنزل، العناية الشخصية، التسوق، الالتزامات، الاستثمار فيه المحافظ، إلغاء تأمين، الصدقة لوحدها والزكاة لوحدها) + **تصنيفات مخفية لحد ما نعرف معلومة عن الشخص**. `proposals/category-tree.json` بقى بالشكل الجديد: `{requires: {مفتاح: وصف}, groups: [{name, icon, kind: spend|saving|movement, mains: [{name, icon, h, s, l, requires?, from: [أسماء التصنيفات القديمة اللي اتدمجت فيه], subs: [[اسم, رمز, requires?]]}]}]}`. مفاتيح `requires`: `hasCar`، `noCar`، `dependents`، `renter`، `domesticWorker`، `business` ⇒ **محتاجة أسئلة جديدة في أسئلة البداية وحقول في ملف المستخدم**. `from` هو خريطة نقل عمليات حساب المالك (المالك وافق «انقلها للتصنيف الجديد» بنسخة ومعاينة). ⚠️ «تأمين» و«وقود ومواصلات» ليهم أكتر من وجهة ⇒ سؤال بالأعداد وقت التنفيذ. الصفحة بتحفظ علامات النسخة دي في `review/choices-v2` (المعرّفات `g<مجموعة>-m<أساسي>-s<فرعي>`)؛ `review/choices` بتاع النسخة الأولى واتطبق خلاص.
  - **المالك طلب «حاجة أدوس عليها من غير ما أكتب»** ⇒ الصفحة بقت بتعلّم بالضغط (دوسة = يتشال، تانية = يرجع). الرئيسي المتعلّم عليه = **يتخفي** (عملياته القديمة بتفضل). الاختيارات بتتحفظ في قاعدة الصفحة: مستند `review/choices` = `{removed: [ids], names: [...], updatedAt}`. المعرّفات: الرئيسي `m<ترتيبه في category-tree.json>`، والفرعي `m<i>-s<j>`. **اقراها بـ`Artifact` `read_db` (`collection: "review"`, `doc_id: "choices"`) قبل البناء، وشيل المتعلّم عليه من `proposals/category-tree.json`.**
- **✅ المالك وافق على النسخة التانية «خلّصت، ابدأ البناء» (من غير علامات — `review/choices-v2` فاضي). البناء بالشرايح:** (1) الشجرة جوه التطبيق للحسابات الجديدة + الرموز + اختيار مقسّم بالمجموعات ← (2) أسئلة البداية الجديدة (سيارة، بتعول مين، إيجار، عمالة، شغل خاص) وإخفاء المشروط ← (3) الرئيسية بالمجموعات «من برا» ← (4) حساب المالك: يضيف الناقص وينقل عمليات المدموج بنسخة ومعاينة بالعدد.
- **🧩 الشريحة (1) — اتكتبت واتجربت في المعاينة، ⚠️ مش على المحاكي لسه:**
  - `src/infrastructure/import/categoryTree.json` (بيانات التطبيق، مش الاقتراح): 21 أساسي + 70 فرعي. **فرق عن صفحة المراجعة (قرار تنفيذ مش شكل):** «السيارة» و«المواصلات» بقوا **تصنيف واحد** `السيارة` فيه `noCar: {name: "المواصلات", icon: "bus-front"}`، وفروع العربية بس عليها `hasCar` والتاكسي والمواصلات العامة من غير شرط. كده ولا فروع مكررة ولا قواعد بتروح لتصنيف مخفي، ونقل «وقود ومواصلات» في حساب المالك وجهته واحدة.
  - `domain/categoryTree.ts` (المجموعات الست بترتيبها + مفاتيح الشروط وأوصافها)، `domain/categoryColors.ts` (HSL ← hex، سقف إضاءة الأصفر/الأخضر، درجات الفرعي، نسخة الغامق)، `domain/categoryOptions.ts` (`groupCategoryOptions`: مجموعات، فرعي تحت أبوه، الأساسي المخفي بيخفي فروعه، `keepId` للتصنيف الحالي).
  - `Category` زاد فيه اختياري: `groupKey`، `requires`، `noCarName`، `noCarIconKey`. النسخة الشاملة ما اتغيرتش (الحقول الاختيارية بتعدي).
  - `infrastructure/import/categoryTreeLoader.ts` (`buildCategoryTree`): المعرّف من الاسم **بنفس طريقة `buildCategories` القديمة** (التصنيف اللي اسمه ما اتغيرش بيحتفظ بمعرّفه — مختبر)، والفرعي `<أبوه>--<اسمه>`. `aliases` للأسماء القديمة و`wordOverrides` لكلمات بعينها (SHORY ← تأمين السيارة، BUPA ← تأمين صحي، البنزينات ← وقود، PARKING ← مواقف وسايس، UBER/CAREEM/TAXI ← تاكسي). **«تأمين» متعمد مجهول** (TAWUNIYA وINSURANCE ما بيتصنفوش) لأن ليه وجهتين.
  - `loadReferences` بقى بياخد الشجرة (اختياري). `container.ts` و`demoContainer.ts` بيزرعوا من الشجرة (الزرع مرة واحدة ⇒ **حساب المالك ما اتلمسش**). `container.ts` بقى **300 سطر بالظبط**.
  - العرض: `CategoryIcon.tsx` (96 رمز باستيراد بالاسم + `Tag` احتياطي)، `CategoryOptions.tsx` (optgroup)، `TransactionRow` بياخد `categoryIconKey` و`categoryDarkColor` (الرمز مكان الحرف لو مش `tag`، واللون بنسختين عن طريق `[data-theme='dark']`). الرئيسية والعمليات بيبعتوهم. «إضافة عملية» و«صفحة العملية» بقوا بالمجموعات.
  - **اتفحص:** typecheck نضيف، **597 اختبار** (جداد: `categoryTree.test.ts` و`categoryOptions.test.ts`).
  - **✅ المعاينة (`?demo=1`، 2026-09-15):** قايمة التصنيف فيها 6 مجموعات (الأكل والشرب 10، البيت والالتزامات 26، التنقل 8، الحياة الشخصية 38، الادخار والاستثمار 5، حركة فلوس 4) والفرعي بـ«—» تحت أبوه. عملية 18.00 على «قهوة ومشروبات» ⇒ الصف فيه **رمز SVG** بلون الفرعي (فاتح `#B65116`، غامق `#EF8F57` واللي اتعرض الغامق لأن المتصفح كان غامق). مفيش أخطاء كونسول.
  - 🔴 **غلطة مني اتصلحت (2026-09-15):** رفعت `versionCode` بـ PowerShell 5.1 (`Get-Content -Raw` من غير `-Encoding UTF8` ثم `WriteAllText`) ⇒ **تعليق عربي في `android/app/build.gradle` اتحول لرموز مبوظة**. رجّعت الملف من git وعدّلت الرقمين بـ`sed`. **أي تعديل على ملف فيه عربي: Edit أو sed، مش PowerShell 5.1 من غير ترميز صريح.**
  - **✅ APK 1.18 (versionCode 19):** `dist-android-apk/masroufy-trial-v19.apk`، SHA256 `532BAF39A68A0FF8E7543ECDF2BDB92F2110C1E2C2D85BCC921980CCA77430F5`، 5,197,664 بايت. **اترفع على درايف المالك** (`H:\My Drive\WORK\مصروفي\`، البصمة مطابقة، و`search_files` لقاه بنفس الحجم).
  - **✅ على المحاكي بحساب المالك — قراءة بس (2026-09-15):** اتثبت بـ`install -r` واتفتح. قفل البصمة ظاهر (ما فتحتوش). من ورا القفل (`--eval` قراءة): 5 صفوف عمليات، **2 منهم ليهم لون تصنيف والاتنين بقوا رمز SVG** (رموز `tokens.json` القديمة زي `utensils`)، صفر حروف، صفر رسايل خطأ. تصنيفات حساب المالك ما اتلمستش (الزرع مرة واحدة). **ما اتجربش على المحاكي:** قايمة الاختيار بالمجموعات (حسابه من غير مجموعات ⇒ المفروض قايمة واحدة زي الأول — مختبر في `categoryOptions.test.ts`).
- **🧩 الشريحة (2) — اتكتبت واتجربت في المعاينة، ⚠️ مش على المحاكي لسه:**
  - `domain/userProfile.ts`: زاد `dependentKinds` (`spouse`/`children`/`parents` — **`spouse` مش `wife`** لأن الست بتعول جوزها؛ الخانة بتكتب «الزوج» أو «الزوجة» حسب النوع)، `hasCar`، `renter`، `domesticWorker`، `business`. كلهم `null` = ما اتجاوبش. «لأ» على بيعول حد بتمسح «مين». الملف القديم من غير الحقول دي بيتقري «ما اتجاوبش» (مختبر).
  - `domain/categoryVisibility.ts`: `factsFromProfile` (الأسرة = زوج/زوجة أو أولاد؛ الأهل لوحدهم مش أسرة) + `presentCategories` (**مجهول = مخفي**؛ «السيارة» ⇒ «المواصلات» ورمز `bus-front` لحد «أيوه»؛ المشروط غير المتأكد `active: false` **في العرض بس** — ولا حاجة متخزنة بتتغير، واسمه بيفضل على العمليات القديمة، والتصنيف الحالي للعملية بيفضل في قايمتها).
  - `useAppData.ts`: حالة `facts` بتتقري من الملف في `prepare()` و`reload()`، و`home`/`txnData`/`budgetData` بيطلعوا للشاشات بعد `presentCategories` (157 سطر). حفظ قسم الحساب بينادي `onRestored` ⇒ `reload` ⇒ الاختيارات بتتحدث على طول.
  - `presentation/components/ProfileAnswerFields.tsx` (+css): `YesNoField` و`DependentKindsField` مشتركين بين أسئلة البداية وقسم الحساب. `OnboardingFlow` بقى **6 خطوات** (خطوة جديدة «حياتك»: سيارة، إيجار، عمالة، شغل خاص؛ و«بتعول مين» في «عنك» لو أيوه). `AccountCard` فيه نفس الأسئلة.
  - **اتفحص:** typecheck نضيف، **603 اختبار** (جداد: `categoryVisibility.test.ts` + أسئلة التصنيفات في `userProfile.test.ts`).
  - **✅ المعاينة (`?demo=1`، 2026-09-15):** أسئلة البداية «سؤال 1 من 6»، و«سؤال 4 من 6» فيه الأربع أسئلة. خلّصت من غير إجابات ⇒ قايمة التصنيف: التنقل فيه **«المواصلات» + تاكسي وتطبيقات + مواصلات عامة بس**، ومخفي: وقود، مواقف وسايس، الأسرة والأطفال، إيجار، عمالة منزلية، مصاريف الشغل (77 اختيار). من الإعدادات: بتعول حد أيوه + الزوجة، سيارة أيوه، إيجار أيوه، شغل خاص أيوه، العمالة فاضية ⇒ «بيانات الحساب اتحفظت.» ⇒ القايمة: **«السيارة» بفروعها السبعة**، وإيجار والأسرة ومصاريف الشغل ظاهرين، والعمالة لسه مخفية (91 اختيار). مفيش أخطاء من التطبيق (أخطاء WebSocket قديمة من سيرفر وقع قبل كده بس).
  - **حساب المالك:** تصنيفاته القديمة مالهاش `requires` ولا `noCarName` ⇒ مفيش حاجة هتختفي عنده. أسئلته الجديدة هتظهر له في أسئلة البداية (لسه ما خلصهاش) وفي قسم الحساب.
  - **✅ APK 1.19 (versionCode 20):** `dist-android-apk/masroufy-trial-v20.apk`، SHA256 `47BFCA2C795455FF921F07D8AC2D4252F9409F074B552B87351410C36D7EAD30`، 5,199,280 بايت — فيه الشريحة (2) بس (البناء خلص قبل ما ملفات الشريحة 3 تتكتب). **اترفع على درايف المالك** (البصمة مطابقة على `H:`، و`search_files` لقاه بنفس الحجم).
  - **✅ على المحاكي بحساب المالك — قراءة بس:** اتثبت فوق 1.18 واتفتح، القفل ظاهر، ومن وراه: 5 صفوف، والتصنيفين اللي ليهم لون لسه برموز SVG، صفر رسايل خطأ. **ما اتجربش على المحاكي:** أسئلة البداية الست وقسم الحساب الجديد (فتحهم محتاج فتح القفل، وحفظهم كتابة في ملف المالك).
- **🧩 الشريحة (3) — المصروف بالمجموعات «من برا» — اتكتبت واتجربت في المعاينة، ⚠️ مش على المحاكي لسه:**
  - **✅ APK 1.20 (versionCode 21):** `dist-android-apk/masroufy-trial-v21.apk`، SHA256 `1B13443657D11AFE9D4D2BBDEC8C750F57A3D1788F1780DF2739D9C30D3C19DC`، 5,200,688 بايت. **اترفع على درايف المالك** (البصمة مطابقة على `H:`، و`search_files` لقاه بنفس الحجم).
  - **✅ على المحاكي بحساب المالك — قراءة بس:** اتثبت فوق 1.19 واتفتح، القفل ظاهر. من وراه: قسم **«توزيع التصنيفات» القديم** موجود (صفين) وعرض المجموعات **مش ظاهر** — ده المقصود لأن تصنيفات المالك لسه مالهاش مجموعات. 5 صفوف عمليات، رمزين SVG، صفر رسايل خطأ.
  - **✅ المعاينة (`?demo=1`، 2026-09-15):** أسئلة البداية الست من غير إجابات، وعمليتين: 32.00 على «قهوة ومشروبات» و120.00 على «تاكسي وتطبيقات». الرئيسية: **«المصروف بالمجموعات»** بصفين بالترتيب: «التنقل» 120.00 (78.9% · 1 عملية) و«الأكل والشرب» 32.00 (21.1% · 1 عملية)، الاتنين برمز المجموعة و«دوس للتفاصيل». دوسة على «التنقل» ⇒ `aria-expanded="true"` وجواها **«المواصلات» 120.00** (الاسم البديل لأن السيارة ما اتجاوبتش) برمزها ولونها، و«الأكل والشرب» فضلت مقفولة. مفيش رسايل خطأ.
  - `domain/groupDistribution.ts`: بياخد `categoryDistribution` الجاهز ويجمّع فرعي ← أساسي ← مجموعة بـ`addMoney` و`rateOfMoney` (هللة، ونسبة بالعُشر). **مجموع المجموعات = الإجمالي بالظبط** (كل عملية ليها شريحة واحدة). بلا تصنيف أو تصنيف اتمسح ⇒ صف «بلا تصنيف». لو ولا تصنيف في التوزيع ليه مجموعة ⇒ `grouped: false`. الترتيب بالمبلغ ولو اتساووا بترتيب المجموعات الثابت.
  - `presentation/components/GroupDistribution.tsx` (+css): `grouped` ⇒ «المصروف بالمجموعات» (رمز المجموعة، المبلغ، شريط، % وعدد العمليات) ودوسة بتفتح التصنيفات الأساسية جوه المجموعة بألوانها ورموزها. `grouped: false` (**حساب المالك الحالي**) ⇒ أكبر 5 تصنيفات **زي الأول بالظبط**. `HomeScreen.tsx` بقى بيستعمل المكوّن (190 سطر).
  - **اتفحص:** typecheck نضيف، **609 اختبار** (6 جداد في `groupDistribution.test.ts`).
  - ⚠️ **ما تشغّلش سيرفر التطوير وبناء الـAPK مع بعض:** البناء بيكتب في `dist-android` وسيرفر Vite بيراقبه ⇒ السيرفر وقع بـ`EBUSY ... dist-android\icon-192.png` (2026-09-15). مش عطل في الكود — وقّف السيرفر قبل `build-android.ps1`.
  - ⚠️ **لسه:** الإخفاء حسب الإجابات (الشريحة 2) — دلوقتي الحساب الجديد بيشوف كل التصنيفات المشروطة. `RulesScreen` و`BudgetScreen` و`HistoryReviewScreen` و`CategoriesScreen` لسه قايمة مسطحة.
- **🧩 الشريحة (4) — نقل حساب المالك للشجرة — بدأت (قراءة بس، ولا كتابة على حسابه):**
  - `domain/categoryMigration.ts` (`planCategoryMigration`، دالة نقية): `create` (تصنيفات الشجرة الناقصة بالمعرّف)، `moves` (كل تصنيف قديم اتدمج ← وجهته + أعداد العمليات/القواعد/التجار/السقوف)، `patches` (حقل التصنيف بس: `categoryId` أو `verifiedCategoryId` — **ولا تأكيد ولا مبلغ بيتلمس**)، `ambiguous` («تأمين» — ما بيتنقلش ويتسأل المالك بالأعداد)، `untouched` (تصنيفات عملها المستخدم بنفسه)، `budgetConflicts` (سقفين لنفس الميزانية على نفس الوجهة — ما يتنقلوش ويتسأل). الربط **بالمعرّف القديم** `cat-<الاسم القديم مطبعن>` مش بالاسم الحالي، فالتصنيف اللي المستخدم غيّر اسمه بيتنقل صح. التطبيق مرتين ما بيكررش (مختبر).
  - **اتفحص:** typecheck نضيف و7 اختبارات في `categoryMigration.test.ts`.
  - `application/useCases/migrateCategories.ts` (`makeMigrateCategories`): بنفس نمط `repairStoredIds`. `preview` قراءة بس. `apply(previewed)`: قراءة كاملة تاني وتطبيق **المعروض بس** (تعديل اتغير بعد المعاينة بيتخطى) ← **نسخة متأكدة الحجم** (`before-category-move`: المستندات اللي هتتعدل + التصنيفات اللي هتتخفي) ← إضافة الناقص بـ`CategoryRepository.save` ← تعديل حقل التصنيف بـ`IdRepairPort.apply` على دفعات 100 ← **إخفاء المدموج بس بعد ما كل التعديلات تنجح** (`active: false`، مش مسح). نسخة ناقصة ⇒ ولا حرف اتكتب. «تأمين» والتعارضات ما بيتلمسوش.
  - ⚠️ في الاختبار: القراءة الكاملة (`memoryIdRepair`) والتصنيفات (`MemoryCategoryRepository`) مخزنين منفصلين، فالتصنيفات المضافة ما بتبانش في القراءة التانية — **غلطة في اختباري مش في الكود** (على Firestore الاتنين نفس المجموعة). الاختبار بقى بيبني الحالة المشتركة بعد التطبيق.
  - `infrastructure/import/legacyCategoryNames.ts`: أسماء `tokens.json` بترتيبها (الحاوية ما بقتش بتستورد `tokens.json`).
  - `presentation/components/CategoryMigrationPanel.tsx`: «افحص نقل التصنيفات» (قراءة بس) ← كام تصنيف هيتضاف، كل تصنيف قديم ← وجهته بالأعداد، والغامض والتعارضات والتصنيفات اللي عملها المستخدم «مش هتتلمس» ← «احفظ نسخة وانقل». **متركّبة جوه `IdRepairPanel`** جنب تنظيف البقايا (عشان `SettingsScreen` على 299 سطر).
  - التوصيل: `container.ts` (**لسه 300 سطر**: الشجرة بتتبني مرة واحدة، و`buildSeedSource` بقى سطر) و`demoContainer.ts` (المعاينة بتقرا تصنيفاتها الحقيقية عشان الفحص ما يقولش إن كله ناقص).
  - **اتفحص:** typecheck نضيف، **620 اختبار** (4 في `migrateCategories.test.ts`: المعاينة ما بتكتبش؛ الترتيب والنتيجة و«تأمين» ما يتلمسش؛ نسخة ناقصة ⇒ ولا كتابة؛ التطبيق التاني ما بيعملش حاجة).
  - **✅ المعاينة (`?demo=1`، 2026-09-15):** بعد أسئلة البداية، الإعدادات ← «افحص نقل التصنيفات» ⇒ «حسابك على شجرة التصنيفات الجديدة — مفيش حاجة تتنقل.» (حساب المعاينة متزروع من الشجرة)، مفيش معاينة ولا زرار نقل، صفر رسايل خطأ، وصفر أخطاء كونسول (غير WebSocket).
  - **✅ APK 1.21 (versionCode 22):** `dist-android-apk/masroufy-trial-v22.apk`، SHA256 `408A63BB5A6722511D30F566FF7D6F56002A9D4BFCBF6B2A1029951343BA6231`، 5,203,324 بايت. **اترفع على درايف المالك** (البصمة مطابقة على `H:`، و`search_files` لقاه بنفس الحجم). اتثبت على المحاكي فوق 1.20 واتفتح بالبصمة (بصمة المحاكي)، وأسئلة البداية **ما ظهرتش** (غالبًا المالك خلّصها من موبايله).
  - **✅ الفحص على حساب المالك (قراءة بس، 2026-09-15، من الإعدادات بإحداثيات متقاسة لايف):** «هيتضاف 80 تصنيف». النقل: محافظ رقمية ← استثمار › محافظ رقمية (4 عملية)، تقسيط ← الالتزامات › تقسيط (4)، وقود ومواصلات ← السيارة (3)، خدمات منزلية ← المنزل (3)، فواتير ومرافق ← المنزل (2)، ملابس وعطور ← العناية الشخصية (1)، تسوق إلكتروني ← التسوق › تسوق إلكتروني (0)، إعلانات وتسويق ← مصاريف الشغل › إعلانات وتسويق (0). «تأمين»: 1 عملية (غامض). مفيش تعارض سقوف.
  - 🔴 **ملاحظة مفتوحة قبل التطبيق:** الفحص **ما عدّش ولا قاعدة ولا تاجر** على التصنيفات القديمة، مع إن القواعد اتزرعت عليها (مثلًا BARQ ← محافظ رقمية). لازم يتفحص (قراءة بس) إيه `categoryId` اللي على القواعد فعلًا قبل النقل — لو بتشاور على القديم، الاستيراد الجاي هيحط عمليات في تصنيفات متخفية.
  - **✅ رد المالك (نافذة، 2026-09-15، مسجل في OVERRIDES §28.1):** «انقل بعد فحص القواعد»، و«تأمين» ← **تأمين السيارة**.
  - **✅ فحص القواعد والتجار (قراءة بس، شاشة «القواعد والتجار» على المحاكي):** «القواعد (0)» و«التجار (0)» و«مفيش قواعد لسه.» ⇒ **حساب المالك مفيهوش ولا قاعدة ولا تاجر**، فعدد صفر في فحص النقل **صحيح** مش عطل. ❓ مفتوح (مش شغل الشريحة دي): الزرع اتعمل مرة واحدة زمان، والقواعد والتجار مش موجودين دلوقتي — التصنيف الآلي على حسابه مالوش مرجع. يتسأل المالك بعدين لو عايز يرجّعهم.
  - **✅ اختيار وجهة الغامض:** `planCategoryMigration(..., choices)` (وجهة مش في الشجرة بتتجاهل)؛ `migrateCategories.preview(choices)` بترجع `{...plan, targets, choices}` و`apply(preview)` بتعيد التخطيط **بنفس الاختيارات**. اللوحة فيها قايمة لكل غامض («يفضل زي ما هو» أو أي تصنيف من الشجرة بالمجموعات)، واختيار الوجهة بيعيد الفحص. **اتفحص:** typecheck نضيف و**622 اختبار** (جداد: الاختيار بيحوّل «تأمين» لنقل عادي ومعاه القاعدة، ووجهة غلط بتتجاهل، والتطبيق بيخفي «تأمين» بعد نقله).
  - **✅ APK 1.22 (versionCode 23):** `dist-android-apk/masroufy-trial-v23.apk`، SHA256 `EA4D9A55051FBEE5AFC686262B87135F8C73FAF75F68B4B4BF3E46B9D6DE3CFC`، 5,203,712 بايت. **اترفع على درايف المالك** (البصمة مطابقة على `H:`، و`search_files` لقاه بنفس الحجم).
  - **✅✅ النقل اتنفذ على حساب المالك بموافقته (2026-09-15 10:23 بتوقيت الجهاز):** افحص ← نفس الأعداد اللي وافق عليها بالظبط ← اختيار «— تأمين السيارة» لـ«تأمين» (الفحص اتعاد: «تأمين» ← «تأمين السيارة»: 1 عملية) ← «احفظ نسخة وانقل» ⇒ «النسخة اتحفظت (24 ك.ب) في Android/data/app.masroufy.personal/files/backups/masroufy-before-category-move-2026-09-15T07-23-16-937Z.json. **اتضاف 80 تصنيف، واتنقل 18 مستند، واتخفى 9 تصنيف قديم.**»
    - **الفحص التاني:** «هيتضاف 0 تصنيف» وكل التصنيفات القديمة «مفيش حاجة عليه» ⇒ مفيش ولا عملية باقية على تصنيف مدموج.
    - 🔴 **عيب في العرض اتكشف بالفحص التاني (اتصلح في الكود، مش على الجهاز لسه):** الخطة كانت لسه بتعرض الـ8 نقلات الفاضية (التصنيفات القديمة موجودة ومخفية) بدل «مفيش حاجة تتنقل». `planCategoryMigration` بقى بيتجاهل التصنيف القديم **المخفي خلاص ومفيش عليه حاجة**، ولو رجع عليه أي حاجة بيظهر تاني (مختبر).
    - ⚠️ **عمليات «وقود ومواصلات» الـ3 بقت على «السيارة»**: اسمها في العرض «المواصلات» لحد ما المالك يجاوب «عندك سيارة؟» بأيوه. و«إعلانات وتسويق» بقى جوه «مصاريف الشغل» المخفية لحد «عندك شغل خاص؟» (عليها 0 عمليات).
    - **النسخة على الجهاز بعيني:** `masroufy-before-category-move-2026-09-15T07-23-16-937Z.json` بحجم **24,325 بايت**.
    - 🔴 **نقص مني اتكشف على الرئيسية (قراءة بس):** الرئيسية لسه بتعرض «توزيع التصنيفات» القديم مش «المصروف بالمجموعات». السبب: الخطة **ضافت الناقص ونقلت المدموج بس، وما حدّثتش التصنيفات الموجودة اللي معرّفها ما اتغيرش** (زي «مطاعم وقهوة») — فضلت من غير `groupKey` وبرموزها وألوانها القديمة. ده **جوه موافقة المالك** (§28: «يضيف الفروع والرموز والألوان الجديدة، من غير تغيير اسم تصنيف عدّله»). **الإصلاح:** الخطة بقت فيها `update` (المجموعة والرمز والألوان وشروط الظهور من الشجرة، **والاسم و`active` زي ما هم**)، والتطبيق بيحفظهم بعد النسخة.
  - **✅ APK 1.23 (versionCode 24):** `dist-android-apk/masroufy-trial-v24.apk`، SHA256 `70742B941058CC28D0353217FA3C177763BB0CED3AF58C7F5B4CB51202642840`، 5,204,232 بايت. **اترفع على درايف المالك** (البصمة مطابقة على `H:`؛ أول `search_files` رجع فاضي — تأخير فهرسة — والتاني لقاه في فولدر «مصروفي» بنفس الحجم).
  - **✅✅ تحديث التصنيفات الموجودة على حساب المالك (جوه موافقة §28، 2026-09-15 10:32):** الفحص: «هيتضاف 0 تصنيف» و«هياخد 11 تصنيف موجود مجموعته ورمزه ولونه الجديد — اسمه وظهوره زي ما هم»، ولا نقل ولا غامض. «احفظ نسخة وانقل» ⇒ «النسخة اتحفظت (4 ك.ب)… اتضاف 0 تصنيف، **واتحدث 11 تصنيف موجود**، واتنقل 0 مستند، واتخفى 0 تصنيف قديم.»
    - **النسخ على الجهاز بعيني:** `masroufy-before-category-move-2026-09-15T07-23-16-937Z.json` (24,325 بايت، النقل) و`masroufy-before-category-move-2026-09-15T07-32-57-887Z.json` (3,957 بايت، التحديث).
    - **الفحص التالت:** «حسابك على شجرة التصنيفات الجديدة — مفيش حاجة تتنقل.»
    - **الرئيسية بعدها (قراءة أسماء ونسب بس):** **«المصروف بالمجموعات» ظهر** بدل القايمة القديمة: «بلا تصنيف» 98.3% · 19 عملية، و«الأكل والشرب» 1.7% · 4 عملية · دوس للتفاصيل. صفين عمليات برموز SVG، صفر رسايل خطأ.
    - ⚠️ **ملاحظة للمالك (مش عطل):** معظم مصروف الفترة دي **بلا تصنيف**، وحسابه **مفيهوش قواعد ولا تجار** (شاشة «القواعد والتجار» = 0) ⇒ التصنيف الآلي مالوش مرجع. ❓ يتسأل: يرجّع القواعد والتجار الافتراضيين (من `rule-reference.json` و`merchant-reference.json` على الشجرة الجديدة)؟
  - **✅ رد المالك (نافذة): «رجّعهم بمعاينة»** (القواعد والتجار الافتراضيين على الشجرة) و**«ارفع»** ⇒ اترفع على GitHub (دمج عادي لتحديثين أسعار `public/prices.json` بس، من غير rebase) — **مفيش commits مش مرفوعة لحد `3ae8440`**.
  - 🧩 `domain/referenceRestore.ts` (`planReferenceRestore`، قراءة بس): الافتراضيين **الناقصين بس** (قاعدة بنفس النص والطريقة أو المعرّف موجودة ⇒ لأ؛ تاجر بنفس الاسم أو المعرّف ⇒ لأ)، واقتراح تصنيف **للعمليات اللي مالهاش تصنيف ومش مؤكدة بس** بـ`categorize`. **اتفحص:** typecheck نضيف و**628 اختبار** (5 في `referenceRestore.test.ts`). **لسه:** حالة الاستخدام واللوحة والتوصيل، وما اتلمسش حساب المالك.
  - ❓ **المالك سأل (2026-09-15): «احنا فين من قاعدة بيانات التجار وتصنيفهم؟ ملف مشترك؟ وصور الشعارات اتضافت؟»** — الرد بالحقيقة من الكود: **القاعدة المشتركة (§25) ما اتبنتش** — كل حساب واخد نسخة خاصة مرة واحدة من `merchant-reference.json` (319 تاجر، 116 مؤكد) تحت `users/{uid}/merchants`، و`firestore.rules` ما فيهاش غير `users/{uid}` ⇒ مفيش مكان مشترك. **حساب المالك: 0 تجار و0 قواعد.** **الشعارات: ولا واحد** — `Merchant.logoAsset/logoSource` موجودين في النوع بس ومش مستعملين، و`public/` فيه أيقونات التطبيق بس. اتسأل في نافذة: يكمّل رجوع الافتراضيين لحسابه دلوقتي ولا يبدأ القاعدة المشتركة على طول.
  - **✅ رد المالك:** «رجّع لحسابي الأول وبعدين المشتركة».
- **➕ طلب المالك (2026-09-15 ~11:45) — «النقط التلاتة» جنب كل عملية — «محتاج تبنيها الأول»:** «كل عملية أصلًا أنا محتاج يكون على شمالها 3 نقط كده يدوس عليها تظهرله قايمة بإعدادات خاصة لكل عملية، سواء هغير تصنيفها أو هضيف ملاحظات ليها أو هربطها بفلوس جت قبل كده أو هضيفها على شخص معين كدين… إلخ». **بتتبني في التطبيق الحالي** (طلب صريح، رغم §29) — OVERRIDES §30.
  - 🧩 **اتكتبت (typecheck نضيف و632+ اختبار؛ ⏳ تجربة المعاينة جارية):**
    - `TransactionRow`: خاصية `onMenu` ⇒ زرار «⋯» في **عمود رابع على شمال الصف** (`.row--menu`/`.row__menu`، `z-index` فوق زرار فتح الصف عشان الدوسة على النقط ما تفتحش التفاصيل). `HomeScreen` («أحدث العمليات») و`TransactionsScreen` بياخدوا `onTransactionMenu`.
    - `presentation/components/TransactionMenu.tsx` (+css): ورقة فيها 6 اختيارات — غيّر التصنيف، ضيف ملاحظة، على شخص كدين أو هدية، **اربطها بدين موجود**، كاش واستبعاد ووسوم، كل التفاصيل. **القايمة بتوجّه بس:** التصنيف والملاحظة والعلامات والتفاصيل بيفتحوا `TransactionSheet` الموجودة (مش نسخة مكررة)، والشخص بيفتح `LinkPersonSheet`.
    - `presentation/screens/SettleWithTransactionSheet.tsx`: الديون المفتوحة المناسبة للاتجاه (`domain/settlementSuggestion.ts`: **داخلة ⇒ `receivable`، خارجة ⇒ `loan_payable` و`custody_payable`**)، والمبلغ المقترح **الأقل بين العملية والمتبقي**، والحفظ بـ`managePeople.settle({…, transactionId})` (الزيادة بترفض بتفسير من `checkSettlement`).
    - `src/app/TransactionActions.tsx` (`useTransactionActions`): حالة القايمة والتفاصيل والربط بشخص والربط بدين — **اتنقلت من `AppShell` (بقى 281 سطر) و`ShellOverlays` (86)**.
    - **اختيارات اتاخدت بقاعدة الساعة 2 (تتقال للمالك):** القايمة على الرئيسية كمان مش العمليات بس؛ 4 اختيارات بتفتح نفس صفحة التفاصيل بدل صفحات منفصلة.
    - **✅ المعاينة (`?demo=1`، 2026-09-15 ~12:20، 634 اختبار):**
      - **الرأس:** الظاهر «مصروفي» بس (لقارئ الشاشة «مصروفي — الرئيسية»).
      - **شريط الشهر (صورة):** «أغسطس 2026» في النص وتحته «28 أغسطس – 27 سبتمبر»، و«الشهر اللي فات» على اليمين بسهم SVG لليمين و«الشهر الجاي» على الشمال بسهم للشمال.
      - **القايمة:** عملية 45.00 على «قهوة ومشروبات» ⇒ زرار «خيارات…» على صفها في «أحدث العمليات» ⇒ ورقة بالعنوان والتاريخ والاتجاه والمبلغ والـ6 اختيارات بالترتيب.
      - **«اربطها بدين موجود»:** القايمة اتقفلت واتفتحت ورقة «اربطها بدين موجود» بجملة الفلوس الخارجة و«مفيش ديون مفتوحة تناسب العملية دي…» وزرار «اربطها» مقفول.
      - **«كل التفاصيل»:** فتحت «تفاصيل العملية» والتصنيف «— قهوة ومشروبات» والملاحظة و«ربط بشخص».
      - مفيش رسايل خطأ ولا أخطاء كونسول.
  - **✅ APK 1.24 (versionCode 25):** `dist-android-apk/masroufy-trial-v25.apk`، SHA256 `C56A448254C06A7E4C8C9020407E8238947D145B600E7441EE87B3228C637A1A`، 5,208,264 بايت، اترفع على `H:\My Drive\WORK\مصروفي\` (البصمة مطابقة).
  - **✅ على المحاكي بحساب المالك — قراءة بس (2026-09-15 ~12:00):** الرأس «مصروفي» بس؛ الشريط: «الشهر اللي فات» يمين بسهم SVG و«الشهر الجاي» شمال والنطاق «28 أغسطس – 27 سبتمبر»؛ **زرار «⋯» على الـ5 صفوف**؛ «المصروف بالمجموعات» ظاهر؛ صفر رسايل خطأ.
  - **✅ فحص «القواعد والتجار الافتراضيين» على حساب المالك (قراءة بس):** «هيتضاف **226 قاعدة و319 تاجر**». «من **1837** عملية من غير تصنيف، **1076** هتاخد تصنيف مقترح». الأكبر: مطاعم وقهوة 343، اشتراكات رقمية 145، بقالة وسوبرماركت 138، تقسيط 65، محافظ رقمية 51، المنزل 49، استثمار 43، اتصالات 38، العناية الشخصية 35، صحة وصيدليات 27، ترفيه 26، إعلانات وتسويق 23.
  - **اتسأل المالك في الشات (~11:56) بالأعداد مع مؤقت 10 دقايق.** المؤقت خلص **من غير رد** ⇒ **اتنفذت الإجابة المقترحة «رجّعهم» بقاعدته المؤقتة** («لو في أسئلة لحد الساعة 2 قم بالموافقة علطول على الإجابة المقترحة لو لم أجاوب في خلال 10 دقايق») — الساعة 12:06.
  - **✅✅ رجوع القواعد والتجار على حساب المالك (1.24):** المعاينة لسه نفس الأعداد ← «احفظ نسخة ورجّعهم» ⇒ «النسخة اتحفظت (1047 ك.ب)… **اتضاف 226 قاعدة و319 تاجر، واتقترح تصنيف لـ1076 عملية (مقترح مش مؤكد)**».
    - **النسخة على الجهاز بعيني:** `masroufy-before-default-rules-2026-09-15T09-07-35-539Z.json` بحجم **1,072,390 بايت**.
    - **الفحص التاني:** «القواعد والتجار الافتراضيين موجودين، ومفيش عملية جديدة تاخد اقتراح.»
    - **الرئيسية بعدها (أسماء ونسب بس):** بلا تصنيف 80.3% · 11 عملية، الادخار والاستثمار 9.6% · 1، التنقل 7.5% · 2، الأكل والشرب 2.5% · 6، الحياة الشخصية 0.0% · 1. **الاقتراحات أغلبها وقعت على فترات قديمة**؛ الفترة الحالية لسه فيها 11 عملية من غير تصنيف (مالهاش قاعدة ولا تاجر معروف).
    - ⚠️ **ما اتجربش:** تأكيد الاقتراحات واحدة واحدة (المالك بيعملها من «مراجعة القديم» أو من قايمة النقط التلاتة).
  - **الجاي بترتيب المالك:** قاعدة التجار المشتركة والشعارات (OVERRIDES §25) — محتاجة خطة تصميم تتعرض عليه الأول.
  - **✅ اتقالّه التقرير في الشات (12:15)** واتسأل عن الرفع في نافذة ⇒ **«استنى»** (5 commits مش مرفوعين من بعد `3ae8440`).
  - **🧭 خطة قاعدة التجار المشتركة اتعرضت واتردت (2026-09-15 ~12:25) ⇒ OVERRIDES §25.1:**
    - **مشكلة اتكشفت:** شروط Brandfetch **بتمنع حفظ الشعارات** (لازم تتحمّل من سيرفرهم وقت العرض). المالك اختار **«جوه التطبيق وأونلاين»**: ملفات شعارات **هو بيجيبها** للمشهورين + Brandfetch أونلاين للباقي.
    - **المشاركة: «المشتريات المؤكدة بس»**. **البداية: «المؤكدين من المرجع»** (116 مؤكد + الباقي مبدئي) — بيتحسبوا من المرجع اللي جوه التطبيق من غير كتابة في فايربيز.
    - **التصميم:** مجموعة `sharedMerchants/{normalizedName}` بحقول (الاسم، الاسم الموحد، أشكال الاسم، معرّف تصنيف من الشجرة بس، `confirmed`، `updatedAt`). مزامنة بالتغييرات من آخر مرة بس. التطبيق بيضيف للحساب التجار الناقصين، والمؤكد بيكمّل تاجر مالوش تصنيف مؤكد، و**عمره ما يكتب فوق تصنيف المستخدم**.
    - **محتاج من المالك بعدين:** Client ID من Brandfetch (حساب مجاني يعمله بنفسه)، ملفات الشعارات، وموافقة نشر `firestore.rules`.
  - **✅ شريحة القاعدة المشتركة (من غير الشعارات) — commits `b3cfc2d` و`b4e4346` و`bcd8b2a`:**
    - `domain/sharedMerchantCatalog.ts`: `contributionFor` (شراء + خارج + تصنيف من الشجرة + اسم من غير 4 أرقام ورا بعض + التاجر مش مؤكد + تصنيف مختلف)، `baselineCatalog` (المرجع جوه التطبيق)، `effectiveEntry` (مش مؤكد ما يغلبش مؤكد المرجع)، `planAccountMerchantSync` (المؤكد بس، بيكمّل الناقص، عمره ما يكتب فوق تصنيف المستخدم، معرّف ثابت `merch-shared-<key>`).
    - `application/ports/SharedMerchantCatalogPort.ts` + `useCases/sharedMerchants.ts` (`sync` بالتغييرات من آخر مرة، و`contribute` من غير قراءة سيرفر لعملية مش هتتبعت).
    - `infrastructure/firestore/sharedMerchantCatalogRepository.ts` (`sharedMerchants/{key}`، `updatedAt` من السيرفر) + `memory/memorySharedMerchantCatalog.ts` + `localSyncCursor.ts` (آخر مزامنة في `localStorage`).
    - التوصيل: `editTransaction` بقى فيه `onCategoryConfirmed` (في الخلفية — فشله ما بيفشّلش التعديل)، و`useAppData.prepare` بيعمل `sync` في الخلفية. `container.ts` بقى 298.
    - **`firestore.rules`**: قراءة لأي داخل، إضافة/تعديل **مش مؤكد** بالحقول الستة بس والمفتاح = الاسم الموحد، ومفيش مسح. **اتنشرت بموافقة المالك «انشر القواعد»** (`firebase deploy --only firestore:rules` ⇒ «compiled successfully… Deploy complete»). من غير تسجيل دخول: القراءة والكتابة **HTTP 403**.
    - **🔴 ثغرة اتكشفت واتصلحت:** مكانش فيه أي زرار يأكد تصنيف **مقترح** زي ما هو (التأكيد كان بس باختيار تصنيف تاني) — وأنا قلت للمالك قبلها «تقدر تأكده». اتضاف زرار **«أكّد التصنيف ده»** في `TransactionSheet` لما التصنيف مقترح.
    - **اتفحص:** typecheck نضيف و**644 اختبار** (9 في `sharedMerchants.test.ts` + اختبار الـhook في `editTransaction.test.ts`). المعاينة `?demo=1` فتحت من غير أخطاء.
    - **APK 1.25 (v26)** SHA256 `C3B6B5B281F60B85A0550975F92346BB7096D74E57823FEB3D2C5DCB1378E516` و**APK 1.26 (v27)** SHA256 `490145B865E83C3A27F69AD3D1A953A32CEF12AE56B36C8C45AADDE9B23BC4F0` — الاتنين على الدرايف (البصمة والحجم مطابقين).
    - **✅ على المحاكي بحساب المالك (1.26، بموافقته «أكّد عملية واحدة»):** عملية شراء خارجة بتصنيف مقترح ← «غيّر التصنيف» ← «أكّد التصنيف ده» ⇒ «اتأكد التصنيف» والزرار اختفى والتصنيف ما اتغيرش. **كاش Firestore على الجهاز (قراءة بس):** قبل = 0 مستند مشترك، بعد = **1 مستند و0 كتابات معلّقة** (يعني السيرفر قبلها). حقوله **الستة المسموحة بس** و`confirmed: false`. **بعد قفل التطبيق وفتحه:** الصف مبقاش مقترح والمستند لسه موجود.
    - ⚠️ **ما اتجربش:** التأكيد من لوحة فايربيز ثم وصول التاجر لحساب تاني بالمزامنة (مختبر بالذاكرة بس)، ورفض القواعد لكتابة `confirmed: true` من مستخدم داخل (مختبر بالذاكرة بس — مفيش Firestore emulator).
    - **الجاي:** الشعارات (محتاجة من المالك Client ID من Brandfetch + ملفات الشعارات).
    - **رد المالك (بعد ما طلب شرح براند فيتش بالعربي لأن الكلام الإنجليزي بيتقلب في النافذة): «ابني مكان الشعار»** ⇒ OVERRIDES §25.1: عرض شعار من ملف جوه التطبيق لو موجود، ومن غيره رمز التصنيف، والأونلاين مقفول لحد ما يبعت رقم التعريف. **النوافذ من هنا ورايح من غير أي كلمة إنجليزي** (ذاكرة `ask-in-choice-windows`).
  - **✅ مكان الشعار — commit `e403121`:**
    - `domain/merchantLogo.ts`: `buildLogoIndex` (أسماء المحل موحّدة، أول تعريف يغلب) و`logoSourceFor` (ملف جوه التطبيق موجود فعلًا ⇐ يغلب، وإلا أونلاين لو مفتوح والدومين سليم، وإلا مفيش).
    - `infrastructure/logos/merchantLogos.ts` + `manifest.json` (**فاضي** لحد ما المالك يبعت الملفات) + مجلد `images/`. 🔴 **كان اسمه `files/` واتغيّر:** `.gitignore` فيه `files/` (عشان الكشف الحقيقي) وده بيتجاهل **أي** فولدر بالاسم ده، فالشعارات ما كانتش هتترفع ولا `.gitkeep` اترفع. الأونلاين = رابط براند فيتش المباشر `cdn.brandfetch.io/domain/<دومين>?c=<رقم التعريف>` (من غير تحميل ولا حفظ)، ومقفول لحد ما `VITE_BRANDFETCH_CLIENT_ID` يتحط في `.env.local` (اتضاف لـ`.env.example`).
    - `TransactionRow` بقى ياخد `logoUrl`: صورة على أرضية بيضا، ولو فشل تحميلها بيرجع رمز التصنيف. `HomeScreen` و`TransactionsScreen` بياخدوا `logoFor` من `app.user.merchantLogos.urlFor`. `container.ts` 299.
    - **اتفحص:** typecheck نضيف و**650 اختبار** (6 في `merchantLogo.test.ts`). **المعاينة `?demo=1`:** شعار تجريبي (أيقونة التطبيق نفسها على محل وهمي «DEMO SHOP») ⇒ عملية يدوية بالاسم ده ظهرت في «أحدث العمليات» **بالشعار** (`naturalWidth` 150، يعني الصورة اتحملت)، ومفيش أخطاء كونسول.
    - ⚠️ **ما اتجربش:** رجوع رمز التصنيف لما الصورة تفشل (الكود `onError` بس)، والشعار الأونلاين فعليًا (مستني رقم التعريف)، وعلى المحاكي (مفيش فرق ظاهر على حساب المالك لحد ما الملفات أو الرقم يوصلوا — **ما اتبناش APK للخطوة دي**).
    - **المالك فتح لوحة براند فيتش (2026-09-15)** واتشرحله: الكارت «Logo API» ← انسخ «Client ID» بس وابعته، ومتبعتش أي «API key» أو حاجة سرية.
    - **✅ الرقم وصل** واتحفظ في `.env.local` بس (git متجاهله). **ما يتكتبش في أي ملف في المستودع.**
    - **رد المالك على «نجيب عناوين المحلات إزاي؟»: «انت تجهزها وأنا أراجع»** ⇒ `proposals/merchant-domains.json` (31 محل مشهور من قايمة المرجع، المش متأكد منه ما اتحطش) + صفحة مراجعة بشعاراتها على جهاز المالك (بتدوس على الغلط). الصفحة اتفتحت عند Claude الأول والـ31 شعار اتحملوا كلهم (يعني الرقم شغال).
    - **✅ رد المالك: «الكل صح»** ⇒ الـ31 محل دخلوا `src/infrastructure/logos/manifest.json` (بالدومين، من غير ملفات).
    - **✅ APK 1.27 (v28)** SHA256 `357A3A616CBE30F36F6A791EA765DE602BE1A4838349978C48C888CF5EE7C5DA` على الدرايف (مطابق). **على المحاكي بحساب المالك (قراءة بس):** شاشة العمليات للشهر اللي فات: 95 صف، **3 شعارات من براند فيتش اتحملوا فعلًا**. محل اسمه فيه «STC» بس مش في القايمة ما خدش شعار (صح). ⚠️ **ملاحظة:** شعار واحد ظهر كحرف «B» منمّق — غالبًا **الشعار البديل بتاع براند فيتش** لما مايلاقوش شعار، مش شعار المحل ⇒ يتصلح في بند 7 تحت.
- **📋 طلبات المالك الجديدة (2026-09-15 ~19:40) — «أحطهم في الجدول إنك تحلهم كلهم»:**

| # | الطلب (كلام المالك باختصار) | الحالة |
|---|---|---|
| 1 | قفل البصمة: التطبيق بيبان وراه وهو مقفول ⇒ «على الأقل بلور» لحد ما البصمة تتأكد | 🧩 كود: **السبب الحقيقي** إن `AppLockGate.css` كان بيستعمل `var(--c-bg)` (متغير مش موجود) فالغطا كان شفاف. دلوقتي طبقة بلون الخلفية + بلور، والتطبيق نفسه متغطي ببلور من أول ما يروح الخلفية. ⏳ المحاكي |
| 2 | جنب العمليات بيشوف حرف جوه الدايرة مش علامة، و**الشريط كله** بتاع العملية يبقى من عيلة لون تصنيفها | 🧩 كود: `row--cat` (خلفية 7% وحد 22% من لون التصنيف)، والحروف اتشالت: رمز التصنيف أو علامة وسم أو «بلا تصنيف» (دايرة متقطعة). ✅ المعاينة. ⏳ المحاكي |
| 3 | المصاريف اللي اتصرفت **بالأحمر** واللي جت **بالأخضر** «زي ما اتفقنا» | 🧩 الصفوف كانت كده أصلًا؛ اتضاف الأحمر لرقم المصروف الكبير في الرئيسية ومجموع اليوم في العمليات. ✅ المعاينة |
| 4 | شريط التاريخ: **ممنوع الشهور الجاية** — آخره الشهر الحالي | 🧩 `shiftPeriodWithin` + `periodIndex` (domain، 3 اختبارات)، سهم «الشهر الجاي» وأزرار الشهور/السنة الجاية مقفولين. ✅ المعاينة (السهم مقفول) |
| 5 | **«معدل الادخار» يتشال خالص**، والسطر يبقى فيه الدخل والمتبقي بس | 🧩 اتشال من الرئيسية والعمليات، والخانات بقت عمودين. (الرقم لسه بيتحسب في حالات الاستخدام — مش معروض). ✅ المعاينة |
| 6 | الكلام دايمًا جنبه علامة (الدخل ليه علامة، والمتبقي ليه علامة…) | 🧩 الدخل/المتبقي/المصروف الشخصي/المتاح اليومي/التوقع/أحدث العمليات + عناوين الميزانية. ⚠️ لسه: «المصروف بالمجموعات» وخانات الميزانية الصغيرة وباقي الشاشات |
| 7 | (من فحص Claude) الشعار البديل بتاع براند فيتش لازم ما يظهرش — يرجع رمز التصنيف | 🧩 `&fallback=404` في الرابط ⇒ الصورة بتفشل ويرجع رمز التصنيف. ⏳ المحاكي |
| 8 | علامات الرأس (الإعدادات والعين والجرس) من إيموجي ملونة ⇒ **مرسومة بلون واحد** زي رموز التصنيفات | 🧩 الرأس والشريط التحتاني كله (كان حروف ⌂ ☰ ◱ ⋯) بقى رسومات. ✅ المعاينة |
| 9 | **الأخضر لون التطبيق الرسمي** — «الثيم كله بالأخضر المخفف جدًا» ويشمل الأزرار المهمة وعلامات الرأس وكله (OVERRIDES §31) | 🧩 `tokens.css` (فاتح وغامق) + التبويب المختار وزرار الإضافة وعلامات الرأس. شريط «قربت تخلص» بقى برتقالي (`--c-warning`) بدل لون الأزرار. ✅ المعاينة (غامق). ⏳ الفاتح على المحاكي |
| 10 | الميزانية «كل حاجة لونها أبيض» ⇒ كل تصنيف بلونه والشريط من نفس العيلة أخف | 🧩 كل سطر تصنيف بخلفية ورمز بلونه، وشريط السقف بلونه (التجاوز بيفضل أحمر). ⏳ المحاكي. ⚠️ التصنيف اللي مالوش سقف لسه مالوش شريط |
| 12 | (2026-09-15 ~23:00، بصورة) اسم البرنامج «مصروفي» فوق: **خط مختلف مضلّع كتير**، **كبير قد المربع الأحمر** اللي رسمه (نص الرأس تقريبًا)، و**مش بيتداس** | ✅ اختار **«تشانجا»** من صفحة خطوط (https://claude.ai/artifact/2o2XwkQiHQQ9hkXcrCQciR). ⏳ التنفيذ |
| 13 | «تقريبي» تبقى **جنب** العنوان، والعنوان «المصروف الشخصي» يتغير لـ**«ما تم صرفه»**، و«تقريبي» في شارة لوحدها على نفس السطر | 🧩 `home__heroHead` (commit بعد `c8e83bb`). ⏳ المحاكي |
| 14 | علامة الدخل مش عاجباه ⇒ علامة **فلوس داخلة: حاجة نازلة، لونها أخضر** | 🧩 `BanknoteArrowDown` بلون الوارد في الرئيسية والعمليات. ⏳ المحاكي |
| 15 | **كل النوافذ المنبثقة** من نفس روح البرنامج، و**تطلع بحركة من الزرار/التبويب اللي اتداس عليه** | 🧩 `usePopOrigin` (مكان آخر دوسة ⇒ `--pop-x/--pop-y`) + `feedback.css`: الخلفية بتتكشف دايرة من مكان الدوسة، والورقة بتطلع لفوق، ورأسها أخضر هادي وعنوانها بلون التطبيق، والخلفية المعتمة فيها لمسة أخضر. ✅ **على المحاكي (1.34):** ورقتي «الكاش» و«تفاصيل العملية» اتفتحوا بـ`popReveal` ورأس أخضر، والرجوع بيقفلهم. |
| 16 | **قاعدة جديدة:** المطابقة مش لازم تبقى بالظبط — «خليك فليكسبل». **تعديل مبلغ العملية مسموح** (200 ⇒ 180) والرقم والمصروف الشامل يتحدثوا (OVERRIDES §32) | 🧩 commit `7189cdd`: `domain/amountEdit.ts` (`planAmountEdit` + `sourceAmountMinor`)، `editTransaction.setAmount`، `Transaction.originalAmountMinor` (بيتحفظ مرة واحدة)، مطابقة إعادة الاستيراد والدمج بالأصلي، ومش أقل من المتوزع/المتسوّى. زرار «عدّل المبلغ» في صفحة العملية. 5 اختبارات. **المطابقة اختيارية** (رد المالك) — هي أصلًا زرار في الإعدادات بس. `types.ts` نزل لـ284 سطر (الميزانية في `budgetEntities.ts`). ⏳ المحاكي |
| 17 | **الكاش لوحده:** معايا كام كاش وتقريبًا اتبقى كام، والرقم الكبير في الرئيسية **بيشمل الكاش (يتكتب تنويه)**، وطريقة يعرف صرف كام من الكاش بس | 🧩 رد «الاتنين». `domain/cashSummary.ts` (`summarizeCash`: الرصيد = بداية محفظة الكاش + الداخل − الخارج **من يوم البداية**، والتحويل ليها زي سحب الصراف داخل؛ المصروف من الكاش في الفترة = عمليات المحفظة أو «اتدفعت كاش»، بعد نصيب الأشخاص، من غير المستبعد والتحويلات) + `loadCashSummary` (`null` = مالوش محفظة كاش) + `useCashSummary` + `CashCard`/`CashSheet`. في الرئيسية: كارت «الكاش اللي معاك تقريبًا» تحت الرقم الكبير، ولما يتداس ورقة فيها رصيد البداية والداخل والخارج واللي اتصرف في الفترة وعمليات الكاش. تنويه تحت الرقم الكبير «الرقم ده بيشمل اللي اتصرف كاش». 3 اختبارات. `container.ts` 295 (بلوك التصنيف التلقائي اتضغط لسطر). ⚠️ الرصيد **تقريبي** والشاشة بتقول كده. **على المحاكي بحساب المالك (1.34، SHA256 `3554C29634EF0BF94D74CC9D750108DB9BDBF3CC62E745F953CF8AC0D0D1D6BF`، على الدرايف مطابق):** الكارت ظاهر والتنويه تحت الرقم الكبير، والدوسة فتحت ورقة «الكاش» (4 خانات + عملية كاش واحدة في الفترة) بحركة `popReveal`، والرجوع قفلها. 🔴 **اتلاحظ واتصلح في 1.35:** (1) محتوى الكارت كان تحت بعض لأن قاعدة `.card` العامة غلبت ⇒ `.card.cashCard`؛ (2) «صرفت كاش في الفترة» طلع صفر مع إن فيه عملية كاش في الفترة، لأن نوعها لسه ما اتحددش والرقم الكبير بيحسبها بالنوع التقديري ⇒ `loadCashSummary` بقى بيستعمل `withEstimatedKinds` زي الرئيسية. **✅ 1.35 (v36، SHA256 `7D3506838D06732B3B3E821447F0D9B400620D91711864E1E474F7FCAF80BEE9`، على الدرايف مطابق):** الكارت بقى صف واحد (`display:flex; row`، ارتفاع 108). «صرفت منه» لسه **0.00** — واتأكدت بقراية من كاش الجهاز (أنواع وعلامات بس، من غير مبالغ ولا أسماء): عملية الكاش الوحيدة في الفترة **«سداد دين عليك» مؤكَّد من المالك**، ومعلّمة «اتدفعت كاش» على محفظة البنك ⇒ **مش مصروف شخصي** فمش داخلة في «صرفت منه» ولا في الرقم الكبير — **صح حسب القواعد مش عطل**. (والتعديل بتاع الأنواع التقديرية لسه صح لعمليات الكاش اللي نوعها ما اتحددش.) |
| 18 | **زرار الرجوع بتاع أندرويد** (مش زرار جوه التطبيق): «لما بعمل رجوع من أندرويد مش بيرضى وبيطلعني برا البرنامج» ⇒ يقفل النافذة المفتوحة أو يرجع للشاشة اللي قبلها | ✅ commit `f22ffa3` (`app/androidBack.ts` + `@capacitor/app` — ARCHITECTURE §30، 3 اختبارات). **على المحاكي بحساب المالك (1.33):** قايمة الإضافة مفتوحة ⇒ رجوع ⇒ **اتقفلت** والتطبيق فضل مفتوح؛ تبويب الميزانية ⇒ رجوع ⇒ **الرئيسية**؛ الرئيسية ⇒ رجوع ⇒ **التطبيق راح الخلفية** (الواجهة بقت شاشة أندرويد). |

**APK 1.33 (v34)** SHA256 `D181DE5464773FB5737BBBE7512DB88F98B00BAC03CFF35405449754484D1B6E` على الدرايف (مطابق). على المحاكي: **اسم «مصروفي» بخط تشانجا** (`Changa` 38px) — بند 12.
| 11 | «عايز دايمًا أشوف أنيميشن وألوان وتصنيف وتشارتات في البرنامج» | 🧩 الشرايط بتكبر والصفوف بتظهر بهدوء (بتتلغي مع «تقليل الحركة»). + **رسم دايري** (`DonutChart`) لتوزيع المجموعات/التصنيفات في الرئيسية، والمجموعة بلون أكبر تصنيف جواها (commit `1d19690`، ⏳ المحاكي). ⚠️ تشارتات تانية (مقارنة الشهور مثلًا) لسه |

**✅ APK 1.28 (v29)** SHA256 `AA400C8EE6A4A15132DAA1404697448FCD5C18B054B220B7845D25E19339F570` على الدرايف (مطابق). **على المحاكي بحساب المالك (قراءة بس، 2026-09-15 ~19:53):**
- **القفل (بند 1):** وقت ما التطبيق مقفول: الغطا لونه خلفية التطبيق بشفافية 72% + `backdrop-filter: blur(12px)`، والتطبيق نفسه `filter: blur(18px)`. ⚠️ **صورة الشاشة وقت البصمة طلعت سودا كلها** — أندرويد بيمنع تصوير حوار البصمة، فالبلور اتأكد بالقراية مش بالصورة.
- **الشكل (بنود 2، 3، 5، 6، 8، 9):** الوضع الفاتح أخضر هادي (`rgb(241,247,243)`)، علامات الرأس والشريط مرسومة بالأخضر، زرار الإضافة أخضر، المصروف الكبير أحمر والدخل أخضر، خانتين بس، علامات جنب التسميات، **صفر حروف** جوه دواير العمليات، والصفوف المصنفة ملونة.
- **الميزانية (بند 10):** 6 سطور تصنيفات كلها ملونة برمزها، وشريط سقف واحد بلون تصنيفه.
- 🔴 **اتلاحظ:** زرار «حدّد سقف» لسه **أسود** (مش بياخد `--c-action`) و«بلا تصنيف» في المجموعات شريطه أسود تقيل ⇒ بيتصلحوا في 1.29.

**✅ APK 1.29 (v30)** SHA256 `8FE7C31FA99E6B45B3ADCCBD3E3FCEFA0C0089C7C720008FBE577E667646F6F6` على الدرايف (مطابق). commits `1d19690` (الرسم الدايري) و`0f152ce` (الزرار والشريط). **على المحاكي بحساب المالك (قراءة بس، ~19:58):**
- **الرسم الدايري ظاهر** في «المصروف بالمجموعات»: 4 أجزاء ملونة (المجموعة بلون أكبر تصنيف جواها، و«بلا تصنيف» رمادي)، والنص في النص أكبر مجموعة ونسبتها. رموز المجموعات وشرايطها بألوانها. (مجموعة نسبتها 0.0% ما بتترسمش — صح.)
- **زرار «حدّد سقف» بقى أخضر** `rgb(30,107,74)`، وسهم «الشهر الجاي» **مقفول** (الفترة الحالية).
- اتبعت للمالك صورتين (الرئيسية والميزانية) — مش منشورين.
- ⚠️ **ما اتجربش:** رجوع رمز التصنيف لما براند فيتش يرجّع 404 على محل حقيقي، والوضع الغامق على المحاكي، ورسم «التصنيفات» للحساب اللي مالوش مجموعات.
- **الجاي من الجدول:** علامات لباقي التسميات (خانات الميزانية الصغيرة وباقي الشاشات)، شريط للتصنيف اللي مالوش سقف في الميزانية (محتاج نسبة من حالة الاستخدام — قاعدة 4)، وتشارتات تانية (مقارنة الشهور). **الرفع لسه مستني المالك.**

**🧩 المالك قال «كمل شغل» (2026-09-15 ~22:30) — الدفعة الجاية من بند 6 و10 و11:**
- `domain/budget.ts`: `CategoryBudgetLine.shareTenthPercent` (نصيب التصنيف من مجموع مصروف التصنيفات، بـ`rateOfMoney` بالهللة). الميزانية: التصنيف اللي **مالوش سقف** بقى ليه شريط نصيبه بلونه + «X% من مصروف التصنيفات».
- `domain/periodBars.ts` (`relativeTenths`): أعمدة «آخر ست فترات» في «المزيد» — الأقدم يمين والحالية شمال (زي منتقي الشهر)، والحالية بلون الأزرار، وغير المتاح شرطة رمادية، والأعمدة بتطلع بحركة.
- علامات مرسومة: خانات الميزانية الأربعة (`Fact` بقى ياخد `icon`)، وأقسام «المزيد» الخمسة، وسهم «المزيد» بقى مرسوم بدل «‹».
- **اتفحص:** typecheck نضيف و**657 اختبار** (4 جداد في `periodBars.test.ts`). ⚠️ **أول تشغيل كامل وقع فيه اختبار واحد** (`fullBackup.test.ts` «exports and restores the same live repositories used by demo screens») **بمهلة الـ5 ثواني** (أخد 5.8 ث تحت ضغط التشغيل الكامل)، ولوحده عدّى في ثانية، والتشغيل الكامل التاني عدّى كله ⇒ **بطء توقيت مش عطل**، بس لو اتكرر يتزود مهلته.
- **✅ APK 1.30 (v31)** SHA256 `06F6D774E7DFEFC39D81802F8DDBA2C5C6D2155F1132B657F5551D0996AE9595` على الدرايف (مطابق)، commit `21c55e4`. **على المحاكي بحساب المالك (قراءة بس، ~22:41):**
  - **«المزيد»:** 5 أقسام بعلامات مرسومة وأسهم مرسومة، و**رسم «آخر ست فترات» فيه 6 أعمدة** (مارس يمين ← أغسطس شمال)، والفترة الحالية بلون الأزرار الغامق، ومفيش فترة غير متاحة.
  - **الميزانية:** الخانات الأربعة بعلاماتها، و**5 شرايط نصيب** للتصنيفات اللي مالهاش سقف بألوانها.
  - اتبعت للمالك صورتين (المزيد والميزانية) — مش منشورين.

**🧩 بند 6 و8 — آخر الإيموجي والرموز النصية (commit `d40b850`، نسخة 1.31):**
- سكربت فحص (بيدور على إيموجي و‹ › ⋯ ✕ ＋ ⚠ في كل `.tsx`) لقى **30 مكان** ⇒ اتبدلوا كلهم بعلامات مرسومة: زراير الإغلاق ✕ ⇒ `X` (14 ورقة)، التحذير ⚠ ⇒ `TriangleAlert`، قايمة الإضافة (✉ 🧾 📄) ⇒ `MessageSquareText` و`ReceiptText` و`FileText`، القمر/الشمس في شاشة الدخول ⇒ `Moon`/`Sun`، النقط التلاتة ⋯ ⇒ `Ellipsis`، «‹» في روابط «محتاجة تأكيد» ⇒ `ChevronLeft`، و«＋» ⇒ `Plus`. **الفحص التاني: صفر.** (الـ«⋮» في شرح التثبيت فضلت لأنها وصف لقايمة كروم نفسها.)
- علامات لعناوين: الحساب، المظهر، التثبيت، التصنيف التلقائي، المحافظ، مطابقة الرصيد، النسخة الاحتياطية، شخص جديد، المحفظة الاستثمارية.
- 🔴 **اتلقط قبل الفحص:** `SettingsScreen` بيستورد نوع `Wallet` من الكيانات، فعلامة المحفظة اتسمت `WalletIcon`.
- **اتفحص:** typecheck نضيف و657 اختبار. `SettingsScreen.tsx` بقى **300 سطر بالظبط** (على الحد — أي إضافة لازم تطلع في مكوّن). ⚠️ **`domain/entities/types.ts` 303 سطر من قبل الجلسة دي** (كان 303 عند `3ae8440`) — مخالفة قديمة للقاعدة 7 ما اتصلحتش، محتاجة تقسيم.
- **✅ APK 1.31 (v32)** SHA256 `B0FDE6EC6AB77314EAF967866EC823B552AD69BD28745E328D1DCE7932FEE376` على الدرايف (مطابق). **على المحاكي بحساب المالك:** قايمة «إضافة» فيها **4 علامات مرسومة وصفر رموز**، وزرار الإغلاق (علامة X) **قفل القايمة فعلًا**. اتبعت للمالك صورة القايمة.
- ⚠️ **ما اتجربش على المحاكي:** باقي الأوراق (الإغلاق فيها نفس التبديل الحرفي)، وشاشة الدخول (القمر/الشمس) لأنها بتظهر بعد تسجيل الخروج بس.
- علامات قايمة الإضافة بقت بلون التطبيق الأخضر (`AddMenu.css`).

**🧩 ملف الحساب في النسخة الشاملة (البند المتفق عليه في OVERRIDES §26 «ومعاها ضم ملف المستخدم للنسخة الشاملة»):**
- **ليه مش جوه `BACKUP_GROUPS`:** أدوات إصلاح المعرّفات (`repairStoredIds`) وتنظيف البقايا (`cleanupOrphans`) بتلف على المجموعات دي، ومستند `users/{uid}/profile/main` مالوش حقل `id` ⇒ كانت ممكن تعتبره معرّف مكسور و«تصلحه». فالملف بقى **جزء منفصل** `FullBackupFile.profile` (`null` = الحساب مالوش ملف).
- `domain/backupProfile.ts`: `checkBackupProfile` (الاسم ≤60، المرتب عدد صحيح مش سالب أو null، يوم الراتب 1–31، النوع، أيوه/لأ، بيعول مين، وقت أسئلة البداية) و`backupChecksumText`: **النسخة القديمة من غير `profile` بصمتها على البيانات بس زي ما كانت** (فلسه بتتقبل)، والجديدة البصمة بتغطي الملف كمان.
- `FullBackupPort`: `readProfile` و`addProfileIfMissing` (Firestore بمعاملة: بيكتب لو المستند مش موجود بس). الذاكرة والمعاينة (`snapshotFullBackup` بياخد مستودع ملف المعاينة نفسه).
- الاستعادة: المعاينة بتقول «ملف الحساب: هيتضاف» أو «موجود — مش هيتكتب فوقه»، ورسالة تحذير جديدة بنفس المعنى. نص الإعدادات بقى بيقول إن النسخة فيها ملف الحساب.
- **اتفحص:** typecheck نضيف و**661 اختبار** (4 جداد في `backupProfile.test.ts`: النسخة بتشيل الملف والاستعادة بتضيفه لحساب مالوش، الحساب اللي ليه ملف ما يتكتبش فوقه، تعديل الملف بيكسر البصمة قبل أي كتابة، ملف بقيمة غلط بيترفض، والنسخة القديمة من غير ملف مقبولة).
- ⚠️ **ما اتجربش على Firestore الحقيقي:** قراءة الملف من السيرفر وكتابته بالمعاملة (مختبرين بالذاكرة بس). **الاستعادة على حساب المالك ممنوعة من غير موافقته** (كتابة).
- **APK 1.32 (v33)** SHA256 `CD6A387F4782E96C3C265F232A30C7779C90DBAB50641874DF7C3D3D5612A996` على الدرايف (مطابق).
- 🔴 **محاولة تجربة «نسخة شاملة» على المحاكي فشلت ومحتاجة تتعاد (2026-09-15 ~22:59):** الخطة كانت: الزرار بيجهّز النسخة كلها (ومنها قراية الملف من السيرفر) ويفتح نافذة الحفظ ⇒ نلغي النافذة من غير حفظ. **اللي حصل:** قست مكان الزرار بعد ما `--eval` عمل `scrollIntoView` — **ده كان غلط مني: تحريك للشاشة من أدوات المطور، والمالك قال `--eval` للقراية بس** — والشاشة رجعت فوق فالدوسة وقعت على **خانة المرتب** (فتحت الكيبورد بس) و«رجوع» قفل الكيبورد. **مفيش حاجة اتكتبت ولا اتحفظت** (قسم الحساب مش بيحفظ غير بزرار الحفظ). الصورة فيها بيانات الحساب ⇒ **ما اتبعتتش لحد**. **المرة الجاية:** سحب بـadb لحد ما الزرار يبان، وقياس بالقراية بس، وبعدين الدوسة.
- 🔴🔴 **المحاولة التانية (1.35، بسحب adb وقياس بالقراية بس) كشفت عطل قديم حقيقي:** «نسخة شاملة» **رفضت** برسالة **«معرّف الميزانية مختلف عن الفترة»** ومفيش نافذة حفظ اتفتحت ولا حاجة اتكتبت. **يعني المالك ما كانش يقدر يعمل نسخة شاملة خالص** (من قبل تعديلات الجلسة دي — الفحص ده قبل ملف الحساب).
  - **السبب (اتأكد بقراية معرّفات بس من كاش الجهاز):** مستند الميزانية `budgets/2026-08` معرّفه جوه البيانات عشوائي (`budget-…`) وسطر سقف التصنيف `budgetId` بيشاور عليه. `setBudget.ensureBudget` كان بيعمل `id: deps.ids.next('budget')`، والمستودع بيخزن بمفتاح الفترة، و`checkFullBackup` بيشترط `id === periodKey`.
  - **التصليح (من غير كتابة على حساب المالك):** (1) `setBudget` بقى `id: period.key` للميزانيات الجديدة؛ (2) `domain/backupBudgetIds.ts`: `normalizeBudgetIds` في النسخة بس (الميزانية القديمة بتاخد مفتاح فترتها وسطورها بتتحوّل) — في `create` و`plan` و`apply`؛ (3) `pointLinesAtLiveBudgets`: الاستعادة على حساب ميزانيته بالمعرّف القديم بترجّع سطور السقوف المضافة للمعرّف الحقيقي المتخزن عشان تبان. 4 اختبارات في `backupBudgetIds.test.ts`.
  - ❓ **سؤال بعدين للمالك:** نصلّح معرّف ميزانيته القديمة في الحساب نفسه (كتابة بنسخة احتياطية)؟ مش ضروري للنسخة دلوقتي.
  - **✅ 1.36 (v37، SHA256 `8C9AB89590E961CDE422BC9009D63B1290A94A083E3311F1A84EC98C724221E1`، على الدرايف مطابق):** «نسخة شاملة» **جهّزت النسخة** (قراية كل البيانات من السيرفر ~66 ثانية + ملف الحساب) و**نافذة الحفظ اتفتحت** باسم `masroufy-backup-2026-09-15…`.
  - 🔴🔴 **بس التطبيق وقع وهو ورا نافذة الحفظ** — من سجل أندرويد: `TransactionTooLargeException: data parcel size 16148748 bytes`، و`capacitorLastPluginCallOptions [size=8072620]`. **السبب:** `localFiles.save` بيبعت **محتوى النسخة كله (~8 ميجا) جوه نداء الإضافة**، وCapacitor بيحفظ آخر نداء في حالة النشاط لما التطبيق يروح الخلفية عشان نافذة النظام ⇒ أكبر من حد أندرويد ⇒ وقوع. **يعني حفظ النسخة الشاملة غالبًا مش شغال على جوال المالك الحقيقي من زمان** (أي حساب بياناته كبيرة). مفيش ملف اتحفظ (النافذة اتلغت والتطبيق كان مات).
  - **التصليح (commit `7cf9fa1`، نسخة 1.37):** `saveTextFile` بيكتب المحتوى على دفعات 256 ألف حرف (`splitForWrite`، من غير ما يقطع إيموجي) في `export-pending.tmp` جوه مجلد التطبيق، و`LocalFilesPlugin.save` بياخد **`sourceName` بس** وبينسخ الملف للمكان المختار بـstream، و**بيمسح المؤقت سواء اتحفظ أو اتلغى**. `repairBackup` بقى بيستعمل نفس دالة التقسيم (مكتوبة مرة واحدة). 3 اختبارات جداد، و679 اختبار كلهم.
  - **✅ على المحاكي بحساب المالك (1.37، SHA256 `6036734533D4EF0CBB02712E6659EAC766F36B826E3EE1EBD6F2BB9519B9ADBA`، على الدرايف مطابق):** «نسخة شاملة» ⇒ نافذة الحفظ اتفتحت بعد ~58 ثانية ⇒ **التطبيق فضل عايش** (نفس رقم العملية 7946 والنافذة مفتوحة) والملف المؤقت موجود (~4 ميجا) ⇒ «رجوع» لغى النافذة ⇒ **رجع للتطبيق نفسه** (نفس العملية) و**الملف المؤقت اتمسح**. سجل أندرويد: **صفر وقوع من التطبيق** (الـ26 سطر `TransactionTooLarge` كلهم من خدمات جوجل `ChimeraSrvcProxy`، مش مننا).
  - ⚠️ **ما اتجربش:** دوسة «SAVE» فعلًا (عشان ما يتحفظش نسخة من بيانات المالك على المحاكي) — يعني كتابة الملف في المكان المختار ما اتشافتش؛ والاستعادة من ملف (كتابة على الحساب). **الأحسن المالك يجرب «نسخة شاملة» على جواله ويحفظها في مكان هو عايزه.**

**✅ ردود المالك في نافذة (2026-09-15 ~23:50):**
- **الرفع: «ارفع دلوقتي»** ⇒ اترفع **33 commit** على GitHub (`3ae8440..764da5e`). اتأكد قبلها إن GitHub مفيهوش حاجة أحدث، وإن رقم تعريف براند فيتش مش في أي ملف متتبع.
- **ميزانية أغسطس القديمة: «صلّحه بنسخة احتياطية»** ⇒ اتبنت أداة `domain/budgetIdRepair.ts` + `application/useCases/repairBudgetIds.ts` + `BudgetIdRepairPanel` (جوه قسم إصلاح البيانات في الإعدادات تحت «القواعد والتجار الافتراضيين»): فحص قراءة بس ⇒ «هيتعدّل X ميزانية وY سقف» ⇒ «احفظ نسخة وصلّح» (نسخة `before-budget-ids` متأكدة الحجم ⇒ كل التعديلات في دفعة واحدة). اختبارين.
  - **✅✅ اتنفذ على حساب المالك (APK 1.38 / v39، SHA256 `B970B99EDB9AF41D65DBC68FB33464E0D9D3B8B413619D30E53BD6A573710145`، على الدرايف مطابق، 2026-09-16 ~00:08):** الفحص قال «هيتعدّل **1 ميزانية و1 سقف تصنيف**» (نفس اللي اتشاف في كاش الجهاز قبلها) ⇒ السكربت كان مشروط يكمّل **بس لو العدد ده بالظبط** ⇒ «احفظ نسخة وصلّح» ⇒ «النسخة اتحفظت (1 ك.ب). **اتصلح 2 مستند**» ⇒ الفحص التاني: «**معرّفات الميزانيات سليمة**». **النسخة على الجهاز:** `masroufy-before-budget-ids-2026-09-15T21-08-24-301Z.json` بحجم **902 بايت**. **الميزانية بعدها:** 6 سطور تصنيفات و**شريط السقف لسه ظاهر** (السقف فضل مربوط بالميزانية).
  - **الرفع بعدها:** فيه 2 commit جداد (أداة التصليح + التسليم) — سُئل المالك في نافذة ⇒ **«استنى»**. ما يترفعوش من غير موافقة جديدة.
  - 🔴 **غلطة تشغيل اتصلحت:** أول محاولة فشلت لأن PowerShell 5.1 بيشيل علامات `"` من النص اللي بيتبعت لـnode ⇒ سكربت القراية كان بيطلع `SyntaxError` (السحب بس اتعمل، مفيش دوسة). **الحل:** السكربتات اللي فيها عربي تتحول ASCII بـ`gen-ascii-probe.cjs` (العربي `\uXXXX`، ومن غير `"`) وتتشغل من Bash مش PowerShell. **مش مرفوع على GitHub:** 8 commits من بعد `3ae8440` (المالك قال «استنى»).
- **🎯 هدف مستقبلي (بعد ما نخلص المشاكل الحالية — نص المالك):** **«المشاريع»** — «زي كأنه هيعمل مجموعة عمليات مشتركة من تصنيفات مختلفة كلها بتخدم المشروع ده، ويقدر يضيف قواعد، يعني مثلًا العملية دي يقدر يضغط عليها ويديها قاعدة إنها تنضاف بشكل أوتوماتيك لو حصلت كل مرة للمشروع الفلاني». مثاله: طالب بيبني ماكت — المواصلات للمكتبة والأدوات… يضيفها يدوي، وعمليات يعلّم عليها من إعداداتها إنها تنضاف أوتوماتيك. **القواعد للمصاريف وللفلوس اللي بتيجي** («ممكن مشروع معين بيجيبله دخل»)، وجوه المشروع يشوف **صرف قد إيه وجاله قد إيه**. «مكان المشاريع يكون فين مش عارف، اقترح إنت».
  - **الاقتراح (اتاخد بقاعدة الساعة 2، والمالك يقدر يغيّره):** صفحة «المشاريع» جوه «المزيد»، و«ضيف لمشروع» في قايمة النقط التلاتة لكل عملية، والقواعد الأوتوماتيك بتتعمل من جوه المشروع (زي قواعد التصنيف: نص في اسم التاجر/الوصف + اتجاه). المشروع **وسم إضافي مش تصنيف** — العملية بتفضل بتصنيفها، والمبلغ ما يتحسبش مرتين (زي الوسوم، spec/02).
  - ❓ **مفتوح للمالك:** يتبني في التطبيق الحالي ولا في نسخة كوتلن (§29: المميزات الجديدة هناك)؟ ما يتحسمش من غيره.
- **🕑 قاعدة مؤقتة من المالك (2026-09-15 الساعة 11:37):** «لو في أسئلة الفترة الجاية لحد الساعة 2 قم بالموافقة علطول على الإجابة المقترحة في حالة إني لم أجاوب في خلال 10 دقايق». النافذة ما بتقفلش لوحدها ⇒ **لحد 14:00 بتتاخد الإجابة المقترحة على طول وتتقال للمالك في الشات** عشان يرجع فيها. بعد 14:00 الأسئلة ترجع في نوافذ.
- **🎨 طلب المالك (2026-09-15، بصورة من الرئيسية):** «على اليمين فوق يتحط اسم البرنامج فقط ونشيل كلمة (الرئيسية)… بالنسبة للشريط اللي مكتوب فيه الشهر والسنة محتاج إعادة تصميم منك بشكل أبسط من كده، واتجاه الأسهم معكوس».
  - **اتنفذ بالإجابة المقترحة (قاعدة الساعة 2):** `ShellHeader` بيكتب «مصروفي» على **كل** الشاشات، واسم الشاشة بقى `visually-hidden` لقارئ الشاشة (`AppShell` بيبعت `screenName`). `PeriodPicker`: سطر مسطح بعرض الشاشة (سهم، الشهر والسنة وتحتهم «28 أغسطس – 27 سبتمبر» بدل ISO، سهم). **الأسهم رسمة SVG** (`ChevronRight`/`ChevronLeft` من lucide-react) بدل الحروف «‹ ›» اللي **بتنعكس لوحدها في النص العربي** (غالبًا سبب «معكوس»). **الاتجاه: يمين = الشهر اللي فات (السهم لليمين)، شمال = الشهر الجاي (السهم للشمال)** — طريقة التطبيقات العربية. ❓ لو المالك قصد العكس، التبديل سطرين.
  - 🧩 **رجوع القواعد والتجار الافتراضيين — الكود اتكتب:** `application/useCases/restoreDefaultReferences.ts` (معاينة ← قراءة تاني وتنفيذ المعروض بس ← نسخة متأكدة `before-default-rules` للعمليات ← `rules.saveMany` و`merchants.saveMany` للناقصين ← اقتراح `categoryId` + `reviewState: 'suggested'` على دفعات) و`presentation/components/DefaultRulesPanel.tsx` (جوه `IdRepairPanel` تحت نقل التصنيفات). التوصيل: `container.ts` (اتشالت 3 سطور فاضية عشان يفضل 300، و`seedRefs` بقى متحسب مرة) و`demoContainer.ts` (منفذ بيقرا من الذاكرة). **اتفحص قبل التوصيل:** 632 اختبار (4 في `restoreDefaultReferences.test.ts`).
  - **✅ الشريحة (4) خلصت على حساب المالك.** الباقي من التصنيفات: `RulesScreen` و`BudgetScreen` و`HistoryReviewScreen` و`CategoriesScreen` لسه قايمة مسطحة (مش مجموعات)، و`CategoriesScreen` مالهاش اختيار أب ولا رمز.
- **رفع الـAPK:** بعد أي نسخة ⇒ `H:\My Drive\WORK\مصروفي\` (الذاكرة `upload-apk-to-drive`).
- **مش مرفوع على GitHub:** `7b0b7f0`، `2ef4954`، `9ed5a2e` + commit القرارات دي (المالك قال «استنى»).

### ▶️ شغل الليل 2026-09-16 — «كمّل كل حاجة متبقية ماعدا الصوت» (Claude Opus 5)
- **ردود المالك:** «طيب كمل كل حاجة متبقية ماعدا الصوت قولي بس الخطة ايه» ⇒ اتقالت الخطة (5 مراحل: بواقي التطبيق الحالي ← تجارب على جواله ← خطة كوتلن مكتوبة ← بناء كوتلن ← المميزات الجديدة هناك). «المشاريع» ⇒ نافذة ⇒ **«في التطبيق الحالي»**. وقبل ما ينام: **«متوقفش شغل… لو عندك سؤال جاوب عليه بنفسك، ولو في حاجة لازم أنا أعملها أجّلها وخش في اللي بعدها»** ⇒ OVERRIDES §33 (الرفع والنشر والدفع برضه بموافقته بس).
- **الترتيب:** (1) بواقي التصنيفات ← (2) المشاريع ← (3) خطة كوتلن مكتوبة. المؤجل لأنه محتاج المالك: ملفات الشعارات، تجارب جواله (حفظ النسخة الشاملة فعلًا، تعديل مبلغ، الوضع الغامق، تأكيد تاجر من كونسول فايربيز)، الرفع.

**🧩 (1) شاشة التصنيفات بالمجموعات + القواعد (commit `aaef6c7`، نسخة 1.39) — قرارات الشاشة في OVERRIDES §33.1:**
- `domain/categoryPalette.ts`: 18 درجة من ألوان أساسيات الشجرة، `swatchColors`، `swatchKeyOf` (اللون الحالي من أنهي درجة)، `firstFreeSwatch`، `hexToHsl`، `childColors` (درجة الفرعي من لون أبوه، واللون المش مفهوم بيتنسخ).
- `domain/categoryEdit.ts` (`planCategorySave`، دالة نقية): مستوى واحد (مفيش فرعي تحت فرعي، واللي تحته فرعيات ما يتنقلش)، الفرعي مالوش مجموعة ولونه من أبوه لما يتعمل أو يتنقل بس، **تغيير لون الأساسي بيرجّع `recolored` بدرجات فرعياته بترتيبهم**، الحاجات اللي الشاشة ما بتعدلهاش (`requires`، `noCarName`، الترتيب) بتفضل، ولون/رمز/مجموعة مش معروفين مرفوضين.
- `domain/categoryManageView.ts`: `manageCategoryView` (كل التصنيفات **والمخفي كمان**، «التصنيفات» اللي من غير مجموعة الأول) و`categoryPlaceChoices` («بلا مجموعة» للحساب القديم بس، والأساسيات اللي ينفع يبقى فرعي تحتها بمجموعاتها).
- `manageCategories.save` بياخد `CategorySaveInput` (**`paletteId` اتشال**) وبيحفظ الفرعيات **قبل** أبوها (لو وقف في النص، الحفظ تاني بيعيد حسابهم لأن لون الأساسي لسه القديم).
- الشاشة: `CategoriesScreen.tsx` (+css): زرار «ضيف تصنيف جديد»، والقايمة بالمجموعات برمز المجموعة، والفرعي تحت أبوه بخط جانبي، وكل صف بلونه ورمزه وعلامة «مخفي» وشرط الظهور. `CategoryForm.tsx`: الاسم، «مكانه» (تصنيف أساسي / فرعي تحت تصنيف)، المجموعة، دواير الألوان («لونه الحالي» لو مش من الدرجات)، شبكة الـ96 رمز (`components/CategoryIconPicker.tsx`)، ظاهر في الاختيارات. `components/categoryStyle.ts` (`--cat-light`/`--cat-dark`).
- `RulesScreen`: اختيار تصنيف القاعدة وتثبيت تصنيف التاجر بالمجموعات (`CategoryOptions`)، والاختيار المبدئي لقاعدة جديدة **أول تصنيف ظاهر** (كان أول تصنيف متخزن حتى لو مخفي ⇒ القاعدة كانت ممكن تروح لتصنيف مش باين).
- `HistoryReviewScreen` مفيهاش اختيار تصنيف (اختيار النوع بس) ⇒ ما اتغيرتش. `BudgetScreen` سطوره للتصنيفات اللي فيها صرف أو سقف مش اختيار ⇒ ما اتغيرتش (تجميعها بالمجموعات مش في الخطة — ممكن بعدين).
- 🔴 **عطل قديم اتلقط بسكربت فحص** (بيدور على `var(--x)` مش متعرف في أي ملف): `--c-accent` مستخدم في تبويب القواعد والاستثمار والإشعارات **ومش متعرف خالص** ⇒ **زرار التبويب المختار كان كلامه أبيض على خلفية شفافة**. وكمان `--c-text-muted` و`--t-label` و`--t-metric` و`--c-on-accent`. اتعرفوا في `tokens.css` كأسماء بديلة للموجود.
- **اتفحص:** typecheck نضيف و**692 اختبار** (11 جداد في `categoryEdit.test.ts`، واختبار `organization` اتعدل من `paletteId` لـ`swatchKey`).
- **✅ المعاينة (`?demo=1`):** «التصنيفات وألوانها» ⇒ «الأكل والشرب» برمزها ⇒ «مطاعم وقهوة» وتحته فرعياته بخط جانبي، كل صف بلونه ورمزه. «ضيف تصنيف جديد» ⇒ «عصاير طازة» فرعي تحت «مطاعم وقهوة» برمز الكوباية ⇒ «اتحفظ «عصاير طازة».» وظهر آخر فرعي تحته بدرجة من لونه. تعديل «مطاعم وقهوة»: «فرعي تحت تصنيف» مقفول مع «تحته فرعيات، فبيفضل تصنيف أساسي.»، اختيار الأزرق ⇒ الأساسي `#206BB6` و**الفرعيات الأربعة درجات أزرق مختلفة** (`#257DD4`، `#1B5C9C`، `#2F85DA`، `#18528B`). «القواعد والتجار»: الاختيار فيه 6 مجموعات والمبدئي «مطاعم وقهوة»، والتبويب المختار بقى أخضر بكلام غامق.
  - اتصلح أثناء المعاينة: دواير الألوان كانت بيضاوي بسبب `button { min-height: 48px }` العام ⇒ `min-height: 0` و40px.
  - 🔴 **غلطة تجربة مني:** أول تعديل، الدوسة على اللون وقعت على «ظاهر في الاختيارات» لأن الشاشة كانت لسه بتتحرك لفوق (`scrollIntoView` ناعم) ⇒ اتحفظ مخفي **في بيانات المعاينة بس** ⇒ رجّعته. **القياس بعد ما الحركة تخلص.**
  - ملاحظة مش من الشغل ده: فرعيات بعض الأساسيات بتطلع بنفس الدرجة (مثلًا تحت البرتقالي: الإزاحة +7 و+12 الاتنين بيتقصوا عند سقف 52) — من حساب الشجرة الأصلي (§28). ممكن يتحسن بعدين.

### ▶️ نقطة الاستكمال للجلسة الجاية (آخر تحديث 2026-09-13)
- **كله مرفوع على GitHub** لحد `a9c3553` (بموافقة المالك «ارفع»). مفيش شغل محلي مش مرفوع.
- **خلص واتحقق على حساب المالك:** إصلاح المعرّفات، تنظيف بقايا الإكسل كلها، **مطابقة الرصيد كاملة (1912 سطر، صفر فروق)**، رصيدي الافتتاح المعتمدين، قفل البصمة، وقسم «الحساب» + أيقونة ⚙️ + يوم الراتب لكل مستخدم (اتجرب حفظه في المعاينة بس).
- **آخر APK:** `dist-android-apk/masroufy-trial-v17.apk` (1.16). **574 اختبار ناجح.**
- **الجاي بالترتيب اللي اختاره المالك:**
  1. **أسئلة البداية للحساب الجديد** — القرارات كلها موجودة (OVERRIDES §26): الاسم/المرتب/يوم الراتب/النوع/بيعول حد (اختياري ما عدا يوم الراتب)، **الكاش = رصيد افتتاح لمحفظة الكاش**، **الديون تتكتب في شاشة الأشخاص**. ومعاها ضم ملف المستخدم للنسخة الشاملة.
  2. **التصنيفات والفرعية بألوان ورموز** + **قاعدة التجار المشتركة** (OVERRIDES §25: مراجعة من لوحة فايربيز، تغيير تصنيف مؤكد يبقى للمستخدم بس، شعارات جوه التطبيق + أونلاين).
  3. **الصوت:** **تجربة جيميناي الصغيرة الأول** بجملة وهمية (OVERRIDES §24) قبل أي شاشة.
- **المالك عايز الأسئلة في نوافذ اختيار** (AskUserQuestion) مش في نص الشات، ومن غير حروف إنجليزي في عناوين الاختيارات.
- **🧩 شريحة أسئلة البداية — الكود اتكتب (typecheck نضيف، 582 اختبار، كل الملفات تحت 300)، ⏳ التجربة في المعاينة جارية (2026-09-13 19:00):**
  - **للحساب الجديد بس** (قرار Claude من نص المالك، OVERRIDES §26): `useAppData.prepare()` لما `seedWallets` يرجع `seeded: true` ⇒ `markOnboardingPending()` بيعلّم الملف `onboardingPending`. الحسابات القديمة (زي حساب المالك) مش بتشوف الأسئلة.
  - **دين قديم من غير عملية** (OVERRIDES §27، نص المالك «لازم نفترض إن في عمليات كاش ملهاش سجل قبل كده»): `Obligation.originTransactionId` بقى `Id | null`، و`managePeople.addOpeningDebt`. مفيش عملية وهمية. النسخة الشاملة (`checkFullBackup`/`checkBackupFinance`) بتقبله وبترفض مبلغه لو مش موجب. شاشة الأشخاص بتكتب جنبه «دين قديم من قبل التطبيق».
  - `application/useCases/onboardNewAccount.ts`: بيتأكد من **كل** القيم قبل أي كتابة ← رصيد افتتاح محفظة الكاش (لو اتكتب) ← أشخاص + ديون قديمة (من غير تكرار عند إعادة المحاولة) ← `completeOnboarding` آخر حاجة.
  - `presentation/screens/OnboardingFlow.tsx` (5 خطوات: الاسم، المرتب ويوم الراتب، النوع و«بتعول حد»، الكاش، الديون) — بيتعرض من `ShellOverlays` لما `app.needsOnboarding`.
  - **التجربة الكاملة على حساب حقيقي جديد محتاجة المالك يعمل حساب تجريبي بنفسه** (Claude ممنوع يعمل حسابات أو يكتب كلمات سر). وضع المعاينة بيبدأ من غير محافظ فالأسئلة المفروض تظهر فيه.
  - **✅ اتجربت كاملة في وضع المعاينة (`?demo=1`، بيانات وهمية، 2026-09-13 19:10):**
    - نافذة «أهلًا بيك في مصروفي» **ظهرت لوحدها** (المحافظ اتزرعت لأول مرة) — «سؤال 1 من 5».
    - مرتب «abc» ⇒ الخطأ ظهر والخطوة فضلت «سؤال 2 من 5». بعدها مرتب 9000 ويوم راتب 25 ⇒ «سؤال 3 من 5» ⇒ النوع و«بتعول حد» ⇒ كاش 750.50 ⇒ دينين («أحمد» ليا عنده 500، «خالد» عليا ليه 200) ⇒ «خلّصت».
    - **بعد الإنهاء:** النافذة اتقفلت من غير خطأ. **الفترة بقت `2026-08-25 ← 2026-09-24`**. **شاشة الأشخاص:** «لك عنده · دين قديم من قبل التطبيق · باقي 500.00 من 500.00» و«قرض عليك · دين قديم من قبل التطبيق · باقي 200.00 من 200.00». **الإعدادات:** الحساب (الاسم، 9000.00، 25، النوع، لأ)، و**كاش 750.50 في 2026-09-13**، والراجحي ما اتلمسش. مفيش أخطاء كونسول في أي خطوة.
  - **⚠️ ما اتجربش:** على المحاكي، ولا على حساب حقيقي جديد (محتاج المالك يعمل حساب تجريبي بنفسه).
  - اتعمل commit `7b0b7f0` (**مش مرفوع** — المالك قال «استنى» على الرفع).
- **🔁 تعديل برد المالك (2026-09-13 19:20) — مسجل في OVERRIDES §26:**
  - **«خليهم يتسألوا لأي حد عادي مدام متسألش قبل كده»** ⇒ الأسئلة بتظهر لأي حساب ملفه مالوش `onboardedAt` (جديد أو قديم، **وحساب المالك كمان**). اتشالت علامة `onboardingPending` و`markOnboardingPending` خالص؛ `needsOnboarding = !onboardedAt`.
  - `onboardNewAccount.ts` ⇒ **`onboardAccount.ts`** وبقى `{ start, finish }` (في الحاوية `user.onboarding`). `start` بيرجّع الملف المتحفظ ورصيد بداية الكاش لو مش صفر، والشاشة **بتملا الخانات بيهم** — فحساب قديم ما يتكتبش فوق بياناته بقيم فاضية. فشل القراءة ⇒ رسالة + «جرّب تاني» ومفيش أسئلة فاضية.
  - **سؤال الكاش لحساب ليه رصيد بداية — رد المالك «يوريه الرصيد ويسيبه يغيّر»:** الرصيد الحالي مكتوب في الخانة ومعاه تاريخه. **نفس المبلغ بالظبط ⇒ المحفظة ما تتلمسش ولا تاريخها.** مبلغ مختلف ⇒ رصيد بداية جديد من النهارده، والشاشة بتقول إن عمليات الكاش اللي قبله ما تتحسبش في رصيد الكاش.
  - **اتفحص:** typecheck نضيف، **584 اختبار** (جداد: الأسئلة لحساب من غير ملف ولحساب حافظ من قسم الحساب، `start` بيرجّع الموجود، نفس رصيد الكاش ما يلمسش المحفظة، رصيد مختلف ⇒ من النهارده).
  - **✅ المعاينة (`?demo=1`، 19:35):** النافذة ظهرت لوحدها ← «سؤال 4 من 5» خانة الكاش فاضية ومعاها جملة الحساب الجديد (رصيد المعاينة صفر) ← 300 ← «خلّصت» ← النافذة اتقفلت ← الإعدادات «كاش — يبدأ من 300.00 في 2026-09-13».
  - المعاينة بتبدأ فاضية، فمسار الحساب القديم اتجرب على المحاكي:
  - **✅ على المحاكي بحساب المالك — من غير «خلّصت» (مفيش كتابة) — APK 1.17 / versionCode 18، `dist-android-apk/masroufy-trial-v18.apk`، SHA256 `6E9712E7161AC3A94F5AC0203907BF8963F78661F0F8DC16B5A68F57E4B21FEE`، 2026-09-13 19:45:**
    - بعد التحديث (`install -r`) والفتح: **قفل البصمة ظهر فوق الأسئلة** (التطبيق كله `inert`)، وبعد البصمة النافذة ظهرت لوحدها «سؤال 1 من 5» — حساب المالك مالوش ملف متحفظ فالاسم فاضي.
    - «التالي» ×3 بإحداثيات متقاسة لايف والتأكد من رقم السؤال بعد كل ضغطة ⇒ «سؤال 4 من 5».
    - **سؤال الكاش:** الخانة فيها **رصيد البداية المعتمد بالظبط** (قيمة CLAUDE.md) والجملة «المكتوب هو رصيد البداية الحالي… (من 2026-09-06)». يعني «خلّصت» من غير تعديل **مش هيلمس محفظة الكاش**.
    - 🔴 **غلطة مني:** أول ضغطة كانت على شاشة القفل (ما قستش القفل قبلها) ولغت تأكيد البصمة — «اتلغى التأكيد — ما اتغيّرش حاجة». **قبل أي ضغطة على المحاكي بعد فتح التطبيق: اتأكد إن `.appLock` مش موجود.**
    - **النافذة سايبها مفتوحة على سؤال 4** — «خلّصت» للمالك (كتابة ملفه).
- **📦 طلب المالك (2026-09-13): بعد أي APK يترفع على جوجل درايف بتاعه في «ملفاتي › WORK › مصروفي».**
  - `masroufy-trial-v18.apk` **موجود هناك فعلًا** بنفس الحجم بالظبط (5,178,276 بايت) — المالك رفعه بنفسه.
  - ⚠️ **قيد:** أداة درايف بتاخد الملف كنص base64 جوه الطلب (كبير جدًا على APK)، والدرايف المتزامن على الجهاز (`G:\My Drive`) حساب تاني مالوش فولدر WORK. ❓ مستني المالك يختار طريقة الرفع.
  - رد المالك: «أنا مديلك أكسس على حسابي على درايف نفسه، ليه ما ترفعش إنت على طول؟» ⇒ اتشرحله إن الأكسس موجود (القراءة والبحث شغالين، وبيها اتأكدت من v18)، بس الأداة بتاخد محتوى الملف كله نص جوه طلب واحد (~7 مليون حرف لـAPK) ⇒ مش هيعدي. واتسأل تاني في نافذة.
  - **✅ قرار المالك: «هضيف حسابي لدرايف الجهاز».** بعد ما يضيفه: كل APK جديد يتنسخ لفولدر «WORK/مصروفي» في الدرايف المتزامن الجديد (حرف درايف جديد غالبًا)، وبعدين يتأكد وصوله بـ`search_files` على `parentId = '1wkfB60rlNz6Gt8zRkRXrDdyZ3XzAkiiM'` بنفس الحجم. **✅ طلع متضاف فعلًا:** الحساب متزامن على الجهاز كدرايف `H:` ⇒ المسار **`H:\My Drive\WORK\مصروفي\`** (فيه v8 و v18). طريقة الرفع من هنا ورايح: `cp dist-android-apk/masroufy-trial-vNN.apk "/h/My Drive/WORK/مصروفي/"` ← مقارنة SHA256 ← `search_files` بنفس الحجم. (`G:` حساب تاني — ممنوع.)
- **ممنوع تكرار أخطاء اتسجلت في §36:** ضغطات المحاكي بإحداثيات ثابتة (قيس المكان لايف واتأكد من `h1`)، الحكم على حفظ قبل إعادة التحميل، ورسالة فيها كلمة طويلة من غير مسافات.
1. **أيقونة الإعدادات في الرأس** — قرار مسجل في OVERRIDES §23.
2. **قسم «الحساب» أول الإعدادات:** الإيميل، تغيير كلمة السر، الاسم، **المرتب**، **موعد استحقاق الراتب**، **النوع (ذكر/أنثى)** — هيتبني عليه بعدين مميزات مختلفة لكل نوع — و**هل بيعول حد**. بعدين: يمنشن اللي بيعولهم على مشترياته ويفلتر بيهم ويعرف دفعلهم كام.
3. **أسئلة أول ما يعمل الحساب** ورا بعض: البيانات اللي فوق + **الكاش اللي معاه دلوقتي** + **هل عليه ديون أو ليه فلوس عند حد**.
4. **خريطة ذهنية** لاللي اتعمل واللي فاضل — ✅ **اتعملت واتبعتت للمالك** (صفحة خاصة على claude.ai: https://claude.ai/code/artifact/cc7d3db1-9736-4519-9b68-492430f748a0). مفيهاش مبالغ ولا أسماء تجار. لو اتحدثت، اتنشر على نفس الرابط.
5. **فكرة للنقاش: إضافة المشتريات بالصوت** — يتكلم، والكلام يتحول لنقاط، وتظهر كاقتراحات يأكدها أو يعدّلها وتتضاف.
6. **تحديث التصنيفات والتصنيفات الفرعية** بألوان ورموز واضحة، و**قاعدة بيانات كبيرة للتجار المشهورين بشعاراتهم وتصنيفهم المبدئي**، متخزنة تلقائيًا.

**✅ ردود المالك (2026-09-13):**
- **آخر 16 مكرر: «نفّذ الاتنين»** — التراجع عن استيراد 09-10 (14) + مسح نسختين الإكسل المطابقتين. قبل التنفيذ اتضاف **نسخة مؤكدة الحجم للتراجع نفسه** (كان بيحذف من غير نسخة) — `verifiedBackup.ts` مشترك بين الإصلاح والتنظيف والتراجع. اختبارين جداد في `importHistory.test.ts` (النسخة فيها بالظبط اللي هيتمسح + نسخة ناقصة ⇒ ولا حذف)، **560 اختبار ناجح**.
  - **✅ التراجع عن استيراد 09-10 اتنفذ على حساب المالك (APK 1.13 / versionCode 14، SHA256 `4FFBC46B8E8F264B8E702C7C6668F77075C3CAE92F3AF71CFF52086CD3073A1A`، 2026-09-13 13:26):**
    - السكربت وقف لو المعاينة مش **بالظبط** «هيتشال 14 عملية من 14» لدفعة 2026-09-10 وزر «تراجع عن 14 عملية» — وطلعت بالظبط.
    - الرسالة: «اتشال 14 عملية. النسخة اتحفظت قبل الحذف (1305 ك.ب)»، والدفعة بقت «**متراجَع عنه**».
    - **النسخة على الجهاز بعيني:** `masroufy-before-revert-…json` بحجم **1,336,389 بايت**. كبيرة لأنها فيها **كل سجلات مصدر الدفعة** (الملف كان ~1900 سطر، 14 بس منهم جداد) — التراجع بيمسح سجلات دفعته دايمًا.
    - المعاينة ما عرضتش «هيفضل … ليها مصدر تاني» ⇒ مفيش عملية تانية كانت معتمدة على سجلات الدفعة دي.
    - **فحص التنظيف بعد التراجع (قراءة بس):** السطور اللي فيها رصيد **1914** (= 1928 − 14 بالظبط)، والكسور **2** (= 16 − 14 بالظبط)، والاتنين «من بقايا الإكسل اللي فضلت · نفس اليوم · ليه نسخة مطابقة حتى في الرصيد».
    - ⚠️ **بس التنظيف لسه بيقول «هتتمسح: 0»** — يعني النسختين دول **مش** من اللي قاعدة التوأم + الرصيد بتأكدهم. **ما اتمسحوش.**
    - **شك قوي (مش مثبت):** واحدة منهم ممكن تكون **عملية الإكسل المتربطة بشخص** (اللي فضلت من التراجع الأصلي بسبب التخصيص). لو كده، مسحها محتاج **نقل ربط الشخص لتوأمها الأول** — وده **كتابة في بيانات دين/شخص المالك ما وافقش عليها** (الموافقة كانت «امسح النسختين»).
    - **التقرير بقى بيقول حالة كل كسر (APK 1.14 / versionCode 15، SHA256 `3C3439874D6024D251953C927D5CCB04BA40D862D2B0597127832DDA4789C3D1`، قراءة بس):** الكسرين الاتنين سطر من بقايا الإكسل، ونسخته المطابقة **حتى في الرصيد** سطر من كشف مسجّل (مش من البقايا):
      1. **حالته «من غير توأم مسجّل»:** قاعدة التوأم (نفس اليوم والمبلغ والاتجاه + عدد التوائم المتاحة + المحفظة) ما عدّتوش، بس ليه نسخة مطابقة **بالرصيد كمان** والسلسلة بتكسر بيه ⇒ **مكرر واضح بدليل أقوى من القاعدة**. داخل موافقة المالك «امسح النسختين» — **بس أداة التنظيف الحالية مش بتمسحه** ⇒ محتاج إضافة قاعدة «نسخة مطابقة بالرصيد + السلسلة بتأكد».
      2. **حالته «متربطة بشخص أو تسوية أو وسم»:** دي عملية الإكسل اللي التراجع الأصلي سابها بسبب تخصيص لشخص. مسحها محتاج **نقل الربط لنسختها من الكشف الأول** ⇒ ده **كتابة في بيانات الأشخاص/الديون** ⇒ **اتبعت سؤال للمالك**.
    - **✅ ردود المالك (مسجلة في OVERRIDES §22):** المتربطة «**انقل الربط وامسح**» · غير المتربطة «**امسحها بالقاعدة دي**» (نسخة مطابقة بالرصيد + السلسلة بتأكد).
    - **بصمة شاشة الأشخاص قبل نقل الربط (قراءة بس):** 5 أرقام، `eb700498ecf00d5a` — لازم تفضل هي هي بعد النقل.
    - **الكود (مفحوص بـ typecheck و573 اختبار، لسه ما اتجربش على الحساب):**
      - `domain/orphanCleanup.ts`: طريق تاني للحذف (ب) للي من غير دليل وللمتربطة: **نسخة مطابقة حتى في الرصيد** لسطر حقيقي، واحدة قصاد واحدة (السطر الحقيقي اللي اتاخد بقاعدة اليوم ما يتحسبش تاني)، **والسلسلة لازم تأكد**. المتربطة: **`relinks`** بتنقل كل ربط (تخصيص/التزام/تسوية/وسم/استثمار) لنسختها — **إلا لو النسخة عليها ربط من نفس النوع** ⇒ `relinkBlocked` وما تتلمسش.
      - `cleanupOrphans.apply`: لو الربط اتغير بعد المعاينة ⇒ يرفض قبل أي كتابة. النسخة فيها المتمسح **ومستندات الربط قبل تعديلها**. الترتيب: نسخة ← نقل الربط ← الحذف. انقطاع بعد النقل ⇒ الفحص التاني بيلاقيها مش متربطة وبيمسحها بقاعدة اليوم (مختبر).
      - اختبارات جديدة: النقل ثم الحذف، المنع لما النسخة عليها ربط، الانقطاع بعد النقل، ومحفظة مختلفة بنسخة مطابقة بالرصيد.
      - APK 1.15 (versionCode 16): `dist-android-apk/masroufy-trial-v16.apk`، SHA256 `DB8E8FBD3921A0F721B5723F011104EFBE674C9AC5D5116F66FC8C3F17E7FD68` — اتثبت على المحاكي.
    - **المعاينة على الحساب (قراءة بس):** «هتتمسح: **2**» · «ربط هيتنقل: **2**» · «سجلات: 0» · مفيش ممنوعين · السلسلة **دلوقتي 2 كسر ← بعد التنظيف 0 كسر** · الزر «احفظ نسخة وامسح 2».
      - ⚠️ **الربط طلع 2 مش 1** زي ما كنت متوقع: غير المتربطة مالهاش ربط، فالاتنين **مستندين ربط لنفس العملية المتربطة** (مثلًا تخصيص + التزام لنفس الشخص). موافقة المالك «انقل الربط وامسح» بتشمل كل ربط العملية، وشرط الأمان (النسخة الحقيقية مالهاش ربط من نفس النوع) عدّى. الإثبات هيبقى **بصمة شاشة الأشخاص قبل وبعد**.
      - الـ79 اللي من غير توأم: الرصيد بيقول **79 حقيقيين و0 مكرر** ⇒ مفيش بقايا مكررة تانية.
    - **✅ اتنفذ واتحقق على حساب المالك (2026-09-13 14:46، APK 1.15):**
      - السكربت اتأكد إن المعاينة **بالظبط** (2 مسح · 2 ربط · 0 سجلات · 0 ممنوع · 0 كسر بعد) قبل الضغط.
      - الرسالة: «اتنقل 2 ربط لعملياتهم الحقيقية، واتمسح 2 مستند». **النسخة على الجهاز بعيني:** `masroufy-before-cleanup-2026-09-13T11-46-06-346Z.json` بحجم **3,168 بايت**.
      - **الفحص التاني: 0 كسر**، والسطور اللي فيها رصيد **1912** = عدد سطور كشف البنك بالظبط.
      - **«طابق الرصيد» للراجحي: «مطابقة كاملة: 1912 سطر، صفر فروق.»** ⇒ **معيار القبول التالت (سلسلة الرصيد) اتحقق على بيانات الحساب الحقيقية**، مش على ملف الاختبار بس.
      - **شاشة الأشخاص بعد نقل الربط: نفس الـ5 أرقام ونفس البصمة `eb700498ecf00d5a`** ⇒ ديون الأشخاص ما اتغيرتش.
      - الـ79 الباقيين من غير توأم **حقيقيين** بشهادة الرصيد وبيفضلوا.
    - **الحالة دلوقتي:** بقايا الإكسل اتقفلت بالكامل. الـ16 سطر الزيادة اتحلوا (14 تراجع + 2 تنظيف). النسخ الاحتياطية الأربعة لكل كتابة موجودة في `Android/data/app.masroufy.personal/files/backups` على المحاكي.
    - 🔴 **غلطة مني اتصلحت في النص:** اللوحة كانت بتقول عن الواحدة اللي الرصيد شايفها مكررة من الـ80 «مكررة **بتاريخ مختلف**» — ده كان افتراض مني، والتقرير بيقول إنها **نفس اليوم**. بقت «شكلها مكررة».
- **الترتيب: «الحساب وأسئلة البداية» الأول** (ومعاه أيقونة الإعدادات في الرأس).
- **الصوت:** المالك سأل «منقدرش نستخدم جيميناي مجاني أو حاجة بسيطة كذكاء اصطناعي يفهم سياق الكلام ويحدد الأنواع والمحلات؟» ⇒ **مستني رد مبني على بحث** (باقة Spark، شروط الطبقة المجانية، Gemini Nano على الجهاز) وبعدها قرار منه. **ما يتبنيش قبل كده.**
- **الشعارات: «شعارات حقيقية»** ومسؤوليتها عليه، و**لما يكون فيه نت يسحب شعارات وأسماء تجار جداد ويحدّث قاعدة تجار موحدة بين كل المستخدمين** («كأن الـ10 أشخاص بيجمعوا بيانات»).
  - 🔴 **ده بيعارض OVERRIDES §2 نصًا:** «لا مشاركة ولا تجميع ولا تحليل عبر المستخدمين، إطلاقًا». المالك صاحب القاعدة ويقدر يغيّرها — **بس لازم يأكد صراحة بعد ما يعرف الأثر** (أسماء المحلات اللي كل واحد بيشتري منها هتتكتب في مجموعة مشتركة، ومفيش سيرفر يراجع اللي بيتكتب). **ما يتسجلش في OVERRIDES ولا يتبني قبل تأكيده.**

**✅ قرارات المالك بعد البحث (2026-09-13) — مسجلة في OVERRIDES §24–26:**
- **الصوت: «جيميناي المجاني عبر فايربيز»** بعد ما اتقاله عن استعمال جوجل لمحتوى الطبقة المجانية ⇒ OVERRIDES §24. المكتبة محتاجة سبب في ARCHITECTURE قبل الإضافة.
- **القاعدة المشتركة: «أيوه»** + مراجعة يدوية كل فترة + تأكيد لبعض الحاجات والباقي يتحدث تلقائي + **تغيير مستخدم لتصنيف مؤكد يبقى ليه هو بس** ⇒ OVERRIDES §25. ❓ مفتوح: مين بيراجع وإزاي.
- **الشعارات: «الاتنين مع بعض»** (جوه التطبيق للمشهورين + خدمة أونلاين للجداد) ⇒ OVERRIDES §25.
- **الحساب: «الراتب لكل مستخدم والباقي اختياري»** ⇒ OVERRIDES §26.

**🧭 قراءة الكود لتصميم قسم الحساب (OVERRIDES §26) — لسه ما اتكتبش كود:**
- **يوم الراتب ليه مصدر واحد بس:** `const payday=DEFAULT_PAYDAY` في `src/app/useAppData.ts:21`. كل الشاشات وحالات الاستخدام (`loadHomeScreen`، `loadBudgetScreen`، `loadHomeHistory`، `reconcileBalance`، `exportBackup`، `PeriodPicker`) **بتاخد `payday` كمعامل أصلًا** ⇒ «الراتب لكل مستخدم» = قراءته من ملف المستخدم في `useAppData` وتمريره. الأثر محدود.
- **مفيش كيان «ملف مستخدم» في `domain/`** (الاسم/المرتب/النوع/بيعول حد). محتاج: كيان + منفذ + مستودع Firestore وذاكرة (قاعدة 6) + حالة استخدام، تحت `users/{uid}/profile` (قواعد Firestore الحالية بتغطيها).
- **كلمة السر:** `AuthPort` فيه `sendPasswordReset` بس. الأبسط والأأمن «تغيير كلمة السر» = **إيميل إعادة تعيين** (مفيش كتابة كلمة سر جوه التطبيق، وClaude مش هيحتاج يكتب كلمة سر المالك في التجربة). تغيير الإيميل نفسه محتاج إعادة دخول وتأكيد — ❓ يتسأل عنه لما نوصله.
- المرتب يتخزن **عدد صحيح بالهللة** (قاعدة 1).

**🧱 طبقة التخزين لقسم الحساب — اتكتبت، ⚠️ لسه ما اتوصلتش ولا اتجربت على المحاكي (2026-09-13):**
- `domain/userProfile.ts`: الكيان + `checkProfile` (التحقق قبل الحفظ، والخطأ جنب حقله) + `parseStoredProfile` (قراءة آمنة: حقل ناقص أو غلط = «ما اتجاوبش»، ويوم الراتب 28، فملف متبوظ ما يوقعش فتح التطبيق). **`null` = ما اتجاوبش مش صفر** (قاعدة 10).
- `application/ports/ProfileRepository.ts` + `infrastructure/memory/memoryProfileRepository.ts` + `infrastructure/firestore/firestoreProfileRepository.ts` (مسار `users/{uid}/profile/main`، مغطى بقواعد الحماية الحالية).
- `application/useCases/manageProfile.ts`: `load` (ملف فاضي لو مفيش، من غير كتابة) · `save` (مفيش كتابة لو حقل غلط) · `completeOnboarding` (بيسجل وقت انتهاء أسئلة البداية).
- **اتفحص:** typecheck نضيف و**9 اختبارات** (`userProfile.test.ts`، `manageProfile.test.ts`).
- **ما اتعملش:** التوصيل في `container.ts`/`demoContainer.ts`، قراءة يوم الراتب من الملف في `useAppData.ts` بدل `DEFAULT_PAYDAY`، قسم «الحساب» في الإعدادات، شاشة أسئلة البداية، أيقونة الإعدادات في الرأس، وضم الملف للنسخة الشاملة.
- **الملفات دي مش في أي commit لسه** — عن قصد (قاعدة 13: commit بعد شريحة مكتملة). لو استلمت من غير الملفات دي في الشجرة، ابدأ القسم ده من الأول.
- `manageProfile` **اتوصّل** في `container.ts` (Firestore) و`demoContainer.ts` (ذاكرة).

**🧩 شريحة قسم الحساب — الكود اتكتب (typecheck نضيف و574 اختبار)، ⚠️ لسه ما اتجربش في المتصفح ولا على المحاكي:**
- `AccountPort` + `firebaseAccount` (الإيميل من `auth.currentUser` + `sendPasswordResetEmail`) + `memoryAccount` للاختبار والمعاينة. `toAuthError` بقى مُصدَّر من `FirebaseAuthAdapter` عشان الرسايل العربية نفسها.
- `manageProfile` بقى فيه `email()` و`sendPasswordReset()` — الشاشة ما بتكلمش فايربيز (قاعدة 4).
- `AccountCard.tsx` (جديد، 146 سطر): **أول قسم في الإعدادات** — الإيميل للقراية + «ابعتلي إيميل لتغيير كلمة السر»، الاسم، المرتب (نص ← `tryParseMoney` ← هللة)، يوم الراتب (1–31)، النوع، «بتعول حد؟» — كلهم اختياري ما عدا يوم الراتب، و«مش عايز أحدد» = `null`. الخطأ جنب حقله.
- `useAppData.ts`: يوم الراتب **state** بيتقري من الملف في `prepare()`، و`applyPayday` بيلغي كاش الشاشات والعرض المحفوظ ويحسب الفترة من تاني. `reload` بيقرا الملف الأول، ولو يوم الراتب اتغير بيسيب الفترة الجديدة تتحمّل لوحدها.
- `ShellHeader`: **أيقونة الإعدادات ⚙️** (OVERRIDES §23) — `AppShell` بيبعت `onOpenSettings` (بقى 299 سطر).
- `SettingsScreen` بقى 299 سطر — **على الحد**؛ أي إضافة جاية لازم تطلع في مكوّن.
- **ما اتعملش:** شاشة أسئلة البداية، ضم الملف للنسخة الشاملة، وتغيير الإيميل نفسه (❓ مستني المالك).
- **تجربة على حساب المالك:** الحفظ = كتابة مستند ملف جديد ببياناته، و«ابعتلي إيميل» = إيميل حقيقي لصندوقه ⇒ **الاتنين بموافقته**. الحفظ يتجرب في وضع المعاينة (ذاكرة) الأول.
- **✅ اتجرب في وضع المعاينة (`?demo=1`، بيانات ذاكرة، متصفح Claude):**
  - أيقونة ⚙️ في الرأس بتفتح الإعدادات، و«الحساب» أول قسم (الإيميل الوهمي، زر إيميل كلمة السر، الحقول، يوم الراتب 28 افتراضي).
  - مرتب «abc» ⇒ الخطأ جنب الحقل وما اتحفظش. قيم صحيحة ⇒ «بيانات الحساب اتحفظت.»، والاسم اتقص من المسافات. **فتح الإعدادات تاني قرا من التخزين:** الاسم، **11000.50**، **27**، النوع، «أيوه».
  - **الرئيسية بعد يوم راتب 27: الفترة `2026-08-27 ← 2026-09-26`** ⇒ `applyPayday` بيحسب الفترة من يوم كل مستخدم. مفيش أخطاء كونسول في أي خطوة.
- **✅ على المحاكي بحساب المالك — قراءة بس (APK 1.16 / versionCode 17، SHA256 `18C7182E43596F79A0268149757BED7D6572686B15E0BB4A1AF618F34870B2EE`):** ⚙️ فتحت الإعدادات، «الحساب» أول قسم، فيه إيميل وزر كلمة السر وكل الحقول، **يوم الراتب 28** (مفيش ملف متحفظ لسه)، ومفيش خطأ.
- **⚠️ ما اتجربش على حساب المالك:** الحفظ، وإيميل تغيير كلمة السر — الاتنين للمالك يعملهم بنفسه من التطبيق أو بموافقته.

**✅ ردود المالك على الأسئلة المفتوحة (2026-09-13، مسجلة في OVERRIDES §25–26):**
1. الكاش في أسئلة البداية = **رصيد افتتاح لمحفظة الكاش** (مش عملية وارد).
2. الديون في أسئلة البداية = **تتكتب في شاشة الأشخاص على طول** (شخص + مبلغ + عليه/ليه، أكتر من شخص).
3. **تغيير الإيميل مش مطلوب** — يكفي إيميل تغيير كلمة السر.
4. مراجعة قاعدة التجار المشتركة **من بره التطبيق** (لوحة تحكم فايربيز) — مفيش شاشة مراجعة.
5. **الرفع على GitHub: «ارفع»** — أول محاولة اترفضت لأن GitHub عليه commit أحدث: **«تحديث الأسعار 2026-09-13» بتاع GitHub Actions، وبيغيّر `public/prices.json` بس**. ولا commit محلي بيلمس الملف ده، فاتدمج بـ`git merge` عادي (من غير rebase ولا إعادة كتابة تاريخ) وبعدين الرفع.
6. **الصوت: «تجربة صغيرة الأول»** (OVERRIDES §24) — زرار تجربة مخفي بجملة وهمية يتأكد إن App Check بيقبل الـAPK المتثبت برا المتجر، وإن فهم العامية المصرية والمحلات مقبول، **قبل** بناء شاشة الصوت. لو فشلت: بدائل للمالك (منها جوجل بلاي تجربة داخلية).

**⚠️ قيدين لازم يتراعوا لما يوم الراتب يبقى من الملف (مقروء من `src/app/useAppData.ts`):**
1. **الفترة الأولى بتتحسب قبل ما الملف يتقري:** `useState(()=>periodForDate(today,payday))` بيشتغل أول ما الشاشة تفتح بـ`DEFAULT_PAYDAY`. المكان الطبيعي لقراية الملف هو `prepare()` (نفس البوتستراب اللي بيزرع المحافظ قبل أي شاشة)، و**بعده الفترة تتعاد بيوم الراتب الحقيقي**.
2. **كل اللي متخزن بمفتاح الفترة لازم يتلغي لو يوم الراتب اتغير:** `keyFor` بيستعمل `period.key` (كاش الشاشات في `ScreenRequests`)، و`homeSnapshot.read(period.key)` بيعرض عرض محفوظ — من غيرهم الأرقام هتبان بحدود شهر غلط. نفس الشيء لما المستخدم يغيّر يوم الراتب من قسم الحساب.

**🔎 بحث للقرارات دي (2026-09-13 — مصادر عامة، مش مجرّب في المشروع):**
- **Firebase AI Logic** (جيميناي من التطبيق مباشرة من غير سيرفر): الطبقة المجانية بتشتغل على باقة **Spark** من غير وسيلة دفع. المفتاح بيفضل عند فايربيز مش جوه الـAPK. **App Check إلزامي من 2 نوفمبر 2026** (Play Integrity) ⇒ لازم يتجرب مع APK متوزع **مش من متجر جوجل**. المصدر: firebase.google.com/docs/ai-logic (pricing، get-started).
- **شروط الطبقة المجانية لـGemini API:** المحتوى المبعوث والردود «ممكن يتستعملوا لتحسين منتجات جوجل»، وممكن مراجعين بشريين يشوفوه، وجوجل بتنصح ما يتبعتش بيانات شخصية. الطبقة المدفوعة مش بتستعمله. المصدر: ai.google.dev/gemini-api/docs/billing.
- **Gemini Nano (ML Kit GenAI Prompt API):** بيشتغل **على الجهاز**. الأجهزة المدعومة فيها Pixel 8+ و**Samsung S24+**. الـPrompt API **alpha**، و**ما لقيتش تأكيد إنه بيدعم العربي**. **مش بيشتغل على المحاكي** (محتاج AICore على جهاز حقيقي). المصدر: developers.google.com/ml-kit/genai/prompt/android.
- **الشعارات أونلاين:** Clearbit Logo API **اتقفلت 8 ديسمبر 2025**. البدائل (Brandfetch — طبقة مجانية كبيرة، logo.dev) **محتاجة مفتاح جوه التطبيق** وكل طلب بيبعت دومين المحل للخدمة. المصدر: docs.brandfetch.com/migrations/migrate-from-clearbit-logo-api.

**❓ نقاط لازم تتسأل قبل التنفيذ (ما اتحسمتش):**
- الصوت: OVERRIDES §2 بيمنع رفع البيانات لأي خدمة غير فايربيز — التعرف على الكلام بيتعمل فين؟
- الشعارات: مصدرها وحقوقها، ومتخزنة جوه التطبيق ولا بتتنزل؟
- موعد الراتب: دلوقتي **رقم ثابت 28** (OVERRIDES §6) — يبقى إعداد لكل مستخدم؟ وده بيغيّر حدود الفترات لكل الشاشات.
- النوع ومين بيعول: إجباري ولا اختياري في أسئلة البداية؟
- ترتيب التنفيذ بين الطلبات دي وبين البند المفتوح (دفعة 09-10 المكررة).

### 🔎 اكتشاف: تاريخ افتتاح محفظة الراجحي في الحساب مختلف عن المعتمد
- شاشة الإعدادات على المحاكي بتعرض تاريخ افتتاح الراجحي **2026-09-07**، والمعتمد في CLAUDE.md وOVERRIDES §6 **2025-01-01**. (ومحفظة الكاش كمان تاريخها 2026-09-07 والمعتمد 2026-09-06.)
- **الأثر:** «طابق الرصيد» بيبدأ من تاريخ الافتتاح وبيتخطى اللي قبله، والكشف بيخلص 2026-09-04 ⇒ **المطابقة على الحساب دلوقتي ما بتقارنش ولا سطر**. يعني معيار «سلسلة الرصيد» ما اتجربش على بيانات الحساب الحقيقية.
- **ما اتعدلش** — ده كتابة على بيانات المالك. سؤال مفتوح تحت.

### ❓ أسئلة مستنية المالك
0. **تاريخ افتتاح الراجحي** في الحساب 2026-09-07 والمعتمد 2025-01-01 (والكاش 2026-09-07 والمعتمد 2026-09-06) — نصلّحهم للمعتمد؟ ولا فيه سبب؟ (المبالغ نفسها ما اتقارنتش هنا عشان الريبو علني.)
1. **جوال القفل فيه متشغّل وبعدين صاحبه شال قفل الشاشة خالص:** الكود مبدئيًا بيوقف القفل ويقول ده (بدل ما يقفل المستخدم بره التطبيق). موافق؟
2. رفع الـcommits على GitHub (آخر مرفوع d497c46).

## 38. نقطة الاستكمال الحالية — 2026-09-16 (Codex)

### نطاق المالك والتسليم الموحد
- هدف العمل: استكمال إصلاحات وميزات التطبيق الحالي، ومنها المشاريع، مع الاختبار وتسليم APK محدث؛ **التوقف قبل تنفيذ الانتقال إلى Native**. لا مشروع Kotlin ولا صوت ضمن الهدف الحالي. لم يُنفّذ أي انتقال.
- تأكيد المالك: **HANDOVER.md هو ملف التسليم الوحيد، كل النماذج تكتب فيه**؛ لا ملف تسليم موازٍ. تحديث شغل كلود غير المحفوظ الموجود عند الاستلام حُفظ كما هو دون استبدال أو حذف.
- آخر commit عند الاستلام `31ce2c5` (1.40)، وملف `dist-android-apk/masroufy-trial-v41.apk` يحمل فعلًا versionName 1.40 وversionCode 41. **لا يحتوي إصلاح الخصوصية أدناه**؛ لم يُبنَ APK جديد لهذه الخطوة بعد.
- لا رفع GitHub ولا نشر ولا تعديل لحساب المالك في هذه الجولة. الرفع ما زال يحتاج تفويضًا ساريًا؛ الرد المسجل الأحدث كان «استنى».

### مكتمل: إخفاء أرقام الحسابات العربية والفارسية
- العطل: `sanitizeAccountNumbers` كان يخفي الأرقام الغربية فقط؛ `حساب ١٢٣٤٥٦٧٨٩٠` كان يمر دون إخفاء.
- `src/infrastructure/firestore/firestoreRepositories.ts`: إخفاء تتابعات الأرقام الغربية والعربية والفارسية والمختلطة مع إبقاء آخر أربع خانات وشكل الأرقام القصيرة والتواريخ كما هو.
- `src/infrastructure/import/bankSmsParser.ts`: `redactSms` توحّد الأرقام قبل التنقيح حتى للمستدعين المباشرين، مثل تصدير النسخة الاحتياطية، الذين لا يمرون بمحلل الرسالة أولًا. اختبار مبلغ عربي كبير يؤكد أنه ما زال يُقرأ بالهللة الصحيحة.
- أضيفت 8 حالات في `privacy.test.ts` و`smsInbox.test.ts`: العربية والفارسية والمختلطة وIBAN، بقاء الأرقام القصيرة والتواريخ، بقاء مبلغ 15000.50، وعدم تغيير النتيجة عند إعادة التنقيح.
- حدود الإصلاح: لم تُعد كتابة المستندات القديمة أو فحصها على الحساب الحقيقي. سياسة قص التتابعات الطويلة في نصوص Firestore ما زالت محافظة كما في الاختبارات السابقة؛ لا ندّعي فهم كل صيغة ممكنة لرقم حساب مفصول. أرقام المبالغ المخزنة كقيم عددية لا يغيرها التنقيح.

### التحقق الفعلي
- `npm run typecheck`: ناجح.
- أول `npm test -- --reporter=dot`: 700 نجاح و1 timeout في اختبار تصدير/استعادة مستودعات المعاينة، مهلة 5 ثوانٍ. لا يُخفى هذا الفشل ولا تُغيّر مهلة الاختبار لإظهاره ناجحًا.
- إعادة ملف النسخة الاحتياطية مع ملفّي التعديل بعامل واحد: **32/32 نجاح**؛ اختبار المعاينة استغرق نحو ثانية.
- إعادة المجموعة كاملة بعامل واحد: `npm test -- --reporter=dot --maxWorkers=1 --minWorkers=1` ⇒ **701/701 نجاح، 79 ملفًا، 61.98 ثانية**. اختبار النسخة نفسه نحو 565ms. اختلاف الزمن يرجّح تنافس موارد أثناء التشغيل المتوازي، وليس دليلًا قاطعًا على سببه.
- لم يُجرَ في هذه الخطوة اختبار Firestore Emulator أو محاكي Android أو بناء APK. لا تعتبر اختبارات المتصفح/الذاكرة إثباتًا لتجربة الهاتف.

### خطة الاستكمال — لا تعتبرها منجزة
1. إصلاح التجهيز الأولي ليُستأنف بعد انقطاع حفظ التصنيفات والقواعد والتجار، دون استرجاع ما حذفه المستخدم عمدًا. يلزم حالة تجهيز مستمرة صريحة واختبارات انقطاع، لا اعتبار وجود تصنيف واحد اكتمالًا.
2. حماية التسوية المتزامنة من تجاوز أصل الدين بأمر حفظ ذري ومفتاح منع إعادة الطلب، مع اختبار تخزين حقيقي معزول؛ منع الزر وحده غير كافٍ. كذلك إصلاح إعادة إنشاء دين افتتاحي تمت تسويته عند إعادة تنفيذ الإعداد.
3. تقوية تحقق aliases في قواعد دليل التجار واختبارات المحاكي، ومعالجة وصول التأكيد الإداري بالمزامنة. لا نشر للقواعد تلقائيًا.
4. إغلاق اختبارات التصنيفات الجديدة، وحفظ/استعادة نسخة شاملة على حساب تجريبي، وتعديل المبلغ والكاش والوضع الغامق؛ ثم بناء واختبار APK بالتوقيع الحالي وعلى المحاكي حسب §27.
5. تنفيذ المشاريع في التطبيق الحالي وفق OVERRIDES §33: ربط إضافي للعمليات دون مضاعفة المجاميع، دخل ومصروف للمشروع، وقواعد تلقائية. لم تُنفَّذ بعد عند هذه النقطة.
6. معالجة الأداء وفق قياس APK، ثم تسليم النسخة وتوثيق النواقص التي تحتاج جهاز المالك. **التوقف عند حد الانتقال إلى Native**.

ملاحظة لتحديث المراجعة السابقة: ملف الحساب أُضيف بالفعل إلى النسخة الاحتياطية بواسطة كلود، وإصلاح حفظ الملف الكبير موثق أعلاه؛ لا تُعِد تنفيذ هذين الإصلاحين من التقرير القديم. المطلوب التحقق النهائي من الحفظ والاستعادة. أسئلة وأرقام الأقسام التاريخية لا تعكس بالضرورة الوضع الحالي.

### تحديث الاستكمال: التجهيز الأولي قابل للاستئناف — 2026-09-16
- **المنجز:** بند 1 من خطة §38 اتنفّذ للحسابات التي تبدأ التجهيز بهذا الإصدار. `ReferenceSeedPort` يفصل تتبع التجهيز عن المراجع القابلة للتحرير. حالة دائمة في `users/{uid}/initialization/references-v1` تبدأ `pending` **قبل أي حفظ** وتصبح `complete` بعد اكتمال المجموعات الثلاث.
- التنفيذ الحقيقي `infrastructure/firestore/referenceSeed.ts` يضيف المستندات الغائبة فقط، بصفحات 100 مستند ومعاملة لكل صفحة تقرأ العلامة ووجود المستندات قبل الكتابة. لو عميل آخر أنهى التجهيز تتوقف الصفحات المتبقية؛ لا يُعاد ما حذفه المستخدم بعد الاكتمال. لا ادعاء بأن الزرع كله معاملة واحدة.
- حساب قديم بلا علامة وفيه تصنيفات يُعتمد كمكتمل دون ملء النواقص تلقائيًا، حفاظًا على الحذف المتعمد. **الانقطاعات القديمة قبل هذا الإصلاح غير قابلة للتمييز تلقائيًا عن الحذف؛ تستخدم أداة استعادة المراجع الحالية بمعاينة.** أول إنشاء للعلامة واستئناف التجهيز يحتاجان اتصالًا لأن التنفيذ يستخدم معاملات؛ بعد الاكتمال يُستخدم الكاش عند غياب الشبكة.
- التنفيذ بالذاكرة متصل بنفس حالة الاستخدام، ويجب الاحتفاظ بنسخته عبر إعادة المحاولة. حاويتا الإنتاج والمعاينة اتحدثتا؛ ملف الحاوية الرئيسي 299 سطرًا.
- **اختبارات:** 7 حالات جديدة `seedRecovery.test.ts` للانقطاع عند التصنيفات والقواعد والتجار وعلامة الاكتمال، الحفاظ على تعديل المستخدم، عدم استرجاع المحذوف، واعتماد حساب قديم دون تخمين.
- `npm run typecheck` نجح بعد تصحيح بيانات التصنيف الوهمية في الاختبار. الفحص الشامل بعامل واحد: **708 اختبارات نجاح و5 متخطاة عمدًا** (اختبارات المحاكي، لم يكن المتغير مفعّلًا)، 80 ملفًا ناجحًا وملف تكامل متخطى، 63.04 ثانية. أُضيف بعده اختبار تكامل سادس لغياب الشبكة وشُغلت مجموعة التكامل كاملة منفصلة.
- **Firestore Emulator حقيقي، مشروع demo فقط:** `tests/integration/referenceSeed.test.ts` ⇒ **6/6 نجاح**: الاستئناف من عميل مستقل بعد التصنيفات، الحفاظ على تعديلها، حذف كل التصنيفات وقاعدة ثم إعادة الفتح دون إرجاعها، عميلان متزامنان مع 205 تجار (3 صفحات)، حساب قديم، علامة تالفة مرفوضة، وإعادة فتح من الكاش بعد `disableNetwork`. الحالات متجمعة في ستة اختبارات.
- إعادة اختبار التكامل: شغّل محاكي Firestore بقواعد المشروع وprojectId `demo-masroufy-audit` على `127.0.0.1:8088`، ثم PowerShell: `$env:MASROUFY_TEST_FIRESTORE='127.0.0.1:8088'; npm.cmd test -- tests/integration/referenceSeed.test.ts --maxWorkers=1 --minWorkers=1`. الاختبارات لا تتصل بأي مشروع حقيقي؛ متغير مختلف لا يفعّلها. يلزم Node في PATH وJava 21 للمحاكي. أوقف المحاكي بعد الاختبار.
- **حدود التحقق:** لا نشر ولا تعديل لحساب المالك، لا APK جديد ولا اختبار Android لهذه الخطوة. اختبار إغلاق العملية وإعادة فتح التطبيق دون نت على الهاتف ما زال ضمن اختبار APK النهائي؛ اختبار تعطيل الشبكة هنا على SDK والمحاكي فقط.
- **التالي:** بند 2 (التسويات المتزامنة وإعادة تنفيذ الإعداد)، ثم قواعد الدليل والمزامنة والمشاريع واختبارات APK. لا بدء للنيتف، وكل التسليم هنا في الملف نفسه.

### تحديث الاستكمال: منع إعادة تنفيذ أسئلة مكتملة — 2026-09-16
- إصلاح التجهيز السابق محفوظ في commit `c2d65ec`. محاكي Firestore الخاص بالاختبار أُوقف بعد انتهاء اختبارات الجولة.
- `onboardAccount.finish` يقرأ حالة الحساب المحفوظة قبل أي كتابة. إذا كانت أسئلة البداية مكتملة يعيد النجاح دون إعادة تغيير الملف أو الكاش أو الديون. هذا يحمي من إعادة إرسال شاشة قديمة بعد إكمال الإعداد، حتى لو كان الدين الافتتاحي سُدّد بالكامل ولم يعد يظهر في الالتزامات المفتوحة.
- اختباران جديدان: إعادة نفس المدخلات بعد سداد الدين لا تنشئ دينًا جديدًا؛ إعادة الإعداد بعد تعديل اسم الحساب ويوم الراتب والكاش لا تكتب فوق تلك التعديلات.
- **الفحص:** typecheck ناجح؛ اختبارات onboarding/manageProfile/people: **28/28 نجاح**. لم تُعد المجموعة كاملة بعد هذا التعديل الصغير، وآخر فحص شامل هو المذكور أعلاه. لا APK أو نشر لهذه الخطوة.
- **حدود مهمة:** هذا إصلاح لإعادة التنفيذ بعد الاكتمال؛ **ليس قفلًا ذريًا لإعداد متزامن من جهازين**، ولا يحل التسويات المتزامنة أو كل سيناريوهات انقطاع أسئلة البداية. بند التسويات الذرية ما زال الأولوية التالية، مع ضرورة تضمين هوية ثابتة للطلبات عند توسيع ضمانات إعداد الديون.
- **نقطة الاستكمال التالية:** تسويات الديون على Firestore (قراءة وفحص وحفظ ذري + منع إعادة الطلب) مع اختبارات عميلين؛ ثم حماية الدليل والمزامنة، المشاريع، واختبار وتسليم APK. لا تعتبر القائمة مكتملة ولا تبدأ Native.

### تحديث الاستكمال: التسويات الذرية — راجعها وكمّلها كلود — 2026-09-18
- **رد المالك (نافذة):** «راجعه وكمّله إنت» على شغل Codex اللي ما اتسجلش (بند 2 من خطة §38). المراجعة لقته كامل ومتسق، **فاتسجل زي ما هو من غير تغيير سطر**.
- **الفكرة:** مستند عدّاد `users/{uid}/concurrency/settlements`. `firestoreSettlementWriter` بيقرا العداد وتسويات الالتزام من السيرفر، وجوه معاملة بيقرا العداد والالتزام ومستند الطلب؛ لو العداد اتغير بيعيد (8 مرات بحد أقصى). `domain/settlementCommand.ts` (`prepareSettlement`) بيرفض الشخص الغلط والمبلغ غير الصحيح وأي زيادة عن المتبقي. **معرّف الطلب الثابت** `stl-<الالتزام>-<الطلب>` بيخلّي إعادة نفس الطلب أثرها مرة واحدة، ونفس الطلب ببيانات مختلفة مرفوض. كل كاتب تعاوني بيزوّد العداد: الحفظ والحذف الجماعي في `peopleRepositories` (الصفحة 499 مستند عشان مكان العداد)، والاستعادة الشاملة (`fullBackupRepository`). `PeopleScreen` و`SettleWithTransactionSheet` بيولّدوا `requestId` مرة لكل فورم. نسخة الذاكرة بتصفّ الطلبات ورا بعض.
- **اتفحص:** typecheck نضيف؛ `npm test` ⇒ **719 نجاح و12 متخطاة** (اختبارات المحاكي من غير المتغير)، 81 ملف. **Firestore Emulator حقيقي (مشروع demo، `127.0.0.1:8088`، ملف إعداد مؤقت برا المشروع):** `tests/integration` كلها **12/12**، منهم 6 للتسويات: جهازين ما يقدروش يسوّوا 140 على دين 100؛ جهازين كل واحد نص من غير ما تضيع دفعة؛ نفس الطلب من جهازين ثم عميل بعد إعادة تشغيل = أثر واحد، وبيانات مختلفة مرفوضة؛ التسويات القديمة محسوبة والحذف بيزوّد العداد؛ الشخص الغلط والدين المش موجود مرفوضين من غير حفظ. المحاكي اتقفل بعدها.
- **حدود معروفة:** أدوات الإصلاح العامة (`removeDocs` في تنظيف البقايا و`idRepair` في إصلاح المعرّفات) بتكتب في التسويات من غير ما تزوّد العداد. الحذف منها بيخلّي الفحص أحفظ مش أوسع، لكن تعديل `obligationId` بأداة إصلاح في نفس لحظة تسوية من جهاز تاني ممكن نظريًا ما يتشافش — نادر جدًا. **ما اتجربش على الجوال ولا على حساب المالك** (كتابة).

### ❗ بلاغ من المالك 2026-09-18: ولا رسالة بنك دخلت
- نص المالك: «جربت موضوع الرسايل على الجوال و ولا رسالة رضيت تخش أو تتفهم». رده في نافذة: السبب المكتوب تحت الرسايل **«مبلغ العملية غير واضح أو يوجد أكثر من مبلغ عملية»** ⇒ الرسايل **وصلت** للتطبيق (فلتر المرسل شغال) والمحلل هو اللي رفضها.
- **السبب المرجح من الكود:** `bankSmsParser.ts` مبني على رسايل مخترعة في الاختبارات (`بمبلغ: 29.99 SAR`، `في: 2026-09-09`) مش رسايل حقيقية: بيشترط كلمة «مبلغ/بمبلغ/المبلغ/amount» قبل الرقم، وبيشترط سنة بأربع أرقام في التاريخ (رسايل الراجحي غالبًا بسنة برقمين).
- **الخطوة:** المالك هيبعت نص رسالة حقيقية في الشات (يقدر يغيّر الأرقام واسم المحل). **ما يتكتبش نصه في المشروع** (المستودع عام) — الاختبار بيتعمل برسالة بنفس الشكل وأرقام وهمية.

### 🔴 زرار الرجوع ما كانش بيقفل «الاشتراكات والفواتير» و«مراجعة العمليات القديمة» — اتصلح 2026-09-18
- اتشاف على المحاكي (1.40، حساب المالك، قراية بس): دوسة بإحداثيات قبل ما شاشة «المزيد» تثبت فتحت «الاشتراكات والفواتير» بدل التصنيفات (الشاشة دي بتقرا بس)، و«رجوع» **ما قفلهاش**. السبب: `useAndroidBack` بيدوّر على `button[aria-label="إغلاق"]`، والشاشتين دول زرار الإغلاق فيهم كلام بس ⇒ القرار كان «تجاهل».
- التصليح: العلامة اتضافت للشاشتين، والرجوع بقى يدوّر كمان على زرار كلامه «إغلاق». اختبار جديد في `androidBack.test.ts` بيفحص كل ملف فيه `role="dialog"` إن فيه `aria-label="إغلاق"` (إلا القفل وأسئلة البداية) — كان هيمسك الشاشتين.
- ⚠️ لسه ما اتجربش على المحاكي — هيتجرب مع نسخة تصليح الرسايل. **درس:** بعد تغيير التبويب استنى وخد صورة قبل الدوسة الجاية.
- **التصليح الأول (قبل ما توصل رسالة المالك):** `bankSmsParser.ts` — **المبلغ** = الرقم اللي جنبه عملة الريال (قبله أو بعده، `SAR`/`SR`/`ر.س`/`ريال`) في سطر مش رصيد ولا متاح ولا حد ولا رسوم ولا عمولة؛ لازم قيمة واحدة (أكتر من قيمة ⇒ رفض). **التاريخ** بسنة برقمين (`26-9-16` أو `16/9/26`) بيتحدد بتاريخ وصول الرسالة: لازم احتمال واحد بس بين 60 يوم قبل الوصول ويوم بعده، وإلا رفض. التاجر من «لدى/عند» أو سطر «من:/إلى:». كلمات الاتجاه زادت (سحب، خصم، سداد، مدفوعات، راتب، استرداد، مرتجع). كل رسالة لسه محتاجة تأكيد المستخدم.
- **الرسايل اللي على جوال المالك بتتقرا تاني بالمحلل الجديد** مع كل تحديث للقايمة (`manageSmsInbox.prepare` بيحلل وقت العرض) — مش محتاج يستقبلها تاني.
- **اتفحص:** typecheck نضيف؛ `bankSms` + `smsInbox` + `privacy` = **43/43** (7 حالات جديدة بأشكال رسايل بنوك **وأرقام وأسماء وهمية**: العملة قبل الرقم مع سنة برقمين، «بـ» من غير «مبلغ» مع يوم أول، تجاهل الرصيد والرسوم، حوالة واردة، ورفض مبلغين مختلفين/رصيد بس/تاريخ بعيد).
- ⚠️ **ما اتأكدش على رسالة المالك الحقيقية لسه** — مستنيها.
- **✅ APK 1.41 (versionCode 42):** `dist-android-apk/masroufy-trial-v42.apk`، SHA256 `8CCA8454011BC455E9208F7B33EEA22253F401E459F27B9B7800FD1933F46168`، 5,262,183 بايت، **على الدرايف مطابق**. فيه: قارئ رسايل البنك الجديد + زرار الرجوع للشاشتين + التسويات الذرية. `npm test` قبله: **726 نجاح و12 متخطاة**.
- ⚠️ **ما اتجربش على المحاكي:** المحاكي كان مقفول يومين واتشغّل من جديد، وبعد إعادة التشغيل بقى طالب **رمز الجهاز (PIN)** — ده رمز دخول و**Claude ما بيكتبش رموز دخول** (قاعدة ثابتة). رسالة تجربة وهمية («Purchase amount SAR 12.50 at TEST SHOP on 16/9/26») اتبعتت للمحاكي بـ`adb emu sms send AlRajhiBank ...` (قبلها `OK`). **المالك يفتح المحاكي بنفسه لو عايز تجربته، أو يجرّب 1.41 على جواله على طول** (ده الإثبات الحقيقي).
- **✅ رسالة المالك الحقيقية وصلت (صورة من تطبيق الرسايل + صورة الخطأ، 2026-09-18) — الشكل (من غير أرقامه):** سطر «شراء PoS» أو «شراء انترنت»، سطر الكارت «عبر…;مدى-سامسونج باي»، **المبلغ «بـSR 24»** (حرف «بـ» لازق في SR ومن غير «مبلغ»)، **المحل في سطر «لـاسم المحل»**، و**التاريخ بسنة برقمين وترتيبه بيختلف**: «26/9/18 09:35» (سنة/شهر/يوم) في الشراء، و«16:47 17/9/26» (الساعة الأول، يوم/شهر/سنة) في رسالة تانية. شراء الإنترنت بالدولار مكتوب فيه «مبلغ:USD …» من غير المبلغ بالريال. صورة الخطأ كانت من النسخة القديمة (السبب بالنص القديم «…أو يوجد أكثر من مبلغ عملية»).
- **التصليح التاني (1.42):** سطر بيبدأ بـ«لـ» = اسم المحل؛ «من:8351» وأي قيمة أرقام بس (حساب) **مش** تاجر؛ علامات الاتجاه المخفية (`U+200E/200F/202A–202E/2066–2069/061C`) بتتشال قبل التحليل. 3 اختبارات جديدة **بنفس الشكل وأرقام وأسماء وهمية** (`bankSms.test.ts`)، ورسالة الدولار مرفوضة بسبب واضح (المبلغ بالريال مش فيها — هتيجي من الكشف). **ما يتكتبش في المشروع أي رقم كارت أو حساب أو اسم محل من صور المالك.**
- **✅ APK 1.42 (versionCode 43):** `dist-android-apk/masroufy-trial-v43.apk`، SHA256 `E5552C1E7D2C5A316ED80F753BD4DE3B2DF1887694B6E2E525090CF13049DF96`، 5,262,283 بايت، **على الدرايف مطابق**. typecheck نضيف، **729 نجاح و12 متخطاة**. ⚠️ **ما اتجربش على جهاز** — المحاكي طالب PIN، والمالك هيجرب على جواله.
- ❓ **لسه مش معروف:** السطر الأول لرسالة «لـDrahim» (محفظة) اتقص في الصورة — لو نوعها مش شراء (تحويل/شحن محفظة) ممكن تترفض بـ«اتجاه الحركة غير واضح». **المالك يقول السبب لو ظهر.**

### 🧩 المشاريع — الشريحة (1): الحسابات والتخزين والنسخة الشاملة (2026-09-18، Claude)
- **قرارات المالك (نافذة، OVERRIDES §34):** «صرفت» = **نصيبي أنا بس** (زي الرئيسية)؛ العملية ممكن تبقى في أكتر من مشروع؛ القاعدة التلقائية **بتسأل عن القديم** وقت عملها والجديد بيتضاف لوحده. قرارات تنفيذ مني: الشيل = «مستبعدة» عشان قاعدة ما ترجعهاش، أرشفة مش مسح، الاسم ≤60 وما يتكررش.
- `domain/entities/projectEntities.ts` + `domain/projects.ts` (دوال نقية: الاسم والقاعدة، `projectLinkId` ثابت من المشروع والعملية، `ruleCandidates`/`planSyncLinks` — القاعدة بتاخد اللي **اتسجل بعدها** بس، `syncStart`، `membershipChanges`، `summarizeProject` = `computePeriodTotals` على `withEstimatedKinds`: صرفت = المصروف الشخصي + المستبعد من الميزانية، جالك = الدخل، و«تقريبي» بعدد غير المؤكد). `categorize.ts` بقى بيصدّر `matchesText` (نفس مطابقة قواعد التصنيف).
- `application/useCases/manageProjects.ts`: `list` (بتراجع القواعد الأول)، `create`/`rename`/`setArchived`، `detail`، `membership`/`setMember`، `addRule` (بترجّع عدد القديم)، `applyRuleToOld`، `setRuleEnabled`. «القديم» = آخر 3 سنين (قراية واحدة بطلب المستخدم). مراجعة الجديد بمؤشر على الجهاز (`localSyncCursor(uid,'project-rules')`) و`TransactionRepository.listCreatedAfter` (حقل واحد بمدى — فهرس تلقائي).
- التخزين: `projects` و`projectLinks` و`projectRules` تحت `users/{uid}` ⇒ **القواعد الحالية بتغطيهم، مفيش نشر**. الذاكرة للاختبار والمعاينة. التوصيل في `app/projectsWiring.ts` (`container.ts` بقى 296 سطر بعد لمّ `manageAssets` في سطر).
- **النسخة الشاملة: 24 مجموعة.** نسخة أقدم من المشاريع (من غير المجموعات التلاتة) **بتتقري فاضية**: البصمة بتتأكد على الأصل، وبعدها النسخة المكمّلة بتتبصم تاني عشان التطبيق بعد المعاينة. تنظيف البقايا بيعرف روابط المشاريع، والدمج بيمنع ربطين لنفس العملية في نفس المشروع.
- **اتفحص:** typecheck نضيف، **738 نجاح و12 متخطاة** (8 للمشاريع + اختبار نسخة قديمة وبصمتها + ربط المشاريع بيتنقل للعملية الموجودة في الدمج).
- **لسه:** الشاشات (المزيد ← المشاريع، «ضيف لمشروع» في النقط التلاتة، القواعد وسؤال القديم) ثم نسخة وتجربة.

### 🧩 المشاريع — الشريحة (2): الشاشات (2026-09-18، Claude)
- **«المزيد» ← «المشاريع»** (`ProjectsScreen.tsx` + css): شرح قصير، «اسم مشروع جديد» + «ضيف»، كروت المشاريع (الاسم، عدد العمليات، «تقريبي» لو فيه نوع مش مؤكد، −صرفت و+جالك)، والمؤرشفة في قسم مقفول. الكارت بيفتح `ProjectDetailView.tsx`: «كل المشاريع» للرجوع، تغيير الاسم، 3 خانات (صرفت أحمر، جالك أخضر، العمليات)، شرح إن «صرفت» نصيبك بس، القواعد التلقائية (اقفل/افتح + «ضيف قاعدة»)، والعمليات بـ`TransactionRow` (النقط التلاتة جنبها = «تشيلها من المشروع؟» والعملية نفسها ما بتتمسحش)، وأرشفة/رجوع.
- **النقط التلاتة ← «ضيف لمشروع»** (`AddToProjectSheet.tsx`): العملية ومبلغها، علامات صح على المشاريع (الإضافة/الشيل فوري)، «أو مشروع جديد» + «اعمل وضيف»، و**«ضيفها أوتوماتيك كل مرة»** بنص العملية جاهز (قابل للتعديل) واتجاهها ⇒ `ProjectRuleForm.tsx` ⇒ بعد الحفظ **سؤال بعدد القديم**: «ضيفهم» / «لأ، الجديدة بس».
- التوصيل: `MoreScreen` (سطر «المشاريع»)، `AppShell` (298 سطر)، `ShellOverlays` (`projects`)، `TransactionMenu` (`project`)، `TransactionActions`.
- **✅ المعاينة (`?demo=1`، 2026-09-18):** «المشاريع» ظهرت في المزيد ⇒ مشروع «ماكت الكلية» اتعمل واتفتح (0.00/0.00/0). عمليتين وهميتين «LIBRARY TEST» (45 و30) ⇒ النقط التلاتة فيها «ضيف لمشروع» ⇒ علامة على المشروع ⇒ «اتضافت لـ«ماكت الكلية».» وظهر «ضيفها أوتوماتيك كل مرة» بالنص جاهز ⇒ «احفظ القاعدة» ⇒ **«فيه 1 عملية قديمة بتنطبق عليها القاعدة دي. تضيفها للمشروع؟»** ⇒ «ضيفهم» ⇒ «اتضاف 1 عملية قديمة، والجديدة هتتضاف لوحدها.» ⇒ الكارت «2 عملية · −75.00» والتفاصيل: صرفت 75.00، جالك 0.00، العمليات 2 («1 بقاعدة»)، والقاعدة «يحتوي «LIBRARY TEST» · الصرف بس».
  - ⚠️ لوحة المعاينة كانت ورا شباك تاني فالرسم كان بيقف (لقطات بتفشل، والدوسات بالإحداثيات بتقع غلط لأن انيميشن الفتح واقف) ⇒ اتجرب بقراية الشاشة والدوس بالسكريبت على الأزرار نفسها. **ده في المعاينة بس** — على المحاكي والجوال النوافذ بتتفتح عادي.
- **اتفحص:** typecheck نضيف، **738 نجاح و12 متخطاة**.
- **✅ APK 1.43 (versionCode 44):** `dist-android-apk/masroufy-trial-v44.apk`، SHA256 `55BE321B30674A3F5FAC919326F3464611DF61F767E9F9771119664D752817D4`، 5,270,911 بايت، **على الدرايف مطابق**. ⚠️ **ما اتجربش على جهاز** (المحاكي طالب PIN).
- **لسه في المشاريع:** ما فيش «مسح قاعدة» (بتتقفل بس، زي قواعد التصنيف)، وما فيش علامة المشروع على صف العملية في شاشة العمليات. ممكن بعدين لو المالك طلب.

### 🧭 خطة النقل لكوتلن — اتكتبت (2026-09-18، Claude) — مسودة، مفيش بناء ولا دفع
- **`KOTLIN_PLAN.md`** (المرجع للنماذج اللي بعدي) + **صفحة خاصة للمالك:** https://claude.ai/artifact/S3LZmJuzDmVbttQmfgGJpv (مفيهاش مبالغ ولا أسماء تجار).
- الأساس: **نفس فايربيز ونفس البيانات** ⇒ مفيش ترحيل. الحسابات تتنقل لـKotlin نقي والـ738 اختبار **مرجع بملفات golden** (نفس المدخلات ⇒ نفس النتايج بالهللة). شريحة شريحة في فولدر `native-android/` واسم تطبيق مختلف (`app.masroufy.native`) جنب القديم. **بعد الرئيسية: قياس وقت الفتح والرجوع قدام التطبيق الحالي — لو مش أسرع بوضوح نقف ونراجع.**
- ⚠️ **خطر لازم يتحقق قبل قرار النشر:** صلاحيات `READ_SMS`/`RECEIVE_SMS` مقيدة في جوجل بلاي (نموذج إعلان وموافقة، ممكن رفض). البدايل مكتوبة في الخطة §7.
- ❓ **4 قرارات للمالك** (§8 في الخطة): التخزين على الموبايل (كاش فايربيز الأصلي مقترح / Room)، الجديد جنب القديم (مقترح) / مكانه، أدوات الإصلاح القديمة تتنقل ولا لأ، البدء دلوقتي ولا بعد تجربته لـ1.43.
- **✅ ردود المالك على الأربعة (نافذة 2026-09-19، OVERRIDES §35):** التخزين «**قاعدة منفصلة ومزامنة**» (مش المقترح) ⇒ Room + مزامنة، والتصميم على الورق في `KOTLIN_PLAN.md` §2.1 (فايربيز الحكم، `outbox` بمعرّفات ثابتة، العمليات اللي ليها شرط محتاجة نت، زرار مزامنة كاملة، اختبارات بعميلين وقطع نت قبل أي شاشة بتكتب)؛ الجديد جنب القديم؛ أدوات الإصلاح تفضل في القديم؛ **البناء بعد ما يجرب 1.43**. الصفحة اتحدثت على نفس الرابط.
- **لحد ما يرد عن 1.43:** مفيش كوتلن. الشغل: تصليحات 1.43 لو ظهرت + تقوية التطبيق الحالي (بنود Codex في §38: تحقق aliases في قواعد التجار المشتركين، والنسخة الشاملة على Firestore Emulator).

### 🧩 تقوية قاعدة التجار المشتركة — بند Codex §38-3 (2026-09-19، Claude)
- **عطل اتقفل:** المزامنة كانت بتقرا اللي `updatedAt` بتاعه اتغير بس ⇒ **تأكيد من لوحة فايربيز من غير ما `updatedAt` يتغير ما كانش بيوصل للحسابات أبدًا**. دلوقتي `sync` بيقرا **كل المؤكدين مرة في اليوم** (`listConfirmed`: `where('confirmed','==',true)`، حقل واحد) بمؤشر تاني على الجهاز (`shared-merchants-confirmed`)؛ أول مزامنة بتقرا كله أصلًا فمفيش قراية زيادة. الـuse case بقى بياخد `clock` و`confirmedCursor`.
- **قواعد Firestore (مش منشورة):** كل اسم بديل في `aliases` لازم نص من 1 لـ80 حرف وحتى 10 (القواعد مفيهاش لفّ ⇒ 10 فحوصات بالخانة). **النشر محتاج موافقة المالك** — القواعد المنشورة لسه القديمة (بتقبل أي حاجة جوه القايمة).
- **اتفحص:** typecheck نضيف؛ `npm test` ⇒ **739 نجاح و19 متخطاة** (اختبار جديد: تأكيد من اللوحة بنفس الوقت بيوصل بعد يوم ومن غير قراية زيادة قبلها). **Firestore Emulator بالقواعد الجديدة:** `tests/integration` كلها **19/19** — منهم 7 جداد (`sharedMerchantsRules.test.ts`): 10 أسماء نصية مقبولة، ورفض رقم/اسم فاضي/أطول من 80/11 اسم/قيمة غلط بعد أسماء سليمة، ولسه `confirmed: true` من التطبيق مرفوض. المحاكي اتقفل بعدها.
- ❓ **مستني المالك:** نشر القواعد (`npx firebase deploy --only firestore:rules`) + الرفع على GitHub.

### 🧩 النسخة الشاملة على Firestore حقيقي — بند Codex §38-4 (2026-09-19، Claude)
- `tests/integration/fullBackupRoundTrip.test.ts` (Firestore Emulator، مشروع demo، **حساب تجريبي مش حساب المالك**): نسخة فيها **كل المجموعات الـ24** و205 دفعة استيراد (عشان قراية السيرفر بصفحات 200) ⇒ استعادة في حساب فاضي ⇒ **القراية من السيرفر بترجّع نفس الصفوف بالظبط** في كل مجموعة ⇒ الاستعادة التانية بتضيف 0 ⇒ نسخة جديدة من الحساب المستعاد بنفس الأعداد. ونسخة أقدم من المشاريع بتتستعاد على Firestore الحقيقي والمشاريع فاضية. **2/2 نجاح**، والمحاكي اتقفل بعدها. `npm test` العادي بيتخطاها (محتاجة المتغير).
- ⚠️ **لسه ما اتجربش:** حفظ الملف في مكان يختاره المالك على جواله (نافذة الحفظ) — ده بيتجرب على جواله بس.
- **✅ بموافقة المالك (نافذة 2026-09-19):** «ارفع دلوقتي» ⇒ اتدمجت 4 تحديثات أسعار من بوت GitHub (`public/prices.json` بس، merge `e1719d5` عشان أرقام الـcommits المذكورة هنا ما تتغيرش) ⇒ typecheck + 739 اختبار ⇒ **اترفع** (`4f22405..e1719d5`). «انشرها» ⇒ **القواعد اتنشرت** على `masroufy-d4b96` («compiled successfully» و«released»). commit التسليم ده نفسه **مش مرفوع** (بعد الموافقة).

### 🔴 القراية التلقائية للرسايل ما كانتش بتلقط ولا رسالة — اتصلحت (2026-09-19، Claude)
- **بلاغ المالك:** «موضوع قراءة الرسايل مش شغال خالص… عندي حوالي 20 عملية في كام يوم مقرأهمش، ولما بدوس تحديث الرسائل مش بيحصل حاجة». آخر رسالة في القايمة ما لهاش علاقة برسايله.
- **السبب (كود أندرويد، مش المحلل):** `SmsSafety.sanitize` بيفلتر **قبل ما الرسالة تتحفظ** وكان بيشترط `SAR|ريال|ر.س` — رسايل الراجحي بتكتب **«SR»** ⇒ كل رسالة كانت بتترمي بصمت (في الاستقبال `BankSmsReceiver` وفي اللمّ `catchUp`). وكمان `catchUp` كان **بيقدّم المؤشر** بعد الرسالة المترمية ⇒ «تحديث الرسائل» عمره ما رجعلها. (قراية «إضافة ← رسائل البنك» بتواريخ ما بتعديش على الفلتر ده، فكانت شغالة مع محلل 1.42.)
- **التصليح (commit `a9b7b08`):** العملة بقت `SAR|SR|USD|EUR|GBP|AED|EGP` (بحدود كلمة) + العربي، والعملات التانية بتتحفظ عشان التطبيق يقول سبب رفضها بدل ما تختفي؛ كلمات الحركة زي المحلل (سحب، خصم، سداد، مدفوعات، دفع، مرتجع…)؛ المبلغ اللي جنبه عملة ما بيتحجبش حتى من غير «مبلغ» (كان مبلغ 5 أرقام زي «بـSR 12500» بيتحجب ويضيع). **مرة واحدة بعد التحديث** (`SmsInboxStore.upgradeFilter`، `filterVersion` 2): المؤشر بيرجع **14 يوم ومش قبل 2026-09-10** ⇒ الرسايل المترمية بتتلم تاني. المعرّف ثابت من نص الرسالة، و**الصيغ اللي كانت بتتقبل قبل كده بتتحفظ بنفس النص بالظبط** (اتقارن القديم والجديد جنب بعض) ⇒ اللي اتسجل أو اتشال ما بيرجعش. التفعيل الجديد بيبدأ من دلوقتي زي الأول.
- `bankSmsParser.redactSms` بقى زي الجافا (المبلغ جنب العملة ما بيتحجبش في النص المعروض).
- **اتفحص:** اختبارات أندرويد (`SmsSafetyTest`، حالة جديدة بشكل رسايل المالك وأرقام وهمية) ⇒ `BUILD SUCCESSFUL`؛ مقارنة القديم والجديد (13/13)؛ typecheck؛ **740 نجاح**.
- **✅ APK 1.44 (versionCode 45):** `dist-android-apk/masroufy-trial-v45.apk`، SHA256 `7D7AF582D5B9AA4C61822758008D095E9469E196FF0E55EEF5C0D6B19EFA8C6E`، 5,271,699 بايت، **على الدرايف مطابق**. ⚠️ **ما اتجربش على جهاز** — المالك بيجربه على جواله (المحاكي طالب PIN).
- **صور المالك التانية (2026-09-19) — أشكال جديدة (من غير أرقامه):** (1) **رسالة الرمز** قبل كل شراء إنترنت: «ننصح بعدم مشاركة الرمز… الرمز:…، بطاقة، مبلغ:SAR…، لدى:…، في:الساعة سنة/شهر/يوم» — **مش عملية**. (2) شراء إنترنت **المبلغ في أول سطر**: «شراء انترنت بـSR 35.62» ثم «عبر…;مدى»، «من…»، «لـاسم»، و«الساعة يوم/شهر/سنة». (3) شراء بالدولار. المالك **لسه ما نزّلش 1.44** وسأل: «تحديث الرسائل» بيقرا كله ولا الجديد بس؟ ⇒ اتشرحله: الجديد بس من آخر مكان وصله (والمعرّف الثابت بيمنع التكرار)، و1.44+ بيرجع 14 يوم مرة واحدة، والقراية بتواريخ من «إضافة ← رسائل البنك» بتقرا الفترة كلها والمراجعة بتعلّم المكرر.
- **التصليح (commit `24522ea`):** رسالة الرمز (`مشاركة الرمز` أو `الرمز: رقم`) **بتترمي قبل التخزين** على الجوال (`SmsSafety`)، وفي القراية بتواريخ (`BankSmsPlugin.SENSITIVE`)، والمحلل بيرفضها بسبب «رسالة تحقق». اتجاه الحوالات بقى يفهم «حوالة داخلية صادرة» و«حوالة محلية واردة». **اتفحص:** اختبارات أندرويد 6/6، و743 اختبار.
- **✅ APK 1.45 (versionCode 46):** `dist-android-apk/masroufy-trial-v46.apk`، SHA256 `4A470EB999643F807F18F3DC42F347C436B87BDC2FB514518718198BC8C89009`، 5,271,727 بايت، **على الدرايف مطابق** — فيه تصليح 1.44 (الالتقاط + المراجعة لآخر 14 يوم) + ده. ⚠️ ما اتجربش على جهاز.

### 🧩 شاشة رسايل البنك — إعادة تصميم (2026-09-19، Claude) — OVERRIDES §36
- **بلاغ المالك بعد 1.45:** القراية التلقائية لقطت فعلًا **31 رسالة** 👍، بس الشاشة «سيئة جدًا وملخبطة» ومش واضح يعمل إيه عشان تتسجل بتصنيفها. قراراته في نافذة: زرار واحد «سجّل الكل»، المجهول «غير مصنف»، و«نفتكره؟» مرة واحدة (أيوه ⇒ تصنيف مؤكد للمحل + اقتراح للقايمة المشتركة، لأ ⇒ يتسأل تاني). وبعد التجربة: الضغطة **مش** تأكيد للتصنيف المقترح.
- **الكود:** `application/useCases/reviewSmsInbox.ts` (جديد: `load/enable/disable/dismiss/recordAll/remember` — بيجمّع صندوق الرسايل مع معاينة `importStatement` نفسها، فمنع التكرار والتصنيف زي الكشف بالظبط)؛ `domain/merchantMemory.ts` (`rememberMerchant`: المحل الموجود بالاسم أو اسم بديل بياخد التصنيف، والجديد بيتعمل، والأرقام بس مش محل)؛ `importStatement.commit` بقى بياخد `chosenCategories` (رقم السطر ← التصنيف) وبيحفظه مؤكد، و`ImportPreviewLine.categorySource` جديد. الشاشة: `SmsInboxSheet.tsx` (من أول وجديد) + `components/SmsReviewRow.tsx` + `components/SmsInboxSettings.tsx` + `SmsInboxSheet.css`. زرار الرئيسية بقى «N رسالة من البنك مستنية تتسجل». التوصيل `smsReview` في `container.ts` (291 سطر) و`demoContainer.ts`.
- **اتفحص:** typecheck نضيف؛ **748 نجاح** و21 متخطاة (جديد `tests/acceptance/smsReview.test.ts`: القايمة بتصنيفها والمرفوضة لوحدها، التسجيل بضغطة والمختار مؤكد والمرفوضة بتفضل، نفس الرسالة تاني = مكررة ومش بتتسجل مرتين، «افتكره» بيثبّت ويرفع اقتراح والتحميل الجاي بيستخدمه، والمحل الموجود بيتحدث من غير نسخة تانية). **المعاينة (`?demo=1`، بيانات وهمية):** الشاشة ظهرت (يوم، محلين، «1 رسالة ما اتفهمتش»، «سجّل الـ2 عملية») ⇒ اختيار تصنيف للمحل المجهول ⇒ السؤال ظهر ⇒ «أيوه افتكره» ⇒ «سجّل» ⇒ «اتسجلت 2 عملية.» والعمليتين في الرئيسية بتصنيفهم، ومفيش أخطاء في الـconsole. ⚠️ اللقطة من لوحة المعاينة طلعت متكسرة (مشكلة الرسم المعروفة) — مفيش صورة.
- ⚠️ **ما اتجربش على جهاز.** المالك يجربه على جواله.
- **✅ APK 1.46 (versionCode 47):** `dist-android-apk/masroufy-trial-v47.apk`، SHA256 `0B626BEA67882C117C6655A1D02B08E652CBD4DDD74588D64EDFD27FB2D7B5F9`، 5,274,931 بايت، اتنسخ للدرايف والبصمة مطابقة على `H:` (بحث الدرايف لسه ما لقاهوش وقت الكتابة — المزامنة بتاخد وقت). ⚠️ ما اتجربش على جهاز.

### 🧭 تغيير الهدف: أندرويد + آيفون، والتصميم في Figma الأول (2026-09-19، Claude) — OVERRIDES §38
- **المالك سأل:** قبل ما نبني من جديد، عايز يصمم التطبيق كله تفاعلي من غير كود (سأل عن FlutterFlow) وClaude يبني عليه، والهدف بقى **جوجل بلاي + آيفون**، سريع ومحمي، باشتراكات.
- **اتعرض عليه (خريطة متوصلة):** https://claude.ai/artifact/Rk1YRKXqzQW2Jzrvy2ocV9 — Figma للتصميم (مقترح)، FlutterFlow مش مناسب كمخطط (التصدير من 39$/شهر، والكود بيقطع الرجوع للتعديل بالسحب، وبيقفل على Flutter)، React Native للبناء (مقترح: الـ12,228 سطر domain+application نضيفين ومش مربوطين بشاشة ولا فايربيز)، Flutter وKMP بدايل. حقايق اتأكدت: جوجل بلاي عنده استثناء «SMS-based money management» لقراية الرسايل (نموذج إعلان ومراجعة)؛ آيفون مفيش قراية رسايل (بديل يتجرب: أتمتة الاختصارات)؛ الاشتراك لازم بدفع المتجرين.
- **قرارات المالك (نافذة):** الإطار **«كوتلن للجهازين»** (مش المقترح)، والاشتراكات **«مجاني بحدود»**. ⇒ `KOTLIN_PLAN.md` اتضاف له قسم تحديث بالتغييرات (Compose Multiplatform، `expect/actual` للرسايل والقفل والـPDF والخلفية، RevenueCat، حساب أبل). **الخطة لسه محتاجة مراجعة كاملة قبل أي بناء.**
- ❓ **مستني المالك:** أنهي مميزات مجانية وأنهي باشتراك؛ باقة Blaze؛ مكتبة فايربيز للجهازين (بعد تجربة صغيرة). وكمان بيضيف حد معاه على GitHub (اتشرحله الخطوات، وهو اللي بيعملها من الموقع — مفيش `gh` على الجهاز).

### 🧩 كوتلن للجهازين — البداية: الخطة + ملفات المرجع + الفلوس والتطبيع (2026-09-19، Claude)
- **موافقة المالك (نافذة):** يبدأ الشغل اللي مش مستني التصميم **دلوقتي** (الخطة، ملفات المرجع، قلب التطبيق بكوتلن، تجربة فايربيز قراية بس). التطبيق الحالي أولوية لو ظهر فيه عطل.
- **`KOTLIN_PLAN.md` اتعاد كتابتها** على الجهازين (Compose Multiplatform، `expect/actual`، الآيفون من GitHub Actions على ماك، الترتيب (أ) دلوقتي و(ب) مستني المالك).
- **ملفات المرجع:** `npm run golden` (`scripts/golden/`) بيشغّل دوال `src/domain` نفسها ويكتب `native-app/golden/*.json` — **بيانات وهمية بس** (المستودع عام). دلوقتي: `money.json` (1110 حالة: التحليل والعرض والنسب والتقسيم ومعدل الادخار) و`normalize.json` (126 حالة).
- **`native-app/`** (Gradle 8.14.3، Kotlin 2.4.20): موديول `core` كوتلن نقي — `Money.kt` و`FormatMoney.kt` و`Normalize.kt` و`JsText.kt` (تقليد `trim`/`\s`/`Math.round` بتاع جافاسكربت عشان المطابقة بالحرف). `nfkc` بـ`expect/actual` (نسخة الآيفون مكتوبة بس **ما اتبنتش** — محتاجة ماك).
- **اتفحص:** `./gradlew :core:jvmTest` ⇒ **20 اختبار / 1236 حالة، صفر فرق** من أول تشغيل. **واتأكد إن الاختبار بيمسك الغلط:** غيرت ناتج في ملف المرجع بالقصد ⇒ فشل بالرسالة الصح ⇒ رجّعته. typecheck نضيف.
- ⚠️ **درس:** أداة الكتابة (Write) بتحوّل `\uXXXX` لحروف فعلية ⇒ حرف تحكم جوه `'…'` كسر الكود. في كوتلن الحروف الخفية بتتكتب بأرقامها (`c.code == 0x0A`). وكوتلن بيعتبر `/*` جوه تعليق بداية تعليق جديد.
- **الجاي:** الفترات (الشهر من يوم 28) ← الأنواع والدفتر ← منع التكرار ← التصنيف ← سلسلة الرصيد…، كل واحد بملف مرجعه.
- **✅ الفترات** (`Period.kt`، 3039 حالة) و**الدفتر** (`EconomicKind.kt` + `Entities.kt` + `Ledger.kt` + `SuggestEconomicKind.kt` + `EstimatedKinds.kt`، 1088 حالة: نصيبك والمجاميع والوسوم من غير مضاعفة والديون والتسويات والاقتراح و«التقريبي») — **صفر فرق**. ملفات المرجع بقت حالة في كل سطر (أصغر، والفرق في git بيبان على الحالة اللي اتغيرت). المجموع دلوقتي **5,363 حالة / 46 اختبار**.
- **✅ منع التكرار** (`Dedupe.kt`: المرجع والتعارض وسطر الكشف بالرصيد واحد لواحد وتشابه الرسايل، و`hashContent` بنفس بصمة FNV بالظبط، و`importFingerprint` بـ`JsText.jsonString` = `JSON.stringify`) و**التصنيف** (`Categorize.kt`: الأولوية الأربعة، `matchesText`، `merchantIndex`، `rememberMerchant`) و**تعديل المبلغ** (`AmountEdit.kt`) — 464 حالة، صفر فرق، ومعاها سيناريوهات مقصودة (إعادة استيراد نفس الكشف، سطرين بنفس الرصيد قصاد سجل واحد، عمود التصنيف).
- ⚠️ **درس:** Gradle كان بيعتبر الاختبار «up-to-date» لما ملفات المرجع بس اللي اتغيرت (مش مدخل معلن) ⇒ ما كانش بيشغّلها. اتصلح بـ`inputs.dir(golden)` في `core/build.gradle.kts`. **لو شفت «BUILD SUCCESSFUL in 1s» بعد `npm run golden` اشتبه.**
- **✅ مطابقة الرصيد** (`Reconcile.kt`) و**قراية CSV والمخططات** (`CsvReader.kt` + `ImportSchemas.kt`، على fixtures الأربعة + حالات مصنوعة وعشوائية، 338 حالة) و**محلل رسايل البنك والحجب** (`BankSmsParser.kt`، 514 حالة فيها أشكال رسايل الراجحي بأرقام وهمية وكل أسباب الرفض) — صفر فرق. الأنماط بتستعمل `JsText.S`/`JsText.B` بدل `\s`/`\b` (في Java `\s` إنجليزي بس و`\b` بيعتبر العربي جزء من الكلمة)، و`JsText.parseIsoMillis` بدل `Date.parse`.
- **مش هتتنقل (§35 — أدوات إصلاح):** `balanceChainCheck.ts` و`chainBreakReport.ts` و`idRepair*` و`orphanCleanup` و`categoryMigration` و`referenceRestore` و`budgetIdRepair`.
- **✅ الميزانية والتحليل** (`Budget.kt` + `Analytics.kt`، 445 حالة: السقف والمتوسط والشذوذ والمتاح اليومي والتوقع والتوزيع) و**التصنيفات** (`CategoryTree.kt` + `CategoryColors.kt` + `CategoryViews.kt` + `CategoryEdit.kt` + `UserProfile.kt`، 283 حالة على شجرة التطبيق الافتراضية + تصنيفات وهمية) — صفر فرق.
- ⚠️ **اتمسك فرق حقيقي:** ترتيب الأسماء العربي بـ`java.text.Collator` غير ICU (آمال/أحمد/إبراهيم) ⇒ الـJVM بقى بيستعمل ICU4J 78.3 (نفس Node). رسالة فشل الاختبار بقت بتقول **مكان أول فرق** بس (`Golden.firstDiff`) — التقرير: `native-app/core/build/test-results/jvmTest/*.xml`.
- ⚠️ **كود الآيفون** (`iosMain`: `nfkc` و`compareArabic`) مكتوب بس **ما اتبناش** — أول بناء على ماك ممكن يطلع فيه أخطاء.
- **✅ الكاش والأيام والبحث وفصل المصروف والتسويات والمشاريع والاشتراكات** (`TransactionViews.kt` + `Projects.kt` + `Recurring.kt`، 282 حالة، منها اشتراكات شهرية وكل ٣ شهور بتتلقى وخدمة مجهولة بتترفض) — صفر فرق. **المجموع: ~7,300 حالة / 13 ملف مرجع.**
- **✅ الاستثمار** (`Quantity.kt` بضرب 128 بت من غير مكتبة + `Assets.kt` + ملف الأسعار، 197 حالة) و**التنبيهات والقفل والشعارات وقاعدة التجار المشتركة** (`Notifications.kt` + `SharedMerchants.kt`، 242 حالة) — صفر فرق. **المجموع ~7,700 حالة / 15 ملف مرجع / 110 اختبار.**
- **فاضل في `core`:** النسخ الاحتياطية (`fullBackup` و`checkFullBackup` و`mergeFullBackup` و`mergeBackup` و`checkBackupFinance` و`backupProfile` و`backupBudgetIds`)، وتحميل المراجع (`categoryTreeLoader` و`referenceLoader`)، وكشف نوع الملف، ومحلل كشف الراجحي PDF (بعد استخراج النص).
- **✅ النسخة الشاملة** (`Backup.kt` + `BackupCheck.kt` + `Sha256.kt` من غير مكتبة، 105 حالة): الفحص بكل رسايل الرفض (24 شكل كسر)، والفحص المالي، والدمج بتحويل الروابط، وتصليح معرّفات الميزانيات، و**بصمة السلامة SHA-256 على نص `canonicalBackup` = نفس بصمة التطبيق الحالي** ⇒ نسخة من التطبيق الحالي هتترجع في الجديد. الصفوف `Map<String, Any?>` زي JSON.
- **✅ كشف الراجحي PDF بعد قراية النص** (`AlrajhiPdf.kt`، 54 حالة على صفحات مصنوعة بنفس مقاسات الأعمدة) و**المراجع الأولية** (`SeedReferences.kt`: 91 تصنيف و226 قاعدة و319 تاجر **بنفس المعرّفات بالظبط**، 25 حالة) — صفر فرق.
- **🎯 معايير القبول التلاتة على الكشف الحقيقي بكود كوتلن** (`RealStatementTest`، على جهاز المالك بس — الملف مش في Git): 1912 صف من غير أخطاء، المجاميع الأربعة مطابقة بالهللة، وسلسلة الرصيد من 4,837.83 لحد **2,707.25 @ 2026-09-04 بصفر فروق**. **واتأكد إنه بيمسك الغلط** (هدف غلط بهللة ⇒ فشل ⇒ رجّعته). `core/build.gradle.kts` بيشغّله لو الملف موجود بس.
- **`.github/workflows/native-core.yml`** (جديد، **ما اترفعش**): على ماك من GitHub — اختبارات المرجع + بناء كود الآيفون. أول مرة هيشتغل بعد الرفع بموافقة المالك، ولو كود `iosMain` فيه غلطة هتبان هناك.
- **قلب التطبيق خلص**: ~8,000 حالة مرجعية في 18 ملف، 125+ اختبار. **مش هيتنقل** (§35): أدوات الإصلاح. **فاضل بعد كده (مش مستني تصميم):** طبقة البيانات (Room + قراية فايربيز) وتجربة مكتبة فايربيز قراية بس — و**المزامنة اللي بتكتب مستنية مراجعة المالك** (KOTLIN_PLAN §2.1).

### 🧭 الرفع وبناء الآيفون على ماك + طلب «زون التحويلات» (2026-09-20، Claude)
- **✅ اترفع بموافقة المالك** (`f9d1c25..4c76cb8`، 25 حفظ ومعاهم دمج تحديث الأسعار اليومي). صفحة الخطة للمالك اتحدثت على نفس الرابط: https://claude.ai/artifact/S3LZmJuzDmVbttQmfgGJpv (اللي خلص، اللي بعده، المستني قراره).
- **🔴 أول بناء على ماك فشل — وده كان الغرض منه:** اختبارات ملفات المرجع **عدّت على الماك** (يعني ICU4J والباقي تمام)، و**بناء كود الآيفون فشل**: كنت مستورد أعضاء NSString كأنها رموز مستقلة (`import platform.Foundation.compare` و`precomposedStringWithCompatibilityMapping`) — اتشالوا. **سجلات GitHub محتاجة تسجيل دخول**، فالـworkflow بقى يطبع أول 20 سطر خطأ كـ«error annotations» عشان تبان من برا. اترفع التصليح (`6509eed`) والنتيجة لسه بتتبني.
- **🆕 طلب المالك — «زون التحويلات» (OVERRIDES §39):** قسم مستقل للتحويلات، وربط طرف تحويل بشخص، وشك تلقائي يسأل «حسابك التاني ولا شخص؟». **قراراته:** في تطبيق كوتلن الجديد · الشك بعد **5 تحويلات في شهر** · «حسابي التاني» ⇒ **العمليات القديمة كلها تتصلح** · الوارد من شخص مربوط = **دخل**. ❓ مفتوح: أنهي نوع اقتصادي يمثل «دخل من شخص» (جدول spec/02 مفيهوش) — يتقرر **قبل** التنفيذ.

### 🌍 تجهيز الإنجليزي ومكان البلاد + بناء الآيفون مستني (2026-09-20، Claude Opus 5)
- **قرارات المالك اللي اتنفذت (OVERRIDES §40):** تجهيز اللغة **من دلوقتي**، وتجهيز **مكان** حزمة البلد من دلوقتي، وClaude يكتب النصوص الإنجليزية والمالك يراجعها.
- **✅ اتعمل — مقسم اللغة في قلب كوتلن:** `Texts.kt` (اللغة + `uiText(key, args…)` + تبديل `{0}`)، `TextKeys.kt` (44 مفتاح)، `TextsArabic.kt` (**الأصل — نفس النص القديم بالحرف**)، `TextsEnglish.kt` (كتابة Claude، **مستنية مراجعة المالك**). اتربط في `EconomicKind.kt` (19 اسم نوع + رسالة الخطأ، والاسم المتخزن `wire` ما اتغيرش) و`FormatMoney.kt` (رموز العملات و«غير متاح») و`SuggestEconomicKind.kt` (14 سبب اقتراح).
- **✅ اتفحص:** `:core:jvmTest` ⇒ **الـ18 ملف مرجع + كشف الحساب الحقيقي + 6 اختبارات لغة جديدة، صفر فرق** — يعني مفيش نص ولا رقم اتغير عند المالك. والاختبار بيتأكد إن كل مفتاح له عربي وإنجليزي، وإن التبديل للإنجليزي بيغيّر الاسم المعروض **من غير** ما يغيّر المتخزن.
- **✅ حزمة البلد (`CountryPack.kt`):** العملة · اللغة · شجرة التصنيفات · `BankSmsReader` · مخططات الكشوف · أسماء المحافظ المبدئية. السعودية مليانة باللي شغال فعلًا (`SaudiBankSmsReader` بينادي نفس المحلل بالحرف — في اختبار بيقارن الناتجين)، و**مصر مش موجودة عن قصد**: محتاجة عينات حقيقية.
- **اللي ما اتعملش:** ~200 نص لسه جوه الكود (رسايل أخطاء الاستيراد والنسخة والميزانية…) — بتتنقل على دفعات مع اختبار بعد كل دفعة. ومحتوى مصر (شجرة + قارئ رسايل + مخطط كشف) — **مستحيل من غير عينات**.
- **⚠️ بناء الآيفون لسه «ما اتأكدش»:** التصليح مكتوب ومحفوظ محليًا (`68dcf80`: استيراد `platform.Foundation` بالاسم + `@OptIn(ExperimentalForeignApi::class)` لـ`NSMakeRange` + تحديث `checkout@v5`/`setup-java@v5`) — **ما اترفعش** بطلب المالك («سيبه دلوقتي»). من غير رفع، بناء الآيفون ما بيتجربش على ماك.
- **الخطوة الجاية:** دفعة تانية من النصوص، وبعدها طبقة البيانات (Room + قراية فايربيز). و«زون التحويلات» (§39) مستني قرار **النوع الاقتصادي لـ«دخل من شخص»** قبل أي كود.
- **❓ مستني المالك:** (أ) مراجعة `TextsEnglish.kt`. (ب) **مين هيجيب عينات بنوك مصر؟** (ج) الرفع عشان بناء الآيفون يتجرب. (د) النوع الاقتصادي للوارد من شخص مربوط (§39).

### 🔤 نقل كل نصوص الواجهة لجدول اللغة (2026-09-20، Claude Opus 5)
- **315 نص** في قلب كوتلن بقوا مفاتيح، بجدول عربي (الأصل) وجدول إنجليزي (كتابة Claude، **مستنية مراجعة المالك**). الأربع دفعات: الأنواع والعملات والاقتراحات ← الميزانية والتحليل والتصنيف والأصول ← منع التكرار والتنبيهات والدفتر والاستيراد والتصنيفات والمشاريع والملف الشخصي ← الفلوس والكميات ورسايل البنك والنسخة الاحتياطية والألوان.
- **اتفحص:** `:core:jvmTest` بعد كل دفعة ⇒ **110 اختبار، صفر فشل**، والـ18 ملف مرجع وكشف الحساب الحقيقي زي ما هم. **واتأكد إن الفحص بيمسك الغلط**: نقطة زيادة في نص واحد ⇒ فشل بالرسالة الصح ⇒ رجّعته.
- **الطريقة اللي خلت ده آمن:** برنامج بياخد النص العربي من الكود ويحوّله لمفتاح آليًا (مفيش إعادة كتابة)، وبيرفض لو النص اتكرر أو لو فيه متغير ما اتحددش.
- **⚠️ اتكشفت مشكلة مؤجلة:** `sourceRecord.reason` بيتخزن كجملة جاهزة، فلو المستخدم حوّل للإنجليزي أسبابه القديمة هتفضل عربي. الحل المقترح (المتخزن = مفتاح + متغيرات) **مؤجل لطبقة البيانات** عشان بيغيّر شكل النسخة الاحتياطية — التفاصيل في OVERRIDES §40.1.
- **الخطوة الجاية:** طبقة البيانات (Room + قراية فايربيز) — مش مستنية تصميم.
- **❓ مستني المالك:** مراجعة النصوص الإنجليزية · مين هيجيب عينات بنوك مصر · الرفع عشان بناء الآيفون يتجرب · النوع الاقتصادي للوارد من شخص مربوط (§39).

### 🔁 تصحيح من المالك + عينات مصر (2026-09-20)
- **الوارد من شخص مربوط: يتسأل مش يتحسب دخل** (OVERRIDES §39.1) — ده **بيلغي** قرار «دخل تلقائي» اللي كان اتاخد نفس اليوم. أي نموذج جاي: **ما تبنيش زون التحويلات قبل** ما تتحدد قايمة اختيارات السؤال، و**فيه ثغرة في جدول spec/02**: مفيش نوع «دعم أو هدية مستلمة».
- **مصر:** المالك هيبعت رسايل بنوك مصرية وكشف حساب مصري (من غير أرقام حسابات كاملة — المستودع عام). لحد ما تيجي، `CountryPack` سعودية بس (OVERRIDES §40.2).

### 🇪🇬 عينات مصر وصلت، وقارئ رسايل QNB اشتغل (2026-09-20، Claude Opus 5)
- المالك بعت صور رسايل QNB مصر وكشف حساب. **العينات مش في المستودع ولا تتحط فيه** (بيانات حقيقية، مستودع عام). الشكل اتسجل في OVERRIDES §40.3 بأمثلة مخترعة.
- **✅ `EgyptBankSms.kt`** بيقرا التلات أشكال (تحويل صادر · تحويل وارد · شراء بالبطاقة)، وبيرفض رمز التحقق والعرض والمرفوض والعملة المش جنيه والمبلغ أو التاريخ الغامض. **الرصيد مش بيتحسب مبلغ عملية.**
- **✅ `EGYPT_PACK`** اتضافت وبتقول ناقصها إيه بصراحة (`gaps = categoryTree, statementReader`) — وفيه اختبار بيمنع إن حزمة تدّعي إنها جاهزة وهي ناقصة.
- **اتفحص:** 120 اختبار، صفر فشل (منهم 10 لمصر). **مفيش golden لمصر** لأن التطبيق الحالي ما بيقراش رسايل مصرية — الضمان اختبارات بالإيد.
- **الخطوة الجاية:** قارئ كشف QNB (الشكل متسجل §40.3) وشجرة تصنيفات مصر. وطبقة الاستخدامات (use cases) لسه ما بدأتش.
- **❓ مستني المالك:** **عملتين في حساب واحد** (جنيه وريال) — التطبيق بيجمع كله في عملة واحدة دلوقتي. لازم يتقرر قبل ما مصر تشتغل.

### 🌍 حساب منفصل لكل بلد (2026-09-20، قرار المالك)
- **الاختيار:** مساحة (space) لكل بلد — مش جمع بعملة واحدة ولا تحويل بسعر صرف (OVERRIDES §41). السبب: سعر الصرف بيخلي أرقام الشهور القديمة تتغير لوحدها.
- **أثره على الكود الجاي:** كل مستودع بيتعمل **مربوط بمساحة**، وفايربيز بيبقى `users/{uid}/spaces/{spaceId}/…`. **التطبيق الحالي ما بيتغيرش** وبياناته بتبقى مساحة «السعودية». ⚠️ أي نموذج جاي: ما تكتبش مستودعات من غير المساحة دي، الرجوع بعدين غالي.
- **❓ مستني المالك:** النسخة الاحتياطية (واحدة للكل ولا لكل مساحة) · المساحة التانية مجانية ولا باشتراك · ديون نفس الشخص في البلدين تتجمع ولا لأ.

### 🧱 طبقة الاستخدامات بدأت — الشاشة الرئيسية بتتطابق (2026-09-20، Claude Opus 5)
- **موديول جديد `native-app/app`** (ARCHITECTURE §31.1): واجهات المستودعات + حالات الاستخدام + تنفيذ في الذاكرة. المترجم نفسه بيمنع أي اعتماد معكوس على `core`.
- **✅ اتعمل:** كل واجهات المستودعات (`LedgerPorts` و`ReferencePorts` و`Support`) · الكيانات الناقصة في `core` (`StorageEntities.kt`: الأشخاص والوسوم ودفعات الاستيراد ومصادرها والميزانيات) · مستودعات الذاكرة للعمليات والتصنيفات والتخصيصات والميزانيات · **حالة الاستخدام الأولى `LoadHomeScreen`**.
- **✅ ملف مرجع جديد `home.json`** (18 حالة) بيتولد من التطبيق الحالي بمستودعات الذاكرة بتاعته — **أول ملف مرجع لحالة استخدام مش لدالة حساب**. `npm run golden` بقى بيشغّل مولّدات async كمان.
- **اتفحص:** `./gradlew jvmTest` ⇒ **121 اختبار، صفر فشل**. **واتأكد إنه بيمسك الغلط:** خليت «أحدث العمليات» 4 بدل 5 ⇒ الاختبار فشل وقال «أول فرق عند .latestIds: الطول المتوقع 5 والناتج 4» ⇒ رجّعته.
- ⚠️ **درس في كتابة المولّد:** أول نسخة طلعت 18 حالة **كلها فاضية** (تواريخ برّه الفترة)، وبعدها حالات من غير «غير متاح» (لأن النوع غير المحدد بيتقدّر تلقائيًا إلا لو المستخدم مؤكده). **لو ملف مرجع جديد عدّى من أول مرة، بص على محتواه الأول.**
- **الخطوة الجاية:** باقي حالات الاستخدام (43 لسه) — الأقرب: شاشة العمليات، والميزانية، والاستيراد.

### ✅ كود الآيفون اتبنى لأول مرة (2026-09-20)
- **اترفع بموافقة المالك** (`06042a6`) و**بناء الماك نجح**: اختبارات ملفات المرجع عدّت على macOS، و**كود الآيفون اتبنى للجهاز الحقيقي والمحاكي** — تصليح `Nfkc.ios.kt` و`Collation.ios.kt` (استيراد `platform.Foundation` بالاسم + `@OptIn(ExperimentalForeignApi::class)`) طلع صح.
- ⚠️ **البناء مش معناه نفس النتيجة.** الاختبارات اللي عدّت على الماك كانت على JVM. عشان كده اتضاف `PlatformTextTest` في `commonTest` — بيختبر **الجزء الوحيد اللي كوده بيختلف من جهاز للتاني** (`nfkc` و`compareArabic`)، وقيمه المتوقعة **متولّدة من التطبيق الحالي** (`normalize('NFKC')` و`localeCompare(…,'ar')`) مش مكتوبة بالإيد. والـworkflow بقى بيشغّله على **محاكي الآيفون** (`:core:iosSimulatorArm64Test`).
- **ليه الاختبارات التانية ما بتشتغلش على الآيفون:** ملفات المرجع بتتقرا من القرص بـ`java.io.File` في `jvmTest`. نقلها لـ`commonTest` محتاج قراية ملفات `expect/actual` — شغل مستقل لو اتقرر.

### 💰 توسيع جدول الأنواع + سكشن «المستحقات» (2026-09-20، قرارات المالك)
- **سبع أنواع وارد جديدة** (OVERRIDES §42): هدية · مساعدة · دعم أو تعويض · أرباح استثمار (دخل) · دور جمعية · استرداد · سلفة شغل (مش دخل). **أول توسيع لجدول spec/02.**
- **الاسترداد بينقّص المصروف** — `reducesExpense` جديد في القاعدة و`computePeriodTotals`. الاسترداد الوحيد اللي بيعمل كده، وفيه اختبار بيمنع غيره.
- **اتفحص:** 130 اختبار، صفر فشل — ملفات المرجع ما اتكسرتش لأنها بتغطي أنواع التطبيق الحالي بس. الأنواع الجديدة في `commonTest` (بتشتغل على الآيفون كمان).
- **«المستحقات»** (OVERRIDES §43) = الاسم الموحد للديون والجمعيات والاشتراكات في سكشن واحد. **ما تستعملش «التسويات»** للسكشن — محجوزة لسداد الدين.
- ⚠️ **الجمعية كمميزة لسه ما اتصممتش** — الموجود هو النوع الاقتصادي بس.

### 🍏 الآيفون اتأكد فعلًا — مش بناء بس (2026-09-20)
- **`e330945`:** خطوة المحاكي بقت **بتفشل لو ما اشتغلش ولا اختبار** (النجاح الفاضي مش نجاح)، وبتطبع العدد كملاحظة تتقرا من برا من غير تسجيل دخول.
- **النتيجة المقروءة من GitHub: «اختبارات اتشغلت على محاكي الآيفون: 9» وكلها عدّت.** يعني على الآيفون: `nfkc` بيطلع نفس ناتج `normalize('NFKC')` بتاع التطبيق الحالي، و`compareArabic` بيرتب الأسماء العربي زي `localeCompare(…,'ar')` بالظبط، وحساب الأنواع الجديدة شغال.
- **اللي لسه:** باقي الاختبارات (ملفات المرجع) بتتقرا من القرص فبتشتغل على الكمبيوتر بس. نقلها للآيفون محتاج قراية ملفات `expect/actual`.

### 🌳 شجرة البرنامج + طلب «الأحداث» (2026-09-20)
- **شجرة البرنامج للمالك** (كل ميزة وحالتها الحقيقية): https://claude.ai/artifact/DwQGM6g5mPpbHvEJorVWhB
- **صفحة مراجعة النصوص الإنجليزية** (المالك بيعلّم فيها وClaude بيقرا منها): https://claude.ai/artifact/V1G2UZL1WkcrxK18YdbEem — بتتخزن في قاعدة الـArtifact (`review/<المفتاح>`).
- **🆕 طلب «الأحداث»** (OVERRIDES §44): فرح · عزا · سفر → اتصرف عليه كام، جه منه كام، ومين نقّط بكام. **النقوط ارتباط متبادل مش هدية**، والفكرة الأقوى إن التطبيق يفتكر مين نقّطك ويفكّرك في حدثه. ❓ مستني المالك: النقوط دخل ولا لأ · التطبيق يفكّر ولا يسجّل بس · الحدث نوع من المشروع ولا مستقل.

---

# 🚩 نقطة البداية لأي جلسة جديدة — 2026-09-22

**اقرا القسم ده الأول. هو أحدث حاجة في الملف.**

## الحالة في سطرين
تطبيق المالك الحالي (1.46، React + Capacitor) **شغال على جواله**. بنبني نسخة جديدة بـKotlin Multiplatform لأندرويد وآيفون في `native-app/`. **قلب التطبيق خلص واتأكد بالأرقام**، وطبقة الاستخدامات بقت **15 من 44** (الرئيسية · العمليات · الميزانية · الاستيراد · التراجع · تنظيف المعلّق · تحديد النوع · التصنيف الجماعي · الإضافة اليدوية · تعديل العملية · ملخص الكاش · مطابقة الرصيد · الأشخاص والديون · القواعد والتجار · الاشتراكات)، والشاشات مستنية تصميم المالك في Figma.

## الأرقام المقيسة (مش تقديرية)
- **155 اختبار على الكمبيوتر، صفر فشل** (اتعدّوا من تقارير JUnit XML لكل مجموعة بعد تشغيل كامل 2026-09-22: قلب 138 + استخدامات 17) · **9 اختبارات على محاكي الآيفون، صفر فشل**
- ⚠️ تصحيح عدّ: الرقم «140» اللي كان متسجل قبل الجلسة دي كان **139** فعليًا (قلب 138 + الرئيسية 1) — نفس درس §6، والعدّ دلوقتي من الـXML مش من الذاكرة
- **27 ملف مرجع** (`native-app/golden/`) ≈ **8,466 حالة** متولّدة من التطبيق الحالي
- كشف حساب المالك الحقيقي: **1,912 صف**، المجاميع الأربعة وسلسلة الرصيد مطابقة **بالهللة**
- **27 نوع اقتصادي** · **314 نص** ليهم مفاتيح بعربي وإنجليزي

## الأوامر اللي بتشتغل فعلًا على الجهاز ده
```bash
cd /e/work/masroufy/native-app && JAVA_HOME="C:\Users\atgs0\Documents\Codex\android-build-tools\java\jdk-21.0.12.1+1" ./gradlew jvmTest --console=plain
```
- `npm run typecheck` للتطبيق الحالي (**مش** `tsc --noEmit` لوحده — القاعدة 14)
- `npm run golden` بيولّد ملفات المرجع من التطبيق الحالي
- بناء الآيفون بيحصل على ماك من GitHub Actions (`.github/workflows/native-core.yml`) — مفيش ماك هنا

## قرارات المالك الجديدة (2026-09-20) — كلها في `OVERRIDES.md`
| § | القرار |
|---|---|
| 40 | تجهيز الإنجليزي والبلاد من دلوقتي · 314 نص بمفاتيح · العربي هو الأصل بالحرف |
| 40.3 | قارئ رسايل QNB مصر اتعمل من عينات حقيقية (**العينات مش في المستودع ولا تتحط**) |
| 41 | **حساب منفصل لكل بلد** (مساحات) — مش جمع بعملة واحدة ولا سعر صرف |
| 41.1 | نسخة احتياطية واحدة للكل · البلاد التانية في الاشتراك · تفاصيل الاشتراك مؤجلة لآخر المشروع |
| 42 | **7 أنواع وارد جديدة** · الاسترداد **بينقّص المصروف** |
| 43 | **«المستحقات»** = الديون + الجمعيات + الاشتراكات. **ممنوع** تسميها «التسويات» |
| 44 | **الأحداث** (فرح/عزا) · النقوط **دخل بس نوعه لوحده** · تظهر في بروفايل الشخص كعلامة **مش دين** |
| 45 | التعاون بين الناس — **مش دلوقتي**، بعد ما التطبيق الجديد يشتغل |
| 46 | رؤية بعيدة: زكاة · «حلال تشك» **يعلّم ما يفتيش** · أحاديث بمصدر ودرجة · تعليم مالي بدل ترشيح أشخاص |
| 47 | المشروع **شخصي ولا شغل** — والرقم المهم بيتقلب |
| 48 | **مصادر الدخل**: المستخدم بيقول اشتغل فين ومن إمتى. البرنامج يستنتج **مين حوّل** مش **ليه المبلغ اتغير** |

## ⚠️ دروس اتعلمناها بالغلط — ما تكررهاش
1. **`tsc --noEmit` لوحده ما بيفحصش حاجة** في المشروع ده (أخفى 3 أعطال). استعمل `npm run typecheck`.
2. **لو ملف مرجع جديد عدّى من أول مرة، بص جواه.** أول نسخة من `home.json` طلعت 18 حالة **كلها فاضية**.
3. **Gradle بيعتبر الاختبار up-to-date** لو ملفات المرجع بس اتغيرت. لو شفت `BUILD SUCCESSFUL in 1s` بعد `npm run golden` اشتبه.
4. **أداة الكتابة بتحوّل `\uXXXX` لحروف فعلية** — في كوتلن اكتب الحروف الخفية بأرقامها.
5. **نجاح من غير ما يشتغل اختبار مش نجاح** — خطوة الآيفون بتعد الاختبارات وبتفشل لو صفر.
6. **ما تقولش رقم من غير ما تعدّه.** قلت «141 اختبار» والحقيقي كان 140.

## اللي مستني المالك
1. **مراجعة النصوص الإنجليزية** — صفحة المراجعة (رابط تحت)
2. **تصميم الشاشات في Figma** — كل الشاشات متوقفة عليه
3. **مراجعة تصميم المزامنة** (`KOTLIN_PLAN.md` §2.1) قبل أي كود بيكتب في التخزين
4. تصميم **الجمعيات** و**الأحداث** و**زون التحويلات** معاه

## اللي مستني شغل (مش مستني قرار)
- **29 حالة استخدام** لسه — الأقرب: المشاريع (`manageProjects`)، التصنيفات (`manageCategories`)، الاستثمار (`manageAssets`)، رسايل البنك (`readBankSms`/`reviewSmsInbox`)، النسخة الشاملة
- **قارئ كشف QNB مصر PDF** — شكل الكشف متسجل في OVERRIDES §40.3
- **شجرة تصنيفات مصرية** (باركنج، سايس)
- **طبقة التخزين** (Room + قراية فايربيز)

## روابط
- **صفحة مراجعة النصوص:** https://claude.ai/artifact/V1G2UZL1WkcrxK18YdbEem
- **شجرة البرنامج:** https://claude.ai/artifact/DwQGM6g5mPpbHvEJorVWhB
- **المستودع:** https://github.com/mohussamarch/tagroba (فرع `main`، كل حاجة مرفوعة)

> ⚠️ **الصفحتين دول ملك حساب المالك الحالي على claude.ai.** أي حساب تاني **مش هيفتحهم** إلا لو المالك شاركهم من زرار Share. وملاحظات المراجعة متخزنة جوه الصفحة الأولى نفسها.

## آخر خطوة: حالة استخدام شاشة العمليات في كوتلن — 2026-09-22

**اتعمل إيه:**
- مولّد ملف مرجع جديد `scripts/golden/transactionsGolden.ts` بيشغّل `makeLoadTransactionsScreen` الحقيقي على 18 حالة وهمية ويكتب `native-app/golden/transactions.json`. الحالات بتغطي: فترة فاضية، كلها «غير محدد ومؤكد» (المجاميع null)، جزئي، كله معروف، وسوم (منها المكرر ومنها اللي وسمه مش موجود)، تجار بأسماء بديلة عربي وإنجليزي، المستودعات الاختيارية (تجار/وسوم) موجودة وغايبة، وتحديد الفترة بالكائن ومرة بـ`periodForDate(today)`.
- في كوتلن: `app/.../usecase/LoadTransactionsScreen.kt` (نقل حرفي للمنطق) + `app/.../memory/MemoryReferenceRepositories.kt` (مستودعات ذاكرة للتجار والوسوم وروابطها بنفس ترتيب التطبيق الحالي) + `TransactionsGoldenTest.kt`.
- **فرق مقصود واحد عن التطبيق الحالي:** لو مفيش فترة ولا تاريخ اليوم، جافاسكربت بياخد ساعة الجهاز؛ كوتلن بيرمي خطأ («لازم فترة أو تاريخ اليوم») لأن مفيش ساعة جوه المنطق — نفس قاعدة `Clock` port. ملف المرجع مش بيغطي الفرع ده أصلًا.

**اتفحص إزاي:**
- `npm run golden` طلّع 18 حالة، واتبص جواها (درس §2): 4 فاضية، 11 فيها وسوم، 11 فيها تجار، 1 مجاميعها null، 9 بمسار `today`، صفر أخطاء. الـ17 ملف مرجع القديمين طلعوا **من غير ولا تغيير محتوى** (`git diff --numstat` فاضي — الفرق كان نهايات سطور ويندوز بس واترجّعوا).
- `./gradlew jvmTest` في نسخة العمل: **140 اختبار، صفر فشل** — اتعدّوا من JUnit XML مش من «BUILD SUCCESSFUL» (درس §5 و§6). ⚠️ **تصحيح لاحق (نفس اليوم):** الخطوة دي اتسجلت الأول «141» ورسالة الـcommit بتاعتها (26053c4) فيها نفس الغلط — العدّ الصح وقتها كان 140، والغلطة الأصلية إن رقم «140» المتسجل قبل الجلسة كان 139 فعليًا.
- **الفحص بيمسك الغلط:** غيّرت `totalCount` من 22 لـ23 في ملف المرجع ⇒ الاختبار فشل وقال «أول فرق عند .totalCount: المتوقع 23 والناتج 22»، ورجّعت القيمة والاختبار عدّى. ⚠️ درس §3 حصل فعلًا: أول محاولة طلعت «BUILD SUCCESSFUL in 3s» لأن Gradle اعتبر الاختبار up-to-date — التشغيل الصح بـ`--rerun`.

**إيه اللي ما اتجربش:**
- اختبارات الآيفون (محتاجة ماك — GitHub Actions هيشغّلها لما يترفع).
- مفيش شاشة ولا تخزين حقيقي — دي طبقة الاستخدامات بس، زي ما هو متفق (الشاشات مستنية Figma).

**الخطوة الجاية:** ~~حالة استخدام الميزانية~~ اتعملت (القسم اللي تحت) — البعدها: الاستيراد.

**أسئلة مستنية المالك:** مفيش جديد من الخطوة دي — القايمة القديمة زي ما هي (مراجعة الإنجليزي، تصميم Figma، مراجعة تصميم المزامنة §2.1، تصميم الجمعيات والأحداث وزون التحويلات).

## الخطوة اللي بعدها: حالة استخدام شاشة الميزانية — 2026-09-22

**اتعمل إيه:**
- `scripts/golden/budgetScreenGolden.ts` بيولّد `native-app/golden/budgetScreen.json` (15 حالة) من `makeLoadBudgetScreen` الحقيقي. الجديد عن اللي قبله: **تاريخ ست فترات سابقة** (فترات موثوقة وغير موثوقة وفاضية — المتوسط والشذوذ بيتحسبوا منهم)، سقوف تصنيفات بـ`notifyEnabled` متقلب، وميزانية غايبة/سقفها null/سقف فعلي.
- في كوتلن: `LoadBudgetScreen.kt` + `BudgetScreenGoldenTest.kt`، و`shiftPeriod` اتنقلت لملف مشترك `Periods.kt` (كانت private في الرئيسية والميزانية محتاجاها).
- **مفتاحين نصوص جداد** (نصوص «تقريبي…» كانت جوه `loadBudgetScreen.ts` مش متنقلة): `BUDGET_SPENT_NEEDS_REVIEW` و`BUDGET_SPENT_ESTIMATED` — العربي نفس النص القديم بالحرف، والإنجليزي **مستني مراجعة المالك** زي باقي الجدول (OVERRIDES §40).

**اتفحص إزاي:**
- الـ15 حالة اتبص جواها (درس §2): 5 من غير ميزانية، 2 سقف null، 8 بسقف، 3 مصروفها مجهول، النوطتين الجداد ظهروا (5 «محتاجة تأكيد» + 1 «اتحدد تلقائي»)، متوسط معروف في 8، شذوذ محسوم في 4، و17 سطر تصنيف بحالة سقف من 23.
- تشغيل كامل بـ`--rerun`: **141 اختبار، صفر فشل** (قلب 138 + استخدامات 3) — من JUnit XML.
- **الفحص بيمسك الغلط:** قلبت `spentKnown` في ملف المرجع ⇒ فشل وقال «أول فرق عند .spentKnown». ⚠️ وأول محاولة استرجاع بالاستبدال العكسي كانت غلط (كانت هتضرب أول `true` حقيقية) — الاسترجاع الصح **بإعادة التوليد** (`npm run golden` حتمي)، والاختبارات عدّت بعدها.

**إيه اللي ما اتجربش:** اختبارات الآيفون (GitHub Actions لما يترفع)، ومفيش شاشة ولا تخزين — طبقة استخدامات بس.

**الخطوة الجاية:** ~~حالة استخدام الاستيراد~~ اتعملت (القسم اللي تحت).

**أسئلة مستنية المالك:** مفيش جديد. النصين الإنجليزيين الجداد دخلوا قايمة المراجعة الموجودة أصلًا.

## الخطوة اللي بعدها: تدفق الاستيراد كامل (معاينة → التزام) — 2026-09-22

**اتعمل إيه:**
- `scripts/golden/importFlowGolden.ts` ⇒ `native-app/golden/importFlow.json`: **14 حالة مكتوبة بالإيد** (مش عشوائية — كل درجة تكرار وكل خطأ التزام ليهم حالة باسمها): معاينة نظيفة بصفوف غلط · التزام افتراضي · اختيار صريح + تصنيف مختار (بيتحفظ مؤكد) · مكرر/تعارض/متشابه ضد الموجود · اختيار تعارض ⇒ رفض · التزام متشابه بقرار صريح · اختيار مكرر ⇒ رفض · تغيير المحتوى بعد المعاينة ⇒ رفض · رقم سطر مش موجود ⇒ رفض · ملف متسجل ببصمته وكله مكرر ⇒ رجوع الدفعة القديمة · بصمة قديمة (hashContent) وفيه صفوف جديدة ⇒ بيكمل · المخطط القديم بمفتاح الرصيد (سجل واحد بيبلع صف واحد، والمبلغ الأصلي OVERRIDES §32 هو اللي بيتطابق) · مسار الصفوف الجاهزة (PDF) · مصدر رسايل. **المراجع (تجار/تصنيفات/قواعد) جوه مدخل كل حالة** عشان الملف مكتفي بذاته.
- في كوتلن: `ImportTypes.kt` + `ImportPreviewStage.kt` + `ImportStatement.kt` (بروتوكول staged→committed بنفس ترتيب الكتابة: الدفعة ← السجلات ← العمليات ← العلامة) + `MemoryImportRepositories.kt` (سجلات المصدر والدفعات والقواعد) + `MemorySupport.kt` (`MemoryUnitOfWork` بلقطة واستعادة، `SequentialIdGenerator`، `FixedClock`) + مستودع العمليات بقى Snapshotable.
- **إضافة في core:** `jsTrim` دالة عامة بتفضح `JsText.trim` (الكائن داخلي عن قصد) — مرحلة المعاينة محتاجاها لمفتاح المرجع المكرر داخل الملف.

**اتفحص إزاي:**
- تشغيل كامل بـ`--rerun`: **142 اختبار، صفر فشل** (قلب 138 + استخدامات 4) — من JUnit XML.
- المطابقة **بالحرف** شاملة رسايل الخطأ العربية وبصمات SHA-256 والمعرّفات التسلسلية ومحتوى المخزّن بعد الالتزام (الدفعات والعمليات والسجلات بترتيب الإدخال).
- **الفحص بيمسك الغلط:** حوّرت `transactionId` واحد في ملف المرجع ⇒ فشل وقال «أول فرق عند .storedSourceRecords[1].transactionId»، والاسترجاع بإعادة التوليد.

**إيه اللي ما اتجربش:**
- **التراجع الفعلي لوحدة العمل عند فشل وسط الكتابة** (`MemoryUnitOfWork.restore`) — ملف المرجع مفيهوش حالة فشل جوه `uow.run` لأن التطبيق الحالي برضه ما بيولّدهاش؛ محتاج اختبار كوتلن مكتوب بالإيد بمستودع بينفجر عمدًا (زي `atomicity.test.ts` في التطبيق الحالي).
- اختبارات الآيفون (GitHub Actions لما يترفع).

**الخطوة الجاية:** ~~اختبار الذرّية المكتوب بالإيد~~ اتعمل (تحت)، وبعده `revertImportBatch` و`resumeStagedBatch`.

**أسئلة مستنية المالك:** مفيش جديد من الخطوة دي.

## الخطوة اللي بعدها: اختبار ذرّية الالتزام بالإيد — 2026-09-22

- `ImportAtomicityTest.kt` (اختبارين): مستودع عمليات **بينفجر عمدًا** وقت `saveMany` — بعد ما الدفعة والسجلات اتكتبوا فعلًا (نقل فكرة `tests/acceptance/atomicity.test.ts`).
  1. **بوحدة عمل حقيقية** (`MemoryUnitOfWork`): بعد الفشل صفر دفعات وصفر سجلات وصفر عمليات، وإعادة المعاينة ما بتشوفش «استيراد سابق».
  2. **من غير تراجع** (`PassthroughUnitOfWork`): الدفعة بتفضل `staged` والسجلات موجودة ومفيش ولا عملية — وده بيثبت إن الانفجار حصل بعد الكتابتين الأولانيين فعلًا، وإن `findByFileHash` بيشوف `committed` بس (ARCHITECTURE §11.1).
- الانفجار بالتفويض (delegation) حوالين `MemoryTransactionRepository` — من غير ما المستودع المشترك يبقى قابل للوراثة.
- **الفحص:** تشغيل كامل — **144 اختبار، صفر فشل** (قلب 138 + استخدامات 6، من JUnit XML).
- **الخطوة الجاية:** ~~`revertImportBatch` و`resumeStagedBatch`~~ اتعملوا (تحت).

## الخطوة اللي بعدها: التراجع عن دفعة + تنظيف المعلّق — 2026-09-22

**اتعمل إيه:**
- `scripts/golden/revertFlowGolden.ts` ⇒ `native-app/golden/revertFlow.json` (14 حالة بالإيد، وظيفتين): **التراجع** — خطة بأربع مصاير (بتتمسح · عليها تسوية · مربوطة بشخص/أصل التزام · ليها مصدر تاني) · تنفيذ بيمسح ويعلّم `reverted` والسجلات بتتمسح كلها · السجل الأحدث الأول · أخطاء (مش موجودة · متراجَع عنها · محجوبة `blocked` لما العمليات متسجلة وولا سجل مصدر — الخطة بتقولها والتنفيذ بيرفض). **التنظيف** — `findStaged` بحد الفحص · `cleanup` بيمسح اللي تحت الدفعة المعلّقة ويعلّمها · رفض غير المعلّق والمش موجود · `cleanupAll` بيكمّل حتى لو دفعة فشلت.
- في كوتلن: `RevertImportBatch.kt` + `ResumeStagedBatch.kt` + `MemoryPeopleRepositories.kt` (الالتزامات والتسويات) + `RevertFlowGoldenTest.kt`.
- **مؤجل بسبب مكتوب:** مسار «نسخة قبل الحذف» (`RepairBackupPort` + `saveVerifiedBackup`, HANDOVER §36) محتاج منفذ ملفات الجهاز — بيتوصّل مع طبقة التخزين. السلوك الحالي = التطبيق الحالي لما `deps.backup` غايبة (نفس المسار بالظبط)، والناتج بيرجّع `backup: null`.

**اتفحص إزاي:** تشغيل كامل — **146 اختبار، صفر فشل** (قلب 138 + استخدامات 8، من JUnit XML). التحوير بيمسك: غيّرت `decision` في ملف المرجع ⇒ فشل عند `.plan.toKeep[2].decision`، ورجّعت بإعادة التوليد.

**الخطوة الجاية:** ~~التصنيف الجماعي وتحديد النوع والإضافة اليدوية~~ اتعملوا (تحت).

## الخطوة اللي بعدها: تحديد النوع + التصنيف الجماعي + الإضافة اليدوية — 2026-09-22

**اتعمل إيه:**
- `scripts/golden/txnEditGolden.ts` ⇒ `native-app/golden/txnEdit.json` (**24 حالة** بالإيد، تلات وظايف):
  - **تحديد النوع** (6): تلخيص بالأربع مجموعات (قاطع «سحب نقدي» · مرجّح «تقسيط» ⇒ needsLook · غامض · مؤكد بيتعد بس) · تحديد فردي · عملية مش موجودة · «مرتب» على حركة صادرة ⇒ رفض التناقض · تأكيد جماعي بيطبّق القاطع بس ويعدّ الباقي «متخطى».
  - **التصنيف الجماعي** (3): خطة (المؤكد بيتسكّت · التاجر المؤكد بيتغلب على القاعدة · قاعدة · عمود الملف · نفس التصنيف مفيش تغيير · مفيش دليل ⇒ مراجعة) · تطبيق بـ`reviewState: suggested` · تأكيد المستخدم بيرفع الحماية.
  - **الإضافة اليدوية** (15): شراء عادي · محفظة كاش (الوسم تلقائي + تصنيف مختار ⇒ مؤكد + قص الملاحظة) · تحويل داخلي بطرفين · و12 حالة رفض (صفر وسالب، تاريخ غلط، محفظة مش موجودة، «غير محدد»، التحويل من غير هدف/لنفسها/هدف مش موجود/عملتين، هدف على غير تحويل، اسم >120، ملاحظة >1000). حالة «المبلغ مش عدد صحيح» مش موجودة عن قصد — نظام أنواع كوتلن بيمنعها وقت الترجمة.
- في كوتلن: `SetEconomicKind.kt` + `CategorizeTransactions.kt` + `AddTransaction.kt` + `MemoryWalletRepository.kt` + `TxnEditGoldenTest.kt`. المراجع والمحافظ جوه مدخل كل حالة (الملف مكتفي بذاته).

**اتفحص إزاي:** تشغيل كامل — **149 اختبار، صفر فشل** (قلب 138 + استخدامات 11، من JUnit XML). التحوير بيمسك: غيّرت `skipped` في نتيجة التأكيد الجماعي ⇒ فشل عند `.result.skipped`، والاسترجاع بإعادة التوليد.

**الخطوة الجاية:** ~~تعديل العملية وملخص الكاش ومطابقة الرصيد~~ اتعملوا (تحت).

## الخطوة اللي بعدها: تعديل العملية + ملخص الكاش + مطابقة الرصيد — 2026-09-22

**اتعمل إيه:**
- `scripts/golden/editDetailGolden.ts` ⇒ `native-app/golden/editDetail.json` (**30 حالة** بالإيد، تلات وظايف):
  - **تعديل العملية** (23): تحميل التفاصيل (الرابط لوسم مش موجود بيتفلتر) · تغيير التصنيف (المختار بيتأكد؛ null بيشيله ويرجّع «محتاجة مراجعة») · تعديل المبلغ (أول تعديل بيحفظ الأصلي مرة واحدة والتاني ما بيلمسوش؛ أقل من المتوزع/المتسوّى بيه ⇒ رفض برسالة core) · الملاحظة (قص، الفاضية بتتشال، >1000 رفض) · وسم الكاش والاستبعاد · الوسوم (جديد بيتعمل، الموجود بالاسم المطبّع بيتعاد استعماله، المكرر على نفس العملية بيتساب، فاضي/طويل رفض، الشيل، الترتيب العربي، `tagsFor` لمجموعة).
  - **ملخص الكاش** (3): يوم راتب 28 (العمليات قبل الفترة — الرصيد بيحسبها والفترة لأ) · يوم راتب 1 (مصروف الفترة والتوزيعات والتقديري والموسوم كاش والتحويل الداخلي الداخل) · مفيش محفظة كاش ⇒ `null` مش صفر.
  - **مطابقة الرصيد** (4): سلسلة سليمة بقراية فترة فترة (`periodsRead` معلن) · مطابقة محفظة الكاش (الطرف الداخل من التحويل وارد عليها ورصيد المصدر ما بيتقارنش بيه) · محفظة مش موجودة ⇒ خطأ · رصيد معلن غلط ⇒ فرق بمكانه وعدد حركات يومه.
- في كوتلن: `EditTransaction.kt` + `LoadCashSummary.kt` + `ReconcileBalanceUseCase.kt` + `EditDetailGoldenTest.kt`، وفي core: `arabicCompare` عامة (بتفضح `compareArabic` الداخلية — لترتيب الوسوم زي `localeCompare('ar')`).
- **مؤجل بسبب مكتوب:** نداء `onCategoryConfirmed` (مساهمة قاعدة التجار المشتركة §25.1) موجود في التوقيع بس مش في ملف المرجع — «ولّع وانسى» بيتوصّل مع طبقة البيانات، وفشله بيتبلع فمش بيأثر على أي ناتج.

**اتفحص إزاي:** تشغيل كامل — **152 اختبار، صفر فشل** (قلب 138 + استخدامات 14، من JUnit XML). درس اتلقط وقت البناء: جافاسكربت بتسقط مفتاح `reference` الغايب من JSON وكوتلن كانت بتكتبه null — الفحص مسكها قبل ما تعدي. والتحوير بيمسك: غيّرت `differenceMinor` في فرق المطابقة ⇒ فشل بمكانه، والاسترجاع بإعادة التوليد.

**الخطوة الجاية:** ~~الأشخاص والديون~~ اتعملت (تحت).

## الخطوة اللي بعدها: الأشخاص والديون والأمانات — 2026-09-22

**اتعمل إيه:**
- `scripts/golden/peopleFlowGolden.ts` ⇒ `native-app/golden/peopleFlow.json` (**21 حالة** بالإيد): القايمة بأرصدتها (مفيش تقاص بين «لك» و«عليك»، القرض والأمانة منفصلين، المتسدد بالكامل بيختفي من النشط، الترتيب بالرصيد والمؤرشف آخر واحد) · إضافة شخص (قص، >80 رفض، الاسم المكرر رفض) · أرشفة (مفيش حذف) · ربط عملية بشخص (دين بيعمل التزام + تخصيص، هدية تخصيص بس، تجاوز قيمة العملية رفض) · التسوية (عادية، **معرّف طلب متكرر بيرجّع نفس التسوية من غير خصم تاني**، نفس المعرّف بمبلغ مختلف رفض، الزيادة عن المتبقي رفض بتفسير، معرّف غلط رفض، شخص غلط رفض) · دين قديم من غير عملية (OVERRIDES §27 — `originTransactionId = null` ومفيش عملية وهمية).
- في كوتلن: `ManagePeople.kt` + منفذ `SettlementWriter` + `MemoryPersonRepository` و`MemorySettlementWriter` (فوق `prepareSettlement` بتاعة core) + `PeopleFlowGoldenTest.kt`.

**اتفحص إزاي:** تشغيل كامل — **153 اختبار، صفر فشل** (قلب 138 + استخدامات 15، من JUnit XML). التحوير بيمسك: غيّرت رصيد أمانة في القايمة ⇒ فشل بمكانه، والاسترجاع بإعادة التوليد.

**الخطوة الجاية:** ~~القواعد والاشتراكات~~ اتعملوا (تحت).

## الخطوة اللي بعدها: القواعد والتجار + الاشتراكات — 2026-09-22

**اتعمل إيه:**
- `scripts/golden/rulesFlowGolden.ts` ⇒ `native-app/golden/rulesFlow.json` (**27 حالة** بالإيد، وظيفتين):
  - **القواعد والتجار** (21): القايمة بترتيب التطبيق (المتساويين بترتيب الحروف العربي، والتصنيف المحذوف بيتعلم «تصنيف محذوف» مش بيختفي) · إضافة قاعدة (الأولوية التلقائية آخر واحد max+10، المكررة بالنص المطبّع ونفس الطريقة رفض) · تعديل وقفل (**مفيش حذف**) · تثبيت تصنيف تاجر وشيله · إعادة تسمية (الاسم المطبّع **ما بيتغيّرش** — مفتاح المطابقة) · أسماء بديلة (المربوط بتاجر تاني رفض صريح).
  - **الاشتراكات** (6): تحميل الشاشة (قراية 12 شهر بطلبات شهرية، المرشحين من `detectRecurring` واللي متسجل بيتفلتر بمفتاح `name:X|عملة`، وملخص كل خطة) · حفظ جديد بمعرّف مشتق ثابت · تعديل موجود · معرّف مش موجود رفض · الخدمة المكررة بنفس العملة رفض · دورة غلط رفض من `validateRecurring`.
- في كوتلن: `ManageRules.kt` + `ManageRecurring.kt` + منفذ `RecurringRepository` + `MemoryRecurringRepository` + `RulesFlowGoldenTest.kt`.
- ⚠️ درس اتلقط بالفحص (§2): أول بذرة للاشتراك المتسجل كانت بمفتاح `spotify` بدل `name:SPOTIFY` — فالمرشح ما اتفلترش والمدفوع طلع صفر. الشكل الصح لمفتاح الخدمة هو ناتج `recurringKey` نفسه.

**اتفحص إزاي:** تشغيل كامل — **155 اختبار، صفر فشل** (قلب 138 + استخدامات 17، من JUnit XML). التحوير بيمسك: غيّرت `paidCount` ⇒ فشل بمكانه، والاسترجاع بإعادة التوليد.

**الخطوة الجاية:** المشاريع والتصنيفات والاستثمار ورسايل البنك والنسخة الشاملة — بنفس الطريقة، واحدة واحدة.
