# خطة النقل لأندرويد نيتف (Kotlin) — مسودة للمراجعة

> 2026-09-18 · Claude · **مسودة — مفيش ولا سطر كوتلن اتكتب، ومفيش أي دفع.** قرارات المالك الأربعة اتاخدت 2026-09-19 (OVERRIDES §35 و§8 هنا). أي قرار جديد بيتسأل ومحدش يحسمه لوحده (CLAUDE.md #17).
> المرجع: OVERRIDES §29 (قرار المالك: «نخلّص كل حاجة متفقين عليها بالوضع الحالي، ثم نقوم بالنقل لكوتلن، ومن بعدها هناك هنضيف مميزات تانية»).

## ⚠️ تحديث 2026-09-19 — الهدف بقى أندرويد + آيفون (OVERRIDES §38) — الخطة دي لازم تتراجع قبل أي بناء
- **الهدف الجديد (نص المالك):** «تطبيق ينفع يشتغل على جوجل بلاي وآيفون (حتى لو كل الفيتشرز مش هتشتغل على آيفون زي قراءة الرسايل)، ويكون سريع وقوي ومحمي كويس، باشتراكات». الاشتراكات = **مجاني بحدود**، والمميزات كلها باشتراك.
- **الإطار: كوتلن للجهازين** (Kotlin Multiplatform + Compose Multiplatform) — اختيار المالك، **مش** المقترح (المقترح كان React Native عشان الـ12 ألف سطر حسابات يتنقلوا زي ما هم). ⇒ الحسابات بتتكتب كوتلن من أول وتتطابق بالاختبارات بالهللة (§3 زي ما هو).
- **التصميم الأول:** المالك بيصمم الشاشات في Figma وبيربطها (نموذج تفاعلي) **قبل** بناء الشاشات، وClaude بيقرا التصميم بربط Figma ويبنيه. ده بيلغي §37 («التصميم بعد كوتلن»).
- **اللي بيتغير في جدول §2 (مسودة، بيتراجع مع المالك):**
  - الواجهة Compose Multiplatform بدل Jetpack Compose بس. التخزين Room (بيدعم الجهازين من نسخة 2.7) — قرار §35 بيفضل.
  - فايربيز على الجهازين: مكتبة GitLive (طرف تالت بتلف مكتبات فايربيز الأصلية) أو كود لكل جهاز — **محتاج تجربة قبل القرار**.
  - اللي خاص بكل جهاز (`expect/actual`): الرسايل (أندرويد بس)، القفل (BiometricPrompt / Face ID)، قراية PDF (PdfBox-Android / PDFKit)، الشغل في الخلفية (WorkManager / BGTaskScheduler).
  - **آيفون من غير رسايل:** بديل يتجرب — أتمتة «الاختصارات» على رسالة البنك بتبعت نصها للتطبيق (App Intent). وإلا: كشف الحساب والإضافة باليد.
  - **الاشتراكات:** دفع جوجل بلاي وأبل (إلزامي)، وRevenueCat (عنده مكتبة كوتلن للجهازين). التحقق محتاج سيرفر ⇒ باقة Blaze — **قرار مستني المالك** (القاعدة 12).
  - الآيفون محتاج حساب مطور أبل (99$ في السنة) وبناء على ماك أو خدمة بناء سحابية.
- **❓ مستني المالك:** أنهي مميزات مجانية وأنهي باشتراك؛ باقة Blaze؛ أنهي مكتبة فايربيز بعد التجربة.

## 0. ليه، وإيه اللي بيتنقل
- **شكوى المالك (§29):** بطء الفتح أول مرة، بطء بعد الرجوع للتطبيق، التمرير والحركة مش ناعمين، والشكل نفسه. السبب الأساسي: التطبيق الحالي React جوه WebView (Capacitor) + مكتبة فايربيز بتاعة الويب.
- **اللي بيتنقل:** كل الموجود في 1.43 — الرئيسية، العمليات، الإضافة (يدوي، رسايل البنك، كشف CSV وPDF)، الميزانية والتحليل، الأشخاص والديون، الاستثمار، التصنيفات والقواعد والتجار والشعارات، المشاريع، الإعدادات والقفل والنسخة الشاملة.
- **الحجم الحالي:** حوالي 26,700 سطر TypeScript (domain 6.4 ألف، application 5.6 ألف في 43 حالة استخدام، infrastructure 4.4 ألف، الشاشات 8.9 ألف، التجميع 1.4 ألف) + 8 ملفات Java أصلية (الرسايل، الملفات، القفل) + **738 اختبار** في 81 ملف.

## 1. اللي مش هيتغير
1. **نفس مشروع فايربيز ونفس البيانات:** Firebase Auth + Firestore بنفس المجموعات تحت `users/{uid}` ونفس شكل المستندات ⇒ **نفس الحساب يشتغل على التطبيقين، ومفيش ترحيل بيانات**. نفس `firestore.rules`.
2. **الفلوس أعداد صحيحة بالهللة** (`Long`)، ممنوع `Double` في أي حساب (CLAUDE.md #1).
3. **نفس الطبقات:** `domain` (Kotlin نقي، ولا Android ولا فايربيز) ← `data` (فايربيز والتخزين المحلي والاستيراد) ← `ui` (الشاشات). الشاشة ما بتحسبش مبلغ.
4. **كل قرارات OVERRIDES** (الشهر المالي من يوم الراتب، «تقريبي»، التصنيفات بالمجموعات، الثيم الأخضر، المشاريع…) بتتنقل زي ما هي.
5. **الاختبارات الحالية هي المرجع:** النسخة الجديدة لازم تطلع نفس الأرقام بالظبط (§3).

## 2. التقنيات المقترحة (كل واحدة محتاجة سبب في ARCHITECTURE قبل ما تتضاف)
| الحاجة | المقترح | ليه |
|---|---|---|
| اللغة والواجهة | Kotlin + Jetpack Compose (Material 3) | اقتراح spec/07 وتوصية جوجل؛ حركة وتمرير أصلي |
| الحالة | ViewModel + StateFlow | الشاشة بتعرض بس، والحساب في الـuse cases |
| التخزين على الموبايل | **Room (قاعدة SQLite على الجوال) + مزامنة مع فايربيز** | **قرار المالك (OVERRIDES §35)** — الشاشات بتقرا من الجوال، والمزامنة في الخلفية (§2.1) |
| التجميع (DI) | يدوي زي `container.ts` | من غير مكتبة إضافية (CLAUDE.md #8) |
| كشف الراجحي PDF | PdfBox-Android (Apache 2.0) | بديل pdf.js؛ **لازم يعدّي اختبار الكشف الحقيقي بصفر فرق** |
| رسايل البنك | نقل كود الجافا الحالي لكوتلن | الاستقبال والطابور المحلي والحجب شغالين ومتجربين |
| القفل | `androidx.biometric` | نفس المستعمل دلوقتي (ARCHITECTURE §27) |
| شغل في الخلفية | WorkManager | الأسعار، ولمّ الرسايل الفايتة |
| الرسومات | Compose Canvas | من غير مكتبة رسومات |
| الشعارات | Coil (تحميل صور) | الشعارات الأونلاين من براند فيتش |

### 2.1 تصميم المزامنة (Room ⇄ فايربيز) — على الورق، بيتراجع مع المالك قبل البناء
- **فايربيز يفضل هو الحكم:** الجوال بيشتغل من نسخته المحلية، والتعارض بيتحل على السيرفر.
- **الجداول:** جدول في Room لكل مجموعة في فايربيز (نفس المعرّفات ونفس الحقول)، + `outbox` للتعديلات اللي لسه ما اتبعتتش، + `sync_state` (آخر مزامنة لكل مجموعة).
- **القراية:** الشاشات بتقرا من Room بس (فورًا حتى من غير نت). مستمعين فايربيز (snapshot listeners) بيحدّثوا Room في الخلفية، والحذف بيوصل كـ«removed».
- **أول تشغيل:** تنزيل كامل مرة واحدة بشريط تقدم (حساب المالك حوالي ألفين عملية + الباقي).
- **الكتابة:** التعديل بيتكتب في Room و`outbox` في معاملة واحدة (الشاشة بتتحدث فورًا)، وشغل في الخلفية (WorkManager) بيبعته لفايربيز بالترتيب ويشيله من `outbox` بعد النجاح. **المعرّفات ثابتة** ⇒ إعادة الإرسال بعد انقطاع ما بتكررش.
- **التعارض:** آخر كتابة تكسب على مستوى المستند (زي فايربيز دلوقتي). **العمليات اللي ليها شرط** (تسوية دين، استعادة نسخة شاملة) **محتاجة نت** وبتتعمل كمعاملة على السيرفر — من غير نت بتقول «محتاج نت» بدل ما تخمّن.
- **كشف الانحراف:** عدد المستندات لكل مجموعة بيتقارن بالسيرفر دوريًا، وزرار «مزامنة كاملة من السيرفر» في الإعدادات.
- **اختبارها قبل أي شاشة بتكتب:** Firestore Emulator بعميلين + قطع النت في النص + إعادة تشغيل التطبيق + تعديل نفس المستند من الجهازين.
- **كاش فايربيز الداخلي:** يتقفل أو يتصغّر عشان البيانات ما تتخزنش مرتين — يتقرر بقياس وقت البناء.

## 3. طريقة الشغل: شريحة شريحة، والمطابقة بالأرقام
- **ملفات مرجعية (golden files):** سكربت بيشغّل حالات الاختبارات الحالية ويطلع مدخلاتها ونتايجها JSON. اختبارات كوتلن بتقرا نفس الملفات ولازم تطلع **نفس النتايج بالهللة**: الفلوس، الفترات، الدفتر (ledger)، التصنيف، منع التكرار، قراية CSV وPDF والرسايل، الميزانية، الأشخاص، الاستثمار، فحص النسخة الشاملة، المشاريع.
- **التطبيق الجديد جنب القديم** وقت البناء (قرار المالك §35): اسم تطبيق مختلف (`app.masroufy.native`) فيتثبتوا الاتنين على نفس الجوال بنفس الحساب. **تجنّب التعديل من الاتنين في نفس اللحظة** (التسويات محمية ذريًا، الباقي لأ).
- **كل شريحة:** domain + اختباراتها ← data ← الشاشة ← APK ← المحاكي ← تجربة المالك ← commit وتسليم. نفس قواعد الشغل الحالية.
- **مكان المشروع:** فولدر جديد `native-android/` (مشروع Gradle لوحده). فولدر `android/` الحالي بتاع Capacitor بيفضل لحد التبديل.

## 4. ترتيب الشرايح
1. **الهيكل:** مشروع Gradle، نفس مفتاح التوقيع بتاع التجربة، الدخول بالإيميل، الثيم الأخضر وخط تشانجا، الشريط السفلي بخمس خانات.
2. **الرئيسية (قراية بس):** الفترة من يوم الراتب، ما تم صرفه وتقريبي، الدخل، المتبقي، الكاش، أحدث العمليات — من نفس البيانات. **أول قياس:** وقت الفتح والرجوع قدام التطبيق الحالي. لو مش أسرع بوضوح ⇒ نقف ونراجع قبل ما نكمل.
3. **العمليات:** القايمة بالأيام، البحث، النقط التلاتة، صفحة العملية (التصنيف، المبلغ، الوسوم، الملاحظة، الكاش والاستبعاد).
4. **الإضافة:** عملية يدوية، رسايل البنك، الكشف CSV وPDF (أصعب جزء: كشف الراجحي الحقيقي بصفر فرق وسلسلة الرصيد).
5. **الميزانية والتحليل:** السقوف، المصروف بالمجموعات، آخر ست فترات، التوقع والمتاح اليومي.
6. **الأشخاص والديون** (بالتسويات الذرية) **والاستثمار** والأسعار.
7. **التصنيفات والقواعد والتجار** (القاعدة المشتركة والشعارات) **والمشاريع**.
8. **الإعدادات:** الحساب وأسئلة البداية، المحافظ، القفل، النسخة الشاملة والاستعادة، وزرار «مزامنة كاملة من السيرفر». أدوات الإصلاح القديمة **مش هتتنقل** — بتفضل في التطبيق القديم (قرار المالك §35).
9. **التبديل:** كل الملفات المرجعية مطابقة + استعمال متوازي من المالك ⇒ النيتف يبقى الأساسي والقديم يتشال.

## 5. بعد النقل — المميزات الجديدة هناك (§29)
ذكر المعالين، مميزات حسب النوع، الأقساط، الدخول بجوجل، النشر على جوجل بلاي، الإشعارات والتطبيق مقفول. الصوت **مستبعد** بطلب المالك (§33).

## 6. التكاليف — ولا حاجة فيها بتتدفع من غير المالك
- **البناء نفسه: 0** (أدوات أندرويد وكوتلن مجانية، ونفس فايربيز المجاني).
- **جوجل بلاي:** 25$ مرة واحدة + تجربة مغلقة 12 مختبِر × 14 يوم للحساب الشخصي الجديد (§29).
- **فايربيز Blaze:** بس لو احتجنا خدمة سيرفر (الإشعارات والتطبيق مقفول)؛ غالبًا 0$ للاستخدام القليل بس محتاج كارت (§29).

## 7. المخاطر
0. **⚠️ المزامنة (قرار المالك §35):** أكبر شغل وأكبر خطر في النقل — غلطة فيها ممكن تخلّي الجوال يعرض أرقام قديمة أو يضيّع تعديل. الحماية في §2.1: فايربيز هو الحكم، معرّفات ثابتة، `outbox` مرتب، اختبارات بعميلين وانقطاع، وزرار مزامنة كاملة.
1. **⚠️ صلاحيات الرسايل على جوجل بلاي:** `READ_SMS` و`RECEIVE_SMS` صلاحيات مقيدة في بلاي — محتاجة نموذج إعلان وموافقة جوجل وممكن تترفض. **لازم نتأكد من السياسة الحالية قبل قرار النشر.** البدايل لو اترفضت: التوزيع بملف APK زي دلوقتي، أو قراية إشعارات تطبيق البنك بدل الرسايل، أو مشاركة الرسالة للتطبيق.
2. **كشف PDF:** مكتبة مختلفة ممكن تقرا النص بترتيب مختلف ⇒ الاختبار الحقيقي هو الحكم.
3. **الوقت:** 26 ألف سطر؛ الترتيب في §4 بيخلّي أي نقطة وقوف فيها تطبيق شغال (القديم بيفضل موجود).
4. **تطبيقين على نفس الحساب** وقت النقل: تعديل من الاتنين في نفس اللحظة ممكن يكتب فوق بعض (غير التسويات).

## 8. قرارات المالك (نافذة 2026-09-19 — OVERRIDES §35)
1. **التخزين على الموبايل:** ✅ «قاعدة منفصلة ومزامنة» ⇒ Room + مزامنة (§2.1). (المقترح كان كاش فايربيز الجاهز.)
2. **التطبيق الجديد:** ✅ جنب القديم لحد ما يتطابق.
3. **أدوات الإصلاح القديمة:** ✅ تفضل في القديم بس.
4. **البداية:** ✅ بعد ما المالك يجرب 1.43 على جواله.
- ❓ **مفتوح لسه:** تصميم المزامنة في §2.1 بيتعرض على المالك قبل أول شريحة بتكتب؛ وقرار النشر على بلاي بعد التأكد من سياسة الرسايل (§7).
