# خطة التطبيق الجديد للجهازين (Kotlin Multiplatform)

> مراجَعة 2026-09-19 · Claude · الأصل مسودة 2026-09-18 لأندرويد بس. **القرارات:** OVERRIDES §35 (التخزين والمزامنة، جنب القديم، أدوات الإصلاح) و**§38** (الجهازين، التصميم في Figma الأول، مجاني بحدود). أي قرار جديد بيتسأل ومحدش يحسمه لوحده (CLAUDE.md #17).
> **الحالة:** المالك وافق يبدأ الشغل اللي **مش مستني التصميم** دلوقتي (نافذة 2026-09-19): الخطة، ملفات المرجع، قلب التطبيق بكوتلن، وتجربة فايربيز قراية بس. **مفيش أي دفع.**

## 0. الهدف وليه
- **الهدف (نص المالك، §38):** «تطبيق ينفع يشتغل على جوجل بلاي وآيفون (حتى لو كل الفيتشرز مش هتشتغل على آيفون زي قراءة الرسايل)، ويكون سريع وقوي ومحمي كويس، باشتراكات» — **مجاني بحدود**.
- **شكوى المالك من الحالي (§29):** بطء الفتح أول مرة، بطء بعد الرجوع، التمرير مش ناعم، والشكل. السبب: React جوه WebView (Capacitor) + مكتبة فايربيز بتاعة الويب.
- **اللي بيتنقل:** كل الموجود في 1.46 — الرئيسية، العمليات، الإضافة (يدوي، رسايل البنك، كشف CSV وPDF)، الميزانية والتحليل، الأشخاص والديون، الاستثمار، التصنيفات والقواعد والتجار، المشاريع، الإعدادات والقفل والنسخة الشاملة.
- **الحجم الحالي (مقاس 2026-09-19):** domain 6,441 سطر، application 5,787، infrastructure 4,402، الشاشات 9,095 + 3,275 تنسيق، التجميع 1,380 — و**748 اختبار** في 83 ملف.
- **الإطار: Kotlin Multiplatform + Compose Multiplatform** — اختيار المالك (المقترح كان React Native عشان الحسابات تتنقل زي ما هي). ⇒ الحسابات بتتكتب كوتلن من أول، و**ملفات المرجع (§3) هي الضمان**.

## 1. اللي مش هيتغير
1. **نفس مشروع فايربيز ونفس البيانات:** Auth + Firestore بنفس المجموعات تحت `users/{uid}` ونفس شكل المستندات ونفس `firestore.rules` ⇒ نفس الحساب على التطبيقات كلها، ومفيش ترحيل.
2. **الفلوس أعداد صحيحة بالهللة** (`Long`)، ممنوع `Double`/`Float` في أي حساب (CLAUDE.md #1).
3. **نفس الطبقات:** `core` (كوتلن نقي: ولا أندرويد ولا آيفون ولا فايربيز) ← `data` (فايربيز والتخزين والاستيراد) ← `ui` (الشاشات). الشاشة ما بتحسبش مبلغ.
4. **كل قرارات OVERRIDES** بتتنقل زي ما هي.
5. **الاختبارات الحالية هي المرجع** (§3).

## 2. التقنيات (كل واحدة محتاجة سبب في ARCHITECTURE قبل ما تتضاف)
| الحاجة | الاختيار | ملاحظة |
|---|---|---|
| اللغة | Kotlin Multiplatform | كود واحد للجهازين؛ اللي خاص بجهاز بيتكتب `expect/actual` |
| الواجهة | Compose Multiplatform | **بعد تصميم المالك في Figma** — مفيش شاشات قبلها |
| الحالة | ViewModel + StateFlow | الشاشة بتعرض بس، والحساب في الـuse cases |
| التخزين على الجوال | Room (بيدعم الجهازين) + مزامنة مع فايربيز | قرار §35 — التصميم §2.1 |
| فايربيز | GitLive (طرف تالت بيلف المكتبات الأصلية) **أو** كود لكل جهاز | **يتقرر بتجربة قراية بس** (§4-أ٤) |
| التجميع | يدوي زي `container.ts` | من غير مكتبة (CLAUDE.md #8) |
| كشف PDF | PdfBox-Android / PDFKit (`expect/actual`) | لازم يعدّي اختبار الكشف الحقيقي بصفر فرق |
| رسايل البنك | **أندرويد بس**: نقل كود الجافا الحالي | آيفون: أتمتة «الاختصارات» ⇒ App Intent (يتجرب)، وإلا الكشف والإضافة باليد |
| القفل | BiometricPrompt / Face ID (`expect/actual`) | |
| الخلفية | WorkManager / BGTaskScheduler | الأسعار، ولمّ الرسايل، وإرسال الـoutbox |
| الاشتراكات | دفع جوجل بلاي وأبل + RevenueCat | **مستني المالك:** المجاني والمدفوع، وباقة Blaze للتحقق على السيرفر |
| الاختبارات | kotlin-test + kotlinx-serialization (قراية ملفات المرجع) | |

### 2.1 تصميم المزامنة (Room ⇄ فايربيز) — على الورق، بيتعرض على المالك قبل أول شريحة بتكتب
- **فايربيز هو الحكم:** الجوال بيشتغل من نسخته المحلية، والتعارض بيتحل على السيرفر.
- **الجداول:** جدول لكل مجموعة (نفس المعرّفات والحقول) + `outbox` للتعديلات اللي ما اتبعتتش + `sync_state` (آخر مزامنة لكل مجموعة).
- **القراية:** الشاشات بتقرا من Room بس. مستمعين فايربيز بيحدّثوا Room في الخلفية، والحذف بيوصل كـ«removed».
- **أول تشغيل:** تنزيل كامل مرة واحدة بشريط تقدم.
- **الكتابة:** Room و`outbox` في معاملة واحدة، والخلفية بتبعته بالترتيب وبتشيله بعد النجاح. **المعرّفات ثابتة** ⇒ إعادة الإرسال ما بتكررش.
- **التعارض:** آخر كتابة تكسب على مستوى المستند. **العمليات اللي ليها شرط** (تسوية دين، استعادة نسخة شاملة) **محتاجة نت** وبتتعمل كمعاملة على السيرفر.
- **كشف الانحراف:** مقارنة الأعداد بالسيرفر دوريًا، وزرار «مزامنة كاملة من السيرفر».
- **اختبارها قبل أي شاشة بتكتب:** Firestore Emulator بعميلين + قطع النت + إعادة التشغيل + تعديل نفس المستند من جهازين.

## 3. المطابقة بالأرقام: ملفات المرجع (golden files)
- سكربت في التطبيق الحالي (`scripts/golden/`) بيشغّل دوال `src/domain` على حالات ثابتة ويكتب مدخلاتها ونتايجها JSON في `native-app/golden/`. اختبارات كوتلن بتقرا نفس الملفات ولازم تطلع **نفس النتايج بالحرف والهللة**.
- **الحالات المرفوعة (المستودع عام):** fixtures و**بيانات وهمية بس**. المطابقة على كشف المالك الحقيقي (`files/`) بتتعمل **على الجهاز بس** ونتيجتها مش بتترفع.
- الحالات بتتولد من نفس الكود ⇒ لو اتغير سلوك في التطبيق الحالي، الملفات بتتولد تاني والفرق بيبان.

## 4. الترتيب
### (أ) دلوقتي — مش مستني التصميم (موافقة المالك 2026-09-19)
1. **الخطة دي** ✅.
2. **الهيكل:** مشروع Gradle في `native-app/` (لوحده، جنب `android/` الحالي)، وموديول `core` (كوتلن نقي) باختبارات على JVM على ويندوز.
3. **قلب التطبيق بالترتيب** — كل جزء بملفات مرجعه قبل اللي بعده: الفلوس والتطبيع ← الفترات (الشهر من يوم 28) ← الأنواع والدفتر (المصروف ونصيبك والتقريبي) ← منع التكرار ← التصنيف ← سلسلة الرصيد والمطابقة ← قراية CSV والرسايل ← الميزانية والتحليل ← الديون والتسويات ← الاستثمار ← المشاريع ← فحص النسخة الشاملة.
4. **تجربة فايربيز قراية بس** على حساب تجريبي أو المحاكي (مش حساب المالك من غير إذن) لاختيار المكتبة.
5. **الآيفون من غير ماك:** GitHub Actions على جهاز ماك (مجاني للمستودع العام) بيبني ويشغّل اختبارات `core` للآيفون. ⚠️ ما يترفعش أسرار أبدًا.

### (ب) مستني المالك
- **المزامنة اللي بتكتب:** تتعرض عليه الأول (§2.1).
- **الشاشات:** بعد تصميمه في Figma — شاشة شاشة، وClaude بيقرا التصميم بربط Figma وبيقول الحالات الناقصة (فاضي، بيحمّل، خطأ، «غير متاح»).
- **الاشتراكات:** المجاني والمدفوع، وباقة Blaze.
- **أول قياس بعد الرئيسية:** وقت الفتح والرجوع قدام التطبيق الحالي — لو مش أسرع بوضوح نقف ونراجع.
- **التبديل:** كل ملفات المرجع مطابقة + استعمال متوازي من المالك.

## 5. بعد النقل — المميزات الجديدة هناك (§29، §37)
الإشعار والتطبيق مقفول، الإضافة بالصوت، ذكر المعالين، مميزات حسب النوع، الأقساط، الدخول بجوجل وأبل، النشر في المتجرين.

## 6. التكاليف — ولا حاجة بتتدفع من غير المالك
- **البناء نفسه: 0.** الأدوات مجانية، وبناء الآيفون على GitHub مجاني للمستودع العام.
- **Figma:** مقعد كامل حوالي 16$ في الشهر (المالك).
- **جوجل بلاي:** 25$ مرة واحدة + تجربة مغلقة 12 مختبِر × 14 يوم للحساب الشخصي الجديد.
- **أبل:** 99$ في السنة — وقت النشر بس.
- **فايربيز Blaze:** للتحقق من الاشتراك على السيرفر؛ حسب الاستخدام وفيه حصة مجانية.

## 7. المخاطر
0. **الحسابات بتتكتب من أول:** غلطة هنا = أرقام غلط. الحماية: ملفات المرجع بالهللة قبل أي شاشة.
1. **المزامنة** (§2.1): أكبر شغل — ممكن تعرض أرقام قديمة أو تضيّع تعديل لو غلط.
2. **فايربيز على الآيفون:** المكتبة طرف تالت أو كود مزدوج — التجربة الأول.
3. **صلاحيات الرسايل على جوجل بلاي:** فيه استثناء «SMS-based money management» (اتأكد 2026-09-19) بنموذج إعلان ومراجعة من موظف، وممنوع ناخد رسايل مش مالية — وده اللي بنعمله (الفلتر قبل التخزين). الآيفون مفيهوش قراية رسايل خالص.
4. **كشف PDF** بمكتبتين مختلفتين ممكن يقروا النص بترتيب مختلف ⇒ الاختبار الحقيقي هو الحكم.
5. **تطبيقين على نفس الحساب** وقت النقل: تعديل من الاتنين في نفس اللحظة ممكن يكتب فوق بعض (غير التسويات).

## 8. قرارات المالك
- §35: التخزين «قاعدة منفصلة ومزامنة» ✅ · جنب القديم ✅ · أدوات الإصلاح في القديم ✅.
- §38: الجهازين بـ«كوتلن للجهازين» ✅ · التصميم في Figma الأول ✅ · «مجاني بحدود» ✅ · البدء في الشغل اللي مش مستني التصميم دلوقتي ✅.
- ❓ **مفتوح:** المجاني والمدفوع؛ باقة Blaze؛ مكتبة فايربيز بعد التجربة؛ تصميم المزامنة قبل أول شريحة بتكتب.
