# مصروفي — تعليمات المشروع

> يُقرأ آليًا في بداية كل جلسة Claude Code داخل هذا المجلد.

## اقرأ أولًا — بهذا الترتيب، وقبل أي سطر كود

1. **`OVERRIDES.md`** — الفروقات المعتمدة. **يعلو على كل ما عداه**
2. **`ARCHITECTURE.md`** — الطبقات وحدودها وحزمة التقنيات ومراحل البناء
3. **`FIREBASE.md`** — إعدادات مشروع فايربيز (مُنشأ فعلًا وجاهز)
4. **`design-source/masroofi-claude-code/spec/01…06`** — المواصفات التفصيلية: المنتج، المحاسبة، نموذج البيانات، الواجهة، الاستيراد، القبول
5. **`design-source/masroofi-claude-code/design/tokens.json`** — الألوان والمقاسات
6. **`design-source/masroofi-claude-code/design/masroofi-v4.html`** — المرجع البصري التفاعلي

`SPEC.md` و `DATA_MODEL.md` نسخة أقدم وأقل تفصيلًا — للتاريخ فقط، ولا يُبنى عليها عند التعارض مع (4).

`design-source/masroofi-claude-code/CLAUDE.md` و `START-HERE-AR.txt` كُتبا لمهمة أندرويد ملغاة — **لا تُتبع تعليماتهما**.

## عند أي تعارض

`OVERRIDES.md` أولًا، ثم `ARCHITECTURE.md`/`FIREBASE.md`، ثم `spec/`، ثم البقية.
لو التعارض غير مغطى في `OVERRIDES.md` — **اسأل، ولا تختر بنفسك**.

## التواصل

- المالك اسمه محمد، **عمره ما كتب كود**. اشرح بالعربي المصرية بصيغة المذكر
- كل مصطلح تقني بالعربي يُكتب جنبه بالإنجليزي
- لا تفترض معرفة بأوامر الطرفية — اكتب الأمر كاملًا جاهزًا للنسخ
- لو فيه غموض، اسأل قبل ما تنفذ

## القواعد الملزمة

1. **كل المبالغ أعداد صحيحة بالوحدة الصغرى** (integer minor units — الهللة). التحويل للريال في العرض فقط. **ممنوع floating point في أي حساب مالي**
2. **Clean Architecture بأربع طبقات:** `domain/` و `application/` و `infrastructure/` و `presentation/`. **الاعتماد يتجه للداخل فقط ولا ينعكس أبدًا**
3. **`domain/` لا يستورد من أي طبقة أخرى** — لا React ولا Firebase ولا `window`. و`application/` يعرف واجهات المستودعات (ports) فقط لا تنفيذها
4. **الشاشة لا تكلّم مستودعًا ولا Firestore مباشرة، ولا تحسب مبلغًا.** تنادي حالة استخدام (use case)
5. **كل منطق ثقيل في حالة استخدام مستقلة** في `application/useCases/` — لا داخل مكوّن React
6. **لكل مستودع تنفيذان:** `infrastructure/firestore/` للتشغيل و `infrastructure/memory/` للاختبار من fixtures. التجميع يدوي في `app/container.ts` بلا مكتبة حقن اعتماديات
7. **حد الملف 300 سطر.** الأطول يُقسم
8. **لا تضيف مكتبة بلا سبب مكتوب** في `ARCHITECTURE.md`
9. **بناء بشرائح رأسية** (vertical slices): من الشاشة للتخزين، تشتغل فعلًا، قبل الانتقال للتالية. **ممنوع** بناء كل الشاشات ثم ربطها
10. **لا رقم بلا مصدر.** إما محسوب أو يعرض «غير متاح». لا صفر مؤكد مكان المجهول
11. **لا تُخزَّن أرقام حسابات كاملة** في فايربيز — آخر أربعة أرقام فقط
12. **Cloud Functions غير متاحة** (باقة Spark). الأسعار عبر GitHub Actions
13. **commit بعد كل شريحة مكتملة**
14. **لا ادعاء نجاح لم يحدث.** ميزة تصميمها ظاهر ليست ميزة مكتملة
15. **fixtures أولًا** ثم البيانات الحقيقية

## أرصدة وإعدادات معتمدة

- الحساب البنكي (الراجحي): **4,837.83 ر.س @ 2025-01-01**
- محفظة الكاش: **1,000.00 ر.س @ 2026-09-06**
- الشهر المالي يبدأ يوم **28** (يوم الراتب)

## معايير قبول المرحلة الأولى

**١. حالات `spec/06-acceptance.md`** على `fixtures/` — كلها.

**٢. المجاميع المرجعية** على `files/transactions_full.csv`:

| البند | ر.س |
|---|---:|
| المدين | 302,171.45 |
| حركات ليست مصروفًا | 201,703.18 |
| المصروف الحقيقي | 100,468.27 |
| الدائن | 300,040.87 |

**٣. سلسلة الرصيد:** من 4,837.83 @ 2025-01-01 تنتهي عند **2,707.25 @ 2026-09-04**، ومطابقة عمود الرصيد في كل سطر بدقة الهللة.

**لا تكمل للمرحلة التالية قبل مطابقة الثلاثة.**

## عند أي قرار جديد

سجّله في `OVERRIDES.md` أو `ARCHITECTURE.md`. الشات ينسى، الملف لا ينسى.
