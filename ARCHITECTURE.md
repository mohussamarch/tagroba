# مصروفي — المعمارية (ARCHITECTURE)

> هذا الملف هو المرجع المعماري الملزم. أي شات أو مطور يشتغل على المشروع يقرأه أولاً.
> آخر تحديث: 2026-09-06

---

## 1. شكل التطبيق (Application shape)

**تطبيق ويب مثبت على الجوال — PWA (Progressive Web App)**

- كود واحد يشتغل على أندرويد وآيفون والويب
- يتثبت من المتصفح بـ Add to Home Screen فيظهر كأيقونة تطبيق
- لا يحتاج متجر تطبيقات ولا حساب مطور
- التوزيع = رابط

**سبب الاختيار:** بناء نسخة iOS أصلية (native) يتطلب جهاز Mac + حساب Apple Developer سنوي. الـ PWA يلغي الحاجزين، ويظل قابلاً للتغليف لاحقاً في تطبيق أصلي بدون إعادة كتابة.

---

## 2. حزمة التقنيات (Tech stack)

| الطبقة | التقنية | السبب |
|---|---|---|
| الواجهة | React + TypeScript | الأكثر دعماً ووثائق، و TypeScript يمنع أخطاء الأنواع مبكراً |
| البناء | Vite | سريع وإعداده بسيط |
| PWA | vite-plugin-pwa | يولّد manifest و service worker |
| التنسيق | CSS Modules أو Tailwind | يُحسم عند البدء |
| الحسابات | Firebase Authentication | جوجل + إيميل وكلمة سر |
| قاعدة البيانات | Cloud Firestore | مزامنة + عمل بدون إنترنت مدمج |
| الاستضافة | Firebase Hosting | مجاني على باقة Spark |
| الأسعار | GitHub Actions مجدولة → prices.json | مجاني بالكامل بدون بطاقة |
| الرسوم البيانية | مكتبة خفيفة (تُحسم عند البناء) | تجنب المكتبات الثقيلة |
| التحكم بالإصدارات | Git + GitHub | إلزامي من اليوم الأول |

**قاعدة:** لا تُضاف مكتبة إلا بسبب مكتوب. كل مكتبة زائدة دين تقني (technical debt).

---

## 3. الطبقات والحدود (Clean Architecture — نسخة عملية)

المعمارية المعتمدة هي **Clean Architecture** بقاعدتها الأساسية، مع حذف الطقوس التي لا تدفع ثمنها في مشروع بهذا الحجم.

### القاعدة الأساسية — قاعدة الاعتماد (The Dependency Rule)

**الاعتماد يتجه للداخل فقط.** الطبقة الداخلية لا تعرف شيئاً عن الطبقة الخارجية.

```
┌───────────────────────────────────────────────────┐
│ presentation/   الشاشات والمكوّنات                 │
│                 تعرض وتستقبل الضغطات فقط           │
└─────────────────────┬─────────────────────────────┘
                      │ ينادي
┌─────────────────────▼─────────────────────────────┐
│ application/    حالات الاستخدام (Use Cases)        │
│                 + واجهات المستودعات (Repository    │
│                   interfaces) — تعريفات فقط        │
└─────────────────────┬─────────────────────────────┘
                      │ ينادي
┌─────────────────────▼─────────────────────────────┐
│ domain/         الكيانات والقواعد والحسابات        │
│                 نقي تماماً — لا يعرف React ولا      │
│                 Firebase ولا المتصفح               │
└───────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────┐
│ infrastructure/ تنفيذ المستودعات على Firestore     │
│                 + محلل الكشف + التصدير + الأسعار   │
│                 يعتمد على application/ ولا يُعتمد   │
│                 عليه من أي طبقة داخلية             │
└───────────────────────────────────────────────────┘
```

**السهم لا ينعكس أبداً.** `domain/` لا يستورد من `application/`، و `application/` لا يستورد من `infrastructure/` ولا من `presentation/`.

### لماذا هذا مهم عملياً هنا

معيار القبول في `SPEC.md` يتطلب تشغيل منطق الاستيراد والتصنيف والاستبعاد على `transactions_full.csv` والحصول على 100,468.27 بالضبط — **دون فايربيز**. هذا مستحيل لو كان المنطق مرتبطاً بقاعدة البيانات مباشرة. الواجهات هي ما يجعل هذا الاختبار ممكناً.

وكذلك: لو تغيّرت قاعدة البيانات لاحقاً، `domain/` و `application/` لا يتغيران بحرف واحد.

### الطبقات بالتفصيل

**`domain/` — النواة**
- الكيانات كأنواع TypeScript خالصة (Entities)
- الحسابات والقواعد: `money`، `categorize`، `reconcile`، `budget`، `period`، `analytics`، `notifications`
- **دوال نقية** (pure functions) قدر الإمكان: نفس المدخل ينتج نفس المخرج، بلا آثار جانبية
- **ممنوع** استيراد React أو Firebase أو أي شيء من المتصفح

**`application/` — حالات الاستخدام والواجهات**

حالة استخدام واحدة = ملف واحد = مدخل واضح ومخرج واضح:

| حالة الاستخدام | المسؤولية |
|---|---|
| `ImportStatement` | قراءة الكشف، منع التكرار، إنشاء الدفعة |
| `RevertImportBatch` | التراجع الكامل عن دفعة |
| `CategorizeTransactions` | تطبيق ترتيب أولوية التصنيف |
| `ReconcileBalance` | مطابقة الرصيد الجاري بالكشف |
| `RecomputePeriodSummary` | إعادة حساب مستند ملخص الفترة |
| `EvaluateNotifications` | آلة حالات الإشعارات |
| `DetectCommitments` | اكتشاف الاشتراكات المتكررة |
| `ValueAssets` | تقييم الأصول بالأسعار المتاحة |

وواجهات المستودعات — **تعريفات بلا تنفيذ**:

```ts
export interface TransactionRepository {
  listByPeriod(periodKey: string): Promise<Transaction[]>
  findByDedupeKeys(keys: string[]): Promise<Transaction[]>
  saveMany(txns: Transaction[]): Promise<void>
  update(id: string, patch: Partial<Transaction>): Promise<void>
}
```

مثلها: `AccountRepository`، `MerchantRepository`، `CategoryRepository`، `RuleRepository`، `BudgetRepository`، `CommitmentRepository`، `AssetRepository`، `ImportBatchRepository`، `PeriodSummaryRepository`، `SettingsRepository`، `PriceSource`.

**`infrastructure/` — التنفيذ**
- `firestore/` — تنفيذ كل واجهة مستودع على Firestore
- `memory/` — تنفيذ في الذاكرة **للاختبار** (يقرأ من CSV)
- `import/` — محلل كشف الراجحي
- `export/` — التصدير والنسخ الاحتياطي
- `prices/` — قراءة `prices.json`

**`presentation/` — الواجهة**
- الشاشات والمكوّنات والثيم
- **تنادي حالات استخدام فقط.** لا تكلّم مستودعاً ولا Firestore ولا تحسب مبلغاً

### كيف تُوصَّل القطع (بدون حاقن اعتماديات)

حالة الاستخدام تستقبل ما تحتاجه كوسائط، ونقطة تجميع واحدة تبنيها:

```ts
// application/useCases/importStatement.ts
export function makeImportStatement(deps: {
  txns: TransactionRepository
  batches: ImportBatchRepository
  parser: StatementParser
}) {
  return async function importStatement(file: File, accountId: string) { /* ... */ }
}

// app/container.ts  — نقطة التجميع الوحيدة
export const importStatement = makeImportStatement({
  txns: new FirestoreTransactionRepository(db, uid),
  batches: new FirestoreImportBatchRepository(db, uid),
  parser: new AlRajhiParser(),
})
```

في الاختبار تُمرَّر تنفيذات `memory/` بدل `firestore/`. لا مكتبة حقن اعتماديات، ولا تعقيد.

### ما استُبعد عمداً من Clean Architecture الكاملة

| العنصر | السبب |
|---|---|
| حاقن اعتماديات (DI container) مثل InversifyJS | تمرير الوسائط اليدوي كافٍ لحجم المشروع، وبلا مكتبة إضافية |
| DTO ومحوّل (mapper) لكل كيان | يُستخدم فقط حيث يختلف شكل Firestore عن شكل الكيان (التواريخ مثلاً)، لا في كل مكان |
| مجلدات `interface-adapters` و `frameworks` المنفصلة | مدمجة في `infrastructure/` — نفس المعنى بملفات أقل |
| Presenters و ViewModels منفصلة | مكوّنات React تؤدي الدور |

**المبدأ:** نأخذ قاعدة الاعتماد والواجهات وحالات الاستخدام — وهي مصدر القيمة كلها. ونترك الطقوس التي تخدم فرقاً كبيرة لا مشروعاً بمطوّر واحد.

### القواعد التي لا تُكسر

1. **`domain/` لا يستورد من أي طبقة أخرى.** لا React، لا Firebase، لا `window`
2. **`application/` لا يستورد من `infrastructure/` ولا من `presentation/`.** يعرف الواجهات فقط
3. **الشاشة لا تكلّم مستودعاً ولا Firestore مباشرة.** تنادي حالة استخدام
4. **الشاشة لا تحسب مبلغاً.** كل حساب في `domain/`
5. **مصدر واحد للحقيقة.** أي رقم محسوب في مكانين = عطل مؤجل

### هيكل المجلدات

```
src/
├── domain/
│   ├── entities/         الأنواع فقط
│   ├── money.ts          حساب المبالغ بالهللة
│   ├── categorize.ts     منطق ترتيب الأولوية
│   ├── reconcile.ts      منطق الرصيد الجاري
│   ├── budget.ts         السقوف والمتوسطات والشذوذ
│   ├── period.ts         الشهر المالي والفترات
│   ├── analytics.ts      النسب والتوقع والتغطية
│   └── notifications.ts  آلة الحالات
├── application/
│   ├── useCases/         ملف لكل حالة استخدام
│   └── ports/            واجهات المستودعات ومصدر الأسعار
├── infrastructure/
│   ├── firestore/        تنفيذ المستودعات
│   ├── memory/           تنفيذ للاختبار من CSV
│   ├── import/           محلل الراجحي
│   ├── export/           التصدير والنسخ الاحتياطي
│   └── prices/           قراءة prices.json
├── presentation/
│   ├── screens/          شاشة لكل وجهة من السبع
│   ├── components/
│   └── theme/
└── app/
    ├── container.ts      نقطة التجميع الوحيدة
    └── routes.tsx
```

**حد الملف: 300 سطر.** الأطول يُقسم.

---

## 4. الأموال (Money) — قاعدة غير قابلة للتفاوض

**كل المبالغ تُخزَّن وتُحسب كأعداد صحيحة بالهللة (integer minor units).**

```ts
type Halalas = number   // 96.47 ر.س  →  9647
```

- التحويل إلى ريال يحدث في طبقة العرض فقط، عند لحظة الطباعة على الشاشة
- ممنوع منعاً باتاً استخدام الأرقام العشرية (floating point) في أي حساب مالي
- السبب: `0.1 + 0.2 !== 0.3` في جافاسكربت وفي كل لغة تستخدم IEEE 754. في تطبيق يطابق أرصدة بنكية بدقة الهللة هذا يكسر المطابقة

---

## 5. فايربيز (Firebase)

### 5.1 الخدمات المستخدمة

| الخدمة | الاستخدام | الباقة |
|---|---|---|
| Authentication | حسابات المستخدمين | مجانية |
| Cloud Firestore | كل بيانات المستخدم | مجانية (Spark) |
| Hosting | استضافة التطبيق | مجانية |
| Cloud Functions | **لا تُستخدم** — تتطلب باقة Blaze | — |

### 5.2 تسجيل الدخول (Authentication)

طريقتان مفعّلتان:
- **حساب جوجل** (Google Sign-In) — الافتراضي والأسرع
- **إيميل وكلمة سر** (Email/Password) — مع استعادة كلمة المرور

كل مستخدم يحصل على معرّف فريد **UID**، وهو المفتاح الذي تُعلَّق عليه كل بياناته.

### 5.3 شكل قاعدة البيانات

**كل بيانات المستخدم تحت مستنده هو.** تفاصيل الحقول في `DATA_MODEL.md`.

```
users/{uid}
users/{uid}/accounts/{id}
users/{uid}/transactions/{id}
users/{uid}/merchants/{id}
users/{uid}/categories/{id}
users/{uid}/rules/{id}
users/{uid}/budgets/{id}
users/{uid}/commitments/{id}
users/{uid}/transfers/{id}
users/{uid}/counterparties/{id}
users/{uid}/assets/{id}
users/{uid}/assetTrades/{id}
users/{uid}/importBatches/{id}
users/{uid}/periodSummaries/{yyyy-mm}
users/{uid}/notificationState/{id}
users/{uid}/settings/app
```

### 5.4 قواعد الحماية (Security Rules)

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null
                         && request.auth.uid == uid;
    }
  }
}
```

**المبدأ:** الحماية تُنفَّذ عند السيرفر لا عند التطبيق. حتى لو عُدِّل كود التطبيق، السيرفر يرفض. لا تُكتب أي منطق حماية في الواجهة على أنه حماية.

**ملاحظة:** إعدادات فايربيز في كود التطبيق (apiKey وغيرها) علنية بطبيعتها ولا تُعد سراً. الذي يحمي البيانات هو القواعد أعلاه فقط.

### 5.5 خصوصية بيانات الكشف

- **لا تُخزَّن أرقام حسابات كاملة.** تُخزَّن آخر أربعة أرقام فقط عند الحاجة للتمييز
- بيانات كل مستخدم معزولة تماماً بالقواعد أعلاه
- لا مشاركة ولا تجميع ولا تحليل عبر المستخدمين

### 5.6 ضبط التكلفة — إلزامي من اليوم الأول

الباقة المجانية: **50,000 قراءة و20,000 كتابة و1 جيجا يومياً**. الحد المقيِّد هو عدد القراءات لا الحجم.

ثلاث قواعد تمنع تجاوزه:

1. **تفعيل العمل بدون إنترنت** (offline persistence) — التطبيق يقرأ من نسخة الجهاز ويجلب المتغيّر فقط
2. **مستندات ملخص جاهزة** — مجاميع كل فترة تُحسب مرة وتُخزَّن في `periodSummaries`. الشاشة الرئيسية تقرأ مستنداً واحداً لا 2000 عملية
3. **كل استعلام محدود بفترة** — لا يوجد استعلام يقرأ كل العمليات

---

## 6. الأسعار (Price feeds) — بدون تكلفة

بما أن Cloud Functions غير متاحة على الباقة المجانية:

```
GitHub Actions (مجدولة مرتين يومياً)
   │ المفتاح مخزَّن في GitHub Secrets
   ▼
تجلب أسعار الذهب والأسهم والبتكوين
   ▼
تكتب public/prices.json وتنشره
   ▼
التطبيق يقرأ prices.json كملف عادي
```

- لا مفاتيح داخل واجهة الجوال — مطابق لما ينص عليه التوصيف
- كل المستخدمين يقرأون نفس الملف — عدد الطلبات الخارجية ثابت مهما زاد المستخدمون
- **عند فشل الجلب:** يُعرض آخر سعر مع توقيته، ولا يُخفى أن السعر قديم
- **الرمز غير المدعوم:** إدخال يدوي

### حالة المصادر

| الأصل | المصدر | الجاهزية |
|---|---|---|
| بتكوين | CoinGecko | جاهز |
| الذهب | GoldAPI أو API Ninjas | جاهز، بحد يومي صغير يكفي |
| الأسهم السعودية | لا يوجد API رسمي مجاني من تداول. رموز `.SR` على مصادر غير رسمية | يُراجع عند التنفيذ |
| الصناديق | لا مصدر آلي | إدخال يدوي |

**قرار المرحلة الأولى:** الاستثمار يبدأ بإدخال يدوي للأسعار. الربط الآلي في المرحلة المكمّلة. السبب: ربط الأسعار مبكراً يستهلك الوقت في إصلاح مصادر متقلبة بدل بناء التطبيق.

---

## 7. مراحل البناء (Build phases)

البناء **شريحة رأسية كاملة في كل مرة** (vertical slice): من الشاشة إلى التخزين، تعمل فعلياً، قبل الانتقال للتالية.

**ممنوع:** بناء كل الشاشات أولاً ثم محاولة ربطها. هذا هو مصنع السباجتي.

| # | الشريحة | معيار الاكتمال |
|---|---|---|
| 0 | تجهيز المشروع + Git + فايربيز + الحسابات | تسجيل دخول ناجح وخروج |
| 1 | استيراد كشف الراجحي + منع التكرار + التصنيف بالقواعد + شاشة العمليات + البحث | استيراد كشف حقيقي وظهور العمليات مصنّفة |
| 2 | الشهر المالي + الرئيسية + التحليل | النسب والصافي ومعدل الادخار والتوقع |
| 3 | الميزانية والسقوف والمتوسطات والشذوذ | سقف يُضبط ويُمسح ويُقارن |
| 4 | مطابقة الرصيد + التصدير + النسخ الاحتياطي | مطابقة كشف كامل بدقة الهللة |
| 5 | الالتزامات والتحويلات والأطراف | الاشتراكات تُكتشف وتُتابع |
| 6 | الإشعارات | آلة الحالات تعمل بلا تكرار |
| 7 | الاستثمار بإدخال يدوي | الأصول والأرباح والخسائر |
| 8 | النشر والتوزيع | رابط يعمل على آيفون وأندرويد |
| 9 | الأسعار الآلية | prices.json يتحدث تلقائياً |

---

## 8. قواعد العمل

- **Git من اليوم الأول.** كل شريحة تنتهي بـ commit. أي خطأ يُتراجع عنه بأمر واحد
- **الملفات المرجعية الثلاثة تُحدَّث مع كل قرار:** هذا الملف و `DATA_MODEL.md` و `SPEC.md`
- **أي قرار معماري جديد يُكتب هنا، لا في الشات.** الشات ينسى، الملف لا ينسى
- **لا تخمين في الأرقام.** أي رقم يُعرض في الواجهة له مصدر محسوب أو حالة "غير متاح"

---

## 9. قرارات اتُّخذت عند التنفيذ (2026-09-07)

هذه القرارات كانت مؤجلة في هذا الملف أو غير مذكورة، وحُسمت عند بناء المرحلة صفر.

### 9.1 التنسيق: CSS عادي بمتغيّرات — لا Tailwind ولا CSS Modules

§2 كان يقول «يُحسم عند البدء». المعتمد: **CSS عادي مع custom properties**.

**السبب:** `design/tokens.json` قائمة متغيّرات جاهزة، وترجمتها إلى
`:root { --c-*: … }` نقل مباشر بلا طبقة وسيطة. Tailwind يضيف اعتمادًا وخطوة بناء
وطبقة تسمية ثالثة بين `tokens.json` والشاشة، ويخالف قاعدة «لا مكتبة بلا سبب» (#8).

**القاعدة المشتقة:** كل قيم الألوان في `src/presentation/theme/tokens.css` وحده.
**ممنوع كتابة قيمة لون (hex/rgb) في أي ملف آخر.** أي لون جديد يُضاف كمتغيّر هناك أولًا.
تبديل الوضع يتم بسمة `data-theme` على `<html>` — فلا تُفقد حالة النموذج عند التبديل (spec/04).

### 9.2 الاختبارات: Vitest

**السبب:** مشغّل الاختبار الأصلي لـ Vite، يقرأ نفس إعداد TypeScript بلا ضبط إضافي،
ولا يضيف سلسلة اعتمادات بناء ثانية كما يفعل Jest. ملف الإعداد `vitest.config.ts` منفصل
عن `vite.config.ts` حتى لا تختلط إعدادات البناء بإعدادات الاختبار.

### 9.3 `firebase-tools` اعتماد تطوير محلي

لم تكن أداة فايربيز مثبّتة على الجهاز ولا على PATH. ثُبّتت كـ devDependency
لتشغيلها بـ `npx firebase` بلا تثبيت عام. النشر يحتاج `npx firebase login`
تفاعليًا من المالك.

### 9.4 حدود الطبقة `domain/` وملف التنسيق

`domain/formatMoney.ts` دالة نقية بلا اعتماد خارجي، فبقيت في `domain/`،
لكن **استعمالها في `presentation/` فقط**. هي المكان الوحيد الذي تتحوّل فيه
الهللة إلى ريال، والتحويل نصّي (قسمة صحيحة + باقٍ) لا عشري.

### 9.5 حماية البيانات الشخصية في Git

`.gitignore` يستبعد **كامل مجلد `files/`** وكل ملفات `*.pdf`.

**السبب:** `files/counterparties.csv` يحتوي أرقام حسابات كاملة، و`transactions_full.csv`
كشف بنكي حقيقي، وملف PDF كشف الحساب. المستودع خاص لكن الاستبعاد لا يعتمد على ذلك.

**الأثر على الاختبارات:** اختبار المجاميع المرجعية يقرأ `files/transactions_full.csv`
من القرص عند وجوده، ويُتخطّى بإعلان صريح عند غيابه — فلا يمر الاختبار زورًا
على جهاز لا يملك الملف.

### 9.6 مجموعة «ليس مصروفًا» — مشتقّة من البيانات لا مفترضة

المجموعة التي تحقّق 201,703.18 بالضبط هي تصنيفات المصدر الستة:

`تحويلات` · `محافظ رقمية` · `تقسيط` · `استثمار` · `سحب نقدي` · `ذهب`

**تحذير موثّق:** الملاحظة القديمة في `files/اقرأني-أولاً.md` تذكر 199,256 لأنها
أغفلت `ذهب` (2,448.06). الرقم المعتمد هو ما في `OVERRIDES.md §7`.

**تعارض مع `spec/02` يُسجَّل ولا يُبتلع:** تصنيف `سداد` (17,233.19) يدخل ضمن
المصروف الحقيقي في هذه المصالحة، بينما `spec/02` يقول إن سداد دين عليك ليس مصروفًا.
السبب أن `التصنيف` في CSV تصنيف إنفاق لا نوع اقتصادي، والنوع الاقتصادي يحتاج
تأكيد المستخدم لكل عملية (`spec/05`: التصنيف الآلي يقترح ولا يقرر).
**المعتمد للمرحلة الأولى** أرقام `OVERRIDES.md §7` لأنها معيار القبول المعلن.
تنقية النوع الاقتصادي لـ `سداد` تتم بتأكيد المستخدم في مرحلة لاحقة، وستغيّر
هذه المجاميع عن عمد وبعلم المالك.

---

## 10. قرارات المرحلة الأولى (2026-09-07)

### 10.1 `observedDirection` — اتجاه السيولة حقيقة مستقلة عن النوع الاقتصادي

أُضيف حقل `observedDirection: 'in' | 'out'` إلى `Transaction`.

**السبب:** `spec/02` يقول «وارد الحساب لا يحدد وحده النوع الاقتصادي»، فالعملية
المستوردة تبدأ بـ `economicKind: 'unclassified'`. لكن كشف التكرار (`spec/05`
الدرجة ٤) يقارن **اتجاه** الحركة. لو استُنتج الاتجاه من النوع الاقتصادي،
لصار كل ما هو غير محدد «صادرًا»، فتفشل مطابقة صفوف الدخل عند إعادة الاستيراد.

**القاعدة:** `observedDirection` حقيقة بنكية تُكتب مرة عند الاستيراد ولا تتغير.
`economicKind` قرار محاسبي يملكه المستخدم. لا يُشتق أحدهما من الآخر.

### 10.2 المجاميع تكون `null` لا صفرًا حين تكون مجهولة

`TransactionsScreenData.incomeMinor` و `expenseMinor` و `remainingMinor`
من نوع `number | null`.

**السبب:** إذا كانت كل عمليات الفترة بنوع اقتصادي غير محدد، فالدخل والمصروف
**مجهولان**. عرض `0.00` وقتها صفر مؤكد مكان المجهول، وهو ما تمنعه القاعدة #10.
الشاشة تعرض «غير متاح» مع تنبيه يشرح السبب وعدد العمليات الناقصة.

هذا اكتُشف بتشغيل التطبيق فعليًا لا بقراءة الكود: الشاشة كانت تعرض
«الدخل 0.00» وتحتها راتب 7,000.

### 10.3 مولّدا معرّفات منفصلان

| المولّد | الاستخدام | السبب |
|---|---|---|
| `SequentialIdGenerator` | الاختبار فقط | حتمي، والحالة تبدأ فارغة |
| `RandomIdGenerator` | التشغيل | التسلسلي يبدأ من 1 كل جلسة فيكتب **فوق** عمليات موجودة في Firestore |

صيغة معرّف التشغيل `<بادئة>-<توقيت base36>-<عشوائي>`؛ التوقيت أولًا
فيوافق الترتيب المعجمي الترتيب الزمني.

### 10.4 وضع المعاينة `?demo=1` — التطوير فقط

`src/app/demoContainer.ts` يبني حاوية بمستودعات ذاكرة فقط، بلا فايربيز وبلا حساب.
محاط بـ `import.meta.env.DEV` في `main.tsx` فيحذفه Vite من بناء الإنتاج.
يحقق شرط `spec/01`: «وضع عرض منفصل قابل للمسح، ولا تُخلط بياناته بالاستيراد الحقيقي».

### 10.5 ~~قيد ذرّية على Firestore~~ — **حُلَّ 2026-09-07**

`FirestoreUnitOfWork` **لا ينفّذ ذرّية حقيقية**. سبب القيد:
معاملة Firestore لا تسمح بقراءات بعد الكتابة ولا تتجاوز 500 عملية،
والاستيراد يكتب مئات المستندات.

**الحالة الفعلية:** `writeBatch` داخل كل مستودع ذرّي **لكل 500 مستند**، لا للدفعة كلها.
**الأثر:** استيراد أكبر من 500 مستند قد ينقطع بين دفعتين، فتُحفظ نصف دفعة.
**هذا لا يستوفي** شرط `spec/06` «صفر أو كامل الدفعة» على الملفات الكبيرة.

**مستوفى بالكامل** في `infrastructure/memory/`، وعليها تُختبر حالات القبول
(OVERRIDES §4). **الحل المؤجل:** كتابة الدفعة بحالة `staged` أولًا، ثم تحويلها
إلى `committed` بكتابة واحدة، مع استئناف عند إعادة الفتح — عمل المرحلة الرابعة.

### 10.6 ~~المراجع في الذاكرة~~ — **حُلَّ 2026-09-07**

في `container.ts` الحالي: العمليات والدفعات وسجلات المصدر على Firestore،
أما التصنيفات والقواعد والتجار فتُبنى في الذاكرة من `rule-reference.json`
و `merchant-reference.json` عند كل تشغيل.

**الأثر:** تعديل قاعدة أو تصنيف **لا يُحفظ بين الجلسات**، رغم أن `spec/05`
ينص على أن ملفات القواعد «مرجع أولي قابل للتحرير».
**السبب:** كتابتها لكل مستخدم عند أول دخول هجرة أولى لم تُبنَ بعد.
**عمل الشريحة التالية.**

### 10.7 حماية أرقام الحسابات عند الكتابة

`sanitizeAccountNumbers` في `firestoreRepositories.ts` يقص أي تتابع من خمسة
أرقام فأكثر إلى `****` + آخر أربعة، ويُطبَّق على كل حقل نصي قبل الكتابة.
تنفيذ لقاعدة CLAUDE.md #11 و OVERRIDES §2 عند حدود الطبقة لا بالاعتماد على الانضباط.

---

## 11. إغلاق القيدين المعلنين في §10 (2026-09-07)

### 11.1 حل §10.5 — بروتوكول علامة الالتزام (commit marker)

الذرّية الحقيقية مستحيلة على Firestore لدفعة بمئات المستندات: المعاملة
لا تسمح بقراءة بعد كتابة ولا تتجاوز 500 عملية. فبدل ادّعاء ذرّية لا توجد،
نُفِّذ ضمان مكافئ **بالأثر**:

```
١. الدفعة تُكتب state='staged'      ← قبل أي شيء
٢. سجلات المصدر ثم العمليات          ← الانقطاع المحتمل هنا
٣. updateState('committed')          ← كتابة واحدة، ذرّية بطبيعتها
```

**ترتيب الخطوة ٢ ليس تفصيلًا.** سجل المصدر هو الفهرس الوحيد الذي يربط
العملية بدفعتها. لو كُتبت العمليات أولًا وانقطع الاتصال قبل السجلات،
تصير العمليات **يتيمة**: موجودة ولا سبيل للوصول إليها من الدفعة، فيستحيل
تنظيفها. اكتُشف هذا بفشل اختبار، لا بالمراجعة.

`findByFileHash` يرى `committed` فقط، فالدفعة المعلّقة لا تُحسب استيرادًا
سابقًا. و`ResumeStagedBatch` ينظّف المعلّق عند فتح التطبيق ويُبلغ المستخدم.

**النتيجة:** لا يظهر للمستخدم أبدًا نصف دفعة معتمدة — وهو ما يطلبه spec/06.
**مختبَر** في `tests/acceptance/atomicity.test.ts` بمستودع ينفجر عمدًا في
أخطر لحظة، مرة مع وحدة عمل حقيقية ومرة بدونها (محاكاة Firestore).

### 11.2 حل §10.6 — زرع المراجع في تخزين المستخدم

`SeedUserReferences` يكتب التصنيفات والقواعد والتجار تحت `users/{uid}`
**عند أول دخول فقط**، وتُقرأ بعدها من التخزين لا من الملفات.

**القاعدة:** لو وُجدت تصنيفات محفوظة فالمستخدم عدّلها، ولا يجوز إعادة
المرجع الأصلي فوقها. مختبَر بحالة يحذف فيها المستخدم قاعدة ويضيف أخرى،
ثم يُستدعى الزرع فلا يمس شيئًا — وهو ما يجعل القواعد «قابلة للتحرير»
فعلًا كما ينص spec/05.

⚠️ **باقٍ في الذاكرة:** `allocations` (تخصيصات الأشخاص) — شاشتها لم تُبنَ،
وهي عمل شريحة الأشخاص والديون في المرحلة الخامسة.

### 11.3 خلل واجهة اكتُشف بالتشغيل: تعذّر الاستيراد الثاني

زر الاستيراد كان في الحالة الفارغة فقط، فبمجرد وجود عملية واحدة
**يستحيل استيراد كشف ثانٍ**. أُضيف زر `＋` في الرأس كما ينص spec/01
(«الرأس: البحث، المظهر، الإضافة»).

اكتُشف بتشغيل التطبيق واستيراد ملف ثم محاولة استيراد ملف آخر —
لا بقراءة الكود ولا باختبار وحدة.

---

## 12. قاعدة فهارس Firestore (2026-09-07)

**لا يعتمد أي استعلام على فهرس مركّب (composite index).**

**السبب — عُرف بالتشغيل الحقيقي:** أول قراءة من فايربيز بعد تسجيل دخول
حقيقي فشلت برسالة `The query requires an index`. الاستعلام كان:

```ts
where('occurredAt', '>=', from), where('occurredAt', '<=', to),
orderBy('occurredAt'), orderBy('sourceOrder')   // ← يفرض فهرسًا مركّبًا
```

الفهرس المركّب لا يُنشأ تلقائيًا، فيفشل التطبيق على **كل مشروع جديد**
حتى يُنشر يدويًا. هذا عبء إعداد وعطل صامت ينتظر كل مستخدم جديد.

**القاعدة المعتمدة:**

| مسموح بلا فهرس مركّب | البديل عند الحاجة لأكثر من ذلك |
|---|---|
| شرط واحد على حقل واحد (مساواة أو مدى) | ترشيح إضافي **في الذاكرة** |
| `orderBy` على حقل واحد بلا شرط مدى | ترتيب **في الذاكرة** |
| `in` على حقل واحد (حد 30 قيمة) | تقطيع الطلب إلى دفعات |

**ما هذا ممكن؟** لأن قيد §5.6 يضمن أن **كل استعلام محدود بفترة**،
فالنتيجة مئات المستندات لا آلافها، وترتيبها أو ترشيحها في الذاكرة
تكلفته معدومة. القيدان يخدمان بعضهما.

**طُبِّق على:**
- `listByDateRange` — حُذف `orderBy` والترتيب صار في الذاكرة
- `findByFileHash` — حُذف شرط `state` والترشيح صار في الذاكرة

`firestore.indexes.json` يبقى فارغًا عمدًا. **أي استعلام جديد يحتاج فهرسًا
مركّبًا يُعاد تصميمه، لا يُضاف له فهرس** — إلا بسبب مكتوب هنا.

---

## 13. المرحلة الثانية: الشهر المالي والرئيسية والتحليل (2026-09-07)

### 13.1 محرك اقتراح النوع الاقتصادي — لماذا وُجد أصلًا

`spec/02` يمنع استنتاج النوع الاقتصادي من اتجاه السيولة، فكل عملية
مستوردة تبدأ `unclassified`. لكن كشفًا حقيقيًا فيه **1912 عملية**، وتحديد
نوع كل واحدة يدويًا مستحيل عمليًا — فتبقى كل أرقام التطبيق «غير متاحة» للأبد.

`domain/suggestEconomicKind.ts` يحل هذا بما يسمح به النص: **يقترح ولا يقرر.**

قياس التغطية على الكشف الحقيقي:

| الثقة | العدد | المبلغ |
|---|---:|---:|
| قاطع (تأكيد جماعي) | 1,250 | 97,815.19 |
| مرجّح (نظرة فردية) | 66 | 19,859.60 |
| **غامض (قرار فردي)** | **596** | **484,537.53** |

الغامض ليس فشلًا للمحرك بل **نتيجته الصحيحة**: هو «عالم انتقال الفلوس»
الذي لا يعرف الكشف وجهته، وهو جوهر المنتج (`OVERRIDES §7-أ`).

**قاعدة لا تُكسر ومختبَرة:** لا يوجد مدخل واحد يُنتج اقتراح **دخل** تلقائيًا.
«لا تصنف تحويلًا غامضًا كراتب افتراضيًا» — spec/02.

**خلل اكتُشف بالتشغيل:** المحرك كان يقرأ عمود التصنيف من الملف فقط،
فمخطط المعاينة (بلا عمود تصنيف) جعل **كل** العمليات غامضة رغم أن قواعد
التطبيق صنّفت Amazon «تسوق إلكتروني» و STC «اتصالات». أُضيف `categoryName`
فصار المحرك يستعمل ما يعرفه التطبيق فعلًا بدل أن يهدره.

### 13.2 المجاميع لها **ثلاث** حالات لا اثنتان

| الحالة | الدخل والمصروف | المتبقي ومعدل الادخار |
|---|---|---|
| ولا عملية محددة | غير متاح | غير متاح |
| **بعضها محدد** | **جزئي مع وسم «لحد دلوقتي»** | **غير متاح** |
| كلها محددة | متاح | متاح |

**لماذا المتبقي `null` في الحالة الوسطى:** هو `الدخل − المصروف`، وكلاهما
ناقص، فالناتج ليس ناقصًا بل **مضلِّلًا**. اكتُشف بالتشغيل: الشاشة عرضت
«الدخل 0.00» و«المتبقي −985.50» وتحتها راتب 7,000 لم يُحدَّد بعد —
رقم يوحي بعجز غير موجود.

الدخل والمصروف الجزئيان يُعرضان لأنهما **صحيحان لحد الآن**، لكن موسومان.

### 13.3 التحليل: كل رقم إما محسوب أو «غير متاح» بسببه

`domain/analytics.ts` — كل دالة ترجّع `null` وسببًا حين لا يكفي الدليل:

| الدالة | متى تكون `null` |
|---|---|
| `dailyAllowance` | بلا سقف يحدده المستخدم، أو انتهت الفترة |
| `forecastPeriodSpend` | قبل مرور 5 أيام من الفترة |
| `categoryDistribution` | لا ترجع null، لكن مجموع الشرائح = الإجمالي بالضبط |

`MIN_DAYS_FOR_FORECAST = 5`: معدل يومين لا يتنبأ بشهر. والتوقع **يذكر
قصوره دائمًا** في نصه، لا في تلميح جانبي.

الأيام المتبقية تُحسب من نطاق الفترة المعلن — **لا 30 يومًا ثابتة** (spec/02).

### 13.4 الفترة حالة واحدة مشتركة بين الشاشتين

`AppShell` يملك `period` وحده ويمرره للرئيسية والعمليات.

**السبب:** لو كانت لكل شاشة فترتها، لعرضت شاشتان أرقامًا مختلفة لنفس
البيانات. مصدر واحد للحقيقة (قاعدة §3-5).

### 13.5 أخطاء واجهة اكتُشفت بالتشغيل لا بالمراجعة

| الخلل | الأثر |
|---|---|
| `busy` لا يُرفع بعد النجاح في `KindsSheet` | الورقة تعلق على «بنحفظ…» وزر الإغلاق معطّل — **المستخدم محبوس** |
| زر الإغلاق `disabled={busy}` | يضاعف الحبس عند أي فشل غير متوقع |

**القاعدة المشتقة:** `busy` يُرفع في `finally` دائمًا، و**زر الإغلاق
لا يُعطَّل أبدًا**. لا يجوز أن يحبس التطبيق مستخدمه في أي حال.

---

## 14. المرحلة الرابعة: المطابقة والتصدير والنسخ الاحتياطي (2026-09-07)

### 14.1 حقلان كانا ناقصين وبدونهما المرحلة مستحيلة

| الحقل | لماذا |
|---|---|
| `Transaction.statedBalanceMinor` | كان يُقرأ من الكشف **ولا يُخزَّن**، فمقارنة عمود الرصيد داخل التطبيق مستحيلة. معيار `OVERRIDES §7-ب` كان مثبتًا في الاختبارات وحدها لا في المنتج |
| `Transaction.walletId` | العمليات لم تكن مرتبطة بمحفظة، فسلسلة رصيد أي محفظة غير قابلة للحساب |

كلاهما اختياري: عملية استُوردت قبل إضافتهما تُعرض «غير منسوبة لمحفظة»
وتُذكر في تقرير المطابقة، **ولا تُنسب لمحفظة بالتخمين**.

### 14.2 ⚠️ **كل مستخدم يبدأ برصيد صفر** — قرار صُحِّح أثناء البناء

كنت سأزرع أرصدة `OVERRIDES §6` (4,837.83 و 1,000.00) لكل من يفتح التطبيق.

**هذا كان خطأ.** التطبيق سيُهدى لأصدقاء (`OVERRIDES §3`)، فحقن أرصدة
المالك في الكود يعني أن كل واحد منهم يفتح التطبيق **فيجد رصيد شخص آخر**.

**المعتمد:** `seedWallets(false)` دائمًا في التطبيق — محفظتان برصيد صفر،
والرصيد الافتتاحي **وتاريخه** يُضبطان من الإعدادات بقرار صريح.
أرقام `OVERRIDES §6` تبقى مرجعًا للمالك يكتبها بنفسه مرة واحدة،
وتُستعمل في الاختبارات عبر `seedWallets(true)`.

### 14.3 تاريخ الافتتاح ليس تفصيلًا — خلل اكتُشف بالتشغيل

المحفظة كانت تُزرع بتاريخ **اليوم** بلا وسيلة لتعديله. النتيجة:
مستخدم جديد يستورد كشفًا يغطي شهورًا ماضية، فلا يُطابَق **ولا سطر**
لأن كل العمليات قبل تاريخ الافتتاح — بلا سبب ظاهر له.

`WalletEditor` يحرر المبلغ **والتاريخ** معًا، ويشرح أثر التاريخ صراحةً.

### 14.4 خلل الفترة الذي أفرغ التصدير — كشفه اختبار

`readAllTransactions` كان يبني فترة البداية بـ`buildPeriod(سنة, شهر)`،
لكن الشهر المالي يبدأ يوم الراتب (28). فـ `from = 2025-01-01` أنتج فترة
تبدأ 2025-01-28، **فتخطّى كل عملية قبل يوم 28** وخرج التصدير فارغًا.

**الصواب:** `periodForDate(from, payday)` — الفترة التي **تحوي** التاريخ
لا التي تبدأ في شهره. صُحِّح في `exportBackup` و `reconcileBalance` معًا.

**قاعدة مشتقة:** لا يُبنى نطاق زمني من (سنة، شهر) حين يكون المطلوب
«الفترة التي تحوي تاريخًا». `periodForDate` هي الأداة الصحيحة دائمًا.

### 14.5 المطابقة تقرأ فترةً فترة

المطابقة تحتاج كل عمليات المحفظة، وهذا يخالف ظاهريًا «كل استعلام محدود
بفترة» (§5.6). الحل: القراءة **فترة فترة** من تاريخ الافتتاح حتى اليوم،
بسقف 60 فترة (5 سنوات). فيبقى كل استعلام محدودًا، ويُعلَن عدد الفترات المقروءة.

### 14.6 هوية الحساب تُشتق من المحفظة لا يكتبها المستخدم

كان المستخدم يكتب «اسم الحساب أو المصدر» يدويًا. هذه الهوية هي
**نطاق تفرّد المرجع البنكي** (`spec/03`)، فخطأ مطبعي واحد يجعل نفس
الكشف يُستورد مرتين كأنه حسابان مختلفان. الآن تُشتق من اسم المحفظة المختارة.

### 14.7 النسخة الاحتياطية والتصدير — محليان بالكامل

`OVERRIDES §2`: «التصدير والنسخ الاحتياطي **مطلبان ولا يلغيهما وجود السحابة**».

التنزيل عبر `Blob` في المتصفح — **الملف لا يمر بأي خادم**، وهو تنفيذ
لشرط «لا تُرفع البيانات لأي خدمة أخرى غير فايربيز».

النسخة تحمل `schemaVersion` و**عدّادات لكل مجموعة**، و`checkBackup`
يرفض قبل أي كتابة عند: ملف ليس من مصروفي · إصدار أحدث من التطبيق ·
عدّاد لا يطابق المحتوى · معرّفات عمليات مكررة (`spec/03`: «الاستعادة
لا تكرر العمليات»).

⚠️ **غير منفَّذ بعد:** الاستعادة الفعلية (الكتابة من ملف). التحقق جاهز
ومختبَر، لكن `restore()` لم تُبنَ — تحتاج قرارًا في سلوك الدمج مع
البيانات القائمة (استبدال؟ دمج؟ رفض إن لم يكن الحساب فارغًا؟).
**قرار للمالك، لا يُختار نيابة عنه.**

---

## 15. رسالة خطأ كانت تكذب (2026-09-07)

المالك اختار كشف **PDF** من جواله، فوصلته:

> ⚠ اقتباس مفتوح ولم يُغلق — الملف ناقص أو تالف عند السطر 3366

**الملف كان سليمًا تمامًا.** السبب أن `File.text()` ينجح على أي ملف
ويرجّع بايتات PDF كأنها نص، فتصل إلى قارئ CSV الذي يشتكي — بحق — من
اقتباس غير متوازن في بايتات ثنائية.

الرسالة اتهمت **ملفه** بالتلف بينما العيب أن التطبيق لا يقرأ PDF.
هذا يخالف CLAUDE.md #14 بمعناه: **رسالة خطأ تدّعي سببًا خاطئًا أسوأ من
غياب الرسالة**، لأنها ترسل المستخدم يبحث عن مشكلة غير موجودة.

**العلاج:** `infrastructure/import/inspectFile.ts` يفحص **قبل** أي تحليل —
توقيع `%PDF-`، أو الامتداد، أو محرف NUL، أو نسبة محارف تحكم فوق 2٪ —
ويقول السبب الحقيقي وما يفعله المستخدم.

**قاعدة مشتقة:** أي مُدخل من ملف يُفحص نوعه قبل تمريره لمحلل يفترض صيغة
بعينها. المحلل يشرح خطأ **في صيغته هو**، لا خطأً في اختيار الملف أصلًا.

⚠️ **الحاجة الحقيقية المكشوفة:** المالك عنده الكشف **PDF لا CSV**،
وهذا مسار غير مبني (`spec/05` يصف `parse_alrajhi.py` وحدوده).
قرار بناء قارئ PDF في المتصفح **معلّق على طلبه** — يحتاج مكتبة
(pdf.js) وسببًا مكتوبًا هنا قبل إضافتها (قاعدة #8).

---

## 16. ثلاث فجوات كشفها المالك بالاستعمال (2026-09-07)

### 16.1 أخطاء فايرستور كانت تصل بالإنجليزية التقنية

أخطاء تسجيل الدخول كانت مترجمة (15 رسالة)، وأخطاء الاستيراد تذكر رقم
السطر وسببه — لكن **أي فشل في القراءة أو الكتابة على فايرستور كان يصل
بنصه الخام**: «Missing or insufficient permissions»، «client is offline».

`infrastructure/firestore/firestoreErrors.ts` يترجم 12 رمزًا إلى:
**ما حدث** · **ما يفعله المستخدم** · **هل تُجدي إعادة المحاولة**.
و`ErrorNotice` يعرضها بثلاث طبقات، والرمز التقني **مطويًا** لا في الواجهة.

الرسائل العربية المكتوبة أصلًا (أخطاء المجال والاستيراد) **تمر كما هي** —
لا تُترجم مرتين.

### 16.2 لا سبيل لتسجيل عملية يدويًا — والكاش لا يصل من أي كشف

زر `＋` كان يفتح الاستيراد مباشرة. فمن اشترى قهوة بالكاش **لا يجد أين
يسجّلها**، ومصروف الكاش كله يبقى غير مرصود — رغم أن `spec/02` يفرد له
قاعدة صريحة: «شراء كاش 100 يخفض رصيد الكاش ويزيد المصروف 100».

`spec/01` كان ينص على هذا أصلًا: «+ | قائمة **فاتورة/حركة**/كشف/شخص».

**المبني:** `AddMenu` يفتح خيارين، و`AddTransaction` + `AddTransactionSheet`.

**فرق جوهري عن الاستيراد:** في الاستيراد الكشف يعطي **الاتجاه** والنوع
مجهول. في الإضافة اليدوية المستخدم يعطي **النوع** والاتجاه نتيجته،
والنوع `economicKindConfirmed: true` فورًا — فلا يمر بدورة الاقتراح.

الشاشة تعرض **أثر النوع قبل الحفظ** («هتتحسب في المصروف» / «نقل فلوس بس»)
بدل أن يكتشفه المستخدم بعده. والكاش يُوسم تلقائيًا من نوع المحفظة.

### 16.3 ⚠️ باقٍ: طرف التحويل الداخلي الثاني

الإضافة اليدوية تسجّل `internal_transfer` كطرف واحد (خصم من محفظة
المصدر). الإضافة إلى المحفظة الهدف **لم تُبنَ**، فنقل 500 من البنك للكاش
يخصم من البنك ولا يزيد الكاش. مذكور في §14 ويبقى قائمًا.

---

## 17. التحويل الداخلي بطرفين (2026-09-07)

`spec/02`: «عملية اقتصادية واحدة لها **طرف خصم وطرف إضافة**… وعرض الدخل
والمصروف لا يعيد حساب الطرفين.»

**الخلل الذي أُغلق:** التحويل الداخلي كان يُخصم من محفظة المصدر
**ولا يظهر في المحفظة الهدف إطلاقًا** — فلوس تختفي من الحساب بلا أثر.
كان مسجَّلًا في §14 ونُفِّذ الآن.

### لماذا حقل لا جدول `WalletPosting`

`spec/03` يعرّف `WalletPosting` كجدول حركات مستقل. المنفَّذ هنا حقل
`Transaction.transferToWalletId` بدلًا منه.

**السبب:** الحالة الوحيدة التي تحتاج طرفين اليوم هي التحويل الداخلي.
جدول الحركات الكامل يفرض إعادة كتابة الاستيراد والمطابقة والدفتر معًا،
والحقل يحقق **نفس المعنى المحاسبي** بأقل تغيير وبلا مخاطرة على معيار
`OVERRIDES §7-ب` القائم.

**متى يُستبدل بالجدول:** حين تظهر حالة ثانية تحتاج أكثر من طرفين، أو
تحويل بعملتين (مرفوض صراحةً الآن برسالة تشرح أنه يحتاج سعر صرف موثّقًا).

### قواعد مطبَّقة

| القاعدة | التنفيذ |
|---|---|
| التحويل بلا محفظة هدف | **مرفوض** — لا يُحفظ ناقصًا |
| التحويل لنفس المحفظة | مرفوض |
| المحفظة الهدف في غير التحويل | مرفوضة |
| اختلاف العملة | مرفوض بتفسير (`spec/02`: لا جمع بلا تحويل موثّق) |
| الرصيد المعلن | يخص محفظة **المصدر** وحدها، فلا يُقارَن به الطرف الداخل |

**مختبَر:** مجموع المحفظتين قبل التحويل وبعده **متطابق بالهللة** —
التحويل ينقل ولا يخلق ولا يفقد.

---

## 18. المرحلة الخامسة (جزئيًا): الأشخاص والديون (2026-09-07)

منطق `domain/ledger.ts` كان مكتوبًا ومختبَرًا بـ25 حالة منذ المرحلة الأولى،
**بلا تخزين ولا شاشة**. هذه الشريحة تصله بالمستخدم.

### ما بُني

| القطعة | الملاحظة |
|---|---|
| مستودعات Firestore | الأشخاص والالتزامات والتسويات والتخصيصات — كل استعلام بحقل واحد (§12) |
| `ManagePeople` | يربط المنطق بالتخزين ولا يعيد حسابًا |
| `PeopleScreen` | ثلاثة أرصدة **منفصلة بصريًا** |
| `LinkPersonSheet` | ربط عملية بشخص من شاشة العمليات |

### لماذا الأرصدة الثلاثة منفصلة بصريًا

`spec/02` يسمح بعرض «له عندك» في خانة واحدة. لكن `spec/06` يمنع سداد
قرض من رصيد أمانة، **والدمج البصري يخفي هذا المنع**: المستخدم يرى رقمًا
واحدًا فيتوقع أن يسدّد منه أي شيء، ثم يُرفض بلا سبب ظاهر.
فالفصل الثلاثي هنا **شرح للقاعدة لا زينة**.

### النمط المتكرر الذي أُغلق مرة أخرى

بعد بناء `PeopleScreen` كان يمكن **إضافة شخص ولا يمكن ربط أي عملية به** —
فتبقى القائمة فارغة أبدًا. نفس نمط زر الإضافة في §16.2.

**قاعدة مشتقة:** أي شاشة تعرض قائمة يجب أن يوجد **طريق مرئي** لملئها من
داخل التطبيق. القائمة الفارغة التي لا سبيل لملئها ميزة ناقصة لا شاشة جاهزة.

### تسريب الوحدة الداخلية إلى المستخدم

رسالة رفض السداد الزائد كانت تقول «المتبقي **3200 هللة**». الهللة وحدة
تخزين داخلية، والمستخدم يفكّر بالريال. صُحِّحت إلى `formatMoney` في
`domain/ledger.ts` و`ManagePeople`.

**قاعدة:** لا يظهر رقم بالهللة في أي نص يراه المستخدم — ولا في رسائل الخطأ.

### ⚠️ باقٍ في هذه المرحلة

- **الاشتراكات المتكررة** (`DetectCommitments`) لم تُبنَ
- **شاشة تفاصيل العملية**: تعديل التصنيف والملاحظة ووسم الكاش من الشاشة.
  الربط بشخص هو الإجراء الوحيد المتاح على عملية قائمة
- **الوسوم**: القاعدة مختبَرة والتخزين والواجهة لم يُبنيا

## 19. قارئ PDF لكشف الراجحي (2026-09-07)

### 19.1 مكتبة جديدة: `pdfjs-dist` — السبب

`CLAUDE.md` #8: «لا تضف مكتبة بلا سبب مكتوب». السبب:

استخراج نص PDF **بإحداثياته** مستحيل يدويًا — الملف مضغوط بـFlate وخطوطه
مضمّنة بترميز خاص، وقراءته تعني كتابة مفسّر PostScript كامل.
`pdfjs-dist` هو قارئ موزيلا، يعمل في المتصفح (المكان الوحيد المتاح لنا —
لا Cloud Functions)، ويعطي لكل قطعة نص مصفوفة تحويل فيها `x` و`y`.

**البديل المرفوض:** رفع الملف لخدمة تحويل خارجية. مرفوض لأن الكشف البنكي
كان سيغادر جهاز المستخدم إلى طرف ثالث.

الحجم تكلفة حقيقية (~٣٠٠ ك.ب مضغوطة)، ولذلك **تُحمَّل عند أول استيراد PDF
فقط** (`import()` ديناميكي) ولا تدخل الحزمة الأولى.

### 19.2 الترميز: خطأ كان سيمر صامتًا

السكربت القديم (`legacy/parse_alrajhi.py`) **يعكس كل كلمة عربية** بعد NFKC،
لأن `pdfplumber` يسلّمها بصيغة العرض معكوسة.

`pdfjs` **لا يفعل ذلك**: NFKC وحدها تعطي النص الصحيح، والعكس يفسده
(`الرصيد` تصبح `ديصرلا`). نُقل السلوك القديم حرفيًا كان سينتج أسماء تجار
وأنواع عمليات مقلوبة **بلا أي خطأ ظاهر**.

الذي يحتاج عكسًا هو **ترتيب الكلمات داخل السطر** (من اليمين لليسار)،
وأقواس الاتجاه `(` و`)` المرآتية.

### 19.3 البنية: المنطق منفصل عن المكتبة

| ملف | يعرف pdfjs؟ | مختبَر؟ |
|---|---|---|
| `pdf/pdfTextExtractor.ts` | نعم | لا — غلاف رفيع |
| `pdf/arabicText.ts` | **لا** | نعم |
| `pdf/alrajhiPdfSchema.ts` | **لا** | نعم — على كلمات بإحداثيات |

المخطط يأخذ `PositionedWord[]` لا ملفًا، فيُختبر من fixtures نصية
بلا PDF أصلًا. لولا الفصل لكان اختبار التحليل يتطلب ملفًا حقيقيًا ممنوعًا رفعه.

### 19.4 الأعمدة — قياسات محققة على الكشف الحقيقي

صفحة A4 عرضها 595 نقطة، والمستند من اليمين لليسار:

| العمود | `x` |
|---|---|
| الرصيد | < 110 |
| دائن | 110–200 |
| مدين | 200–310 |
| التاريخ | > 450 |
| تفاصيل العملية | 310–500 |

العملية = سطر واحد فيه **الثلاثة مبالغ والتاريخ معًا**. نوعها في السطر
الذي فوقه، وتفاصيلها في الأسطر التي تحته حتى العملية التالية.

### 19.5 التحقق — على كشف حقيقي من 68 صفحة

| المعيار | الناتج من PDF |
|---|---|
| عدد العمليات | 1912 — مطابق لعدد صفوف CSV |
| المدين | 302,171.45 ✓ |
| الدائن | 300,040.87 ✓ |
| سلسلة الرصيد | **0 سطر مختلف من 1912** |

المبالغ تُقرأ نصًا (`"839.30 SAR"` ⇒ `83930`) بلا `parseFloat` إطلاقًا.
